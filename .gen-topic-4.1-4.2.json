{
  "4.1.a": [
    {
      "id": "4.1.a.1",
      "term": "Inside Local",
      "weight": "high",
      "info": "<p>The <strong>Inside Local</strong> address is the <strong>private IP address</strong> of a host sitting on the inside (private) network, <strong>before</strong> any NAT translation occurs. This is the 'real' address the host is physically configured with — the IP you'd see if you logged into the host and typed <code>ipconfig</code> or <code>ip addr</code>. Inside Local addresses are almost always RFC 1918 private addresses: <code>10.0.0.0/8</code>, <code>172.16.0.0/12</code>, or <code>192.168.0.0/16</code>.</p><p><strong>Reading the name:</strong> 'Inside' tells you which network the host lives on (the private/internal network). 'Local' tells you this is the address as seen <strong>locally</strong> on that network — before any translation dresses it up for the internet. Inside Local = 'the real, unchanged address of an internal host.'</p><p><strong>In the NAT table:</strong> In <code>show ip nat translations</code>, the Inside Local appears in the column labeled <code>Inside local</code>. For an internal host <code>192.168.1.10</code> talking to <code>8.8.8.8</code>, the Inside Local column will show <code>192.168.1.10:1025</code> (with source port for PAT). This is what was in the packet before the router rewrote the source IP.</p><p><strong>Exam trap:</strong> Students often confuse 'Inside Local' with 'the address on the outside.' Inside Local is always on the inside, always the private IP. If the address in a question is a private RFC 1918 address and the question says 'inside,' it is the Inside Local.</p>",
      "visual": { "type": "comparison", "params": { "left": { "label": "Inside Local", "items": ["Private RFC 1918 IP", "Host's REAL configured IP", "Pre-translation source", "e.g., 192.168.1.10"] }, "right": { "label": "Inside Global", "items": ["Public IP", "What internet sees", "Post-translation source", "e.g., 203.0.113.5"] } } },
      "hack": {
        "memory": "Inside Local = 'Inside network, Local (real) address.' It's the private IP the host is configured with — what you'd see on the host itself. Always RFC 1918 (10.x, 172.16-31.x, 192.168.x). Rule: 'Local = real, Global = disguise.' If you're inside, your Local is your real address; your Global is your public disguise.",
        "practice": "In Packet Tracer: configure PAT with inside host 192.168.1.10 and outside interface 203.0.113.1. Ping 8.8.8.8 from the host. Run 'show ip nat translations' on the router. Identify the Inside Local column — it must show 192.168.1.10. Then ping from another inside host (192.168.1.11) and verify that new Inside Local entry appears. Repeat until you can read the column instantly.",
        "effort": "low",
        "meta": "Jeremy's IT Lab Day 44 (NAT) diagrams all four addresses. Wendell Odom OCG Chapters 10-11 explains the naming. Exam tests the four-address table constantly — drag-and-drop matching 'Inside Local' to the private IP. Mnemonic: 'Local = the address that feels at home on that side.'"
      }
    },
    {
      "id": "4.1.a.2",
      "term": "Inside Global",
      "weight": "high",
      "info": "<p>The <strong>Inside Global</strong> address is the <strong>public IP</strong> that represents an inside host to the outside world. It is the <strong>translated</strong> source address — what the internet sees when an internal host sends a packet out. For PAT, this is typically the outside interface's public IP; for static or dynamic NAT, it is an address from the configured pool.</p><p><strong>Reading the name:</strong> 'Inside' still refers to the host's home network (the host originates on the inside). 'Global' means this is the address as seen <strong>globally</strong> on the public internet. Inside Global = 'the public disguise of an inside host.'</p><p><strong>How translation produces it:</strong> When host <code>192.168.1.10</code> (Inside Local) sends a packet outbound, the NAT router rewrites the source IP to <code>203.0.113.5</code> (Inside Global). External servers see the connection as originating from 203.0.113.5. When replies return, the router looks up <code>203.0.113.5</code> in the translation table and rewrites the destination back to <code>192.168.1.10</code>.</p><p><strong>In the NAT table:</strong> The Inside Global appears in the column labeled <code>Inside global</code>. For PAT, multiple Inside Local hosts share the same Inside Global IP but are distinguished by unique source ports: <code>192.168.1.10:1025 → 203.0.113.1:1025</code>, <code>192.168.1.11:1026 → 203.0.113.1:1026</code>.</p><p><strong>Exam trap:</strong> If you see a public IP in the 'inside' columns of a NAT translation, it is the Inside Global. Inside Global is the only 'inside' address that is public.</p>",
      "visual": { "type": "packet-flow", "params": { "nodes": ["Host 192.168.1.10", "NAT rewrites src IP", "Source: 203.0.113.5", "Internet sees 203.0.113.5"], "color": "#3b82f6" } },
      "hack": {
        "memory": "Inside Global = 'Inside host, Global (public) face.' The translated public IP representing an internal host. Local = real, Global = disguise. Internal host's real IP is Local (192.168.1.10); its internet-facing mask is Global (203.0.113.5). If it's public AND on the inside side of the table = Inside Global.",
        "practice": "In Packet Tracer with PAT configured: send outbound traffic from 3 inside hosts. Run 'show ip nat translations'. Note the Inside Global column — all three rows will show the SAME public IP (the outside interface) but with DIFFERENT source ports. This hammers home that PAT multiplexes many Inside Locals onto one Inside Global.",
        "effort": "low",
        "meta": "Jeremy's IT Lab Day 44. Wendell Odom OCG Chapters 10-11. The exam loves 'identify the Inside Global' questions — they show 'show ip nat translations' and expect you to pick the public IP in the inside column. Inside Global is the #1 tested NAT address type."
      }
    },
    {
      "id": "4.1.a.3",
      "term": "Outside Local",
      "weight": "med",
      "info": "<p>The <strong>Outside Local</strong> address is how an outside (external) host appears to the inside network — the external host's address as seen <strong>from the inside's perspective</strong>. In almost every CCNA-level NAT scenario, <strong>Outside Local equals Outside Global</strong> because we don't typically translate the destination address of outbound traffic.</p><p><strong>When they differ:</strong> Outside Local only diverges from Outside Global when <strong>outside NAT</strong> (also called destination NAT or twice-NAT) is configured — a rare, advanced scenario where the router also translates the external host's address for inside hosts to see. The Outside Local would then be a fake/mapped address the inside network has been told to use for the outside host.</p><p><strong>Reading the name:</strong> 'Outside' means the host lives on the external network. 'Local' means this is how the address appears <strong>locally on the inside</strong> — the version the inside hosts see. In default NAT, that's just the real public IP, so Outside Local = Outside Global.</p><p><strong>In the NAT table:</strong> The <code>Outside local</code> column typically matches the <code>Outside global</code> column. For a flow from <code>192.168.1.10</code> to <code>8.8.8.8</code>, both Outside columns will show <code>8.8.8.8</code>. If they differ, someone has configured outside NAT — an advanced topic beyond CCNA scope.</p><p><strong>Exam trap:</strong> Students assume Outside Local is somehow 'private' because 'Local' sounds private. It is not. In default NAT, Outside Local is a public IP identical to Outside Global. The 'Local/Global' distinction is about whose perspective you're reading it from, not public vs private.</p>",
      "visual": { "type": "comparison", "params": { "left": { "label": "Outside Local", "items": ["External host as seen from inside", "Usually = Outside Global", "Differs only with outside NAT", "Rare on CCNA"] }, "right": { "label": "Outside Global", "items": ["External host's real public IP", "What the external host actually has", "Seen on the outside", "e.g., 8.8.8.8"] } } },
      "hack": {
        "memory": "Outside Local = how inside sees outside. Outside Global = outside's real address. 99% of the time these match — both are the public IP of the external host. They only differ in advanced 'outside NAT' scenarios (rare on CCNA). If the question doesn't mention destination NAT, assume Outside Local = Outside Global.",
        "practice": "In Packet Tracer: set up PAT with inside 192.168.1.0/24 and outside interface to an external server 8.8.8.8. Ping from a host. 'show ip nat translations' — Outside Local and Outside Global will both read 8.8.8.8. Confirm visually that the two 'outside' columns are identical. This makes the 'usually they match' rule permanent.",
        "effort": "low",
        "meta": "Jeremy's IT Lab Day 44. Wendell Odom OCG Chapters 10-11. The exam rarely trips students on Outside Local beyond the matching exercise. Remember: 'Local' = the perspective, not the private-ness. Outside Local is still public in default scenarios."
      }
    },
    {
      "id": "4.1.a.4",
      "term": "Outside Global",
      "weight": "med",
      "info": "<p>The <strong>Outside Global</strong> address is the <strong>real public IP of an external host</strong> — the address the outside host is actually configured with on the internet. This is the destination address in an outbound packet from your inside host, and the source address of replies coming back. Think: <code>8.8.8.8</code> (Google DNS), <code>1.1.1.1</code> (Cloudflare), <code>142.250.80.46</code> (Google).</p><p><strong>Reading the name:</strong> 'Outside' = the host is external. 'Global' = the address as seen on the global internet — the host's real, unchanged address. Outside Global = 'the actual public address of the external host.'</p><p><strong>In the NAT table:</strong> The <code>Outside global</code> column shows the real destination of the flow. For a host reaching Google DNS, this column will show <code>8.8.8.8</code>. Outside Global does not change during normal NAT — the router only rewrites the source (inside) addresses of outbound packets.</p><p><strong>Key mental model:</strong> In basic source NAT (what CCNA tests), the router rewrites two 'inside' addresses (local ↔ global) and leaves the two 'outside' addresses alone. So the Outside Global and Outside Local are identical in the translation table, and they are the external host's real public IP.</p><p><strong>Exam trap:</strong> If a question lists <code>8.8.8.8</code> and asks which address type it is, the answer depends on which column it sits in. If it's in the Outside Global column of the translation, it's the Outside Global. 'Global' here means 'the internet's view,' which matches the real public IP on both sides.</p>",
      "visual": { "type": "packet-flow", "params": { "nodes": ["Inside host sends to 8.8.8.8", "Dst = Outside Global", "Packet reaches 8.8.8.8", "Reply src = Outside Global"], "color": "#8b5cf6" } },
      "hack": {
        "memory": "Outside Global = real public IP of the external host. The one you'd ping from anywhere on the internet. No translation, no disguise — just the raw address. 'Outside Global' = 'outside host's genuine identity on the global internet.' Memorize: Inside has Local/Global (real/disguise). Outside has Local/Global (but usually equal).",
        "practice": "In Packet Tracer: ping 8.8.8.8 from an inside PC. Run 'show ip nat translations'. Locate the Outside Global column — it shows 8.8.8.8. Now ping a different external server (say 1.1.1.1) — a new translation entry appears with 1.1.1.1 in Outside Global. This pattern-matching fixes the column in your memory.",
        "effort": "low",
        "meta": "Jeremy's IT Lab Day 44. Wendell Odom OCG Chapters 10-11. Exam questions frequently give you the 'show ip nat translations' output and ask for the Outside Global — always the rightmost column, always the real public IP of the external host."
      }
    },
    {
      "id": "4.1.a.5",
      "term": "Memory trick",
      "weight": "high",
      "info": "<p>The four NAT address types confuse even experienced network engineers. A reliable mental model turns a guessing game into instant recall. Use this two-axis rule:</p><ul><li><strong>First word (Inside/Outside):</strong> tells you <strong>which network the host lives on</strong>. Inside = private network. Outside = public network / internet.</li><li><strong>Second word (Local/Global):</strong> tells you <strong>whose perspective you're looking at</strong>. Local = the address as seen on the inside (private) network. Global = the address as seen on the outside (public) internet.</li></ul><p><strong>Combine them:</strong></p><ul><li><strong>Inside Local</strong> = inside host, as seen on the inside = private IP (real).</li><li><strong>Inside Global</strong> = inside host, as seen on the internet = public IP (disguise).</li><li><strong>Outside Local</strong> = outside host, as seen on the inside = usually the real public IP.</li><li><strong>Outside Global</strong> = outside host, as seen on the internet = real public IP.</li></ul><p><strong>Shortcut for the exam:</strong> 'Inside Local' is the only private address. If a question shows four addresses and one is RFC 1918, that one is Inside Local. The other three are usually public (often two are identical — Outside Local = Outside Global).</p><p><strong>Alternative memory aid:</strong> 'Local is what Locally happens on the inside network.' Inside Local happens inside (private). Outside Local happens inside too — it's how inside sees outside. 'Global is what the internet sees' — which for both sides is the public IP.</p>",
      "visual": { "type": "hierarchy", "params": { "root": "4 NAT address types", "children": [{ "name": "Inside (host is private)", "children": [{ "name": "Inside Local = private IP" }, { "name": "Inside Global = public disguise" }] }, { "name": "Outside (host is public)", "children": [{ "name": "Outside Local = same public IP (usually)" }, { "name": "Outside Global = real public IP" }] }] } },
      "hack": {
        "memory": "Two-axis rule: First word = which host (Inside=private, Outside=public). Second word = whose view (Local=inside view, Global=internet view). 'Inside Local = private real IP' is the anchor — memorize this one first, the other three fall into place. Only 1 of the 4 is RFC 1918 private: Inside Local.",
        "practice": "Draw 4 labeled boxes on paper: Inside Local, Inside Global, Outside Local, Outside Global. Under each, write what IP belongs there for a flow from 192.168.1.10 to 8.8.8.8 through a NAT router using 203.0.113.5 as the public IP. Answers: Inside Local = 192.168.1.10, Inside Global = 203.0.113.5, Outside Local = 8.8.8.8, Outside Global = 8.8.8.8. Repeat 3 times until automatic.",
        "effort": "low",
        "meta": "Jeremy's IT Lab Day 44 introduces this mnemonic pattern. Wendell Odom OCG Chapters 10-11. The drag-and-drop exam question is worth easy points — practice the 4-box drill until you can fill it in under 15 seconds. Missing this wastes a guaranteed correct answer."
      }
    }
  ]
}
