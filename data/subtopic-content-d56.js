/**
 * subtopic-content-d56.js — Expanded study content for CCNA 200-301
 * Domains 5 (Security Fundamentals) and 6 (Automation and Programmability).
 *
 * Each entry: info, visual (animation config), hack (memory/practice/effort/meta).
 * Loaded via <script> tag; attaches to window.subtopicContentD56.
 */
window.subtopicContentD56 = {

  /* ══════════════════════════════════════════════════════════════
     DOMAIN 5 — SECURITY FUNDAMENTALS (15%)
  ══════════════════════════════════════════════════════════════ */

  // ── 5.1 Define key security concepts ──────────────────────────

  "5.1.a": {
    info: "<p>A <strong>threat</strong> is any potential danger that could exploit a vulnerability and cause harm to a system, network, or organization. In security terminology, the threat is the <em>who</em> or <em>what</em> that poses the danger — not the weakness itself and not the technique used to attack.</p><p>Threats fall into several categories. <strong>External threats</strong> include hackers, organized crime groups, nation-state actors, and script kiddies using automated tools. <strong>Internal threats</strong> come from disgruntled employees, careless users, or contractors with excess access. <strong>Environmental threats</strong> include natural disasters (floods, earthquakes, power outages) that can destroy infrastructure. <strong>Accidental threats</strong> arise from misconfigurations, human error, and poor change management.</p><p>The CCNA exam uses a specific vocabulary chain: <strong>Threat</strong> (the actor/danger) exploits a <strong>Vulnerability</strong> (the weakness) using an <strong>Exploit</strong> (the technique/tool) to cause an <strong>Impact</strong> (the damage). Understanding this chain is essential because scenario questions will describe a situation and ask you to identify which term applies.</p><ul><li><strong>Structured threats</strong> — highly motivated, technically skilled attackers (APTs, nation-states)</li><li><strong>Unstructured threats</strong> — script kiddies, opportunistic attackers using pre-built tools</li><li><strong>Internal threats</strong> — employees, contractors, or partners with legitimate access</li></ul>",
    visual: { type: "shield", params: { items: ["Attackers", "Malware", "Natural Disasters", "Insider Threats"], color: "#ef4444" } },
    hack: {
      memory: "Threat = the TIGER outside the fence. It wants in, but it needs a hole (vulnerability) to get through. The tiger is the danger, not the hole.",
      practice: "Create flashcards with 10 scenarios. For each, classify it as a threat, vulnerability, exploit, or mitigation. Example: 'A disgruntled employee with admin access' = threat. 'An unpatched server running SMBv1' = vulnerability. Drill until instant recognition.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51 (Network Security Fundamentals). Wendell Odom OCG Chapter 27 (Security Architectures). Most students confuse threat vs vulnerability — the exam loves 'which term best describes...' scenario questions.",
    },
    micro: [
      { id: "5.1.a.1", term: "Threat",                       def: "Potential danger/actor. The WHO or WHAT — hacker, malware, insider, disaster.", weight: "high" },
      { id: "5.1.a.2", term: "Threat categories",            def: "External (hackers, nation-state), internal (employees), environmental (disasters), accidental (human error).", weight: "med" },
      { id: "5.1.a.3", term: "Attack chain vocab",           def: "Threat exploits Vulnerability using Exploit → Impact. Know each term's role.", weight: "high" }
    ]
  },

  "5.1.b": {
    info: "<p>A <strong>vulnerability</strong> is a weakness or flaw in a system, software, configuration, or process that can be exploited by a threat actor. Vulnerabilities are <em>passive</em> — they exist whether or not anyone exploits them. They represent the gap in your defenses that an attacker could use to gain unauthorized access or cause damage.</p><p>Vulnerabilities exist at every layer of the network stack:</p><ul><li><strong>Software vulnerabilities</strong> — unpatched operating systems, outdated firmware, known CVEs (Common Vulnerabilities and Exposures) in applications</li><li><strong>Configuration vulnerabilities</strong> — default credentials left unchanged, open ports with no ACLs, misconfigured firewalls, unnecessary services running</li><li><strong>Design vulnerabilities</strong> — weak encryption algorithms (WEP, DES), protocols that transmit in cleartext (Telnet, HTTP, SNMPv1/v2c)</li><li><strong>Human vulnerabilities</strong> — weak passwords, susceptibility to social engineering, lack of security awareness training</li><li><strong>Physical vulnerabilities</strong> — unlocked server rooms, exposed console ports, unmonitored wiring closets</li></ul><p>The relationship between vulnerabilities and risk is direct: <strong>Risk = Threat x Vulnerability x Impact</strong>. Reducing vulnerabilities (through patching, hardening, and proper configuration) is the most controllable way to reduce overall risk. The CCNA exam frequently pairs vulnerabilities with their specific mitigations — if you know the weakness, you should immediately know the fix.</p>",
    visual: { type: "shield", params: { items: ["Unpatched Software", "Default Credentials", "Open Ports", "Misconfigurations"], color: "#f59e0b" } },
    hack: {
      memory: "Vulnerability = the HOLE in the fence. The tiger (threat) gets in through the hole. The hole exists even if no tiger ever comes — it's still a weakness.",
      practice: "Run a mental audit of a default Cisco switch config. List 5 vulnerabilities: no enable secret, Telnet enabled, no port security, no DHCP snooping, no banners. For each, write the hardening command that fixes it.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51 (Network Security Fundamentals). Wendell Odom OCG Chapter 27. Cisco pairs vulnerability with mitigation in exam questions — know common weakness-fix pairs cold.",
    },
    micro: [
      { id: "5.1.b.1", term: "Vulnerability",                def: "Weakness/flaw (passive). Exists until mitigated. NOT the attacker — the hole the attacker uses.", weight: "high" },
      { id: "5.1.b.2", term: "Common vulnerability types",   def: "Software (unpatched), config (defaults, open ports), design (weak crypto), human (weak passwords), physical (open rooms).", weight: "high" },
      { id: "5.1.b.3", term: "Risk formula",                 def: "Risk = Threat × Vulnerability × Impact. Reducing vulnerabilities is the most controllable factor.", weight: "med" }
    ]
  },

  "5.1.c": {
    info: "<p>An <strong>exploit</strong> is the specific technique, tool, code, or method used to take advantage of a vulnerability. Exploits are the <em>action</em> that bridges the gap between a vulnerability existing and actual damage occurring. Without an exploit, a vulnerability is just a theoretical weakness; the exploit makes it a real attack.</p><p>Exploits take many forms depending on the vulnerability they target:</p><ul><li><strong>Buffer overflow</strong> — sending more data than a program's buffer can hold, overwriting adjacent memory to inject malicious code or crash the application. This is one of the oldest and most dangerous exploit classes.</li><li><strong>SQL injection</strong> — inserting malicious SQL commands into web form inputs to manipulate backend databases, extract data, or bypass authentication</li><li><strong>Cross-site scripting (XSS)</strong> — injecting malicious scripts into web pages viewed by other users</li><li><strong>Brute-force attacks</strong> — systematically trying every possible password combination until the correct one is found</li><li><strong>Zero-day exploits</strong> — attacks against vulnerabilities that have no patch available yet, making them extremely dangerous</li></ul><p>The attack lifecycle follows a predictable chain: a <strong>threat actor</strong> identifies a <strong>vulnerability</strong>, develops or obtains an <strong>exploit</strong> to target it, and uses it to cause <strong>impact</strong>. Understanding this chain helps you identify where to apply mitigations — you can reduce threats (access controls), reduce vulnerabilities (patching, hardening), or disrupt exploits (IPS, firewalls).</p>",
    visual: { type: "packet-flow", params: { nodes: ["Attacker", "Exploit Code", "Vulnerability", "Compromised System"], color: "#ef4444" } },
    hack: {
      memory: "Exploit = the LOCKPICK that fits the hole. Threat has the lockpick, vulnerability is the lock, exploit is the action of picking it. Always the ACTION, never the weakness.",
      practice: "Map three attack chains on paper: 1) Hacker -> unpatched Apache -> buffer overflow -> web server compromised. 2) Phisher -> untrained user -> fake login page -> credentials stolen. 3) Insider -> excessive privileges -> data exfiltration -> IP theft. Label each element as threat/vulnerability/exploit/impact.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. The exam tests the attack lifecycle — exploit is always the action/technique, never the weakness itself. Buffer overflow is the most commonly cited exploit example.",
    },
    micro: [
      { id: "5.1.c.1", term: "Exploit",                      def: "The technique/tool/code to take advantage of a vulnerability. ACTION, not weakness.", weight: "high" },
      { id: "5.1.c.2", term: "Buffer overflow",              def: "Most commonly cited exploit. Send more data than buffer holds → overwrite memory → execute malicious code.", weight: "high" },
      { id: "5.1.c.3", term: "Zero-day exploit",             def: "Attack against a vulnerability with NO patch available. Extremely dangerous — no defense exists yet.", weight: "high" },
      { id: "5.1.c.4", term: "SQL injection / XSS",          def: "Web-app exploits. SQL: inject SQL commands via form inputs. XSS: inject scripts executed by other users' browsers.", weight: "med" }
    ]
  },

  "5.1.d": {
    info: "<p><strong>Mitigation</strong> (also called a countermeasure or control) is any action, device, procedure, or technique that reduces the likelihood or impact of a security threat. Mitigation does not eliminate risk entirely — it reduces it to an <strong>acceptable level</strong>. The goal is to make attacks more difficult, more expensive, or less rewarding for the attacker.</p><p>Mitigations are categorized by how they work:</p><ul><li><strong>Preventive controls</strong> — stop attacks before they happen: firewalls, ACLs, port security, encryption, strong authentication</li><li><strong>Detective controls</strong> — identify attacks in progress or after the fact: IDS, syslog, SNMP monitoring, security cameras</li><li><strong>Corrective controls</strong> — fix damage after an attack: backups, disaster recovery plans, incident response procedures</li><li><strong>Deterrent controls</strong> — discourage attackers from attempting: login banners, security cameras, legal warnings</li></ul><p>For the CCNA, specific mitigations map to specific threats:</p><ul><li>Social engineering/phishing → <strong>user awareness training</strong></li><li>Unauthorized access → <strong>AAA (RADIUS/TACACS+), ACLs, port security</strong></li><li>ARP spoofing → <strong>Dynamic ARP Inspection (DAI)</strong></li><li>Rogue DHCP → <strong>DHCP snooping</strong></li><li>Eavesdropping → <strong>encryption (IPsec VPN, SSH, WPA2/WPA3)</strong></li><li>Brute force → <strong>login block-for, account lockout, MFA</strong></li></ul>",
    visual: { type: "shield", params: { items: ["Patching", "Firewalls", "ACLs", "Encryption", "Training"], color: "#10b981" } },
    hack: {
      memory: "Mitigation = MENDING the fence. Patch the holes, add barbed wire, post guards. Each attack type has a specific best mitigation — learn the pairs.",
      practice: "Build a two-column flashcard deck: left side = attack type, right side = best Cisco mitigation. Phishing = training. ARP spoof = DAI. Rogue DHCP = DHCP snooping. Brute force = login block-for. Drill until automatic.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. Cisco loves 'which is the best mitigation for X?' questions. The answer is always the most SPECIFIC control, not a general one.",
    },
    micro: [
      { id: "5.1.d.1", term: "Mitigation / Countermeasure",  def: "Action reducing likelihood/impact of threat. Reduces risk to acceptable level — doesn't eliminate.", weight: "high" },
      { id: "5.1.d.2", term: "Control categories",           def: "Preventive (firewall, ACL), Detective (IDS, syslog), Corrective (backup, IR), Deterrent (banner, cameras).", weight: "high" },
      { id: "5.1.d.3", term: "ARP spoofing → DAI",           def: "Dynamic ARP Inspection uses DHCP snooping binding table to validate ARP replies.", weight: "high" },
      { id: "5.1.d.4", term: "Rogue DHCP → DHCP snooping",   def: "Only trusted ports (toward legitimate server) can send DHCP offers. Blocks rogue servers.", weight: "high" },
      { id: "5.1.d.5", term: "Phishing → training",          def: "Technology can't fully defend. User awareness training is the primary countermeasure.", weight: "high" }
    ]
  },

  "5.1.e": {
    info: "<p><strong>Social engineering</strong> is the broader category of attacks that manipulate human psychology to bypass security controls. Rather than attacking technology, social engineers exploit trust, fear, urgency, and helpfulness to trick people into revealing information or performing actions that compromise security. <strong>Phishing</strong> is the most common and most tested form of social engineering.</p><p>Key social engineering attack types for the CCNA:</p><ul><li><strong>Phishing</strong> — mass emails impersonating trusted entities (banks, IT department) with malicious links or attachments designed to steal credentials or install malware</li><li><strong>Spear phishing</strong> — targeted phishing aimed at a specific individual or organization, using personalized information to increase credibility</li><li><strong>Whaling</strong> — spear phishing specifically targeting executives (the 'big fish') with high-value access</li><li><strong>Vishing</strong> — voice phishing via phone calls, often impersonating IT support or authority figures</li><li><strong>Smishing</strong> — SMS/text message phishing with malicious links</li><li><strong>Pretexting</strong> — creating a fabricated scenario to manipulate the target (e.g., 'I'm from IT, I need your password to fix your account')</li><li><strong>Tailgating/piggybacking</strong> — physically following an authorized person through a secured door without badging in</li></ul><p>The critical exam point: <strong>user awareness training is the primary mitigation for social engineering</strong>. Technical controls (spam filters, URL filtering) help but cannot stop a well-crafted social engineering attack. The human is both the target and the defense — trained users become security sensors rather than security holes.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Attacker", "Fake Email", "Victim Clicks", "Credentials Stolen"], color: "#f59e0b" } },
    hack: {
      memory: "PHISHing = fishing for passwords. SPEAR phishing = aimed at one fish. WHALING = going after the big fish (CEO). VISHING = Voice phishing. SMISHING = SMS phishing. Each variant starts with a clue in the name.",
      practice: "Review 5 real phishing examples at phishingquiz.withgoogle.com. For each, identify: the urgency trigger, the spoofed sender, the suspicious URL, and the action requested. Then classify each as phishing, spear phishing, or whaling.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. If the exam asks 'what is the best defense against phishing?' the answer is ALWAYS user awareness training — not a firewall, not an IPS.",
    },
    micro: [
      { id: "5.1.e.1", term: "Social engineering",           def: "Manipulating humans to reveal credentials or bypass security. Bypasses technical controls entirely.", weight: "high" },
      { id: "5.1.e.2", term: "Phishing",                     def: "Email deception attack. Fake login pages, malicious attachments, urgency tactics.", weight: "high" },
      { id: "5.1.e.3", term: "Spear phishing",               def: "Targeted phishing aimed at specific individual/org. More research, more convincing than bulk phishing.", weight: "med" },
      { id: "5.1.e.4", term: "User awareness training",      def: "Primary defense against social engineering. Technology alone cannot stop it.", weight: "high" }
    ]
  },

  "5.1.f": {
    info: "<p><strong>Denial of Service (DoS)</strong> attacks overwhelm a target system with traffic or requests, consuming its resources (bandwidth, CPU, memory, connection tables) until it can no longer serve legitimate users. The goal is not to steal data but to make a service <strong>unavailable</strong>.</p><p>A <strong>single-source DoS</strong> originates from one attacker. A <strong>Distributed DoS (DDoS)</strong> uses hundreds or thousands of compromised systems (a <strong>botnet</strong>) to generate attack traffic simultaneously, making it exponentially harder to block because the traffic comes from many different legitimate-looking IP addresses.</p><p>Common DoS/DDoS attack types:</p><ul><li><strong>SYN flood</strong> — sends thousands of TCP SYN packets without completing the 3-way handshake, filling the target's connection table with half-open connections</li><li><strong>UDP flood</strong> — overwhelms a target with UDP packets to random ports, forcing the target to respond with ICMP Destination Unreachable messages</li><li><strong>ICMP flood (ping flood/smurf)</strong> — floods the target with ICMP Echo Requests, consuming bandwidth and processing power</li><li><strong>Amplification/reflection attacks</strong> — exploits protocols like DNS or NTP where a small query generates a large response, amplifying traffic volume by 50-100x when reflected off public servers toward the victim</li><li><strong>Application-layer attacks (Layer 7)</strong> — targets specific services like HTTP with legitimate-looking requests that consume server resources (Slowloris, HTTP flood)</li></ul><p>Mitigations include <strong>rate limiting</strong>, <strong>SYN cookies</strong> (for SYN floods), <strong>blackhole routing</strong> (sending attack traffic to null), <strong>scrubbing centers</strong> (cloud services like Cloudflare or Akamai that filter attack traffic), and <strong>ingress filtering</strong> (BCP38/RFC 2827 to prevent IP spoofing at network edges).</p>",
    visual: { type: "packet-flow", params: { nodes: ["Botnet (1000s)", "Flood Traffic", "Target Server", "Legitimate Users Blocked"], color: "#ef4444" } },
    hack: {
      memory: "DoS = one person blocking a doorway. DDoS = a flash mob blocking every entrance. The 'D' means DISTRIBUTED — many zombies (botnet) attacking at once. SYN flood = most common type.",
      practice: "In Packet Tracer, configure 'ip tcp intercept' or rate limiting on a router to mitigate SYN floods. Also configure 'no ip directed-broadcast' to prevent smurf attacks. These are real Cisco mitigations.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. Know DoS vs DDoS (single vs distributed), SYN flood mechanics, and that DDoS requires distributed mitigation (scrubbing centers, not just ACLs).",
    },
    micro: [
      { id: "5.1.f.1", term: "DoS",                          def: "Denial of Service. Overwhelms target from a single source. Blockable by ACL or firewall rule.", weight: "high" },
      { id: "5.1.f.2", term: "DDoS",                         def: "Distributed DoS. Many sources (botnet) attack simultaneously. Needs scrubbing centers/CDN defense.", weight: "high" },
      { id: "5.1.f.3", term: "SYN flood",                    def: "Flood TCP SYNs without completing handshake. Target's half-open connection queue exhausts.", weight: "high" },
      { id: "5.1.f.4", term: "Reflection/amplification",     def: "Attacker spoofs victim's IP in queries to open servers. Servers reply to victim with large responses.", weight: "med" }
    ]
  },

  "5.1.g": {
    info: "<p>A <strong>Man-in-the-Middle (MITM)</strong> attack occurs when an attacker secretly positions themselves between two communicating parties, intercepting and potentially altering traffic without either party's knowledge. The attacker can eavesdrop on confidential data, modify messages in transit, or inject malicious content.</p><p>The two primary Layer 2 MITM attacks tested on the CCNA are:</p><ul><li><strong>ARP spoofing/poisoning</strong> — the attacker sends gratuitous ARP replies to the victim claiming that the attacker's MAC address is associated with the default gateway's IP address. The victim's ARP cache gets poisoned, and all traffic intended for the gateway now flows through the attacker. The attacker forwards it onward so the connection still works, making the attack invisible to the user. Mitigation: <strong>Dynamic ARP Inspection (DAI)</strong>.</li><li><strong>DHCP spoofing</strong> — the attacker sets up a rogue DHCP server on the network that responds to DHCP Discover messages faster than the legitimate server. The rogue server assigns itself as the default gateway, DNS server, or both, redirecting all traffic through the attacker. Mitigation: <strong>DHCP snooping</strong> (only allows DHCP server messages on trusted ports).</li></ul><p>Other MITM variants include <strong>DNS spoofing</strong> (poisoning DNS responses to redirect users to fake websites) and <strong>SSL stripping</strong> (downgrading HTTPS connections to HTTP). The common thread is that the attacker intercepts the communication channel itself. Encryption (HTTPS, IPsec, SSH) is the strongest general mitigation because even if traffic is intercepted, it cannot be read or modified.</p>",
    visual: { type: "handshake", params: { leftLabel: "Client", rightLabel: "Server", steps: ["ARP Request (Who has GW?) ->", "<- Fake ARP Reply (Attacker's MAC)", "Traffic now flows through Attacker"] } },
    hack: {
      memory: "MITM = the postal worker who opens your mail, reads it, reseals it, and delivers it. You never know. ARP spoofing = lying about your address. DHCP spoofing = giving out fake directions.",
      practice: "In a Packet Tracer lab, enable DHCP snooping and DAI on a switch. Configure the server-facing port as trusted. Attempt to connect a rogue DHCP server on an untrusted port and verify it gets blocked. This ties directly into 5.7.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 51 + Day 53 (DHCP Snooping/DAI). Wendell Odom OCG Chapter 27 + Chapter 30. ARP spoofing and DHCP spoofing are THE two MITM examples Cisco tests. Study mitigations (DAI, DHCP snooping) in section 5.7.",
    },
    micro: [
      { id: "5.1.g.1", term: "Man-in-the-Middle (MITM)",     def: "Attacker inserts between two communicating parties, intercepting and/or modifying traffic.", weight: "high" },
      { id: "5.1.g.2", term: "ARP spoofing",                 def: "Attacker sends forged ARP replies. Hosts ARP-cache the attacker's MAC for other IPs. Traffic flows through attacker.", weight: "high" },
      { id: "5.1.g.3", term: "DHCP spoofing",                def: "Rogue DHCP server gives victims attacker's IP as gateway/DNS. Attacker becomes MITM for all outbound traffic.", weight: "high" },
      { id: "5.1.g.4", term: "MITM mitigations",             def: "DAI (stops ARP spoof), DHCP snooping (stops rogue DHCP), HTTPS/VPN (encrypts even if intercepted).", weight: "high" }
    ]
  },

  "5.1.h": {
    info: "<p><strong>Malware</strong> (malicious software) is any software intentionally designed to damage, disrupt, or gain unauthorized access to computer systems. The CCNA exam tests your ability to distinguish between malware types based on their <strong>propagation method</strong> and <strong>primary behavior</strong>.</p><p>Key malware types:</p><ul><li><strong>Virus</strong> — attaches itself to legitimate files or programs. Requires <strong>user action</strong> to spread (opening an infected file, running an infected program). Cannot self-replicate across the network on its own. Example: a macro virus embedded in a Word document.</li><li><strong>Worm</strong> — <strong>self-replicating</strong> malware that spreads across networks without any user interaction. Worms exploit vulnerabilities in network services to propagate automatically. They consume bandwidth and system resources as they spread. Example: the SQL Slammer worm spread to 75,000 hosts in 10 minutes.</li><li><strong>Trojan horse</strong> — disguises itself as legitimate software to trick users into installing it. Does NOT self-replicate. Once installed, the hidden payload can create backdoors, steal data, or install additional malware. Example: a fake 'free antivirus' that actually installs a keylogger.</li><li><strong>Ransomware</strong> — encrypts the victim's files and demands payment (usually cryptocurrency) for the decryption key. Modern ransomware often combines with worm capabilities for rapid spread and adds data exfiltration ('double extortion'). Example: WannaCry, which used the EternalBlue exploit to spread as a worm.</li></ul><p>Additional malware types to know: <strong>Spyware</strong> (secretly monitors user activity), <strong>Adware</strong> (displays unwanted advertisements), <strong>Rootkit</strong> (hides deep in the OS to maintain persistent access), and <strong>Keylogger</strong> (records keystrokes to capture passwords). The primary exam differentiator is <strong>how the malware spreads</strong>: worms are automatic, viruses need user action, and trojans use deception.</p>",
    visual: { type: "comparison", params: { left: { label: "Self-Spreading", items: ["Worm — network propagation", "No user action needed", "Consumes bandwidth"] }, right: { label: "User-Activated", items: ["Virus — attaches to files", "Trojan — disguised payload", "Ransomware — encrypts data"] } } },
    hack: {
      memory: "WORM = Wriggles On its own through the network (no user needed). VIRUS = Very Infected, Requires User Start. TROJAN = looks like a gift, soldiers hide inside. RANSOM = your files are kidnapped, pay to get them back.",
      practice: "Create a comparison table from memory: Malware Type | How It Spreads | User Action Required? | Primary Damage | Example. Fill it for virus, worm, trojan, and ransomware. Then add spyware and rootkit as bonus rows.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. The exam loves 'which malware type does X?' questions. The KEY differentiator is propagation: worm = automatic, virus = user action, trojan = deception.",
    },
    micro: [
      { id: "5.1.h.1", term: "Virus",                        def: "Malware that attaches to legit files. Requires USER ACTION (open file) to spread.", weight: "high" },
      { id: "5.1.h.2", term: "Worm",                         def: "Self-propagating malware. Spreads AUTOMATICALLY across network without user action. More dangerous than virus.", weight: "high" },
      { id: "5.1.h.3", term: "Trojan",                       def: "Malware disguised as legitimate software. Relies on DECEPTION. User installs willingly.", weight: "high" },
      { id: "5.1.h.4", term: "Ransomware",                   def: "Encrypts victim's files, demands payment for decryption key. Major modern threat.", weight: "high" },
      { id: "5.1.h.5", term: "Spyware / Adware",             def: "Spyware = steals data silently. Adware = pushes unwanted ads. Usually less destructive than ransomware.", weight: "med" }
    ]
  },

  // ── 5.2 Describe security program elements ────────────────────

  "5.2.a": {
    info: "<p><strong>User awareness programs</strong> educate all employees about security threats, organizational policies, and safe computing practices. This is the <strong>first line of defense</strong> against social engineering, phishing, and insider threats — because the most sophisticated firewall in the world cannot stop a user from voluntarily entering their credentials into a fake login page.</p><p>An effective security awareness program includes multiple components:</p><ul><li><strong>Regular training sessions</strong> — mandatory annual (or quarterly) training covering current threats, password hygiene, data handling, and the organization's acceptable use policy (AUP)</li><li><strong>Simulated phishing campaigns</strong> — sending fake phishing emails to employees to test their ability to recognize and report them, with follow-up training for those who click</li><li><strong>Clear reporting procedures</strong> — employees must know exactly how to report suspicious emails, lost devices, or potential security incidents (a helpdesk ticket, a dedicated email address, etc.)</li><li><strong>Acceptable Use Policies (AUP)</strong> — written policies defining what employees can and cannot do with company systems, including personal device use, social media, and data sharing</li></ul><p>The goal is to transform every user from a potential vulnerability into a <strong>security sensor</strong> who can recognize and report threats. The CCNA exam consistently frames user awareness as THE primary mitigation for social engineering attacks. If a question describes a phishing attack and asks for the best defense, the answer is training — not a technical control.</p>",
    visual: { type: "hierarchy", params: { root: "Security Program", children: [{ name: "User Awareness", children: [{ name: "Phishing Simulations" }, { name: "Policy Training" }, { name: "Reporting Procedures" }] }] } },
    hack: {
      memory: "Users are the weakest link AND the first line of defense. Awareness training turns liabilities into sensors. Technology can't fix a user who willingly hands over their password.",
      practice: "Download a sample Acceptable Use Policy template (SANS has free ones). Read it and identify 5 common violations: personal USB drives, sharing passwords, clicking unknown links, tailgating, using public WiFi for work. These show up as exam scenarios.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. Cisco ALWAYS frames user awareness as the answer for social engineering mitigation. If the question is about phishing defense, think training first, technology second.",
    },
    micro: [
      { id: "5.2.a.1", term: "User awareness",               def: "General security education for all users. Phishing recognition, password hygiene, acceptable use.", weight: "high" },
      { id: "5.2.a.2", term: "Awareness beats tech for SE",  def: "Social engineering bypasses technology. Trained users are the primary defense.", weight: "high" },
      { id: "5.2.a.3", term: "Simulated phishing",           def: "Organizations send fake phishing emails to test users. Those who click get additional training.", weight: "med" }
    ]
  },

  "5.2.b": {
    info: "<p><strong>Security training</strong> goes beyond basic awareness by providing <strong>role-specific technical education</strong> to IT staff, security teams, and personnel with elevated access. While awareness tells everyone 'the stove is hot,' training teaches the right people <em>how to put out fires</em>.</p><p>Security training includes:</p><ul><li><strong>Incident response training</strong> — teaching IT staff how to identify, contain, eradicate, and recover from security incidents following established runbooks</li><li><strong>Secure configuration and hardening</strong> — training network engineers on proper device hardening (disabling unused services, SSH over Telnet, strong passwords, ACLs)</li><li><strong>Compliance training</strong> — role-specific requirements for PCI-DSS (payment handling), HIPAA (healthcare data), SOX (financial reporting), or GDPR (data privacy)</li><li><strong>Tabletop exercises</strong> — simulated incident scenarios where teams walk through their response procedures without actual system changes</li><li><strong>Penetration testing awareness</strong> — understanding how attackers think, so defenders can anticipate and prevent attack techniques</li></ul><p>The key distinction for the exam: <strong>awareness</strong> is for ALL employees (general security hygiene), while <strong>training</strong> is for technical/specialized staff (specific skills and procedures). Training should be ongoing and include hands-on practice — a one-time presentation is awareness, not training. Organizations should also conduct <strong>regular assessments</strong> to verify that training is effective and being retained.</p>",
    visual: { type: "hierarchy", params: { root: "Security Training", children: [{ name: "IT Staff", children: [{ name: "Incident Response" }, { name: "Hardening" }] }, { name: "All Employees", children: [{ name: "Policy Compliance" }, { name: "Phishing Tests" }] }] } },
    hack: {
      memory: "Awareness = knowing the stove is hot (everyone). Training = knowing HOW to put out the fire (technical staff). Both matter, but the exam tests which is which.",
      practice: "Create two lists: 'Awareness topics' (phishing recognition, password hygiene, clean desk, reporting) and 'Training topics' (incident response, hardening configs, forensics, pen testing). If an exam question specifies IT staff, pick from the training list.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. The exam distinguishes awareness (general users) from training (technical/role-specific). If the question specifies IT staff or incident response, the answer is training.",
    },
    micro: [
      { id: "5.2.b.1", term: "Training (role-specific)",     def: "Technical/role-based skill building. IT admins, incident responders. Deeper than awareness.", weight: "high" },
      { id: "5.2.b.2", term: "Awareness vs training",        def: "Awareness = general users (recognize phishing). Training = IT/technical staff (how to respond to incidents).", weight: "high" }
    ]
  },

  "5.2.c": {
    info: "<p><strong>Physical access controls</strong> prevent unauthorized individuals from physically entering secure areas like server rooms, wiring closets, and data centers. Physical security is the foundation of all security — if an attacker can physically touch a device, most logical security controls can be bypassed.</p><p>Key physical access controls for the CCNA:</p><ul><li><strong>Badge readers / proximity cards</strong> — use RFID or smart card technology to authenticate users at entry points. Each badge is uniquely assigned, creating an audit trail of who entered and when.</li><li><strong>Mantraps (security vestibules)</strong> — small rooms with two interlocking doors where only one door can be open at a time. A person enters through door 1, it closes and locks, then door 2 opens after authentication. This <strong>specifically prevents tailgating</strong> — an unauthorized person following an authorized person through a door.</li><li><strong>Biometric scanners</strong> — fingerprint readers, iris scanners, or facial recognition for high-security areas. These are 'something you are' and cannot be shared or stolen like a badge.</li><li><strong>Security guards</strong> — human verification at entry points, visitor escort policies, and response capability. Guards can make judgment calls that automated systems cannot.</li><li><strong>Key locks and cipher locks</strong> — mechanical or combination locks for lower-security areas like wiring closets</li></ul><p>Physical access controls follow a layered approach (defense in depth): perimeter fence → building entrance (guard + badge) → server room (mantrap + biometric) → individual rack (cage lock). Each layer adds difficulty for an unauthorized person.</p>",
    visual: { type: "handshake", params: { leftLabel: "Person", rightLabel: "Secure Area", steps: ["Badge Scan ->", "<- Door 1 Opens", "Enter Vestibule, Door 1 Closes ->", "<- Door 2 Opens (Authenticated)"] } },
    hack: {
      memory: "MANTRAP = MAN gets TRAPPED between two doors. Like an airlock — you can't piggyback through. It specifically defeats TAILGATING.",
      practice: "Draw a layered physical security diagram for a data center: perimeter fence with gate → badge reader at building entrance → mantrap at server room → biometric at rack cage. Label what each control prevents.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. Mantrap/security vestibule is a favorite exam question — it specifically prevents tailgating. Know the term 'security vestibule' as Cisco's preferred modern term.",
    },
    micro: [
      { id: "5.2.c.1", term: "Security vestibule (mantrap)", def: "Two-door airlock. One opens only after the other closes. Prevents tailgating. Cisco's preferred modern term.", weight: "high" },
      { id: "5.2.c.2", term: "Tailgating",                   def: "Unauthorized person follows authorized person through access-controlled door. Security vestibule stops this.", weight: "high" },
      { id: "5.2.c.3", term: "Badge access",                 def: "Physical ID cards (prox, smartcard). Common primary physical auth control.", weight: "med" }
    ]
  },

  "5.2.d": {
    info: "<p><strong>Surveillance systems</strong> monitor physical spaces to detect, deter, and record unauthorized activity. They serve three security functions simultaneously: <strong>deterrence</strong> (people behave differently when they know they're being watched), <strong>detection</strong> (catching incidents in progress), and <strong>forensics</strong> (providing evidence for investigation after events).</p><p>Key surveillance components:</p><ul><li><strong>CCTV cameras (Closed-Circuit Television)</strong> — provide continuous video monitoring and recording of sensitive areas. Modern IP cameras can integrate with analytics software for motion detection, facial recognition, and behavior analysis. Coverage should include all entry/exit points, server rooms, and sensitive areas.</li><li><strong>Motion sensors</strong> — detect physical movement in restricted areas and trigger alerts or alarms. Useful for areas that should have no activity during certain hours (server rooms at night, wiring closets on weekends).</li><li><strong>Video analytics</strong> — AI-powered systems that analyze camera feeds in real-time to detect anomalies like unauthorized access, loitering, or removed objects</li><li><strong>Access logs</strong> — electronic records from badge systems showing who entered which areas and when, providing a digital audit trail</li></ul><p>In the context of security control categories: surveillance systems are primarily <strong>detective controls</strong> (they identify when something happens) and <strong>deterrent controls</strong> (they discourage bad behavior). They are NOT preventive controls — a camera cannot physically stop someone from entering an area, only record that they did.</p>",
    visual: { type: "shield", params: { items: ["CCTV Cameras", "Motion Sensors", "Deterrence", "Detection", "Forensics"], color: "#6366f1" } },
    hack: {
      memory: "Cameras have three D's: Deter, Detect, Document (forensics). Motion sensors ALERT. Together they're the eyes and ears of physical security.",
      practice: "Classify each physical security control as preventive, detective, deterrent, or corrective. Cameras = detective + deterrent. Locks = preventive. Guards = preventive + detective. Backup generator = corrective. The exam tests these categories.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. Low exam depth on surveillance specifics, but know that cameras and motion sensors are detective/deterrent controls — they record and discourage but don't physically prevent.",
    },
    micro: [
      { id: "5.2.d.1", term: "Surveillance cameras",         def: "Detective + deterrent control. Records evidence, discourages casual wrongdoing. Doesn't physically stop anything.", weight: "low" },
      { id: "5.2.d.2", term: "Motion sensors",               def: "Detective control. Triggers alert/recording when movement detected. Used in secure areas after hours.", weight: "low" }
    ]
  },

  "5.2.e": {
    info: "<p><strong>Environmental controls</strong> protect network equipment from physical and environmental threats beyond human intrusion — theft, fire, heat, power loss, and accidental damage. These controls ensure the physical infrastructure supporting the network remains operational and secure.</p><p>Key environmental controls:</p><ul><li><strong>Cable locks</strong> — physically secure laptops, monitors, and portable equipment to desks or racks using steel cables and keyed or combination locks. Prevents opportunistic theft in shared spaces.</li><li><strong>Equipment cages and locked cabinets</strong> — metal enclosures that restrict physical access to network gear (switches, routers, patch panels) in shared spaces like open offices or multi-tenant data centers. Only authorized personnel have keys.</li><li><strong>Fire suppression</strong> — FM-200 or Novec 1230 clean agent systems that extinguish fires without damaging electronics (unlike water sprinklers). Data centers require specialized suppression that won't destroy the equipment it's protecting.</li><li><strong>HVAC (Heating, Ventilation, Air Conditioning)</strong> — maintains proper temperature (64-75F / 18-24C) and humidity (40-60%) to prevent equipment overheating and component failure</li><li><strong>UPS (Uninterruptible Power Supply)</strong> — provides battery backup during power outages, giving enough time to safely shut down equipment or switch to generator power</li><li><strong>Cable management</strong> — organized cable routing prevents accidental disconnections, improves airflow for cooling, and makes troubleshooting easier</li></ul><p>Environmental controls are classified as <strong>physical/environmental security</strong> in the security framework. They protect the availability pillar of the CIA triad — ensuring equipment stays powered, cooled, and physically intact.</p>",
    visual: { type: "shield", params: { items: ["Cable Locks", "Equipment Cages", "Fire Suppression", "HVAC", "UPS"], color: "#8b5cf6" } },
    hack: {
      memory: "Environmental = protecting gear from the ENVIRONMENT: heat (HVAC), fire (suppression), power loss (UPS), theft (cages/locks). If it's not a human attack, it's an environmental threat.",
      practice: "Mental walkthrough of a proper server room: locked door (physical access), equipment cages (per-tenant), HVAC (cooling), FM-200 (fire), UPS + generator (power), cameras (surveillance), cable management (organization). List all controls.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 51. Wendell Odom OCG Chapter 27. Low exam weight but testable. Cable locks and equipment cages are the most likely to appear. Know they fall under physical/environmental controls, not logical/technical.",
    },
    micro: [
      { id: "5.2.e.1", term: "Equipment locks / cages",      def: "Physical controls protecting hardware. Rack locks, data hall cages. Physical category (not logical/technical).", weight: "low" },
      { id: "5.2.e.2", term: "Cable locks",                  def: "Secure laptops/equipment to fixed objects. Low-tech but effective anti-theft.", weight: "low" }
    ]
  },

  // ── 5.3 Configure and verify device access control ────────────

  "5.3.a": {
    info: "<p>The <code>enable password</code> command sets a password for entering <strong>privileged EXEC mode</strong> (the <code>#</code> prompt). However, this password is stored in the running configuration as <strong>clear text (Type 0)</strong>, making it visible to anyone who can view the config with <code>show running-config</code>.</p><p>This is a critical security flaw — if the config is backed up to a TFTP server, emailed, or viewed by an unauthorized person, the password is immediately compromised. For this reason, <strong>never use <code>enable password</code> in production</strong>. Always use <code>enable secret</code> instead.</p><p>Key facts about <code>enable password</code>:</p><ul><li>Stored as <strong>Type 0 (plaintext)</strong> in the config — completely readable</li><li>Even with <code>service password-encryption</code> enabled, it only gets Type 7 obfuscation, which is trivially reversible</li><li>If both <code>enable password</code> and <code>enable secret</code> are configured on the same device, <strong><code>enable secret</code> always takes precedence</strong></li><li>The <code>enable password</code> is effectively ignored when <code>enable secret</code> exists — you cannot use either password interchangeably</li></ul><p>The exam frequently presents a <code>show running-config</code> output where both commands appear and asks which password the device will accept. The answer is always the <code>enable secret</code> password.</p>",
    visual: { type: "comparison", params: { left: { label: "enable password", items: ["Stored in PLAINTEXT", "Visible in show run", "Type 0 — no hash", "NEVER use this"] }, right: { label: "enable secret", items: ["Stored as MD5 hash", "Hidden in show run", "Type 5 — hashed", "ALWAYS use this"] } } },
    hack: {
      memory: "enable PASSWORD = PLAIN to see (plaintext, Type 0). enable SECRET = Securely encrypted (hashed, Type 5). If both exist, SECRET always wins. If you see 'password' in a config, that's the wrong one.",
      practice: "In Packet Tracer: configure both <code>enable password cisco</code> and <code>enable secret cisco123</code>. Run <code>show running-config | include enable</code>. Notice 'cisco' is visible but 'cisco123' is a hash. Try logging in — only 'cisco123' works.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 4 (CLI basics) + Day 52 (Device Security). Wendell Odom OCG Chapter 6 (CLI config) + Chapter 27. The exam tests that enable secret ALWAYS wins over enable password. Know Type 0 = plaintext.",
    },
    micro: [
      { id: "5.3.a.1", term: "enable secret",                def: "Sets priv-exec password with HASH (Type 5 MD5 or Type 8/9). Preferred. Cannot be reversed.", weight: "high" },
      { id: "5.3.a.2", term: "enable password",              def: "LEGACY. Stores password in plaintext (Type 0) unless service password-encryption. WEAK.", weight: "high" },
      { id: "5.3.a.3", term: "secret ALWAYS wins",           def: "If both 'enable secret' and 'enable password' are set, 'secret' is used. 'password' is ignored.", weight: "high" },
      { id: "5.3.a.4", term: "Type 0 = plaintext",           def: "Unencrypted in running config. Visible with 'show running-config'. Avoid in production.", weight: "high" }
    ]
  },

  "5.3.b": {
    info: "<p>The <code>enable secret</code> command sets a <strong>hashed password</strong> for privileged EXEC mode. By default, it uses <strong>MD5 hashing (Type 5)</strong>, which is a one-way hash — the original password cannot be recovered from the hash stored in the config. This is fundamentally more secure than <code>enable password</code>.</p><p>Password type numbers you must know for the exam:</p><ul><li><strong>Type 0</strong> — plaintext, no protection at all (<code>enable password</code>)</li><li><strong>Type 5</strong> — MD5 hash, used by <code>enable secret</code> by default. Recognizable by the <code>$1$</code> prefix in the hash string</li><li><strong>Type 7</strong> — Vigenere cipher, applied by <code>service password-encryption</code>. Trivially reversible — NOT secure</li><li><strong>Type 8</strong> — PBKDF2-SHA-256, stronger than MD5. Available in newer IOS versions</li><li><strong>Type 9</strong> — scrypt hash, the strongest option. Requires significant computational resources to crack</li></ul><p>When you configure <code>enable secret MyPassword</code>, the device immediately hashes the password and stores only the hash. Even an administrator viewing <code>show running-config</code> cannot see the original password — they see something like <code>enable secret 5 $1$mERr$hx5rVt7rPNoS4wqbXKX7m0</code>.</p><p>If both <code>enable password</code> and <code>enable secret</code> are configured, <strong><code>enable secret</code> always takes precedence</strong>. The <code>enable password</code> is effectively overridden and ignored. Cisco recommends using only <code>enable secret</code> and removing any <code>enable password</code> configuration.</p>",
    visual: { type: "state-machine", params: { states: ["User EXEC (>)", "Privileged EXEC (#)"], active: 1, transitions: true } },
    hack: {
      memory: "Type 0 = plaintext. Type 5 = MD5 (enable secret default). Type 7 = Vigenere (reversible junk). Type 8 = PBKDF2. Type 9 = scrypt (strongest). Remember: 0-5-7-8-9, and 5 is the one that matters most.",
      practice: "Configure <code>enable secret MyPass123</code> in Packet Tracer. Then <code>show running-config | include enable</code> and observe the Type 5 hash starting with <code>$1$</code>. Try to log in with 'MyPass123' — it works despite the hash.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 4 + Day 52. Wendell Odom OCG Chapter 6 + Chapter 27. Memorize Type numbers cold: 0 = plain, 5 = MD5, 7 = Vigenere (weak), 8 = PBKDF2, 9 = scrypt. The exam loves asking which type is which.",
    },
    micro: [
      { id: "5.3.b.1", term: "Type 0",                       def: "Plaintext. No encryption. Visible in running-config. AVOID.", weight: "high" },
      { id: "5.3.b.2", term: "Type 5",                       def: "MD5 hash. One-way, but MD5 is now considered weak by modern standards.", weight: "high" },
      { id: "5.3.b.3", term: "Type 7",                       def: "Vigenère cipher. REVERSIBLE — can be decrypted easily with online tools. Only applied by 'service password-encryption'.", weight: "high" },
      { id: "5.3.b.4", term: "Type 8",                       def: "PBKDF2 with SHA-256. Strong modern hash. Use when available.", weight: "high" },
      { id: "5.3.b.5", term: "Type 9",                       def: "scrypt. Strongest current Cisco password hash. Memory-hard, resistant to GPU attacks.", weight: "high" }
    ]
  },

  "5.3.c": {
    info: "<p>The <strong>console port</strong> (<code>line console 0</code> or <code>line con 0</code>) provides direct physical access to the device via a <strong>rollover cable</strong> (RJ-45 to DB-9 or USB). This is the only way to access a device during initial setup or password recovery. Because it requires physical proximity, it's considered an out-of-band management method.</p><p>Securing the console port requires two steps — setting a password AND enabling login enforcement:</p><ul><li><code>line console 0</code> — enter console line configuration mode</li><li><code>password [password]</code> — set the console password (simple line password method)</li><li><code>login</code> — tells the device to actually prompt for the password. <strong>Without this command, the password is configured but never enforced</strong></li><li><code>exec-timeout [minutes] [seconds]</code> — automatically disconnects idle sessions (e.g., <code>exec-timeout 5 0</code> = 5 minutes). Default is 10 minutes. Setting <code>exec-timeout 0 0</code> disables the timeout (never recommended)</li></ul><p>The better approach uses <strong>local authentication</strong> instead of a simple line password:</p><ul><li><code>line console 0</code></li><li><code>login local</code> — authenticates against the local username database instead of a line password</li><li><code>exec-timeout 5 0</code></li></ul><p>Using <code>login local</code> provides individual accountability — each admin has their own username, so you can track who accessed the device via console. A simple line password is shared among all admins, eliminating accountability.</p>",
    visual: { type: "handshake", params: { leftLabel: "Admin (Console)", rightLabel: "Switch/Router", steps: ["Physical Cable Connected ->", "<- Password Prompt", "Correct Password ->", "<- Privileged Access"] } },
    hack: {
      memory: "Console = PHYSICAL access. Line console 0 = the ONE physical port. Two steps: set password + enable login. Forgetting 'login' = password exists but is never asked for. Like installing a lock but never closing the door.",
      practice: "In Packet Tracer: <code>line con 0</code> → <code>password cisco</code> → <code>login</code> → <code>exec-timeout 5 0</code>. Disconnect and reconnect — verify the prompt appears. Then switch to <code>login local</code> with a username and test.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 4 (CLI) + Day 52 (Device Security). Wendell Odom OCG Chapter 6 + Chapter 27. The exam tests exact commands. The critical 'gotcha': forgetting <code>login</code> means the password is set but never enforced.",
    },
    micro: [
      { id: "5.3.c.1", term: "Console password config",      def: "line console 0 → password [pw] → login. Critical: 'login' is required or password is NOT enforced.", weight: "high" },
      { id: "5.3.c.2", term: "'login' gotcha",               def: "Password set but no 'login' = password is ignored. Classic exam trap question.", weight: "high" },
      { id: "5.3.c.3", term: "logging synchronous",          def: "Under line console. Prevents log messages from interrupting commands being typed. QoL setting.", weight: "med" },
      { id: "5.3.c.4", term: "exec-timeout",                 def: "Auto-logout after N minutes idle. 'exec-timeout 10 0' = 10 min 0 sec. Security best practice.", weight: "med" }
    ]
  },

  "5.3.d": {
    info: "<p><strong>VTY (Virtual Teletype) lines</strong> provide remote management access to Cisco devices via Telnet or SSH. Most devices have <strong>16 VTY lines (0-15)</strong>, allowing up to 16 simultaneous remote sessions. VTY lines are purely logical — they don't correspond to physical ports.</p><p>Securing VTY lines is critical because they're accessible from anywhere on the network:</p><ul><li><code>line vty 0 15</code> — configure all 16 VTY lines at once</li><li><code>login local</code> — authenticate against the local username database (preferred over simple <code>login</code> + <code>password</code>)</li><li><code>transport input ssh</code> — restrict access to SSH only, blocking Telnet (which sends credentials in cleartext)</li><li><code>access-class [ACL] in</code> — apply an ACL to restrict which source IP addresses can connect. This is different from <code>ip access-group</code> which applies to interfaces</li><li><code>exec-timeout 5 0</code> — disconnect idle sessions after 5 minutes</li></ul><p>Critical VTY gotcha: <strong>if no password is set and no <code>login local</code> is configured on VTY lines, the device refuses ALL remote connections</strong>. The error message is 'Password required, but none set.' This is a security feature — Cisco won't allow passwordless remote access by default.</p><p>Best practice VTY hardening combines all controls: SSH only (<code>transport input ssh</code>), local authentication (<code>login local</code>), source IP restriction (<code>access-class</code>), and idle timeout (<code>exec-timeout</code>). For SSH to work, you also need a hostname, domain name, and RSA key pair configured.</p>",
    visual: { type: "handshake", params: { leftLabel: "Remote Admin", rightLabel: "Switch/Router", steps: ["SSH Connection ->", "<- Username Prompt", "Credentials Sent ->", "<- CLI Access Granted"] } },
    hack: {
      memory: "VTY = Virtual TeletYpe = remote access. 0 to 15 = 16 lines. No password on VTY = NO remote access at all (connection refused). access-CLASS (not access-group) restricts who can SSH in.",
      practice: "Full VTY hardening lab: <code>line vty 0 15</code> → <code>login local</code> → <code>transport input ssh</code> → <code>access-class 10 in</code> → <code>exec-timeout 5 0</code>. Then test: Telnet should be refused, SSH should work only from permitted IPs.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 4 + Day 52 + Day 43 (SSH). Wendell Odom OCG Chapter 6 + Chapter 27. Critical exam topic. The 'no password = no access' gotcha is tested frequently. Know access-class (VTY) vs access-group (interfaces).",
    },
    micro: [
      { id: "5.3.d.1", term: "VTY password config",          def: "line vty 0 15 → password [pw] → login (or login local). Password protects remote access.", weight: "high" },
      { id: "5.3.d.2", term: "No password = no access",      def: "If VTY has no password configured, remote login is REJECTED by default on IOS. Access denied.", weight: "high" },
      { id: "5.3.d.3", term: "access-class (VTY)",           def: "Apply ACL to VTY to restrict which IPs can connect: 'access-class [ACL] in'. Use for management IP restrictions.", weight: "high" },
      { id: "5.3.d.4", term: "access-class vs access-group", def: "access-class = VTY lines. access-group = physical interfaces. Don't confuse them.", weight: "high" }
    ]
  },

  "5.3.e": {
    info: "<p>The <strong>AUX (Auxiliary) port</strong> (<code>line aux 0</code>) was designed for <strong>out-of-band management via modem</strong>, allowing administrators to dial in remotely for management when the network is down. It is configured identically to the console port — with password, login, and exec-timeout commands.</p><p>In modern networks, the AUX port is <strong>rarely used</strong> because SSH over a dedicated management network provides better remote access. The AUX port remains on devices for backward compatibility and emergency out-of-band access scenarios (when all network connectivity is lost and a modem is the only option).</p><p>Best practices for the AUX port:</p><ul><li>If not in use: disable it with <code>line aux 0</code> → <code>no exec</code> — this prevents any EXEC sessions from being established through the port</li><li>If in use: secure it the same way as the console — <code>password</code>, <code>login</code> (or <code>login local</code>), <code>exec-timeout</code>, and <code>transport input none</code> to block network protocols</li><li>Never leave it unsecured — even though modem access is rare, an attacker with physical access could connect a device to the AUX port</li></ul><p>Comparison with other management lines: <strong>Console (con 0)</strong> = direct physical cable, used for initial setup and password recovery. <strong>AUX (aux 0)</strong> = modem dial-in, rarely used. <strong>VTY (vty 0 15)</strong> = remote network access via Telnet/SSH. All three should be secured or disabled.</p>",
    visual: { type: "comparison", params: { left: { label: "Console (con 0)", items: ["Direct physical cable", "Local access only", "Always used for setup"] }, right: { label: "AUX (aux 0)", items: ["Modem dial-in access", "Remote out-of-band", "Rarely used — disable it"] } } },
    hack: {
      memory: "AUX = AUXiliary = the backup phone line. Think old-school modem port. Best practice: <code>no exec</code> to disable it. Same security config as console if you keep it.",
      practice: "In Packet Tracer: <code>line aux 0</code> → <code>no exec</code> to disable it. Verify with <code>show line</code>. Then compare all three management methods: console, AUX, VTY — know how to secure each.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. Low exam weight but testable. Know that AUX exists, it's for modem access, and best practice is <code>no exec</code> if not in use.",
    },
    micro: [
      { id: "5.3.e.1", term: "AUX port",                     def: "Auxiliary port. Legacy dial-in modem access to router. Rarely used today. 'line aux 0' to configure.", weight: "low" },
      { id: "5.3.e.2", term: "no exec (AUX)",                def: "Best practice if AUX unused. Prevents anyone plugging in from getting exec prompt.", weight: "low" }
    ]
  },

  "5.3.f": {
    info: "<p><strong>Local user authentication</strong> creates username/password pairs stored directly on the device itself, providing individual accountability for each administrator. This is significantly more secure than shared line passwords because you can track <em>who</em> logged in, not just <em>that someone</em> logged in.</p><p>Configuration commands:</p><ul><li><code>username [name] privilege [0-15] secret [password]</code> — creates a local user with a specific privilege level and an MD5-hashed password (Type 5). Always use <code>secret</code>, never <code>password</code></li><li><code>login local</code> — configured under console/VTY lines to tell the device to authenticate against the local username database</li></ul><p>Privilege levels control what commands are available:</p><ul><li><strong>Level 0</strong> — minimal commands: <code>logout</code>, <code>enable</code>, <code>disable</code>, <code>help</code>, <code>exit</code></li><li><strong>Level 1</strong> — default user EXEC level: <code>show</code> commands, <code>ping</code>, <code>traceroute</code> (the <code>&gt;</code> prompt)</li><li><strong>Level 15</strong> — full privileged EXEC: all commands including <code>configure terminal</code> (the <code>#</code> prompt)</li><li><strong>Levels 2-14</strong> — custom levels where you can assign specific commands using <code>privilege exec level [N] [command]</code></li></ul><p>Local authentication is appropriate for small networks with few devices. For larger environments, centralized authentication via <strong>RADIUS or TACACS+</strong> (covered in 5.8) is preferred because maintaining local user databases on hundreds of devices is impractical.</p>",
    visual: { type: "hierarchy", params: { root: "Local User Database", children: [{ name: "admin (priv 15)", children: [{ name: "Full access" }] }, { name: "helpdesk (priv 5)", children: [{ name: "Limited commands" }] }, { name: "readonly (priv 1)", children: [{ name: "Show commands only" }] }] } },
    hack: {
      memory: "LOCAL = credentials stored LOCALLY on the device. <code>login local</code> = check the LOCAL database. Priv 0 = almost nothing. Priv 1 = show commands. Priv 15 = god mode. Use <code>secret</code> not <code>password</code>.",
      practice: "Lab: <code>username admin privilege 15 secret Cisco123</code> then <code>line con 0</code> → <code>login local</code>. Test login. Create a priv 1 user and verify limited commands. Try <code>configure terminal</code> from priv 1 — it should be denied.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. The exam expects <code>username X secret Y</code> (not 'password') and that <code>login local</code> activates it. Without <code>login local</code>, the local database is ignored.",
    },
    micro: [
      { id: "5.3.f.1", term: "username X secret Y",          def: "Creates local user with hashed password. Preferred over 'username X password Y' (plaintext).", weight: "high" },
      { id: "5.3.f.2", term: "login local",                  def: "On line (VTY/console) — activates local user database. Without it, local usernames are ignored.", weight: "high" },
      { id: "5.3.f.3", term: "privilege 15",                 def: "Max privilege level. 'username admin privilege 15 secret X' gives instant enable-mode access on login.", weight: "high" }
    ]
  },

  "5.3.g": {
    info: "<p>The <code>service password-encryption</code> global configuration command applies a <strong>weak Type 7 Vigenere cipher</strong> to all plaintext passwords currently in the running configuration and all future passwords set with the <code>password</code> keyword. It affects line passwords, username passwords (set with <code>password</code> instead of <code>secret</code>), and <code>enable password</code>.</p><p>What <code>service password-encryption</code> does and does NOT do:</p><ul><li><strong>DOES:</strong> Obfuscates Type 0 (plaintext) passwords to Type 7 — prevents casual shoulder-surfing when viewing the config</li><li><strong>DOES NOT:</strong> Affect <code>enable secret</code> (already MD5/Type 5) or any passwords configured with the <code>secret</code> keyword</li><li><strong>DOES NOT:</strong> Provide real security — Type 7 is trivially reversible with free online tools in seconds. It's a Vigenere cipher with a fixed, publicly known key</li><li><strong>DOES NOT:</strong> Un-encrypt passwords if disabled — once encrypted, passwords stay encrypted even after <code>no service password-encryption</code> (but new passwords will be plaintext)</li></ul><p>The security hierarchy from weakest to strongest: <strong>Type 0 (plaintext)</strong> → <strong>Type 7 (Vigenere, reversible)</strong> → <strong>Type 5 (MD5, one-way hash)</strong> → <strong>Type 8 (PBKDF2)</strong> → <strong>Type 9 (scrypt)</strong>. Best practice: always use the <code>secret</code> keyword for passwords (gives Type 5+), AND enable <code>service password-encryption</code> as a baseline to catch any passwords that accidentally use <code>password</code> instead.</p>",
    visual: { type: "comparison", params: { left: { label: "Without service pw-enc", items: ["password cisco (plaintext)", "Visible in show run", "Anyone can read it"] }, right: { label: "With service pw-enc", items: ["password 7 0822455D0A16", "Obfuscated in show run", "Still easily cracked"] } } },
    hack: {
      memory: "Type 7 = TRIVIALLY reversible. It's like ROT13 — better than nothing, but barely. Real security = Type 5 (<code>secret</code>). Type 7 stops shoulder-surfing, nothing more.",
      practice: "Lab: Set <code>line con 0</code> → <code>password cisco</code>. Run <code>show run</code> — see plaintext. Then <code>service password-encryption</code> → <code>show run</code> — see Type 7 hash. Google 'Type 7 decoder' and paste the hash to prove it's crackable in seconds.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 6 + 27. Type 7 = weak/reversible (Vigenere). Type 5 = MD5 (one-way). Always prefer <code>secret</code>. <code>service password-encryption</code> is a safety net, not real encryption.",
    },
    micro: [
      { id: "5.3.g.1", term: "service password-encryption",  def: "Applies Type 7 (Vigenère — REVERSIBLE) to plaintext passwords. Not real security — only obscures casual viewing.", weight: "high" },
      { id: "5.3.g.2", term: "Type 7 weakness",              def: "Vigenère cipher. Reversible with online Cisco Type 7 decoders. Same password always produces same cipher.", weight: "high" },
      { id: "5.3.g.3", term: "Always prefer 'secret'",       def: "Use 'enable secret' and 'username X secret Y'. Both use one-way hashing, can't be reversed.", weight: "high" }
    ]
  },

  "5.3.h": {
    info: "<p><strong>Banners</strong> display messages to users connecting to a network device. They serve legal, informational, and security purposes. The CCNA tests three banner types and their display order.</p><p>Banner types in display order:</p><ul><li><strong>MOTD (Message of the Day)</strong> — <code>banner motd # text #</code> — displayed <strong>first</strong>, before any login prompt, to ALL connections (console, VTY, AUX). Used for <strong>legal disclaimers</strong> warning that unauthorized access is prohibited and will be prosecuted. This creates legal standing for enforcement.</li><li><strong>Login banner</strong> — <code>banner login # text #</code> — displayed <strong>after MOTD but before authentication</strong>. Used for additional pre-login notices (e.g., 'Enter your credentials to continue').</li><li><strong>EXEC banner</strong> — <code>banner exec # text #</code> — displayed <strong>after successful authentication</strong>. Used for post-login information (e.g., scheduled maintenance windows, current alerts).</li></ul><p>The delimiter character (<code>#</code> in the examples) can be any character that doesn't appear in the banner text. It marks the beginning and end of the message.</p><p>Critical security guidelines for banners:</p><ul><li><strong>DO:</strong> Include a legal warning in the MOTD ('Authorized users only. All activity is monitored and recorded.')</li><li><strong>DO NOT:</strong> Include the device hostname, model, IOS version, IP address, or location — this gives attackers reconnaissance information</li><li><strong>DO NOT:</strong> Use the word 'Welcome' — a lawyer could argue this implies invitation, weakening legal enforcement</li></ul>",
    visual: { type: "handshake", params: { leftLabel: "User", rightLabel: "Device", steps: ["Connect ->", "<- MOTD Banner (legal warning)", "<- Login Banner (pre-auth)", "Authenticate ->", "<- EXEC Banner (post-auth)"] } },
    hack: {
      memory: "Display order: M-L-E = MOTD → Login → EXEC. Mnemonic: My Lawyer Explains. MOTD = first thing everyone sees (legal warning). Login = before auth. EXEC = after auth. NEVER say 'Welcome' or reveal device info.",
      practice: "Configure all three: <code>banner motd #Unauthorized access prohibited#</code>, <code>banner login #Enter credentials#</code>, <code>banner exec #Maintenance window Sunday 2AM#</code>. Disconnect and reconnect to verify the M-L-E order.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. The exam tests display order (MOTD → Login → EXEC) and that MOTD should contain a legal notice but NEVER device info or 'Welcome.'",
    },
    micro: [
      { id: "5.3.h.1", term: "Banner display order",         def: "MOTD → Login banner → password prompt → EXEC banner. MOTD first, EXEC last (post-login).", weight: "high" },
      { id: "5.3.h.2", term: "MOTD banner content",          def: "Should contain LEGAL NOTICE (unauthorized access prohibited). NEVER 'Welcome' or device info.", weight: "high" },
      { id: "5.3.h.3", term: "banner motd # text #",         def: "Delimiter character (commonly #) surrounds text. Shown before login prompt.", weight: "med" }
    ]
  },

  "5.3.i": {
    info: "<p>The <code>security passwords min-length [0-16]</code> global configuration command enforces a <strong>minimum character length</strong> for all passwords subsequently configured on the device. This is a proactive control that prevents administrators from setting dangerously short passwords.</p><p>Key characteristics:</p><ul><li>Applies to <strong>all password types</strong>: enable passwords/secrets, line passwords, and local usernames</li><li><strong>Prospective only</strong> — does NOT retroactively reject or change existing passwords that are shorter than the new minimum. Only enforces the minimum on new or changed passwords</li><li>Valid range is <strong>0 to 16 characters</strong>. Cisco recommends a minimum of 10 characters</li><li>When a password below the minimum is attempted, IOS displays a warning but in some versions may still accept it. Use in conjunction with <code>enable secret</code> and <code>service password-encryption</code> as part of a complete password policy</li></ul><p>Example configuration:</p><ul><li><code>security passwords min-length 10</code> — rejects any new password shorter than 10 characters</li><li>After this, attempting <code>enable secret abc</code> (3 chars) will fail with: <code>% Password too short</code></li><li>But <code>enable secret MyStr0ngP@ss</code> (12 chars) will succeed</li></ul><p>This command is part of a broader password policy that should include: minimum length (this command), complexity (enforced by AAA server policies — IOS has no native complexity enforcement), rotation (managed administratively), and hashing (<code>secret</code> keyword for all passwords).</p>",
    visual: { type: "gauge", params: { level: 60, label: "Min Password Length (e.g., 10 chars)", color: "#3b82f6" } },
    hack: {
      memory: "MIN-LENGTH = the bouncer checking your ID at the door. Too short? Rejected. But existing guests (passwords already set) aren't kicked out. Only applies to NEW passwords going forward.",
      practice: "Lab: <code>security passwords min-length 10</code>. Try <code>enable secret short</code> — rejected. Try <code>enable secret LongEnough1</code> — accepted. Then check: does the old short password still work? Yes — min-length is not retroactive.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. Quick exam point: exact command is <code>security passwords min-length X</code>. Remember: only affects NEW passwords, not existing ones.",
    },
    micro: [
      { id: "5.3.i.1", term: "security passwords min-length", def: "Global command. Enforces minimum length for NEW passwords. Does NOT affect existing passwords.", weight: "high" },
      { id: "5.3.i.2", term: "Enforcement scope",            def: "Only applies to passwords configured AFTER the command. Existing weak passwords remain.", weight: "med" }
    ]
  },

  "5.3.j": {
    info: "<p>The <code>login block-for [seconds] attempts [count] within [seconds]</code> global configuration command provides <strong>brute-force login protection</strong> by temporarily blocking all login attempts after too many consecutive failures within a defined time window.</p><p>Command syntax breakdown:</p><ul><li><code>login block-for 120 attempts 3 within 60</code> means: if <strong>3 failed login attempts</strong> occur <strong>within 60 seconds</strong>, <strong>block ALL logins for 120 seconds</strong></li><li><strong>block-for</strong> — the lockout duration in seconds</li><li><strong>attempts</strong> — the number of failed attempts that trigger the lockout</li><li><strong>within</strong> — the time window for counting failures</li></ul><p>During the lockout period (called <strong>quiet mode</strong>), ALL login attempts are rejected — including legitimate admin attempts. To prevent locking yourself out, configure a whitelist:</p><ul><li><code>login quiet-mode access-class [ACL-name|number]</code> — specifies an ACL of trusted source IPs that are exempt from the lockout. Admins connecting from these IPs can still log in during quiet mode</li></ul><p>Additional login enhancement commands:</p><ul><li><code>login delay [seconds]</code> — adds a delay between consecutive login attempts (slows brute-force attacks)</li><li><code>login on-failure log</code> — generates a syslog message on failed login attempts for monitoring</li><li><code>login on-success log</code> — generates a syslog message on successful logins for audit trails</li></ul><p>This feature protects VTY lines from automated brute-force password guessing attacks. Without it, an attacker can try thousands of password combinations per minute against SSH or Telnet.</p>",
    visual: { type: "state-machine", params: { states: ["Normal Login", "3 Failures in 60s", "BLOCKED (120s)", "Normal Login"], active: 2, transitions: true } },
    hack: {
      memory: "Read the command as a sentence: 'Block FOR 120 seconds after 3 ATTEMPTS WITHIN 60 seconds.' Block-for = lockout time. Attempts = threshold. Within = counting window. Quiet-mode ACL = admin escape hatch.",
      practice: "Lab: <code>login block-for 60 attempts 3 within 30</code>. Intentionally fail 3 SSH logins quickly — verify lockout. Then add <code>login quiet-mode access-class ADMIN_IPS</code> with your IP permitted and verify you can still log in during lockout.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. The exam tests exact syntax and parameter order. Practice typing the full command from memory: block-for → attempts → within.",
    },
    micro: [
      { id: "5.3.j.1", term: "login block-for",              def: "Global command. Throttles login attempts. Syntax: 'login block-for [sec] attempts [N] within [sec]'.", weight: "high" },
      { id: "5.3.j.2", term: "Block-for example",            def: "'login block-for 120 attempts 3 within 60' = block login for 120s after 3 failed attempts in 60s.", weight: "high" },
      { id: "5.3.j.3", term: "Parameter order",              def: "block-for THEN attempts THEN within. Memorize the exact order — exam tests this.", weight: "high" }
    ]
  },

  // ── 5.4 Describe security password policy ─────────────────────

  "5.4.a": {
    info: "<p><strong>Password complexity</strong> refers to the requirements that make passwords resistant to guessing and brute-force attacks. A strong password policy combines both <strong>length</strong> and <strong>character diversity</strong> to maximize the number of possible combinations an attacker must try.</p><p>The four character categories for password complexity (<strong>ULNS</strong>):</p><ul><li><strong>Uppercase letters</strong> (A-Z) — 26 characters</li><li><strong>Lowercase letters</strong> (a-z) — 26 characters</li><li><strong>Numbers</strong> (0-9) — 10 characters</li><li><strong>Symbols/Special characters</strong> (!@#$%^&*) — approximately 32 characters</li></ul><p>Using all four categories with a 12-character password gives approximately 94^12 = 4.75 x 10^23 possible combinations — making brute-force attacks computationally infeasible. However, modern security guidance emphasizes that <strong>length matters more than complexity</strong>. A 16-character passphrase like 'correct horse battery staple' is stronger than an 8-character complex password like 'P@$$w0rd' because the longer password has more entropy despite less complexity.</p><p>Cisco IOS password controls:</p><ul><li><code>security passwords min-length [0-16]</code> — enforces minimum length on new passwords</li><li>IOS does <strong>NOT natively enforce complexity rules</strong> (no built-in requirement for mixed character types)</li><li>True complexity enforcement requires <strong>AAA server policies</strong> (RADIUS/TACACS+ servers like Cisco ISE can enforce complexity rules)</li></ul>",
    visual: { type: "gauge", params: { level: 85, label: "Password Strength", color: "#10b981" } },
    hack: {
      memory: "Complexity = ULNS — Uppercase, Lowercase, Numbers, Symbols. But length beats complexity: a 20-char passphrase > an 8-char P@$$w0rd. IOS enforces length only, not complexity — that needs a RADIUS/TACACS+ server.",
      practice: "Compare password strength: calculate time to brute-force an 8-char complex password vs a 16-char simple passphrase. Use an online calculator like howsecureismypassword.net. Understand why NIST now recommends length over complexity.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. The exam tests concepts, not math. Know: complexity + length = strongest, IOS enforces length only, AAA servers enforce complexity.",
    },
    micro: [
      { id: "5.4.a.1", term: "Password complexity",          def: "Mix of upper/lower case, numbers, symbols. IOS doesn't enforce; AAA servers (ISE) do.", weight: "high" },
      { id: "5.4.a.2", term: "Password length",              def: "Longer = stronger. IOS enforces with 'security passwords min-length'. Longer+complex = strongest.", weight: "high" },
      { id: "5.4.a.3", term: "NIST guidance",                def: "Modern NIST recommends long passphrases over complex short passwords. Length beats complexity.", weight: "med" }
    ]
  },

  "5.4.b": {
    info: "<p><strong>Password management</strong> encompasses the policies and practices that govern the lifecycle of passwords — from creation through retirement. These policies aim to minimize the risk of password compromise over time.</p><p>Key password management elements:</p><ul><li><strong>Rotation/expiration</strong> — forcing password changes at regular intervals (traditionally every 60-90 days). The idea is that even if a password is compromised, the window of exposure is limited to the rotation period.</li><li><strong>Password history</strong> — preventing reuse of the last N passwords (e.g., 'remember last 12 passwords'). This prevents users from cycling back to favorite passwords after a forced change.</li><li><strong>Account lockout</strong> — disabling an account after N failed login attempts to prevent brute-force attacks. Cisco implements this with <code>login block-for</code>.</li><li><strong>Password aging</strong> — tracking how old a password is and forcing a change before or at expiration</li></ul><p>Important modern guidance shift: <strong>NIST SP 800-63B</strong> (2017, updated 2024) now recommends <strong>against forced periodic rotation</strong> unless there is evidence of compromise. Research shows forced rotation leads users to choose weaker, predictable passwords (Password1 → Password2 → Password3). NIST instead recommends: long passphrases, checking against known-breached password lists, and changing only when compromised.</p><p>The CCNA exam may test <strong>both</strong> traditional rotation policies AND the newer NIST guidance. Know the traditional approach (rotate every 90 days) but also understand why the industry is shifting away from it.</p>",
    visual: { type: "state-machine", params: { states: ["New Password", "Active (90 days)", "Expired", "Must Change"], active: 1, transitions: true } },
    hack: {
      memory: "Rotation = CHANGE it regularly. History = CAN'T REUSE old ones. Lockout = too many failures = account frozen. NIST says: stop forcing rotation — users just pick weaker passwords. Change only when compromised.",
      practice: "Design two password policies: 'Traditional' (rotate 90 days, history 12, lockout after 5, min 8 chars) vs 'NIST Modern' (no forced rotation, min 15 chars, check against breach lists, change on compromise). Know both for the exam.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. The exam may test traditional rotation policies even though NIST has updated guidance. Know both perspectives and be ready for either.",
    },
    micro: [
      { id: "5.4.b.1", term: "Traditional rotation (90 days)", def: "Old guidance: change passwords every 60-90 days. Widely enforced by compliance standards.", weight: "med" },
      { id: "5.4.b.2", term: "Modern NIST guidance",         def: "Only change when compromised. Frequent rotation leads to weak patterns (Password1, Password2...).", weight: "med" },
      { id: "5.4.b.3", term: "Password history",             def: "Prevents reuse of last N passwords. Enforced on AAA server or OS (not usually IOS).", weight: "low" }
    ]
  },

  "5.4.c": {
    info: "<p><strong>Multi-Factor Authentication (MFA)</strong> requires two or more authentication factors from <strong>different categories</strong> to verify identity. It is one of the most effective security controls because it ensures that compromising a single factor (like stealing a password) is not enough to gain access.</p><p>The three authentication factor categories:</p><ul><li><strong>Something you KNOW</strong> — password, PIN, passphrase, security question answer. These are knowledge-based and can be forgotten, guessed, or stolen through phishing.</li><li><strong>Something you HAVE</strong> — smart card, hardware token (RSA SecurID), mobile phone (for push notifications or OTP codes), USB security key (YubiKey). These are possession-based and must be physically stolen.</li><li><strong>Something you ARE</strong> — biometrics: fingerprint, facial recognition, iris scan, voice pattern, retina scan. These are inherence-based and are the hardest to forge but cannot be changed if compromised.</li></ul><p>Critical exam distinction: using two factors from the <strong>SAME</strong> category is <strong>NOT true MFA</strong>. A password plus a security question are both 'something you know' — this is merely two-step verification, not multi-factor authentication. Both could be compromised through the same phishing attack.</p><p>True MFA examples: password (Know) + phone OTP (Have), PIN (Know) + fingerprint (Are), badge (Have) + iris scan (Are). Each combination requires the attacker to compromise fundamentally different systems.</p>",
    visual: { type: "shield", params: { items: ["Something You KNOW (password)", "Something You HAVE (token/phone)", "Something You ARE (biometric)"], color: "#3b82f6" } },
    hack: {
      memory: "MFA = Know-Have-Are (KHA). Must use DIFFERENT categories. Password (Know) + Phone OTP (Have) = real MFA. Password + Security Question = NOT MFA (both Know). Password + PIN = NOT MFA (both Know).",
      practice: "Create 10 authentication scenarios and classify each: 'Password + fingerprint' = MFA (Know + Are). 'PIN + security question' = NOT MFA (both Know). 'Badge + phone OTP' = MFA (Have + Have)? Actually NO — same category. Tricky ones build exam readiness.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. Guaranteed exam question. The trap: two passwords, password + security question, or password + PIN are all NOT MFA because they're the same factor category.",
    },
    micro: [
      { id: "5.4.c.1", term: "MFA (Multi-Factor Auth)",      def: "Authentication using 2+ factors from DIFFERENT categories. Something you know + have + are.", weight: "high" },
      { id: "5.4.c.2", term: "Three factor categories",      def: "Know (password, PIN, security question). Have (token, phone, smartcard). Are (biometric).", weight: "high" },
      { id: "5.4.c.3", term: "MFA trap (same category)",     def: "Password + PIN = NOT MFA (both 'know'). Password + security question = NOT MFA. Must be different categories.", weight: "high" }
    ]
  },

  "5.4.d": {
    info: "<p>This subtopic reinforces the critical concept that <strong>true MFA requires factors from DIFFERENT categories</strong>. The strength of MFA comes from the attacker needing to compromise fundamentally different systems — stealing a password (knowledge) doesn't help them bypass a fingerprint scanner (biometric), and vice versa.</p><p>Why different categories matter:</p><ul><li>A phishing attack can steal passwords AND security questions (both 'Know') in one email</li><li>A stolen laptop could have both a saved password AND a smart card inserted (Know + Have) — but the attacker still can't replicate a fingerprint (Are)</li><li>The more diverse the factors, the more independent attack vectors are required</li></ul><p>Common exam scenarios to practice:</p><ul><li><strong>Password + SMS OTP</strong> = True MFA (Know + Have). The OTP is sent to a phone you possess.</li><li><strong>Password + email OTP</strong> = Weaker MFA (Know + Have, but email may be on the same compromised device)</li><li><strong>Password + fingerprint</strong> = True MFA (Know + Are). Two completely independent systems.</li><li><strong>PIN + password</strong> = NOT MFA (Know + Know). Both are knowledge-based.</li><li><strong>Smart card + PIN</strong> = True MFA (Have + Know). The card is possession, the PIN is knowledge.</li><li><strong>Badge + fingerprint</strong> = True MFA (Have + Are). No knowledge factor but still two different categories.</li></ul><p>The CCNA exam may also mention <strong>two-factor authentication (2FA)</strong> as a specific case of MFA using exactly two factors. All 2FA is MFA, but MFA can use three or more factors for extremely high-security environments.</p>",
    visual: { type: "comparison", params: { left: { label: "True MFA", items: ["Password + Phone OTP", "PIN + Smart Card", "Password + Fingerprint"] }, right: { label: "NOT MFA", items: ["Password + Security Q", "PIN + Password", "Two passwords"] } } },
    hack: {
      memory: "Test: can BOTH factors be stolen the same way? If a phishing email could grab both → NOT MFA. If stealing one factor tells you nothing about the other → real MFA. Different CATEGORIES, not just different credentials.",
      practice: "Flash drill: 10 rapid-fire combos. For each, say 'MFA' or 'NOT MFA' instantly. Badge + PIN = MFA. Two passwords = NOT. Retina + fingerprint = NOT (both Are). Password + YubiKey = MFA. Speed matters for the exam.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. Cisco specifically tests the 'different categories' requirement. This is one of the most commonly missed questions — students think any two credentials = MFA.",
    },
    micro: [
      { id: "5.4.d.1", term: "Different categories required", def: "MFA requires factors from DIFFERENT categories. Not just 'two things' — specifically different types.", weight: "high" },
      { id: "5.4.d.2", term: "Valid MFA examples",           def: "Password + TOTP token (know + have). Fingerprint + card (are + have). Password + fingerprint (know + are).", weight: "high" }
    ]
  },

  "5.4.e": {
    info: "<p><strong>Digital certificates</strong> use <strong>Public Key Infrastructure (PKI)</strong> to verify the identity of devices, users, and services. A certificate is essentially a digitally signed document that binds a public key to an identity, vouched for by a trusted third party called a <strong>Certificate Authority (CA)</strong>.</p><p>The <strong>X.509</strong> standard defines the certificate format. Key fields in an X.509 certificate:</p><ul><li><strong>Subject</strong> — the entity the certificate identifies (e.g., www.cisco.com)</li><li><strong>Issuer</strong> — the CA that signed the certificate</li><li><strong>Public key</strong> — the subject's public key used for encryption and verification</li><li><strong>Validity period</strong> — start date and expiration date</li><li><strong>Serial number</strong> — unique identifier assigned by the CA</li><li><strong>Digital signature</strong> — the CA's signature proving the certificate is authentic and unmodified</li></ul><p>The <strong>chain of trust</strong> works hierarchically: a <strong>Root CA</strong> (self-signed, pre-trusted) signs <strong>Intermediate CA</strong> certificates, which in turn sign <strong>end-entity certificates</strong> (server certs, client certs). Your browser trusts the root CA, which vouches for the intermediate, which vouches for the server — creating a chain of trust from the server back to a root you trust.</p><p>Certificates are used in: <strong>HTTPS/TLS</strong> (web security), <strong>IPsec VPN authentication</strong> (stronger than pre-shared keys), <strong>802.1X</strong> (EAP-TLS), <strong>SSH</strong> (certificate-based authentication), and <strong>code signing</strong> (verifying software authenticity).</p>",
    visual: { type: "hierarchy", params: { root: "Root CA", children: [{ name: "Intermediate CA", children: [{ name: "Server Certificate" }, { name: "Client Certificate" }] }] } },
    hack: {
      memory: "PKI = Public Key Infrastructure. CA = Certificate Authority = the notary who stamps your ID as legit. X.509 = the standard certificate format. Chain of trust: Root CA → Intermediate CA → End Certificate.",
      practice: "Click the lock icon on any HTTPS website in Chrome. View the certificate: find the subject, issuer, validity dates, public key, and trace the chain up to the root CA. This makes the abstract concept concrete.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. The exam tests PKI concepts, not crypto math. Know: what a CA does, X.509 contents (subject, issuer, public key, validity), and the chain of trust model.",
    },
    micro: [
      { id: "5.4.e.1", term: "Digital certificate (X.509)",  def: "Electronic document binding a public key to an identity. Signed by a CA. Used for HTTPS, SSL/TLS, IPsec.", weight: "high" },
      { id: "5.4.e.2", term: "Certificate Authority (CA)",   def: "Trusted third party that signs certificates. Chain of trust: root CA → intermediate CA → end-entity cert.", weight: "high" },
      { id: "5.4.e.3", term: "X.509 contents",               def: "Subject (owner), Issuer (CA), public key, validity (not-before/not-after), serial number, signature.", weight: "high" },
      { id: "5.4.e.4", term: "PKI",                          def: "Public Key Infrastructure. Framework for issuing, distributing, and revoking digital certificates.", weight: "med" }
    ]
  },

  "5.4.f": {
    info: "<p><strong>Biometric authentication</strong> uses unique physical or behavioral characteristics to verify identity. Biometrics are the <strong>'something you are'</strong> factor in MFA — the hardest factor to steal, share, or replicate because it's inherent to the individual.</p><p>Common biometric types:</p><ul><li><strong>Fingerprint scanning</strong> — the most widely deployed biometric. Fast, inexpensive, and familiar to users. Can be fooled by high-quality copies but modern sensors include liveness detection.</li><li><strong>Facial recognition</strong> — uses camera analysis of facial geometry. Convenient (touchless) but affected by lighting, aging, and can sometimes be fooled by photos or masks.</li><li><strong>Iris scanning</strong> — analyzes the unique patterns in the colored ring of the eye. Extremely accurate (1 in 1.2 million false match rate) but expensive and requires user cooperation.</li><li><strong>Retina scanning</strong> — maps blood vessel patterns in the back of the eye. The most accurate biometric but requires very close proximity to the scanner and is uncomfortable for users.</li><li><strong>Voice recognition</strong> — analyzes vocal characteristics. Convenient for phone-based authentication but affected by illness, background noise, and can be spoofed with recordings.</li></ul><p>Key advantage: biometrics cannot be forgotten (like passwords) or lost (like tokens). Key disadvantage: <strong>biometrics cannot be changed if compromised</strong>. If your fingerprint data is stolen from a database, you cannot get new fingerprints — unlike resetting a password. This is why biometrics should always be combined with another factor (MFA), never used alone.</p><p>Biometric accuracy is measured by <strong>FAR</strong> (False Acceptance Rate — accepting an impostor) and <strong>FRR</strong> (False Rejection Rate — rejecting a legitimate user). The <strong>CER</strong> (Crossover Error Rate, where FAR = FRR) indicates overall accuracy — lower CER means better biometric system.</p>",
    visual: { type: "shield", params: { items: ["Fingerprint", "Facial Recognition", "Iris Scan", "Voice Pattern"], color: "#8b5cf6" } },
    hack: {
      memory: "Biometrics = Body metrics = Something you ARE. Strongest single factor but CANNOT BE CHANGED if compromised. You can reset a password but you can't reset your fingerprint. Always pair with another factor.",
      practice: "Build a pros/cons table: Fingerprint (cheap, fast, liftable), Iris (very accurate, expensive), Face (touchless, fooled by photos), Voice (convenient, noise-sensitive). Know trade-offs for scenario questions.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 52. Wendell Odom OCG Chapter 27. The exam tests biometrics as 'something you are' in MFA. Key disadvantage = non-changeable if compromised. Know FAR/FRR/CER for bonus points.",
    },
    micro: [
      { id: "5.4.f.1", term: "Biometrics",                   def: "'Something you ARE.' Fingerprint, iris, facial recognition, voice. Physical characteristics.", weight: "high" },
      { id: "5.4.f.2", term: "Biometric disadvantage",       def: "CANNOT BE CHANGED if compromised. Unlike passwords, you can't reset your fingerprint.", weight: "high" },
      { id: "5.4.f.3", term: "FAR / FRR / CER",              def: "FAR = False Accept Rate (bad). FRR = False Reject Rate. CER = Crossover Error Rate (FAR=FRR, tuning point).", weight: "low" }
    ]
  },

  // ── 5.5 Describe IPsec VPNs ──────────────────────────────────

  "5.5.a": {
    info: "<p>A <strong>site-to-site VPN</strong> creates a <strong>permanent, encrypted tunnel</strong> between two network devices (typically routers or firewalls) at different physical locations. All traffic between the two sites flows through this tunnel automatically — no VPN client software is needed on end-user devices because the encryption happens at the network edge.</p><p>Key characteristics of site-to-site VPNs:</p><ul><li><strong>Always on</strong> — the tunnel is established when the routers boot and remains active continuously. Traffic matching the configured 'interesting traffic' ACL is automatically encrypted and sent through the tunnel.</li><li><strong>Router-to-router</strong> — encryption occurs between the two VPN endpoint devices, not on individual workstations. End users are completely unaware of the VPN.</li><li><strong>No client software</strong> — because the routers handle all encryption/decryption, user devices simply send normal unencrypted traffic to their default gateway.</li><li><strong>Statically configured</strong> — both endpoints must have matching IPsec configurations (algorithms, authentication, interesting traffic definitions).</li></ul><p>Common use cases: connecting branch offices to headquarters, linking data centers across the internet, extending private networks between business partners. The tunnel makes the remote sites appear to be on the same private network even though traffic traverses the public internet. Technologies include <strong>IPsec</strong> (most common), <strong>GRE over IPsec</strong> (when routing protocols need to cross the VPN), and <strong>DMVPN</strong> (Dynamic Multipoint VPN for hub-and-spoke topologies with many branches).</p>",
    visual: { type: "packet-flow", params: { nodes: ["Branch Router", "Encrypted Tunnel", "HQ Router"], color: "#3b82f6" } },
    hack: {
      memory: "Site-to-Site = a PERMANENT bridge between two buildings. Always on, router-to-router, no client software. Like a dedicated highway between offices — the cars (users) don't know the road is encrypted.",
      practice: "Diagram a site-to-site VPN: Branch LAN (10.1.0.0/24) → Branch Router → Internet (encrypted tunnel) → HQ Router → HQ LAN (10.2.0.0/24). Label tunnel endpoints, protected subnets, and interesting traffic ACL.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 54 (VPN Fundamentals). Wendell Odom OCG Chapter 16. The exam tests site-to-site vs remote access. Site-to-site = always-on, router-to-router. Remote access = on-demand, client-to-router.",
    },
    micro: [
      { id: "5.5.a.1", term: "VPN",                          def: "Virtual Private Network. Creates a secure encrypted tunnel across an untrusted network (internet).", weight: "high" },
      { id: "5.5.a.2", term: "Site-to-site VPN",             def: "Always-on router-to-router (or firewall-to-firewall). Connects entire networks.", weight: "high" },
      { id: "5.5.a.3", term: "Remote access VPN",            def: "On-demand client-to-router. Individual user VPN (AnyConnect, OpenVPN, WireGuard).", weight: "high" }
    ]
  },

  "5.5.b": {
    info: "<p>A <strong>remote access VPN</strong> allows individual users to securely connect to the corporate network from any location over the internet using <strong>VPN client software</strong> installed on their device. Unlike site-to-site VPNs, remote access connections are <strong>on-demand</strong> — the user initiates the connection when needed and disconnects when done.</p><p>Key characteristics of remote access VPNs:</p><ul><li><strong>On-demand</strong> — the user manually connects (or auto-connects via policy) when they need corporate network access</li><li><strong>Client-to-site</strong> — encryption occurs between the user's device (running VPN client software) and a VPN concentrator/firewall at the corporate site</li><li><strong>Client software required</strong> — the user's device must have VPN client software installed (e.g., Cisco AnyConnect, GlobalProtect, or built-in OS VPN clients)</li><li><strong>Individual user authentication</strong> — each user authenticates with their own credentials (username/password, certificate, MFA), providing per-user accountability</li></ul><p>Two main implementation types:</p><ul><li><strong>IPsec-based</strong> — traditional approach using IPsec protocols. Requires dedicated client software. Provides full network-level access (Layer 3 tunnel).</li><li><strong>SSL/TLS-based (clientless or AnyConnect)</strong> — uses HTTPS (TCP 443) to establish the VPN. Clientless SSL VPN provides web-based access through a browser portal. Cisco AnyConnect provides full tunnel access over SSL/TLS. SSL VPNs work through most firewalls because they use the same port as HTTPS.</li></ul><p>Remote access VPNs are essential for remote workers, traveling employees, and BYOD scenarios where devices need secure access to corporate resources from untrusted networks (home WiFi, hotel, coffee shop).</p>",
    visual: { type: "packet-flow", params: { nodes: ["Remote User", "VPN Client", "Internet", "VPN Concentrator", "Corporate Network"], color: "#8b5cf6" } },
    hack: {
      memory: "Remote Access = TEMPORARY tunnel from your laptop. You DIAL IN when you need it, disconnect when done. Client software required. Like a phone call vs a leased line (site-to-site).",
      practice: "Build a two-column comparison flashcard: Site-to-Site (permanent, router-to-router, no client, always on) vs Remote Access (on-demand, client-to-site, needs software, user-initiated). Drill scenario questions: 'traveling employee' = remote access, 'two offices' = site-to-site.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. The exam gives scenarios: 'traveling employee needs HQ resources' = remote access VPN. 'Two offices need constant connectivity' = site-to-site VPN.",
    },
    micro: [
      { id: "5.5.b.1", term: "Traveling employee scenario",  def: "Remote access VPN. Single user needs access from anywhere. AnyConnect client connects to HQ.", weight: "high" },
      { id: "5.5.b.2", term: "Branch-to-HQ always-on scenario", def: "Site-to-site VPN. Branch office connects to HQ continuously. Users don't know VPN exists.", weight: "high" },
      { id: "5.5.b.3", term: "Clientless SSL VPN",           def: "Remote access via browser HTTPS. No VPN client software required. Limited to web-based apps.", weight: "med" }
    ]
  },

  "5.5.c": {
    info: "<p><strong>IPsec (Internet Protocol Security)</strong> is a framework of protocols that provides security services for IP traffic. It operates at <strong>Layer 3</strong> and can protect any protocol that runs over IP. IPsec provides four core security services:</p><ul><li><strong>Confidentiality (Encryption)</strong> — ensures data cannot be read by unauthorized parties. Achieved through encryption algorithms like <strong>AES</strong> (128, 192, or 256-bit) or the older <strong>3DES</strong>. Only the intended recipient with the correct key can decrypt the data.</li><li><strong>Integrity (Hashing)</strong> — ensures data has not been modified in transit. Achieved through hash algorithms like <strong>SHA-256</strong>, <strong>SHA-384</strong>, or the older <strong>MD5</strong>. A hash of the data is computed and verified at both ends — any modification changes the hash, revealing tampering.</li><li><strong>Authentication (Identity Verification)</strong> — proves the sender is who they claim to be. Achieved through <strong>pre-shared keys (PSK)</strong> or <strong>digital certificates</strong>. Prevents an attacker from impersonating a legitimate VPN peer.</li><li><strong>Anti-replay Protection</strong> — prevents an attacker from capturing encrypted packets and retransmitting them later. Each IPsec packet includes a <strong>sequence number</strong>; the receiver tracks sequence numbers and drops duplicates or out-of-window packets.</li></ul><p>These services are delivered by two IPsec protocols: <strong>AH (Authentication Header)</strong> and <strong>ESP (Encapsulating Security Payload)</strong>. ESP provides all four services; AH provides all except confidentiality (no encryption). The combination of IPsec with IKE (Internet Key Exchange) for key management creates a complete VPN security solution.</p>",
    visual: { type: "shield", params: { items: ["Confidentiality (Encryption)", "Integrity (Hashing)", "Authentication (Identity)", "Anti-Replay (Sequence #s)"], color: "#10b981" } },
    hack: {
      memory: "IPsec = CIA-R: Confidentiality, Integrity, Authentication, anti-Replay. Remember 'CIA Rocks.' Encryption = AES. Hashing = SHA. Authentication = PSK or certs. Anti-replay = sequence numbers.",
      practice: "Build a matrix: Security Service | What It Prevents | Algorithm | Protocol (AH/ESP). Confidentiality (eavesdropping, AES, ESP only). Integrity (tampering, SHA, both). Authentication (impersonation, PSK/certs, both). Anti-replay (replay attacks, sequence #, both).",
      effort: "low",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. The exam loves: 'which IPsec service ensures data hasn't been modified?' = Integrity. 'Which ensures data can't be read?' = Confidentiality. Map each service to its definition.",
    },
    micro: [
      { id: "5.5.c.1", term: "Confidentiality (IPsec)",      def: "Data CAN'T BE READ by eavesdroppers. Provided by encryption (AES, 3DES, DES).", weight: "high" },
      { id: "5.5.c.2", term: "Integrity (IPsec)",            def: "Data HAS NOT BEEN MODIFIED in transit. Provided by HMAC hashing (MD5, SHA).", weight: "high" },
      { id: "5.5.c.3", term: "Authentication (IPsec)",       def: "Confirms peer IDENTITY. Provided by pre-shared keys (PSK) or digital certificates.", weight: "high" },
      { id: "5.5.c.4", term: "Anti-replay (IPsec)",          def: "Prevents attacker from re-sending captured packets. Sequence numbers detect replays.", weight: "med" }
    ]
  },

  "5.5.d": {
    info: "<p><strong>Authentication Header (AH)</strong> is one of the two core IPsec protocols. It provides <strong>integrity, authentication, and anti-replay protection</strong> but does <strong>NOT provide encryption (no confidentiality)</strong>. Data protected by AH alone can still be read by anyone who intercepts it — AH only proves it hasn't been tampered with and verifies the sender's identity.</p><p>AH technical details:</p><ul><li><strong>IP Protocol Number 51</strong> — AH is identified in the IP header as protocol 51 (like TCP is 6 and UDP is 17)</li><li><strong>Authenticates the ENTIRE packet</strong> — including the outer IP header (except mutable fields like TTL and header checksum). This is more thorough than ESP but creates a critical problem with NAT.</li><li><strong>Incompatible with NAT</strong> — because AH authenticates the IP header, if NAT changes the source or destination IP address, the authentication hash breaks and the packet is dropped. This is why AH is rarely used in modern networks where NAT is ubiquitous.</li><li><strong>Hash algorithms</strong> — uses HMAC-MD5 or HMAC-SHA for integrity checking</li></ul><p>AH is largely obsolete in practice because <strong>ESP can provide all the same services (integrity, authentication, anti-replay) PLUS encryption</strong>. The only theoretical advantage of AH is that it authenticates the IP header itself, but this is rarely worth the NAT incompatibility trade-off. Modern VPN deployments almost exclusively use ESP.</p>",
    visual: { type: "encapsulation", params: { layers: [{ label: "IP Header", color: "#6366f1" }, { label: "AH Header (Proto 51)", color: "#f59e0b" }, { label: "Original Payload (NOT encrypted)", color: "#94a3b8" }] } },
    hack: {
      memory: "AH = Authentication Header = NO encryption. Protocol 51. 'AH, I forgot to encrypt!' AH authenticates the IP header too, which BREAKS NAT. That's why nobody uses AH anymore.",
      practice: "Draw AH packet format: [New IP | AH | Original IP + Data (visible/readable)]. Circle what's authenticated (everything). Circle what's encrypted (nothing). Compare with ESP immediately.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. Key exam facts: AH = protocol 51, no encryption, authenticates IP header, incompatible with NAT. ESP = protocol 50, encryption + everything else.",
    },
    micro: [
      { id: "5.5.d.1", term: "AH (Authentication Header)",   def: "IP protocol 51. Provides integrity + auth BUT NOT encryption. Authenticates entire packet including IP header.", weight: "high" },
      { id: "5.5.d.2", term: "AH + NAT incompatibility",     def: "AH authenticates the IP header. NAT changes the IP header → AH breaks. Can't use AH through NAT.", weight: "high" },
      { id: "5.5.d.3", term: "AH = protocol number 51",      def: "IP protocol 51, not TCP/UDP. Memorize.", weight: "high" }
    ]
  },

  "5.5.e": {
    info: "<p><strong>Encapsulating Security Payload (ESP)</strong> is the dominant IPsec protocol, providing <strong>all four security services</strong>: confidentiality (encryption), integrity, authentication, and anti-replay protection. ESP is the standard choice for virtually all modern IPsec VPN deployments.</p><p>ESP technical details:</p><ul><li><strong>IP Protocol Number 50</strong> — ESP is identified in the IP header as protocol 50</li><li><strong>Encrypts the payload</strong> — everything after the ESP header is encrypted, including the original IP header in tunnel mode</li><li><strong>Authenticates ESP header + payload</strong> — but NOT the outer IP header. This is less comprehensive than AH's authentication scope, but critically, it means <strong>ESP is compatible with NAT</strong> because NAT changes the outer IP header (which ESP doesn't authenticate)</li><li><strong>NAT Traversal (NAT-T)</strong> — when ESP needs to traverse NAT devices, it encapsulates ESP packets inside UDP port 4500. This allows ESP to work through NAT/PAT devices that would otherwise block protocol 50</li></ul><p>ESP provides everything AH does, plus encryption:</p><ul><li>Confidentiality — <strong>AES-128, AES-256</strong> (preferred), or 3DES (legacy)</li><li>Integrity — <strong>SHA-256, SHA-384, SHA-512</strong>, or HMAC-MD5 (legacy)</li><li>Authentication — pre-shared keys or digital certificates (via IKE)</li><li>Anti-replay — sequence numbers with a sliding window</li></ul><p>Because ESP provides a superset of AH's capabilities and is NAT-compatible, it has almost entirely replaced AH in practice. The exam expects you to know that ESP = 50 = full protection, and AH = 51 = no encryption.</p>",
    visual: { type: "encapsulation", params: { layers: [{ label: "IP Header", color: "#6366f1" }, { label: "ESP Header (Proto 50)", color: "#10b981" }, { label: "Encrypted Payload", color: "#3b82f6" }, { label: "ESP Trailer + Auth", color: "#10b981" }] } },
    hack: {
      memory: "ESP = Encapsulating Security Payload = FULL protection. Protocol 50. 'ESP has Everything (50 = the whole package).' AH = 51 = 'Almost, Half-way' — no encryption. ESP works with NAT, AH doesn't.",
      practice: "Build a head-to-head table: Feature | AH (51) | ESP (50). Encryption: No/Yes. Integrity: Yes/Yes. Auth: Yes/Yes. Anti-Replay: Yes/Yes. NAT Compatible: No/Yes. Authenticates IP Header: Yes/No. ESP wins every practical category.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. ESP vs AH is a classic exam question. ESP = 50 = encryption + everything. AH = 51 = no encryption, breaks with NAT.",
    },
    micro: [
      { id: "5.5.e.1", term: "ESP (Encapsulating Security Payload)", def: "IP protocol 50. Provides encryption + integrity + auth. Standard IPsec choice in modern VPNs.", weight: "high" },
      { id: "5.5.e.2", term: "ESP = protocol 50",            def: "IP protocol 50. AH = 51. ESP is the one with encryption.", weight: "high" },
      { id: "5.5.e.3", term: "ESP works with NAT",           def: "ESP doesn't include IP header in authentication. NAT traversal (UDP 4500) makes it work across NAT.", weight: "high" },
      { id: "5.5.e.4", term: "ESP vs AH rule",               def: "Use ESP (encryption). AH only provides integrity. Modern deployments use ESP exclusively.", weight: "high" }
    ]
  },

  "5.5.f": {
    info: "<p><strong>IKE (Internet Key Exchange) Phase 1</strong> establishes a secure, authenticated management channel between two IPsec peers. This channel (called the <strong>ISAKMP Security Association</strong>) protects the Phase 2 negotiation that follows. Think of Phase 1 as the two peers introducing themselves, verifying identities, and agreeing on how to communicate securely.</p><p>Phase 1 performs three key functions:</p><ul><li><strong>Algorithm negotiation</strong> — peers propose and agree on encryption algorithm (AES), hash algorithm (SHA), authentication method (PSK or certificates), and Diffie-Hellman group (for key exchange)</li><li><strong>Diffie-Hellman key exchange</strong> — peers generate a shared secret key over an insecure channel without actually transmitting the key. This is mathematically elegant — each peer contributes half the key material.</li><li><strong>Peer authentication</strong> — peers verify each other's identity using pre-shared keys (both peers know the same secret) or digital certificates (PKI-based)</li></ul><p>Phase 1 can operate in two modes:</p><ul><li><strong>Main Mode</strong> — 6 messages exchanged, provides identity protection (peer IPs are encrypted). More secure but slower.</li><li><strong>Aggressive Mode</strong> — 3 messages exchanged, faster but exposes peer identities in cleartext. Used when one peer has a dynamic IP address.</li></ul><p>The result of Phase 1 is one <strong>bidirectional ISAKMP SA</strong> — a secure management tunnel used exclusively to protect the Phase 2 negotiation. No user data flows through this SA.</p>",
    visual: { type: "handshake", params: { leftLabel: "Peer A", rightLabel: "Peer B", steps: ["Propose Algorithms ->", "<- Accept Algorithms", "Exchange Keys (DH) ->", "<- Exchange Keys (DH)", "Authenticate ->", "<- Authenticate", "ISAKMP SA Established"] } },
    hack: {
      memory: "Phase 1 = INTRODUCE yourself and SHAKE HANDS before doing business. Three steps: Negotiate (what algorithms?), Exchange keys (DH), Authenticate (prove who you are). Result = ISAKMP SA (management tunnel).",
      practice: "Draw the Phase 1 sequence: 1) Algorithm proposal/acceptance, 2) Diffie-Hellman key exchange, 3) Authentication (PSK or cert). Label the output: one bidirectional ISAKMP SA. Then draw Phase 2 beside it.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. Phase 1 = authenticate peers + build management tunnel. Phase 2 = negotiate data tunnel. The exam tests the PURPOSE of each phase.",
    },
    micro: [
      { id: "5.5.f.1", term: "IKE Phase 1",                  def: "Authenticate peers, build bidirectional management (ISAKMP) SA. Happens first.", weight: "high" },
      { id: "5.5.f.2", term: "IKE Phase 2",                  def: "Negotiate IPsec SAs (one per direction) for data traffic. Uses the Phase 1 tunnel to protect negotiation.", weight: "high" },
      { id: "5.5.f.3", term: "Phase order",                  def: "Phase 1 ALWAYS first. Phase 2 can't happen without Phase 1 because it negotiates inside the P1 tunnel.", weight: "high" }
    ]
  },

  "5.5.g": {
    info: "<p><strong>IKE Phase 2 (Quick Mode)</strong> negotiates the <strong>IPsec Security Associations</strong> that will protect the actual user data traffic. Phase 2 runs <em>inside</em> the secure tunnel established by Phase 1 — the ISAKMP SA encrypts and authenticates the Phase 2 negotiation.</p><p>Phase 2 performs:</p><ul><li><strong>IPsec SA negotiation</strong> — peers agree on the encryption and integrity algorithms for the data tunnel (may differ from Phase 1 algorithms)</li><li><strong>Interesting traffic definition</strong> — peers confirm which traffic will be protected (defined by crypto ACLs that specify source/destination subnets)</li><li><strong>SA lifetime negotiation</strong> — how long the IPsec SAs are valid before rekeying (typically 3600 seconds or 4,608,000 KB of data)</li></ul><p>Key Phase 2 facts:</p><ul><li>Creates <strong>two unidirectional SAs</strong> — one for each direction of traffic (Peer A → Peer B, and Peer B → Peer A). Each SA has its own SPI (Security Parameter Index).</li><li>Phase 2 uses <strong>Quick Mode</strong> — faster than Phase 1 because it runs inside an already-secured channel</li><li>Optional <strong>Perfect Forward Secrecy (PFS)</strong> — performs a new Diffie-Hellman exchange for Phase 2 keys, ensuring that compromising Phase 1 keys doesn't compromise Phase 2 data</li></ul><p>Summary of both phases: <strong>Phase 1</strong> = one bidirectional ISAKMP SA (management). <strong>Phase 2</strong> = two unidirectional IPsec SAs (data). Phase 1 protects the negotiation of Phase 2. Phase 2 protects the actual user traffic.</p>",
    visual: { type: "handshake", params: { leftLabel: "Peer A", rightLabel: "Peer B", steps: ["Propose IPsec SAs ->", "<- Accept SAs", "Exchange nonces ->", "<- Exchange nonces", "IPsec Tunnel UP (2 unidirectional SAs)"] } },
    hack: {
      memory: "Phase 2 = QUICK Mode = quickly build the DATA tunnel. Phase 1 = 1 bidirectional SA (management). Phase 2 = 2 unidirectional SAs (data, one per direction). Two phases, different tunnel types.",
      practice: "Draw the complete IKE/IPsec flow on one page: Phase 1 → ISAKMP SA (bidirectional, management) → Phase 2 → 2 IPsec SAs (unidirectional, data). Label the purpose of each. This is the master diagram.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. Key facts: Phase 1 = one bidirectional SA (ISAKMP). Phase 2 = two unidirectional SAs (IPsec, one per direction). Phase 2 = Quick Mode.",
    },
    micro: [
      { id: "5.5.g.1", term: "Phase 1 = 1 bidirectional SA",  def: "ISAKMP SA. Used only for Phase 2 negotiation. Not for user data.", weight: "high" },
      { id: "5.5.g.2", term: "Phase 2 = 2 unidirectional SAs", def: "IPsec SAs. One for inbound, one for outbound. Protect actual data traffic.", weight: "high" },
      { id: "5.5.g.3", term: "Quick Mode (Phase 2)",          def: "Phase 2 negotiation mode. Faster than Phase 1 because it reuses the P1 tunnel.", weight: "med" }
    ]
  },

  "5.5.h": {
    info: "<p><strong>Tunnel mode</strong> encrypts the <strong>ENTIRE original IP packet</strong> — both the IP header and the payload — and encapsulates it inside a completely new IP packet. The original source and destination IP addresses are hidden inside the encrypted payload, invisible to anyone inspecting the packet in transit.</p><p>Tunnel mode packet structure:</p><ul><li><strong>New outer IP header</strong> — uses the tunnel endpoint addresses (e.g., the public IPs of the two VPN routers). This is what routers on the internet use to forward the packet.</li><li><strong>ESP header</strong> — identifies this as an IPsec packet</li><li><strong>Encrypted original IP header</strong> — the real source/destination (e.g., 10.1.1.50 → 10.2.2.100) is hidden inside the encrypted payload</li><li><strong>Encrypted original payload</strong> — the actual data being transmitted</li></ul><p>Key facts about tunnel mode:</p><ul><li><strong>Default mode for site-to-site VPNs</strong> — because you want to hide internal addressing from the internet</li><li><strong>Hides the original IP addresses</strong> — an eavesdropper only sees traffic between the two router public IPs, not the actual endpoints</li><li><strong>More overhead</strong> — adds a complete new IP header (20 bytes) plus ESP header/trailer</li><li>Analogy: putting a sealed letter inside a new envelope with different addresses on the outside</li></ul>",
    visual: { type: "encapsulation", params: { layers: [{ label: "New IP Header (Router IPs)", color: "#6366f1" }, { label: "ESP Header", color: "#10b981" }, { label: "Original IP Header (ENCRYPTED)", color: "#3b82f6" }, { label: "Original Payload (ENCRYPTED)", color: "#3b82f6" }] } },
    hack: {
      memory: "TUNNEL mode = EVERYTHING hidden in the tunnel. Original IP header + payload = both encrypted. Like a sealed letter inside a new envelope. Default for site-to-site VPNs.",
      practice: "Draw side by side: Tunnel mode [New IP | ESP | Encrypted(Old IP + Data)] vs Transport mode [Original IP | ESP | Encrypted(Data only)]. Circle what's visible to an eavesdropper in each. Tunnel hides everything; transport exposes the IP header.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. Tunnel = encrypts whole packet, hides original IPs (site-to-site default). Transport = encrypts payload only (host-to-host). The exam tests which mode hides the original IP.",
    },
    micro: [
      { id: "5.5.h.1", term: "IPsec Tunnel mode",            def: "Entire original packet (incl. IP header) encrypted and wrapped in new IP header. Hides original IPs. Site-to-site default.", weight: "high" },
      { id: "5.5.h.2", term: "IPsec Transport mode",         def: "Only payload encrypted. Original IP header preserved. Used host-to-host (end-to-end, not gateway-to-gateway).", weight: "high" },
      { id: "5.5.h.3", term: "Which hides original IP?",     def: "TUNNEL mode. Transport mode leaves IP header visible. Exam test point.", weight: "high" }
    ]
  },

  "5.5.i": {
    info: "<p><strong>Transport mode</strong> encrypts <strong>only the payload</strong> of the original IP packet, leaving the original IP header <strong>intact and visible</strong>. The source and destination IP addresses are not hidden — anyone intercepting the packet can see who is communicating, just not the content of the communication.</p><p>Transport mode packet structure:</p><ul><li><strong>Original IP header</strong> — unchanged and visible, showing the real source and destination IPs</li><li><strong>ESP header</strong> — inserted between the IP header and the payload</li><li><strong>Encrypted payload</strong> — the original data (TCP/UDP segment and application data) is encrypted</li><li><strong>ESP trailer and authentication</strong> — integrity and authentication data</li></ul><p>Key facts about transport mode:</p><ul><li><strong>Used for host-to-host communication</strong> — when two specific devices need encrypted communication between themselves (not between networks)</li><li><strong>Both endpoints must be IPsec-capable</strong> — unlike tunnel mode where only the routers need IPsec</li><li><strong>Less overhead</strong> — no additional IP header is added (saves 20 bytes per packet)</li><li><strong>Does NOT hide addressing</strong> — an eavesdropper sees the real source and destination IPs but cannot read the data</li><li>Common use case: encrypting traffic between two servers in the same data center, or GRE tunnel protection</li></ul><p>The exam comparison: <strong>Tunnel mode</strong> = encrypts entire packet including IP header (default for site-to-site VPN). <strong>Transport mode</strong> = encrypts payload only, IP header visible (used for host-to-host or when combined with GRE).</p>",
    visual: { type: "encapsulation", params: { layers: [{ label: "Original IP Header (VISIBLE)", color: "#94a3b8" }, { label: "ESP Header", color: "#10b981" }, { label: "Payload (ENCRYPTED)", color: "#3b82f6" }] } },
    hack: {
      memory: "TRANSPORT mode = only the CARGO (payload) is encrypted. The shipping label (IP header) stays readable. Used when both hosts know each other's address and just need the data encrypted.",
      practice: "Scenario drill: 'Two routers connecting branches' = tunnel. 'Two servers in the same DC' = transport. 'Employee laptop to corporate VPN' = tunnel. 'GRE tunnel needing encryption' = transport. Run 5+ scenarios until automatic.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. Tunnel = whole packet encrypted (site-to-site default). Transport = payload only (host-to-host). Most exam questions ask which mode hides the original IP header.",
    },
    micro: [
      { id: "5.5.i.1", term: "Site-to-site = Tunnel mode",   def: "Gateway-to-gateway IPsec always uses Tunnel mode. Entire original packet hidden inside the tunnel.", weight: "high" },
      { id: "5.5.i.2", term: "Host-to-host = Transport mode", def: "End-to-end IPsec (e.g., between two servers). Transport mode protects just the payload.", weight: "high" }
    ]
  },

  "5.5.j": {
    info: "<p><strong>GRE over IPsec</strong> combines two technologies to solve a fundamental limitation: IPsec alone <strong>cannot carry multicast, broadcast, or routing protocol traffic</strong>. GRE (Generic Routing Encapsulation) handles the encapsulation of any protocol, and IPsec handles the encryption.</p><p>The problem with IPsec alone:</p><ul><li>IPsec works with unicast traffic only — it cannot encapsulate <strong>multicast</strong> packets</li><li>Routing protocols like <strong>OSPF and EIGRP</strong> use multicast for neighbor discovery and route exchange (OSPF uses 224.0.0.5/6, EIGRP uses 224.0.0.10)</li><li>Without multicast support, you cannot run dynamic routing protocols across a pure IPsec tunnel</li></ul><p>GRE solves this by encapsulating ANY protocol inside a GRE tunnel:</p><ul><li>GRE can carry <strong>multicast, broadcast, and non-IP protocols</strong></li><li>However, GRE provides <strong>zero encryption</strong> — packets are encapsulated but not protected</li></ul><p>The combination — <strong>GRE over IPsec</strong> — wraps traffic in GRE first (enabling multicast/routing protocol support), then encrypts the entire GRE tunnel with IPsec. The layering order is: Original Packet → GRE Encapsulation → IPsec Encryption → New IP Header.</p><p>An alternative technology is <strong>DMVPN (Dynamic Multipoint VPN)</strong>, which builds on GRE and IPsec to create scalable hub-and-spoke VPN topologies where spokes can dynamically create direct spoke-to-spoke tunnels. DMVPN uses mGRE (multipoint GRE) and NHRP (Next Hop Resolution Protocol).</p>",
    visual: { type: "encapsulation", params: { layers: [{ label: "IPsec (Encryption)", color: "#10b981" }, { label: "GRE (Encapsulation)", color: "#f59e0b" }, { label: "Original Packet (Multicast/Routing)", color: "#3b82f6" }] } },
    hack: {
      memory: "GRE = the moving truck (carries anything, including multicast). IPsec = the armored escort (encrypts everything). GRE over IPsec = armored moving truck. 'How do you run OSPF over a VPN?' = GRE over IPsec.",
      practice: "List what GRE adds that IPsec lacks: multicast, broadcast, non-IP protocols, routing protocols (OSPF/EIGRP). Then explain: GRE encapsulates these into unicast → IPsec encrypts the unicast. Problem solved.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 54. Wendell Odom OCG Chapter 16. Classic exam question: 'How do you run OSPF over an IPsec VPN?' Answer: GRE over IPsec. Pure IPsec can't carry multicast — GRE bridges that gap.",
    },
    micro: [
      { id: "5.5.j.1", term: "GRE",                          def: "Generic Routing Encapsulation. Cisco tunneling protocol. Carries multicast/broadcast and multiple L3 protocols.", weight: "high" },
      { id: "5.5.j.2", term: "IPsec can't carry multicast",  def: "Pure IPsec is unicast only. Routing protocols (OSPF, EIGRP) use multicast, so pure IPsec can't run them.", weight: "high" },
      { id: "5.5.j.3", term: "GRE over IPsec",               def: "Wrap traffic in GRE (adds multicast support) then encrypt with IPsec. Enables OSPF over VPN.", weight: "high" },
      { id: "5.5.j.4", term: "DMVPN",                        def: "Dynamic Multipoint VPN. Cisco solution for scalable hub-and-spoke with dynamic spoke-to-spoke tunnels.", weight: "med" }
    ]
  },

  // ── 5.6 Configure and verify ACLs ────────────────────────────

  "5.6.a": {
    info: "<p><strong>Standard ACLs</strong> filter traffic based on <strong>source IP address only</strong>. They cannot examine the destination IP, protocol, or port number. This makes them simple but imprecise — they either allow or block all traffic from a given source, regardless of where it's going or what protocol it uses.</p><p>Standard ACL characteristics:</p><ul><li><strong>Numbered range: 1-99</strong> (expanded range: 1300-1999)</li><li>Match criteria: <strong>source IP address only</strong> (with wildcard mask)</li><li>Syntax: <code>access-list [1-99] permit|deny [source] [wildcard]</code></li><li>Example: <code>access-list 10 permit 192.168.1.0 0.0.0.255</code> — permits any source in 192.168.1.0/24</li></ul><p><strong>Placement rule: place standard ACLs as close to the DESTINATION as possible.</strong> Because standard ACLs can only match the source IP, placing them near the source would block that source from reaching ALL destinations — not just the one you intended. By placing near the destination, you block traffic only where it's not wanted while allowing the source to still reach other destinations.</p><p>Example: if you want to block 10.0.0.0/24 from reaching the server at 192.168.1.100 but still allow it to reach 172.16.0.0, place the standard ACL on the router interface closest to 192.168.1.100 (outbound). Placing it near the source (inbound) would block 10.0.0.0/24 from reaching both destinations.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Source IP", "Standard ACL (checks source only)", "Permit/Deny"], color: "#3b82f6" } },
    hack: {
      memory: "Standard = Source only, numbers 1-99, placed near DESTINATION. Mnemonic: 'SD' — Standard → Destination. Standard is Simple — Source only, placed at the end of the path.",
      practice: "In Packet Tracer: <code>access-list 10 permit 192.168.1.0 0.0.0.255</code>, apply outbound on the interface nearest the destination: <code>ip access-group 10 out</code>. Verify with <code>show access-lists</code> and test traffic.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 33-35 (Standard ACLs). Wendell Odom OCG Chapters 3-4 (Vol 2). ACL placement is a top-5 most tested CCNA concept. Standard = destination, Extended = source. Memorize as absolute rule.",
    },
    micro: [
      { id: "5.6.a.1", term: "Standard ACL",                 def: "Matches source IP ONLY. Numbered 1-99, 1300-1999. Simple, broad filtering.", weight: "high" },
      { id: "5.6.a.2", term: "Standard ACL placement",       def: "As close to DESTINATION as possible. Broad source matching means you'd block legitimate traffic if placed near source.", weight: "high" },
      { id: "5.6.a.3", term: "Standard ACL syntax",          def: "'access-list [1-99] {permit|deny} [source] [wildcard]'. Or named: 'ip access-list standard [name]'.", weight: "high" },
      { id: "5.6.a.4", term: "host keyword",                 def: "Shortcut for exact match. 'host 10.1.1.1' = '10.1.1.1 0.0.0.0'.", weight: "med" },
      { id: "5.6.a.5", term: "any keyword",                  def: "Shortcut for all. 'any' = '0.0.0.0 255.255.255.255'.", weight: "med" }
    ]
  },

  "5.6.b": {
    info: "<p><strong>Extended ACLs</strong> provide granular traffic filtering by examining <strong>source IP, destination IP, protocol (TCP/UDP/ICMP), and port numbers</strong>. This precision allows you to permit or deny specific types of traffic while allowing others — for example, blocking HTTP to one server while permitting SSH to the same server.</p><p>Extended ACL characteristics:</p><ul><li><strong>Numbered range: 100-199</strong> (expanded range: 2000-2699)</li><li>Match criteria: source IP, destination IP, protocol, source port, destination port</li><li>Syntax: <code>access-list [100-199] permit|deny [protocol] [source] [src-wildcard] [destination] [dst-wildcard] eq [port]</code></li><li>Example: <code>access-list 100 permit tcp 10.0.0.0 0.0.0.255 host 192.168.1.100 eq 80</code> — permits HTTP (TCP 80) from 10.0.0.0/24 to 192.168.1.100 only</li></ul><p><strong>Placement rule: place extended ACLs as close to the SOURCE as possible.</strong> Because extended ACLs can precisely match on source, destination, protocol, and port, they can block ONLY the specific unwanted traffic without affecting anything else. Placing them near the source prevents unwanted traffic from consuming bandwidth as it traverses the network. The granularity makes near-source placement safe — no collateral damage.</p><p>Port matching operators: <code>eq</code> (equal), <code>gt</code> (greater than), <code>lt</code> (less than), <code>neq</code> (not equal), <code>range [start] [end]</code>. Common ports: HTTP=80, HTTPS=443, SSH=22, Telnet=23, DNS=53, DHCP=67/68, FTP=20/21, SMTP=25, SNMP=161.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Source IP + Port", "Extended ACL (checks src, dst, proto, port)", "Destination IP + Port"], color: "#8b5cf6" } },
    hack: {
      memory: "Extended = EVERYTHING (src, dst, protocol, port). Numbers 100-199. Placed near SOURCE. Mnemonic: 'ES' — Extended → Source. Extended has Extra fields, so block precisely and early.",
      practice: "Write this from memory: <code>access-list 100 permit tcp 10.0.0.0 0.0.0.255 host 192.168.1.100 eq 443</code>. Apply inbound on source-side: <code>ip access-group 100 in</code>. Test: HTTPS should work, HTTP (80) should be denied by implicit deny.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 34-35 (Extended ACLs). Wendell Odom OCG Chapters 3-4 (Vol 2). Extended ACL syntax is HEAVILY tested. Practice writing the full command from scratch: protocol → source+wildcard → destination+wildcard → eq port.",
    },
    micro: [
      { id: "5.6.b.1", term: "Extended ACL",                 def: "Matches source + destination + protocol + port. Numbered 100-199, 2000-2699. Granular filtering.", weight: "high" },
      { id: "5.6.b.2", term: "Extended ACL placement",       def: "As close to SOURCE as possible. Granular match = can block specific traffic without affecting others.", weight: "high" },
      { id: "5.6.b.3", term: "Extended ACL syntax",          def: "'access-list [100-199] {permit|deny} [proto] [src wildcard] [dst wildcard] [operator port]'.", weight: "high" },
      { id: "5.6.b.4", term: "Port operators",               def: "eq (equals), gt, lt, neq, range. Example: 'eq 80' matches HTTP. 'range 20 21' matches FTP.", weight: "high" }
    ]
  },

  "5.6.c": {
    info: "<p><strong>Named ACLs</strong> use descriptive text names instead of numbers, making configurations self-documenting and easier to manage. They can be either standard or extended. Named ACLs are the modern best practice for all ACL configurations.</p><p>Configuration syntax:</p><ul><li><code>ip access-list standard [NAME]</code> — creates a named standard ACL and enters ACL config mode</li><li><code>ip access-list extended [NAME]</code> — creates a named extended ACL and enters ACL config mode</li><li>Inside ACL config mode, entries are typed without the <code>access-list</code> prefix: just <code>permit</code> or <code>deny</code></li></ul><p>Example:<br><code>ip access-list extended ALLOW_WEB</code><br><code>&nbsp;10 permit tcp any any eq 80</code><br><code>&nbsp;20 permit tcp any any eq 443</code><br><code>&nbsp;30 deny ip any any log</code></p><p>Key advantages over numbered ACLs:</p><ul><li><strong>Self-documenting</strong> — <code>BLOCK_GUEST_TELNET</code> is immediately understandable vs <code>access-list 150</code></li><li><strong>Sequence number editing</strong> — insert entries between existing ones (<code>15 permit...</code>), delete specific entries (<code>no 20</code>), or resequence the entire ACL (<code>ip access-list resequence [NAME] [start] [increment]</code>)</li><li><strong>Easier maintenance</strong> — no need to remove and recreate the entire ACL to add or change one entry</li></ul><p>Note: modern IOS versions also assign sequence numbers to numbered ACLs, but named ACLs are still preferred for readability and manageability.</p>",
    visual: { type: "comparison", params: { left: { label: "Numbered ACL", items: ["access-list 100 permit...", "Hard to identify purpose", "Difficult to edit mid-list"] }, right: { label: "Named ACL", items: ["ip access-list ext WEB_TRAFFIC", "Self-documenting name", "Edit with sequence numbers"] } } },
    hack: {
      memory: "Named = NICE names. <code>BLOCK_TELNET</code> is instantly clear vs <code>access-list 150</code>. Named ACLs enter a sub-config mode where you type entries directly. Sequence numbers let you edit surgically.",
      practice: "Lab: <code>ip access-list extended ALLOW_WEB</code> → <code>10 permit tcp any any eq 80</code> → <code>20 permit tcp any any eq 443</code>. Then insert: <code>15 permit tcp any any eq 8080</code>. Delete: <code>no 20</code>. Verify with <code>show access-lists</code>.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 35. Wendell Odom OCG Chapter 4 (Vol 2). The exam tests named ACL syntax and sequence number editing. Know the sub-config mode and <code>no [seq#]</code> to delete individual entries.",
    },
    micro: [
      { id: "5.6.c.1", term: "Named ACL",                    def: "Alternative to numbered. 'ip access-list {standard|extended} [name]' → sub-config mode.", weight: "high" },
      { id: "5.6.c.2", term: "Sequence numbers",             def: "Each ACE has a seq number (10, 20, 30...). Insert by specifying seq. Delete with 'no [seq#]'.", weight: "high" },
      { id: "5.6.c.3", term: "resequence command",           def: "'ip access-list resequence [name] [start] [increment]'. Renumber entries for easier insertion.", weight: "med" }
    ]
  },

  "5.6.d": {
    info: "<p><strong>Wildcard masks</strong> are used in ACLs to specify which bits of an IP address to check (0) and which to ignore (1) when matching. They are the <strong>inverse of subnet masks</strong> — where a subnet mask uses 1s for 'network' and 0s for 'host,' a wildcard mask uses 0s for 'must match' and 1s for 'don't care.'</p><p>Conversion method: subtract each subnet mask octet from 255:</p><ul><li>/24 = 255.255.255.0 → wildcard <strong>0.0.0.255</strong> (check first 3 octets, ignore last)</li><li>/16 = 255.255.0.0 → wildcard <strong>0.0.255.255</strong> (check first 2 octets, ignore last 2)</li><li>/28 = 255.255.255.240 → wildcard <strong>0.0.0.15</strong> (255-240=15)</li><li>/30 = 255.255.255.252 → wildcard <strong>0.0.0.3</strong> (255-252=3)</li><li>/27 = 255.255.255.224 → wildcard <strong>0.0.0.31</strong> (255-224=31)</li><li>/25 = 255.255.255.128 → wildcard <strong>0.0.0.127</strong> (255-128=127)</li></ul><p>In binary, wildcard bits work as follows: a <strong>0 bit</strong> means 'this bit must match the specified address exactly.' A <strong>1 bit</strong> means 'this bit can be anything (don't care).' For example, wildcard 0.0.0.255 in binary is <code>00000000.00000000.00000000.11111111</code> — the first 24 bits must match, the last 8 can be anything.</p><p>Wildcard masks can also create non-contiguous patterns (e.g., matching only even or odd IP addresses), but this is rarely used on the CCNA and mainly a theoretical point.</p>",
    visual: { type: "binary-breakdown", params: { bits: "00000000.00000000.00000000.11111111", label: "Wildcard 0.0.0.255 (/24)", highlight: [24, 25, 26, 27, 28, 29, 30, 31] } },
    hack: {
      memory: "Wildcard = 255 minus subnet mask, per octet. 0 = MUST match (check). 1 = DON'T care (ignore). Opposite of subnet mask. /24 → 0.0.0.255. /30 → 0.0.0.3. /28 → 0.0.0.15.",
      practice: "Speed drill: convert these to wildcards in under 60 seconds without a calculator: /24, /16, /28, /30, /27, /25, /32, /0. Then reverse: wildcard 0.0.0.63 = what prefix? (/26). Build speed.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 33-35. Wendell Odom OCG Chapters 3-4 (Vol 2). Wildcard math appears on nearly every exam. The fastest method: 255 minus each octet. Practice until automatic — no thinking, just subtraction.",
    },
    micro: [
      { id: "5.6.d.1", term: "Wildcard mask",                def: "INVERSE of subnet mask. 0 = must match, 1 = don't care. 255.255.255.0 → 0.0.0.255.", weight: "high" },
      { id: "5.6.d.2", term: "Quick math: 255 − mask octet", def: "/24 mask 255.255.255.0 → 0.0.0.255. /26 mask ...192 → 255-192=63 → 0.0.0.63.", weight: "high" },
      { id: "5.6.d.3", term: "Single host wildcard",         def: "0.0.0.0 = exact IP match. 'host X.X.X.X' is shortcut for 'X.X.X.X 0.0.0.0'.", weight: "high" }
    ]
  },

  "5.6.e": {
    info: "<p>Two <strong>special wildcard mask shortcuts</strong> simplify common ACL entries and appear frequently on the exam:</p><ul><li><strong><code>host</code> keyword</strong> = wildcard <code>0.0.0.0</code> — means 'match this exact IP address, every bit must match.' Example: <code>permit host 10.0.0.1</code> is equivalent to <code>permit 10.0.0.1 0.0.0.0</code>. All 32 bits are checked.</li><li><strong><code>any</code> keyword</strong> = wildcard <code>255.255.255.255</code> — means 'match any IP address, no bits are checked.' Example: <code>deny any</code> is equivalent to <code>deny 0.0.0.0 255.255.255.255</code>. Zero bits are checked.</li></ul><p>Common wildcard masks and their meanings:</p><ul><li><code>0.0.0.0</code> = match exactly one host (<code>host</code> keyword)</li><li><code>0.0.0.255</code> = match a /24 network (last octet is 'don't care')</li><li><code>0.0.255.255</code> = match a /16 network</li><li><code>0.255.255.255</code> = match a /8 network</li><li><code>255.255.255.255</code> = match any IP (<code>any</code> keyword)</li></ul><p>The exam uses both long-form (with explicit wildcard) and shortcut syntax interchangeably. You must be able to read and write both forms instantly. When reviewing <code>show access-lists</code> output, IOS displays the shortcut form — <code>host</code> for /32 matches and <code>any</code> for match-all.</p>",
    visual: { type: "comparison", params: { left: { label: "Long Form", items: ["10.0.0.1 0.0.0.0", "10.0.0.0 0.0.0.255", "0.0.0.0 255.255.255.255"] }, right: { label: "Shortcut", items: ["host 10.0.0.1", "10.0.0.0 0.0.0.255 (/24)", "any"] } } },
    hack: {
      memory: "0.0.0.0 = Host (all zeros = check ALL bits = one exact host). 255.255.255.255 = Any (all ones = check NO bits = any address). <code>host</code> and <code>any</code> are just shortcuts for these wildcards.",
      practice: "Translate both directions: <code>permit 10.1.1.1 0.0.0.0</code> → <code>permit host 10.1.1.1</code>. <code>deny 0.0.0.0 255.255.255.255</code> → <code>deny any</code>. <code>permit host 192.168.1.1</code> → <code>permit 192.168.1.1 0.0.0.0</code>. Practice until both forms are interchangeable in your mind.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 33-35. Wendell Odom OCG Chapters 3-4 (Vol 2). The exam uses both forms interchangeably. <code>host</code> = 0.0.0.0 wildcard. <code>any</code> = 255.255.255.255 wildcard. Know both directions instantly.",
    },
    micro: [
      { id: "5.6.e.1", term: "host keyword",                 def: "'host 10.1.1.1' = '10.1.1.1 0.0.0.0'. Exact single-host match.", weight: "high" },
      { id: "5.6.e.2", term: "any keyword",                  def: "'any' = '0.0.0.0 255.255.255.255'. Matches all IPs.", weight: "high" },
      { id: "5.6.e.3", term: "Both forms valid",             def: "Exam uses both. 'permit host 10.1.1.1' and 'permit 10.1.1.1 0.0.0.0' are identical.", weight: "high" }
    ]
  },

  "5.6.f": {
    info: "<p>Every ACL has an invisible <strong>implicit deny any</strong> at the very end. This rule is not displayed in <code>show access-lists</code> output but is always present and always enforced. If a packet does not match ANY explicit permit or deny statement in the ACL, it hits the implicit deny and is <strong>dropped</strong>.</p><p>Critical implications of the implicit deny:</p><ul><li>An ACL with <strong>only deny statements</strong> blocks ALL traffic — the explicit denies block what they match, and the implicit deny catches everything else</li><li>An ACL with <strong>only one permit</strong> effectively becomes a whitelist — only the explicitly permitted traffic passes; everything else is denied</li><li>You must include <strong>at least one permit statement</strong> for the ACL to allow any traffic at all</li><li>If you want to deny specific traffic but allow everything else, you need: specific deny entries followed by <code>permit any</code> (or <code>permit ip any any</code> for extended ACLs)</li></ul><p>The implicit deny does NOT increment match counters in <code>show access-lists</code> because it's not a visible rule. If you want to see how many packets are being caught by the implicit deny, add an explicit <code>deny any log</code> (or <code>deny ip any any log</code>) as the last entry — this makes it visible and generates syslog messages for each dropped packet.</p><p>A common troubleshooting mistake: applying an ACL to an interface and then wondering why all traffic stopped — check if the ACL has any permit statements. If not, the implicit deny is blocking everything.</p>",
    visual: { type: "layer-stack", params: { layers: ["permit 10.0.0.0/24", "permit 192.168.1.0/24", "deny 172.16.0.0/16", "(implicit deny any)"], highlight: 3 } },
    hack: {
      memory: "Every ACL ends with an INVISIBLE 'deny all.' It's always there, you just can't see it. If you forget to permit something, it's gone. The invisible wall at the end catches EVERYTHING that wasn't explicitly allowed.",
      practice: "Lab: Create <code>access-list 10 permit 10.0.0.0 0.0.0.255</code> and apply it. Test: 10.x.x.x works, everything else blocked. Now add <code>access-list 10 deny any log</code> to make the implicit deny visible — watch the syslog messages for dropped packets.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 33-35. Wendell Odom OCG Chapters 3-4 (Vol 2). The implicit deny is one of the most tested ACL concepts. If a question shows an ACL without <code>permit any</code>, ask: what gets silently blocked?",
    },
    micro: [
      { id: "5.6.f.1", term: "Implicit deny any",            def: "Every ACL has invisible 'deny any' at the end. Traffic NOT explicitly permitted = BLOCKED.", weight: "high" },
      { id: "5.6.f.2", term: "Always end with explicit deny?", def: "Optional but best practice — makes implicit deny visible. Also enables match-counter logging.", weight: "high" },
      { id: "5.6.f.3", term: "Missing permit any trap",      def: "ACL only permits specific traffic → everything else silently dropped. Classic exam 'what's broken?' scenario.", weight: "high" }
    ]
  },

  "5.6.g": {
    info: "<p>ACLs process rules in <strong>top-down order</strong>, and the <strong>first matching rule wins</strong>. Once a packet matches a permit or deny statement, that action is applied immediately and <strong>processing stops</strong> — no further rules are evaluated. This makes <strong>rule order critical</strong>.</p><p>Key ordering principles:</p><ul><li><strong>Specific rules before general rules</strong> — if you want to deny one host but permit its entire subnet, the host deny MUST come first. If the subnet permit is first, the host matches it and is permitted before the deny is ever checked.</li><li><strong><code>permit any</code> at the top = game over</strong> — everything matches immediately, and all subsequent deny rules are never reached</li><li><strong>The most restrictive rules go first</strong> — deny specific threats, then permit broader categories, then the implicit deny catches the rest</li></ul><p>Example of correct order:<br><code>10 deny host 10.0.0.5</code> — blocks the specific host<br><code>20 permit 10.0.0.0 0.0.0.255</code> — permits the rest of the subnet<br><code>(implicit deny any)</code> — blocks everything else</p><p>Example of BROKEN order:<br><code>10 permit 10.0.0.0 0.0.0.255</code> — host 10.0.0.5 matches HERE and is permitted<br><code>20 deny host 10.0.0.5</code> — NEVER reached for 10.0.0.5<br>Result: 10.0.0.5 is permitted even though you tried to deny it.</p><p>Exam technique: when presented with an ACL and asked 'what happens to packet X,' trace through the rules top-to-bottom, find the FIRST match, and that's your answer. Stop reading — the rest is irrelevant.</p>",
    visual: { type: "layer-stack", params: { layers: ["Rule 1: deny host 10.0.0.5", "Rule 2: permit 10.0.0.0/24", "Rule 3: deny any (implicit)"], highlight: 0 } },
    hack: {
      memory: "FIRST MATCH WINS. Like a bouncer with a checklist — checks top to bottom, acts on the FIRST match, ignores everything below. ORDER MATTERS. Specific before general. Always.",
      practice: "Write two ACLs: one correct (deny host first, permit subnet second) and one broken (reversed). Trace a packet from 10.0.0.5 through each. See how the broken order lets the host through. This cements the concept permanently.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 33-35. Wendell Odom OCG Chapters 3-4 (Vol 2). Exam questions present ACLs and ask 'what happens to packet X?' Trace top-to-bottom, find first match, done. Don't read further.",
    },
    micro: [
      { id: "5.6.g.1", term: "Top-down processing",          def: "Router evaluates ACEs in order (sequence number). First match wins. Stops processing immediately.", weight: "high" },
      { id: "5.6.g.2", term: "Order matters",                def: "Specific rules must come BEFORE general rules. 'deny host X' above 'permit subnet' — reverse and host X would match 'permit' first.", weight: "high" },
      { id: "5.6.g.3", term: "Trace exam method",            def: "Given packet + ACL, trace ACE-by-ACE until first match. Action at first match = the result.", weight: "high" }
    ]
  },

  "5.6.h": {
    info: "<p><strong>Standard ACLs should be placed as close to the DESTINATION as possible.</strong> This is one of the most tested CCNA concepts and must be memorized as an absolute rule.</p><p>Why near the destination? Because standard ACLs can only filter on <strong>source IP</strong>. If you place a standard ACL near the source, you block that source from reaching <em>all</em> destinations — not just the one you intended. The ACL has no way to distinguish where the traffic is going.</p><p>Example scenario: you want to block PC-A (10.0.0.50) from reaching Server-B (192.168.1.100) but still allow PC-A to reach Server-C (172.16.0.200).</p><ul><li><strong>WRONG: Place standard ACL near source (Router A, inbound)</strong> — blocks 10.0.0.50 from reaching BOTH Server-B AND Server-C. The ACL only sees the source; it can't distinguish the destinations.</li><li><strong>RIGHT: Place standard ACL near destination (Router nearest Server-B, outbound)</strong> — blocks 10.0.0.50 from reaching Server-B only. Traffic to Server-C takes a different path and is unaffected.</li></ul><p>The logic: standard ACLs are blunt instruments (source only), so you must apply them at the very end of the path, closest to where you want to block the traffic, to minimize collateral damage.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Source", "Router A", "Router B", "Standard ACL HERE (near Dest)", "Destination"], color: "#10b981" } },
    hack: {
      memory: "Standard = near Destination. Mnemonic: SD = Standard → Destination. Because standard only checks source, placing it near source blocks that source from reaching EVERYWHERE. Place at the end to be surgical.",
      practice: "Draw a network: Source → Router A → Router B → Router C → Destination. Also add a second destination off Router B. Place a standard ACL near the source and trace which destinations break. Move it near the intended destination and trace again. See the difference.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 33-35. Wendell Odom OCG Chapters 3-4 (Vol 2). ACL placement = top-5 tested CCNA topic. Standard = destination, Extended = source. This alone answers many exam questions.",
    },
    micro: [
      { id: "5.6.h.1", term: "Standard ACL near destination", def: "Blocks all source traffic. Must place near destination so legitimate traffic isn't blocked en route.", weight: "high" },
      { id: "5.6.h.2", term: "Extended ACL near source",     def: "Specific match (src+dst+proto+port). Place near source to save bandwidth by dropping early.", weight: "high" },
      { id: "5.6.h.3", term: "Why placement matters",        def: "Wrong placement = traffic dropped unnecessarily OR consumes bandwidth before being dropped.", weight: "high" }
    ]
  },

  "5.6.i": {
    info: "<p><strong>Extended ACLs should be placed as close to the SOURCE as possible.</strong> This is the complementary rule to standard ACL placement and is equally important for the exam.</p><p>Why near the source? Because extended ACLs can match on <strong>source IP, destination IP, protocol, and port number</strong>, they can precisely identify and block ONLY the specific unwanted traffic. There's no risk of collateral damage because the ACL can distinguish between different types of traffic from the same source.</p><p>Example scenario: you want to block HTTP (TCP 80) from 10.0.0.0/24 to Server 192.168.1.100 but allow all other traffic from that subnet.</p><ul><li><strong>RIGHT: Place extended ACL near source (Router A, inbound)</strong> — blocks only HTTP to 192.168.1.100. All other traffic (SSH, HTTPS, ping, traffic to other destinations) passes normally. The unwanted HTTP traffic is killed immediately, saving bandwidth across the network.</li><li><strong>WRONG: Place extended ACL near destination</strong> — still works logically, but the unwanted HTTP traffic consumes bandwidth across every link between source and destination before being dropped. Wasteful.</li></ul><p>The logic: extended ACLs are precision instruments (source + destination + protocol + port), so place them early to block bad traffic as soon as possible and save network bandwidth. The specificity ensures no legitimate traffic is accidentally blocked.</p><p>Memory aid: <strong>Standard → Destination (SD)</strong>. <strong>Extended → Source (ES)</strong>. Two letters each, easy to memorize.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Source", "Extended ACL HERE (near Src)", "Router B", "Router C", "Destination"], color: "#f59e0b" } },
    hack: {
      memory: "Extended = near Source. Mnemonic: ES = Extended → Source. Extended is specific enough to block precisely, so kill the bad traffic early and save bandwidth across the entire path.",
      practice: "Scenario: 'Block HTTP from 10.0.0.0/24 to 192.168.1.100.' Identify the source-side router. Write the ACL: <code>access-list 100 deny tcp 10.0.0.0 0.0.0.255 host 192.168.1.100 eq 80</code> + <code>access-list 100 permit ip any any</code>. Apply inbound on the source-side interface.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 33-35. Wendell Odom OCG Chapters 3-4 (Vol 2). The exam gives a network diagram and asks where to place the ACL. Standard = destination, Extended = source. This pair of rules answers many questions.",
    },
    micro: [
      { id: "5.6.i.1", term: "ACL placement decision",        def: "Given topology diagram, determine where to apply ACL. Standard → closest to dest. Extended → closest to source.", weight: "high" },
      { id: "5.6.i.2", term: "Scenario question method",     def: "Read: standard or extended? Read: source + destination. Apply the placement rule. Check direction.", weight: "high" }
    ]
  },

  "5.6.j": {
    info: "<p>ACLs are applied to router interfaces using the <code>ip access-group</code> command, specifying both the ACL and the <strong>direction</strong> (inbound or outbound). The direction is always from the <strong>router's perspective</strong>.</p><p>Application command: <code>ip access-group [ACL-name|number] in|out</code></p><ul><li><strong><code>in</code> (inbound)</strong> — filters packets <strong>entering</strong> the interface, BEFORE the routing decision. The router checks the ACL first; if permitted, the packet is then routed. If denied, the packet is dropped immediately.</li><li><strong><code>out</code> (outbound)</strong> — filters packets <strong>leaving</strong> the interface, AFTER the routing decision. The router makes the routing decision first, determines the exit interface, then checks the ACL on that interface. If denied, the packet is dropped before being transmitted.</li></ul><p>Rules and constraints:</p><ul><li><strong>One ACL per direction per protocol per interface</strong> — you can have one IPv4 ACL inbound AND one IPv4 ACL outbound on the same interface, but not two inbound IPv4 ACLs</li><li>The direction is relative to the ROUTER, not the end user. 'In' = traffic coming INTO the router through that interface. 'Out' = traffic going OUT of the router through that interface.</li><li>A packet generated BY the router itself (e.g., routing protocol updates, ping from the router) is NOT checked by outbound ACLs on the originating interface</li></ul><p>Verification: <code>show ip interface [interface]</code> displays which ACLs are applied to an interface and in which direction. Look for 'Outgoing access list' and 'Inbound access list' in the output.</p>",
    visual: { type: "handshake", params: { leftLabel: "Inbound Traffic", rightLabel: "Router Interface", steps: ["Packet arrives ->", "<- ACL IN checks (before routing)", "Routing decision ->", "<- ACL OUT checks (after routing)", "Packet exits ->"] } },
    hack: {
      memory: "IN = coming INTO the router. OUT = going OUT of the router. Think from the ROUTER's view, not the user's. One ACL per direction per interface. Draw the router, draw arrows: IN points at the router, OUT points away.",
      practice: "Lab: <code>int Gi0/0</code> → <code>ip access-group 100 in</code>. Verify: <code>show ip interface Gi0/0</code> — look for 'Inbound access list is 100'. Test traffic in both directions to confirm only inbound is filtered.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 33-35. Wendell Odom OCG Chapters 3-4 (Vol 2). The exam tests direction understanding. Draw the router, label interfaces, draw traffic arrows. IN = arrow toward router. OUT = arrow away.",
    },
    micro: [
      { id: "5.6.j.1", term: "ACL direction: in",            def: "Traffic ENTERING the router through this interface. Before routing decision.", weight: "high" },
      { id: "5.6.j.2", term: "ACL direction: out",           def: "Traffic LEAVING the router through this interface. After routing decision.", weight: "high" },
      { id: "5.6.j.3", term: "ip access-group [ACL] {in|out}", def: "Apply ACL on an interface. Must specify direction.", weight: "high" },
      { id: "5.6.j.4", term: "Think from router's view",     def: "Direction is relative to the ROUTER, not the client. In = into router. Out = out of router.", weight: "high" }
    ]
  },

  "5.6.k": {
    info: "<p>To restrict which IP addresses can remotely manage a device via VTY lines (SSH/Telnet), use <code>access-class</code> — NOT <code>ip access-group</code>. This is a critical distinction the exam tests.</p><p>Configuration:</p><ul><li><code>access-list 5 permit host 10.0.0.50</code> — standard ACL permitting only the admin workstation</li><li><code>line vty 0 15</code></li><li><code>access-class 5 in</code> — applies the ACL to VTY line access</li></ul><p>Key differences between the two application commands:</p><ul><li><strong><code>access-class [ACL] in</code></strong> — used on <strong>VTY lines</strong> to control which source IPs can establish remote management sessions (SSH/Telnet)</li><li><strong><code>ip access-group [ACL] in|out</code></strong> — used on <strong>interfaces</strong> to control which traffic flows through the router</li></ul><p>This is a critical security best practice. Without an <code>access-class</code> on VTY lines, <strong>any IP that can reach the device can attempt to log in</strong>. Even with strong passwords and SSH, limiting source IPs adds defense in depth — an attacker would need to compromise a whitelisted IP first.</p><p>A standard ACL is almost always used for <code>access-class</code> because you only need to filter on source IP (who is connecting). Extended ACLs work too but are rarely needed for this purpose.</p>",
    visual: { type: "handshake", params: { leftLabel: "Admin (10.0.0.50)", rightLabel: "line vty 0 15", steps: ["SSH attempt ->", "<- access-class checks source IP", "Source in ACL? PERMIT ->", "<- Login prompt"] } },
    hack: {
      memory: "access-CLASS = for VTY lines (who can SSH in). access-GROUP = for interfaces (what traffic flows through). CLASS = CLI access. GROUP = traffic flow. Don't mix them up — it's a common exam trap.",
      practice: "Lab: <code>access-list 5 permit host 10.0.0.50</code> → <code>line vty 0 15</code> → <code>access-class 5 in</code>. SSH from 10.0.0.50 (works) and from 10.0.0.99 (rejected). Verify with <code>show line vty 0</code>.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 35. Wendell Odom OCG Chapter 4 (Vol 2). The exam specifically tests <code>access-class in</code> for VTY vs <code>ip access-group in|out</code> for interfaces. This distinction is a guaranteed trap question.",
    },
    micro: [
      { id: "5.6.k.1", term: "access-class (VTY)",           def: "Apply ACL to VTY lines: 'access-class [ACL] in'. Controls who can SSH/Telnet to the router.", weight: "high" },
      { id: "5.6.k.2", term: "ip access-group (interface)",  def: "Apply ACL to physical interface: 'ip access-group [ACL] {in|out}'. Filters transit traffic.", weight: "high" },
      { id: "5.6.k.3", term: "Don't confuse the two",        def: "access-class for VTY, access-group for interfaces. Classic exam trap.", weight: "high" }
    ]
  },

  "5.6.l": {
    info: "<p>Modern IOS automatically assigns <strong>sequence numbers</strong> to ACL entries in increments of 10 (10, 20, 30...). Sequence numbers enable surgical editing of ACLs without removing and recreating the entire list.</p><p>Editing operations with sequence numbers:</p><ul><li><strong>Insert an entry</strong> — specify a sequence number between existing entries: entering ACL config mode with <code>ip access-list extended MY_ACL</code>, then typing <code>15 permit tcp any host 10.0.0.1 eq 443</code> inserts the new rule between entries 10 and 20</li><li><strong>Delete an entry</strong> — <code>no [sequence-number]</code> removes that specific line. Example: <code>no 20</code> deletes only line 20</li><li><strong>Resequence</strong> — <code>ip access-list resequence [NAME] [start] [increment]</code> renumbers all entries. Example: <code>ip access-list resequence MY_ACL 10 10</code> renumbers starting at 10 in increments of 10. Useful when you've run out of space between entries (e.g., 14 and 15 both exist).</li></ul><p>Before sequence numbers, any change to an ACL required deleting the entire ACL and recreating it from scratch — a process that briefly left the interface unprotected (no ACL applied) and was error-prone. Sequence numbers solved this by allowing in-place editing.</p><p>Best practice: leave gaps between sequence numbers (increments of 10 or 20) to allow room for future insertions without needing to resequence.</p>",
    visual: { type: "layer-stack", params: { layers: ["10 deny host 10.0.0.5", "15 permit tcp any host 10.0.0.1 eq 443 (INSERTED)", "20 permit 10.0.0.0/24", "30 deny any (implicit)"], highlight: 1 } },
    hack: {
      memory: "Sequence numbers = line numbers in a text editor. Insert between lines, delete specific lines, renumber the whole file. Edit surgically — never destroy and rebuild the entire ACL.",
      practice: "Lab: Create a named ACL with 3 entries (10, 20, 30). Insert at 15. Delete 20 with <code>no 20</code>. Resequence with <code>ip access-list resequence MY_ACL 10 10</code>. Verify each step with <code>show access-lists</code>.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 35. Wendell Odom OCG Chapter 4 (Vol 2). The exam tests ACL editing: <code>no [seq]</code> to delete, manual sequence to insert, <code>resequence</code> to renumber. Know you don't need to recreate the ACL.",
    },
    micro: [
      { id: "5.6.l.1", term: "ACL edit — delete entry",      def: "Under 'ip access-list' sub-config: 'no [seq#]'. Removes a single ACE without affecting others.", weight: "high" },
      { id: "5.6.l.2", term: "ACL edit — insert entry",      def: "Type '[seq#] permit/deny ...' under the named ACL sub-config. Inserts at that sequence number.", weight: "high" },
      { id: "5.6.l.3", term: "ip access-list resequence",    def: "Renumber existing ACEs to open space for inserts. E.g., 'resequence myacl 10 10' → 10, 20, 30...", weight: "med" }
    ]
  },

  "5.6.m": {
    info: "<p>Several <code>show</code> commands are essential for <strong>verifying and troubleshooting ACLs</strong>. The exam tests both which commands to use and how to interpret their output.</p><p>Key verification commands:</p><ul><li><strong><code>show access-lists</code></strong> — displays ALL ACLs (IPv4, IPv6, MAC) with sequence numbers and <strong>match counts</strong> (how many packets matched each rule). Match counts are invaluable for troubleshooting — a rule with 0 matches may be in the wrong order or the traffic isn't flowing as expected.</li><li><strong><code>show ip access-lists</code></strong> — displays only IPv4 ACLs with sequence numbers and match counts. Same as above but filtered to IPv4.</li><li><strong><code>show running-config | include access</code></strong> — shows ACL definitions AND where they're applied (both <code>ip access-group</code> on interfaces and <code>access-class</code> on VTY lines). Useful for finding which interface an ACL is applied to.</li><li><strong><code>show ip interface [interface]</code></strong> — shows which ACLs are applied to a specific interface. Look for 'Inbound access list is [name/number]' and 'Outgoing access list is [name/number]'. If it says 'not set,' no ACL is applied in that direction.</li></ul><p>Troubleshooting workflow: 1) <code>show access-lists</code> to verify the rules and check match counts. 2) <code>show ip interface</code> to verify the ACL is applied to the correct interface in the correct direction. 3) If match counts aren't incrementing, the ACL may be on the wrong interface or in the wrong direction.</p>",
    visual: { type: "comparison", params: { left: { label: "Show Commands", items: ["show access-lists", "show ip access-lists", "show ip interface Gi0/0"] }, right: { label: "What They Show", items: ["All ACLs + match counts", "IPv4 ACLs only", "ACLs applied to interface"] } } },
    hack: {
      memory: "<code>show access-lists</code> = see the rules + hit counts. <code>show ip interface X</code> = see WHERE ACLs are applied. If hit count = 0, the ACL isn't matching traffic — wrong interface, wrong direction, or wrong rule.",
      practice: "Lab: Apply an ACL, generate traffic, run <code>show access-lists</code>. Watch match counters increment. If a rule shows 0 matches, troubleshoot: is the ACL on the right interface? Right direction? Is the rule order correct?",
      effort: "low",
      meta: "Jeremy's IT Lab Day 33-35. Wendell Odom OCG Chapters 3-4 (Vol 2). The exam shows <code>show access-lists</code> output and asks you to interpret it. Practice reading: sequence number → action (permit/deny) → criteria → match count.",
    },
    micro: [
      { id: "5.6.m.1", term: "show access-lists",            def: "Displays all ACLs and their entries with match counts. Primary ACL verification command.", weight: "high" },
      { id: "5.6.m.2", term: "show ip interface [int]",      def: "Shows which ACL is applied on interface and direction. Useful for tracking application.", weight: "high" },
      { id: "5.6.m.3", term: "show ip access-lists",         def: "Like show access-lists but IPv4 only. Same output format.", weight: "med" }
    ]
  },

  // ── 5.7 Configure and verify Layer 2 security ────────────────

  "5.7.a": {
    info: "<p><strong>DHCP snooping</strong> is a Layer 2 security feature that acts as a firewall between untrusted hosts and legitimate DHCP servers. It validates DHCP messages by categorizing switch ports as <strong>trusted</strong> or <strong>untrusted</strong>, then filtering DHCP messages based on port trust state.</p><p>How DHCP snooping works:</p><ul><li><strong>Trusted ports</strong> — connected to legitimate DHCP servers or uplinks to other switches. ALL DHCP message types are permitted (Discover, Offer, Request, ACK, NAK, Release, Decline).</li><li><strong>Untrusted ports (default)</strong> — connected to end-user devices. Only <strong>DHCP client messages</strong> are allowed (Discover, Request, Release, Decline). <strong>DHCP server messages are blocked</strong> (Offer, ACK, NAK) — preventing rogue DHCP servers from distributing false gateway/DNS information.</li></ul><p>What DHCP snooping prevents:</p><ul><li><strong>Rogue DHCP server attacks</strong> — an attacker sets up a fake DHCP server that assigns itself as the default gateway, enabling man-in-the-middle attacks. DHCP snooping blocks server messages from untrusted ports.</li><li><strong>DHCP starvation attacks</strong> — an attacker sends thousands of DHCP Discover messages with spoofed MAC addresses to exhaust the IP pool. Rate limiting (covered in 5.7.d) mitigates this.</li></ul><p>DHCP snooping also builds a <strong>binding table</strong> that maps MAC addresses to IP addresses, VLANs, and interfaces. This table is the foundation for <strong>DAI (Dynamic ARP Inspection)</strong> and <strong>IP Source Guard</strong> — making DHCP snooping a prerequisite for those features.</p>",
    visual: { type: "comparison", params: { left: { label: "Trusted Port", items: ["Connected to DHCP server", "Allows ALL DHCP messages", "Manually configured"] }, right: { label: "Untrusted Port (default)", items: ["Connected to end users", "Only allows DHCP client msgs", "Blocks rogue DHCP servers"] } } },
    hack: {
      memory: "DHCP Snooping = a SNOOPY guard checking DHCP packets. Untrusted ports can only ASK for addresses (client msgs), never GIVE them (server msgs). Trusted ports = real DHCP servers only.",
      practice: "In Packet Tracer: enable DHCP snooping globally and on VLAN 10. Set the uplink/server port as trusted. Connect a rogue DHCP server to an untrusted port — its Offer/ACK messages should be dropped.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 53 (DHCP Snooping). Wendell Odom OCG Chapter 30. DHCP snooping is foundational — DAI and IP Source Guard depend on the binding table. Learn snooping first, then DAI builds on top.",
    },
    micro: [
      { id: "5.7.a.1", term: "DHCP snooping",                def: "L2 security feature. Blocks rogue DHCP servers. Only trusted ports forward DHCP offers.", weight: "high" },
      { id: "5.7.a.2", term: "Trusted vs untrusted ports",   def: "Trusted = facing legit DHCP server. Untrusted = user ports, block DHCP server messages (offers).", weight: "high" },
      { id: "5.7.a.3", term: "DHCP snooping binding table",  def: "IP ↔ MAC ↔ port ↔ VLAN. Built from observed DHCP transactions. Used by DAI and IP Source Guard.", weight: "high" }
    ]
  },

  "5.7.b": {
    info: "<p><strong>DHCP snooping configuration</strong> requires three steps — miss any one and it won't work:</p><ul><li><strong>Step 1: Enable globally</strong> — <code>ip dhcp snooping</code> in global config. This activates the feature on the switch but doesn't start filtering on any VLAN yet.</li><li><strong>Step 2: Enable per VLAN</strong> — <code>ip dhcp snooping vlan [vlan-id]</code> (e.g., <code>ip dhcp snooping vlan 10,20,30</code>). Snooping only works on explicitly specified VLANs.</li><li><strong>Step 3: Trust the server port</strong> — <code>ip dhcp snooping trust</code> on the interface connected to the legitimate DHCP server (or the uplink to the switch where the server resides). All other ports default to untrusted.</li></ul><p>Optional but recommended commands:</p><ul><li><code>ip dhcp snooping verify mac-address</code> — global command that verifies the source MAC in the Ethernet frame matches the client-hardware-address (chaddr) in the DHCP payload. Prevents MAC spoofing in DHCP messages.</li><li><code>ip dhcp snooping limit rate [pps]</code> — per-interface command to rate-limit DHCP packets (covered in 5.7.d)</li><li><code>no ip dhcp snooping information option</code> — disables Option 82 insertion. If the DHCP server doesn't support Option 82, leaving this enabled (default) can cause DHCP to fail because the server drops packets with unexpected options.</li></ul><p>Important gotcha: DHCP snooping inserts <strong>Option 82 (Relay Agent Information)</strong> by default. If your DHCP server doesn't expect Option 82 and drops those packets, DHCP stops working entirely. In lab environments, you often need <code>no ip dhcp snooping information option</code>.</p>",
    visual: { type: "hierarchy", params: { root: "ip dhcp snooping", children: [{ name: "ip dhcp snooping vlan 10,20", children: [{ name: "interface Gi0/1: trust" }, { name: "interface Gi0/2-24: untrusted (default)" }] }] } },
    hack: {
      memory: "Three steps: 1) GLOBAL enable, 2) VLAN enable, 3) TRUST the server port. Forget any one and it fails. Gotcha: Option 82 can break DHCP — disable with <code>no ip dhcp snooping information option</code> if needed.",
      practice: "Type the full config from memory: <code>ip dhcp snooping</code> → <code>ip dhcp snooping vlan 10</code> → <code>int gi0/1</code> → <code>ip dhcp snooping trust</code>. Then add <code>no ip dhcp snooping information option</code>. Practice until eyes-closed.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. The exam tests exact commands and that you need BOTH global AND per-VLAN enablement. The Option 82 gotcha is a common lab troubleshooting issue.",
    },
    micro: [
      { id: "5.7.b.1", term: "ip dhcp snooping (global)",    def: "Enables DHCP snooping globally on switch. Step 1.", weight: "high" },
      { id: "5.7.b.2", term: "ip dhcp snooping vlan [#]",    def: "Enables snooping per-VLAN. Both global AND per-VLAN required.", weight: "high" },
      { id: "5.7.b.3", term: "ip dhcp snooping trust",       def: "Interface command on trusted port (uplink toward DHCP server). Default = untrusted.", weight: "high" },
      { id: "5.7.b.4", term: "Option 82 gotcha",             def: "By default DHCP snooping inserts Option 82. Relay may reject it. Use 'no ip dhcp snooping information option' if needed.", weight: "med" }
    ]
  },

  "5.7.c": {
    info: "<p>The <strong>DHCP snooping binding table</strong> is a database that the switch builds by monitoring DHCP transactions on untrusted ports. Each time a client successfully obtains an IP address via DHCP, the switch records the binding.</p><p>Each binding table entry contains:</p><ul><li><strong>MAC address</strong> — the client's hardware address</li><li><strong>IP address</strong> — the address assigned by the DHCP server</li><li><strong>Lease time</strong> — how long the binding is valid (matches the DHCP lease)</li><li><strong>VLAN</strong> — which VLAN the client is on</li><li><strong>Interface</strong> — which switch port the client is connected to</li></ul><p>The binding table serves as the <strong>source of truth</strong> for other security features:</p><ul><li><strong>DAI (Dynamic ARP Inspection)</strong> — uses the binding table to validate that ARP replies match known MAC-to-IP mappings. If an ARP reply claims a MAC-IP pair not in the binding table, it's dropped.</li><li><strong>IP Source Guard</strong> — uses the binding table to verify that the source IP in packets matches the IP assigned to that port. Prevents IP address spoofing.</li></ul><p>Verification: <code>show ip dhcp snooping binding</code> displays the entire table. If the table is empty, either DHCP snooping isn't configured properly, no clients have obtained addresses yet, or leases have expired. The binding table can also be saved to a file for persistence across reboots: <code>ip dhcp snooping database [url]</code>.</p>",
    visual: { type: "layer-stack", params: { layers: ["MAC: aa:bb:cc:dd:ee:ff", "IP: 10.0.0.50", "VLAN: 10", "Interface: Gi0/5", "Lease: 86400 seconds"], highlight: 1 } },
    hack: {
      memory: "The binding table = DHCP snooping's memory. It records WHO (MAC) got WHAT address (IP) on WHICH port (interface). DAI and IP Source Guard both READ this table for their validation.",
      practice: "Lab: Enable DHCP snooping, let a client obtain an address via DHCP, then <code>show ip dhcp snooping binding</code>. Verify the entry has MAC, IP, VLAN, and interface. This table is what DAI reads.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. The binding table links DHCP snooping to DAI. Exam question: 'What does DAI use to validate ARP?' Answer: the DHCP snooping binding table.",
    },
    micro: [
      { id: "5.7.c.1", term: "Binding table = DAI source",   def: "DAI uses the DHCP snooping binding table to validate ARP replies. Without snooping, DAI has no data.", weight: "high" },
      { id: "5.7.c.2", term: "show ip dhcp snooping binding", def: "View the binding table: IP, MAC, port, VLAN, lease expiration.", weight: "high" }
    ]
  },

  "5.7.d": {
    info: "<p><strong>DHCP snooping rate limiting</strong> restricts the number of DHCP packets an untrusted port can receive per second. This is the primary defense against <strong>DHCP starvation attacks</strong>, where an attacker floods the network with thousands of DHCP Discover messages (each with a spoofed MAC) to exhaust the DHCP server's entire IP address pool.</p><p>Configuration: <code>ip dhcp snooping limit rate [packets-per-second]</code> — applied per interface.</p><ul><li>Typical rate for end-user access ports: <strong>10-20 packets per second</strong> (a normal PC sends maybe 1-4 DHCP packets during a lease renewal)</li><li>If the rate is exceeded, the port is placed in <strong>err-disabled</strong> state — shutting down the port entirely</li><li>Recovery requires manual <code>shutdown</code> / <code>no shutdown</code>, or automatic recovery via <code>errdisable recovery cause dhcp-rate-limit</code></li></ul><p>DHCP starvation attack mechanics: the attacker uses a tool (like Yersinia or Gobbler) to send rapid DHCP Discover messages, each with a different spoofed source MAC address. The DHCP server assigns a unique IP to each 'client,' eventually exhausting its pool. Legitimate clients can no longer get IP addresses. Rate limiting stops this by shutting down the port when the flood is detected.</p><p>Rate limiting should be applied to <strong>all untrusted ports</strong> (access ports) but NOT to trusted ports (uplinks, server ports) where legitimate DHCP traffic volume may be higher.</p>",
    visual: { type: "gauge", params: { level: 40, label: "Rate Limit: 15 DHCP pkts/sec", color: "#f59e0b" } },
    hack: {
      memory: "Rate limiting = speed bumps for DHCP. Normal PC: 1-4 DHCP packets at a time. Starvation attack: thousands per second. Rate limit catches the flood and err-disables the port.",
      practice: "Lab: <code>int gi0/5</code> → <code>ip dhcp snooping limit rate 10</code>. Generate excessive DHCP traffic and watch the port go err-disabled. Recover with <code>shutdown</code> / <code>no shutdown</code>. Then configure auto-recovery: <code>errdisable recovery cause dhcp-rate-limit</code>.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. The exam may ask about DHCP starvation: what is it (MAC spoofing to exhaust the pool) and how to prevent it (DHCP snooping + rate limiting).",
    },
    micro: [
      { id: "5.7.d.1", term: "DHCP starvation attack",       def: "Attacker requests many DHCP leases using spoofed MACs. Exhausts DHCP pool. Legit clients get denied.", weight: "high" },
      { id: "5.7.d.2", term: "DHCP starvation mitigation",   def: "DHCP snooping + port security (limit MACs) + rate limiting DHCP messages per port.", weight: "high" },
      { id: "5.7.d.3", term: "ip dhcp snooping limit rate",  def: "Per-port rate limit for DHCP packets. Prevents starvation and rogue flooding.", weight: "med" }
    ]
  },

  "5.7.e": {
    info: "<p><strong>Dynamic ARP Inspection (DAI)</strong> validates ARP packets on untrusted ports by cross-referencing them against the <strong>DHCP snooping binding table</strong>. It prevents <strong>ARP spoofing/poisoning attacks</strong> — a common man-in-the-middle technique where an attacker sends fake gratuitous ARP replies to poison victims' ARP caches and redirect traffic through the attacker.</p><p>How DAI works:</p><ul><li>When an ARP packet arrives on an <strong>untrusted port</strong>, the switch checks the sender's MAC-IP binding against the DHCP snooping binding table</li><li>If the MAC-IP pair <strong>matches</strong> a binding table entry → the ARP packet is <strong>forwarded</strong></li><li>If the MAC-IP pair <strong>does NOT match</strong> → the ARP packet is <strong>dropped</strong> and a log entry is generated</li><li>ARP packets on <strong>trusted ports</strong> are forwarded without inspection</li></ul><p>DAI critical dependency: <strong>DHCP snooping must be enabled first</strong> because DAI reads the binding table built by DHCP snooping. Without DHCP snooping, there is no binding table, and DAI has nothing to validate against. For hosts with <strong>static IP addresses</strong> (not assigned by DHCP), you must create <strong>ARP ACLs</strong> to manually define trusted MAC-IP bindings.</p><p>DAI can also perform additional validation checks: <strong>source MAC validation</strong> (compares Ethernet source MAC to ARP sender MAC), <strong>destination MAC validation</strong> (for ARP replies, compares Ethernet dest MAC to ARP target MAC), and <strong>IP address validation</strong> (checks for invalid/unexpected IPs).</p>",
    visual: { type: "handshake", params: { leftLabel: "Host", rightLabel: "Switch (DAI)", steps: ["ARP Reply: I am 10.0.0.1 ->", "<- Check binding table", "MAC-IP matches? PERMIT ->", "<- MAC-IP mismatch? DROP"] } },
    hack: {
      memory: "DAI = the ARP detective. Checks every ARP packet against the binding table. If the story doesn't match the records, DROPPED. Prerequisite: DHCP snooping must be enabled first (provides the binding table).",
      practice: "Lab: Enable DHCP snooping first (global + VLAN + trust server port). Then enable DAI: <code>ip arp inspection vlan 10</code>. Trust uplinks: <code>ip arp inspection trust</code>. Verify with <code>show ip arp inspection</code>.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. DAI depends on DHCP snooping. Exam question: 'What must be enabled before DAI?' = DHCP snooping. 'What does DAI use?' = DHCP snooping binding table.",
    },
    micro: [
      { id: "5.7.e.1", term: "DAI (Dynamic ARP Inspection)", def: "L2 security. Validates ARP replies against DHCP snooping binding table. Stops ARP spoofing.", weight: "high" },
      { id: "5.7.e.2", term: "DAI requires DHCP snooping",   def: "DAI uses the binding table that DHCP snooping builds. Enable snooping FIRST.", weight: "high" },
      { id: "5.7.e.3", term: "ip arp inspection vlan [#]",   def: "Enable DAI per VLAN.", weight: "high" },
      { id: "5.7.e.4", term: "ip arp inspection trust",      def: "Interface command. Trusted ports (uplinks) bypass ARP inspection. User ports = untrusted (default).", weight: "high" }
    ]
  },

  "5.7.f": {
    info: "<p><strong>DAI configuration</strong> follows a pattern nearly identical to DHCP snooping — enable per VLAN and trust specific ports:</p><ul><li><strong>Prerequisite:</strong> DHCP snooping must already be enabled and building the binding table</li><li><strong>Step 1:</strong> <code>ip arp inspection vlan [vlan-id]</code> — enable DAI on specific VLANs (e.g., <code>ip arp inspection vlan 10,20</code>)</li><li><strong>Step 2:</strong> <code>ip arp inspection trust</code> — applied per interface on trusted ports (uplinks, trunk ports to other switches, ports to routers). ARP packets on trusted ports are forwarded without inspection.</li><li>All other ports default to <strong>untrusted</strong> — ARP packets are validated against the binding table</li></ul><p>Handling static IP hosts: hosts with static IPs don't go through DHCP, so they have no binding table entry. DAI would drop their ARP packets. The solution is <strong>ARP ACLs</strong>:</p><ul><li><code>arp access-list STATIC_HOSTS</code></li><li><code>permit ip host 10.0.0.200 mac host aaaa.bbbb.cccc</code></li><li><code>ip arp inspection filter STATIC_HOSTS vlan 10</code></li></ul><p>DAI also supports rate limiting to prevent ARP-based DoS attacks: <code>ip arp inspection limit rate [pps]</code> on untrusted interfaces. Default is 15 ARP packets per second. If exceeded, the port goes err-disabled.</p><p>The full Layer 2 security stack in order: <strong>DHCP snooping</strong> (builds binding table, blocks rogue DHCP) → <strong>DAI</strong> (validates ARP against binding table, blocks ARP spoofing) → <strong>IP Source Guard</strong> (validates source IPs against binding table, blocks IP spoofing).</p>",
    visual: { type: "hierarchy", params: { root: "ip arp inspection vlan 10", children: [{ name: "Trusted Ports (trunks, uplinks)", children: [{ name: "ip arp inspection trust" }] }, { name: "Untrusted Ports (default)", children: [{ name: "ARP checked vs binding table" }] }] } },
    hack: {
      memory: "DAI config mirrors DHCP snooping: enable per VLAN, trust uplinks. Same pattern. DHCP snooping MUST come first. For static IPs, use ARP ACLs to manually define bindings.",
      practice: "Full chain from scratch: DHCP snooping (global → VLAN → trust server) → DAI (VLAN → trust uplinks). For static hosts, add ARP ACL. Practice the entire chain as one workflow.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. The exam tests the full dependency chain: DHCP snooping → binding table → DAI. Trust the right ports on BOTH features.",
    },
    micro: [
      { id: "5.7.f.1", term: "Dependency chain",             def: "DHCP snooping → populates binding table → DAI reads binding table → validates ARP.", weight: "high" },
      { id: "5.7.f.2", term: "Trust same ports on both",     def: "Port connecting to legit DHCP server + ARP source = trusted for both DHCP snooping AND DAI.", weight: "high" },
      { id: "5.7.f.3", term: "Full L2 security stack",       def: "Port security + DHCP snooping + DAI + IP Source Guard = comprehensive access-layer security.", weight: "med" }
    ]
  },

  "5.7.g": {
    info: "<p><strong>Port security</strong> restricts which and how many MAC addresses can send traffic through a switch port. It prevents unauthorized devices from connecting to the network and defends against <strong>MAC flooding attacks</strong> that can overflow the switch's CAM (Content Addressable Memory) table.</p><p>What port security protects against:</p><ul><li><strong>Unauthorized device connections</strong> — only devices with approved MAC addresses can use the port. An unauthorized laptop plugged into a conference room port would be blocked.</li><li><strong>MAC flooding attacks</strong> — an attacker sends thousands of frames with random source MACs to fill the CAM table. When the table is full, the switch falls back to <strong>flooding mode</strong> (sending frames out all ports like a hub), allowing the attacker to see all network traffic. Port security limits the number of MACs per port, preventing the flood.</li><li><strong>Policy enforcement</strong> — enforce one-device-per-port policies in environments where security is critical</li></ul><p>Port security works only on <strong>access ports</strong> — you must configure <code>switchport mode access</code> before enabling port security. It does NOT work on dynamic ports or trunk ports. The feature tracks MAC addresses seen on each port and takes action when the configured limits are exceeded.</p>",
    visual: { type: "shield", params: { items: ["Limit MAC addresses per port", "Prevent unauthorized devices", "Stop MAC flooding attacks", "Enforce device policies"], color: "#3b82f6" } },
    hack: {
      memory: "Port security = a bouncer checking MAC IDs at the door. Only approved MACs get in. Too many strangers = the port shuts down. Prevents MAC flooding (CAM table overflow → switch acts like a hub).",
      practice: "Understand the MAC flooding attack: attacker sends random source MACs → CAM table fills → switch floods all traffic → attacker captures everything. Port security stops this by limiting MACs per port.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 53 (Port Security). Wendell Odom OCG Chapter 30. Port security purpose, config, and violation modes are tested as a group. Study all three together as one unit.",
    },
    micro: [
      { id: "5.7.g.1", term: "Port security",                def: "L2 feature. Limits which MAC addresses can connect on a port. Prevents rogue device attachment.", weight: "high" },
      { id: "5.7.g.2", term: "Max MACs per port",            def: "Default 1 when enabled. Configurable with 'switchport port-security maximum [N]'.", weight: "high" },
      { id: "5.7.g.3", term: "Purpose",                      def: "Stops MAC flooding attacks + enforces 'one-device-per-port' policies. First line of access-layer defense.", weight: "high" }
    ]
  },

  "5.7.h": {
    info: "<p><strong>Port security configuration</strong> requires a specific sequence of commands. The interface MUST be an access port first — port security does not work on dynamic or trunk ports.</p><p>Configuration steps in order:</p><ul><li><strong>Step 1:</strong> <code>switchport mode access</code> — set the port to access mode (PREREQUISITE)</li><li><strong>Step 2:</strong> <code>switchport port-security</code> — enable port security on the interface</li><li><strong>Step 3:</strong> <code>switchport port-security maximum [count]</code> — set maximum allowed MAC addresses (default: 1)</li><li><strong>Step 4:</strong> <code>switchport port-security violation protect|restrict|shutdown</code> — set what happens on violation (default: shutdown)</li><li><strong>Step 5 (optional):</strong> <code>switchport port-security mac-address [H.H.H]</code> — statically define an allowed MAC address</li><li><strong>Step 5 (alternative):</strong> <code>switchport port-security mac-address sticky</code> — auto-learn and save MACs to running-config</li></ul><p>Example full configuration:<br><code>interface GigabitEthernet0/1</code><br><code>&nbsp;switchport mode access</code><br><code>&nbsp;switchport port-security</code><br><code>&nbsp;switchport port-security maximum 2</code><br><code>&nbsp;switchport port-security violation restrict</code><br><code>&nbsp;switchport port-security mac-address sticky</code></p><p>Verification: <code>show port-security interface [intf]</code> shows the security status, violation mode, maximum MACs, current count, and any violations. <code>show port-security address</code> shows all secured MAC addresses across all interfaces.</p>",
    visual: { type: "layer-stack", params: { layers: ["switchport mode access", "switchport port-security", "switchport port-security maximum 2", "switchport port-security violation shutdown", "switchport port-security mac-address sticky"], highlight: 1 } },
    hack: {
      memory: "ACCESS mode first (prerequisite), then enable port-security, then max + violation + sticky/static. Without <code>switchport mode access</code>, port security refuses to activate. Defaults: max=1, violation=shutdown.",
      practice: "Full lab: <code>switchport mode access</code> → <code>switchport port-security</code> → <code>switchport port-security maximum 2</code> → <code>switchport port-security mac-address sticky</code> → <code>switchport port-security violation restrict</code>. Connect a device, verify with <code>show port-security interface</code>. Connect a third device — violation triggered.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. The exam expects commands in correct order. Most common mistake: enabling port-security on a trunk or dynamic port — it ONLY works on access ports.",
    },
    micro: [
      { id: "5.7.h.1", term: "Port-security prereq",         def: "Port must be ACCESS mode. 'switchport mode access' first. Fails on trunk or dynamic.", weight: "high" },
      { id: "5.7.h.2", term: "switchport port-security",     def: "Interface command. Enables port security on the port.", weight: "high" },
      { id: "5.7.h.3", term: "maximum / mac-address",        def: "'port-security maximum [N]' sets max MACs. 'port-security mac-address [MAC]' statically allows specific MAC.", weight: "high" },
      { id: "5.7.h.4", term: "Config order",                 def: "(1) mode access, (2) port-security enable, (3) max, (4) mac-address or sticky, (5) violation mode.", weight: "high" }
    ]
  },

  "5.7.i": {
    info: "<p><strong>Sticky MAC learning</strong> automatically discovers MAC addresses on a port and saves them to the running configuration as static entries. It's the practical middle ground between manually typing MAC addresses (tedious) and relying on dynamic learning (lost on port bounce).</p><p>How sticky learning works:</p><ul><li>Configured with <code>switchport port-security mac-address sticky</code></li><li>When a device sends traffic, the switch <strong>learns its MAC dynamically</strong> and immediately <strong>writes it to the running-config</strong> as a static entry: <code>switchport port-security mac-address sticky [H.H.H]</code></li><li>Sticky MACs survive <strong>port bounces</strong> (link flaps) because they're in the running-config</li><li>Sticky MACs do <strong>NOT survive switch reboots</strong> unless you manually save: <code>copy running-config startup-config</code></li></ul><p>Three MAC learning modes compared:</p><ul><li><strong>Dynamic (default)</strong> — MACs learned automatically, stored only in CAM table, lost on port bounce or reboot</li><li><strong>Sticky</strong> — MACs learned automatically, written to running-config, persist across port bounces but lost on reboot without saving</li><li><strong>Static</strong> — MACs manually configured (<code>switchport port-security mac-address [H.H.H]</code>), stored in running-config, most secure but labor-intensive</li></ul><p>Sticky is the most commonly used option in practice because it provides security (only the first device to connect is allowed) without the administrative burden of manually recording every MAC address.</p>",
    visual: { type: "state-machine", params: { states: ["Dynamic (default — learned, lost on bounce)", "Sticky (learned, saved to running-config)", "Static (manually configured)"], active: 1, transitions: true } },
    hack: {
      memory: "STICKY = the MAC STICKS to the config like tape. Auto-learned but written to running-config. Must <code>copy run start</code> to survive reboot. Three modes: Dynamic (volatile) → Sticky (semi-persistent) → Static (manual, strongest).",
      practice: "Lab: Enable sticky. Connect a device. <code>show running-config interface Gi0/1</code> — see the sticky MAC auto-added. Bounce the port — MAC survives. <code>reload</code> without saving — MAC gone. Then save and reload — MAC persists.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. Exam question: 'How to auto-learn and retain MACs?' = sticky. Critical detail: must SAVE config (<code>copy run start</code>) or sticky MACs are lost on reboot.",
    },
    micro: [
      { id: "5.7.i.1", term: "switchport port-security mac-address sticky", def: "Auto-learn connected MACs and ADD them to running-config. Convenient — no manual MAC entry.", weight: "high" },
      { id: "5.7.i.2", term: "Sticky reboot gotcha",         def: "Sticky MACs are in RUNNING-config. Must 'copy run start' or they're lost on reboot (re-learned after).", weight: "high" },
      { id: "5.7.i.3", term: "Static vs sticky",             def: "Static = manually typed in config. Sticky = auto-learned but saved as static. Both persistent.", weight: "high" }
    ]
  },

  "5.7.j": {
    info: "<p><strong>Port security violation modes</strong> define what the switch does when an unauthorized MAC address is detected (i.e., when the maximum MAC count is exceeded or a non-permitted MAC appears). There are three modes, increasing in severity:</p><ul><li><strong>Protect</strong> — drops traffic from the violating MAC address <strong>silently</strong>. No syslog message, no SNMP trap, violation counter does NOT increment. The port stays up. Legitimate traffic from approved MACs continues normally. This mode is invisible — you won't know a violation occurred unless you're watching packet counters closely.</li><li><strong>Restrict</strong> — drops traffic from the violating MAC AND <strong>generates a syslog message</strong>, sends an SNMP trap, and <strong>increments the violation counter</strong>. The port stays up and continues forwarding traffic from approved MACs. This is the 'alert but don't kill' mode.</li><li><strong>Shutdown (DEFAULT)</strong> — places the port in <strong>err-disabled state</strong> immediately. ALL traffic is stopped (including from approved MACs). A syslog message and SNMP trap are sent. The port LED turns amber. <strong>Requires manual recovery</strong> (<code>shutdown</code> / <code>no shutdown</code>) or automatic recovery (<code>errdisable recovery</code>).</li></ul><p>Configuration: <code>switchport port-security violation protect|restrict|shutdown</code></p><p>Choosing the right mode: Shutdown is most secure but causes disruption (the entire port goes down). Restrict logs the violation without disruption. Protect is the least disruptive but provides no visibility. Most production environments use shutdown (default) for critical ports and restrict for less sensitive ports.</p>",
    visual: { type: "comparison", params: { left: { label: "Less Disruptive", items: ["PROTECT: drop silently", "RESTRICT: drop + log + count"] }, right: { label: "Most Secure", items: ["SHUTDOWN: err-disable port", "Requires manual recovery", "Default violation mode"] } } },
    hack: {
      memory: "P-R-S: Protect (silent drop), Restrict (drop + Report), Shutdown (Slam the port). Severity left to right. Default = Shutdown. Key fact: Protect has NO log — invisible. Restrict has a log — visible. Shutdown kills the port.",
      practice: "Lab: Test each mode sequentially. Set <code>violation protect</code> + trigger violation → traffic drops but nothing visible. Set <code>violation restrict</code> → see syslog message. Set <code>violation shutdown</code> → port goes err-disabled, verify with <code>show interfaces status</code>.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. The exam tests all three modes. Key facts: Protect = NO log. Restrict = log + port stays up. Shutdown = err-disabled (DEFAULT). Know what each does and doesn't do.",
    },
    micro: [
      { id: "5.7.j.1", term: "Protect mode",                 def: "Drops violating frames silently. NO log, NO SNMP trap. Port stays up. Weakest enforcement.", weight: "high" },
      { id: "5.7.j.2", term: "Restrict mode",                def: "Drops violating frames + LOGS syslog/SNMP trap + increments counter. Port stays up.", weight: "high" },
      { id: "5.7.j.3", term: "Shutdown mode (default)",      def: "Port → err-disabled. Requires manual intervention. Logs + increments violation count. DEFAULT.", weight: "high" },
      { id: "5.7.j.4", term: "switchport port-security violation", def: "Interface command. Takes 'protect', 'restrict', or 'shutdown'. Default is 'shutdown'.", weight: "high" }
    ]
  },

  "5.7.k": {
    info: "<p>When a switch port enters <strong>err-disabled state</strong> (triggered by port security violations, BPDU guard, DHCP rate limiting, or other protective features), it is effectively shut down. All traffic is blocked. The port status shows 'err-disabled' in <code>show interfaces status</code>.</p><p>Two recovery methods:</p><ul><li><strong>Manual recovery</strong> — an administrator must physically (or remotely) enter the interface and issue <code>shutdown</code> followed by <code>no shutdown</code>. This is the default behavior. The admin should first investigate and resolve the cause before recovering the port.</li><li><strong>Automatic recovery (errdisable recovery)</strong> — configures the switch to automatically re-enable err-disabled ports after a timer expires:<br><code>errdisable recovery cause [reason]</code> — enables auto-recovery for a specific cause (e.g., <code>psecure-violation</code>, <code>bpduguard</code>, <code>dhcp-rate-limit</code>)<br><code>errdisable recovery interval [seconds]</code> — sets the recovery timer (default: 300 seconds / 5 minutes)</li></ul><p>Verification commands:</p><ul><li><code>show errdisable recovery</code> — shows which causes have auto-recovery enabled and the timer interval</li><li><code>show interfaces status err-disabled</code> — lists all currently err-disabled ports and the cause</li></ul><p>Best practice: enable auto-recovery for known causes with a reasonable timer (120-300 seconds), but always investigate recurring err-disabled events. If a port keeps going err-disabled, there's an underlying problem (e.g., a rogue device, a misconfigured BPDU) that needs to be resolved.</p>",
    visual: { type: "state-machine", params: { states: ["Active", "Violation Detected", "Err-Disabled", "Manual/Auto Recovery", "Active"], active: 2, transitions: true } },
    hack: {
      memory: "Err-disabled = the port is in JAIL. Manual recovery = you bail it out (<code>shut</code> / <code>no shut</code>). Auto recovery = parole after a timer. Default timer: 300 seconds (5 min). Always investigate WHY before recovering.",
      practice: "Lab: Trigger err-disabled via port security. Recover manually: <code>shut</code> → <code>no shut</code>. Then enable auto-recovery: <code>errdisable recovery cause psecure-violation</code> → <code>errdisable recovery interval 30</code>. Trigger again and watch it auto-recover. Verify with <code>show errdisable recovery</code>.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 53. Wendell Odom OCG Chapter 30. The exam tests both recovery methods. Manual = <code>shut</code> / <code>no shut</code>. Auto = <code>errdisable recovery cause</code> + <code>interval</code>. Know both.",
    },
    micro: [
      { id: "5.7.k.1", term: "err-disabled recovery manual", def: "'shutdown' then 'no shutdown' on the port. Returns to normal operation.", weight: "high" },
      { id: "5.7.k.2", term: "errdisable recovery cause",    def: "Global command to auto-recover. E.g., 'errdisable recovery cause psecure-violation' enables auto-recovery from port security.", weight: "high" },
      { id: "5.7.k.3", term: "errdisable recovery interval", def: "Time (seconds) before auto-recovery attempt. Default 300s.", weight: "med" }
    ]
  },

  // ── 5.8 Compare AAA concepts ──────────────────────────────────

  "5.8.a": {
    info: "<p><strong>Authentication</strong> answers the question <strong>'Who are you?'</strong> It is the process of verifying the identity of a user, device, or system attempting to access the network. Authentication is always the <strong>first step</strong> in the AAA framework — you cannot authorize someone you haven't identified.</p><p>Authentication methods include:</p><ul><li><strong>Username and password</strong> — the most common method, verified against a local database or centralized AAA server</li><li><strong>Digital certificates</strong> — PKI-based identity verification, used in EAP-TLS for 802.1X</li><li><strong>Biometrics</strong> — fingerprint, facial recognition, iris scan ('something you are')</li><li><strong>Hardware tokens</strong> — RSA SecurID, YubiKey ('something you have')</li><li><strong>Multi-factor</strong> — combining two or more of the above from different categories</li></ul><p>In Cisco networks, authentication is configured via the <strong>AAA framework</strong>. The device (authenticator) can authenticate users against: a <strong>local database</strong> (usernames stored on the device), a <strong>RADIUS server</strong>, or a <strong>TACACS+ server</strong>. Configuration: <code>aaa new-model</code> enables the AAA framework, and <code>aaa authentication login default [method-list]</code> defines the authentication method order.</p><p>The AAA sequence is always: <strong>Authentication (who?) → Authorization (what?) → Accounting (when/what did they do?)</strong>. Each step depends on the previous one — you must authenticate before you can authorize, and accounting records require a known identity.</p>",
    visual: { type: "handshake", params: { leftLabel: "User", rightLabel: "AAA Server", steps: ["Username: admin ->", "Password: ******* ->", "<- Identity Verified", "<- Access Granted (Authentication PASSED)"] } },
    hack: {
      memory: "Authentication = WHO are you? Show your ID at the door. First A in AAA. You can't authorize (what can you do?) until you authenticate (who are you?). Identity first, permissions second, logging third.",
      practice: "Map the AAA sequence to a building: 1) Authentication = showing your badge at the entrance (proving identity), 2) Authorization = your badge opens certain floors but not others (permissions), 3) Accounting = security cameras recording which floors you visited (audit trail).",
      effort: "low",
      meta: "Jeremy's IT Lab Day 50 (AAA). Wendell Odom OCG Chapter 10 + Chapter 27. Distinguish the three A's clearly: Authentication = identity, Authorization = permissions, Accounting = logging. The exam loves 'which AAA function does X?'",
    },
    micro: [
      { id: "5.8.a.1", term: "Authentication",               def: "Proves WHO you are. Credentials verified (password, certificate, token).", weight: "high" },
      { id: "5.8.a.2", term: "Authorization",                def: "Defines WHAT you can do. Privilege levels, command restrictions, access scope.", weight: "high" },
      { id: "5.8.a.3", term: "Accounting",                   def: "Records WHAT you DID. Audit trail of actions for compliance and forensics.", weight: "high" },
      { id: "5.8.a.4", term: "AAA order",                    def: "Authenticate first → Authorize → Account. Order matters — can't authorize unknown user.", weight: "med" }
    ]
  },

  "5.8.b": {
    info: "<p><strong>Authorization</strong> answers the question <strong>'What are you allowed to do?'</strong> After a user's identity is verified (authentication), authorization determines their <strong>privileges, permissions, and access rights</strong>. Authorization without authentication is meaningless — you must know WHO before deciding WHAT they can do.</p><p>What authorization controls:</p><ul><li><strong>Privilege levels</strong> — which IOS privilege level the user operates at (1 = basic show, 15 = full admin)</li><li><strong>Command restrictions</strong> — which specific CLI commands the user can execute (TACACS+ supports per-command authorization)</li><li><strong>Network access</strong> — which VLANs, subnets, or network segments the user can reach</li><li><strong>Service access</strong> — which services (SSH, console, VPN) the user can use</li><li><strong>Time restrictions</strong> — when the user can access the network (business hours only, etc.)</li></ul><p>Authorization is typically enforced by the <strong>AAA server</strong> (RADIUS or TACACS+), which returns authorization attributes in the authentication response. For example, a RADIUS server might return a VLAN assignment for a wireless user, or a TACACS+ server might return a privilege level and command set for a network admin.</p><p>Configuration: <code>aaa authorization exec default [method-list]</code> controls which privilege level users get after login. <code>aaa authorization commands [level] default [method-list]</code> controls which commands are permitted at each level.</p>",
    visual: { type: "hierarchy", params: { root: "Authenticated User", children: [{ name: "Admin (Priv 15)", children: [{ name: "All commands" }, { name: "All interfaces" }] }, { name: "Helpdesk (Priv 5)", children: [{ name: "show commands only" }, { name: "No config mode" }] }] } },
    hack: {
      memory: "Authorization = WHAT can you do? You're in the building (authenticated), but which doors can you open? Your badge level determines access. Priv 15 = all doors. Priv 1 = lobby only.",
      practice: "Scenario drill: Admin logs in → TACACS+ returns priv 15 → full CLI access. Helpdesk logs in → TACACS+ returns priv 5 → show commands only, no config mode. Different users, different authorization.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 50. Wendell Odom OCG Chapter 10 + 27. Authorization = privilege levels and command restrictions. It defines WHAT the user can do AFTER proving WHO they are.",
    },
    micro: [
      { id: "5.8.b.1", term: "Privilege levels",             def: "0-15. 15 = full access (enable). 1 = user-mode. Custom levels let you authorize specific commands.", weight: "high" },
      { id: "5.8.b.2", term: "Per-command authorization",    def: "TACACS+ feature. Authorize/deny individual IOS commands per user. RADIUS can't do this.", weight: "high" },
      { id: "5.8.b.3", term: "privilege exec level",         def: "Assign commands to custom privilege levels. Enables role-based access without full enable rights.", weight: "med" }
    ]
  },

  "5.8.c": {
    info: "<p><strong>Accounting</strong> answers the question <strong>'What did you do?'</strong> It tracks and logs all user activity for auditing, billing, forensics, and compliance. Accounting creates the <strong>audit trail</strong> that proves who did what, when, and for how long.</p><p>What accounting records include:</p><ul><li><strong>Login/logout timestamps</strong> — when the user connected and disconnected</li><li><strong>Commands entered</strong> — every CLI command typed during the session (critical for change tracking and incident investigation)</li><li><strong>Data transferred</strong> — bytes sent/received (used for billing in ISP environments)</li><li><strong>Resources accessed</strong> — which services, networks, or systems were used</li><li><strong>Session duration</strong> — total connection time</li></ul><p>Accounting data is sent to a centralized <strong>RADIUS or TACACS+ server</strong> for storage and analysis. This centralization is essential — local device logs can be cleared by a malicious admin, but centralized accounting records on a separate server provide tamper-resistant evidence.</p><p>Accounting is critical for compliance frameworks: <strong>SOX</strong> (financial auditing), <strong>HIPAA</strong> (healthcare data access), <strong>PCI-DSS</strong> (payment card data), and <strong>GDPR</strong> (data privacy). These regulations require provable audit trails of who accessed what data and when.</p><p>Configuration: <code>aaa accounting exec default start-stop group tacacs+</code> logs session start/stop events. <code>aaa accounting commands 15 default start-stop group tacacs+</code> logs all priv 15 commands entered.</p>",
    visual: { type: "layer-stack", params: { layers: ["User logged in: 09:15:00", "Command: show running-config", "Command: configure terminal", "Command: interface Gi0/1", "User logged out: 10:30:00"], highlight: 2 } },
    hack: {
      memory: "Accounting = the SECURITY CAMERA recording everything. Who logged in, what they typed, when they left. The audit trail that answers 'what happened?' Essential for compliance and forensics.",
      practice: "Configure accounting on a lab device: <code>aaa accounting exec default start-stop group tacacs+</code>. Log in, run commands, review accounting records on the server. See the trail of every command.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 50. Wendell Odom OCG Chapter 10 + 27. Authentication = identity. Authorization = permissions. Accounting = logging. Accounting is often the forgotten A, but it's critical for compliance.",
    },
    micro: [
      { id: "5.8.c.1", term: "Accounting purpose",           def: "Audit trail. Records who logged in, what commands they ran, when they logged out. For compliance and forensics.", weight: "high" },
      { id: "5.8.c.2", term: "Accounting methods",           def: "Commands, exec (session start/stop), system events. Each can be logged separately.", weight: "med" },
      { id: "5.8.c.3", term: "Compliance value",             def: "HIPAA, PCI-DSS, SOX require audit trails. Accounting provides the evidence for audits.", weight: "med" }
    ]
  },

  "5.8.d": {
    info: "<p><strong>TACACS+ (Terminal Access Controller Access-Control System Plus)</strong> is a Cisco-developed AAA protocol optimized for <strong>device administration</strong> (managing routers, switches, and firewalls via CLI).</p><p>TACACS+ key characteristics:</p><ul><li><strong>TCP port 49</strong> — uses TCP for reliable, connection-oriented communication. If a packet is lost, TCP retransmits it.</li><li><strong>Full packet encryption</strong> — encrypts the ENTIRE packet payload (not just the password). This protects all data exchanged between the device and the AAA server.</li><li><strong>Separates AAA functions</strong> — authentication, authorization, and accounting are independent processes. You can authenticate against one server, authorize against another, and account to a third. This flexibility is unique to TACACS+.</li><li><strong>Per-command authorization</strong> — can control exactly which CLI commands each user is permitted to execute. Example: allow the helpdesk to run <code>show</code> commands but deny <code>configure terminal</code>.</li><li><strong>Cisco proprietary</strong> — developed by Cisco, primarily used in Cisco environments</li></ul><p>TACACS+ is preferred for device administration because of per-command authorization — the ability to control which specific CLI commands each admin can run. This granularity is not available with RADIUS, which can only set a privilege level. Example: TACACS+ can allow 'show ip route' but deny 'clear ip route *' at the same privilege level.</p>",
    visual: { type: "shield", params: { items: ["TCP Port 49", "Full Packet Encryption", "Separates AAA Functions", "Cisco Proprietary", "Best for Device Admin"], color: "#6366f1" } },
    hack: {
      memory: "TACACS+ = TCP 49, Total encryption, Three separate AAA functions, per-command authorization. The T's: TCP, Total, Three, TACACS. Cisco-made for managing Cisco devices.",
      practice: "Build a TACACS+ vs RADIUS comparison table from memory: Port (49 vs 1812/1813), Protocol (TCP vs UDP), Encryption (full vs password-only), AAA Separation (yes vs no), Standard (Cisco vs open), Best for (device admin vs network access). Fill all 6 rows.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 50. Wendell Odom OCG Chapter 10 + 27. TACACS+ vs RADIUS is one of the most tested AAA topics. Scenario: 'which protocol for device admin?' = TACACS+. 'For 802.1X?' = RADIUS.",
    },
    micro: [
      { id: "5.8.d.1", term: "Device admin → TACACS+",       def: "Cisco-proprietary. Full packet encryption. Separates AAA. Per-command authorization. Ideal for router/switch mgmt.", weight: "high" },
      { id: "5.8.d.2", term: "Network access → RADIUS",      def: "Open standard. 802.1X wired/wireless, VPN authentication, WPA2-Enterprise. Multi-vendor.", weight: "high" }
    ]
  },

  "5.8.e": {
    info: "<p><strong>RADIUS (Remote Authentication Dial-In User Service)</strong> is an open-standard AAA protocol defined in <strong>RFC 2865 (auth) and RFC 2866 (accounting)</strong>. It is the standard protocol for <strong>network access control</strong> — authenticating users connecting to the network via 802.1X, wireless, and VPN.</p><p>RADIUS key characteristics:</p><ul><li><strong>UDP ports 1812/1813</strong> — port 1812 for authentication/authorization, port 1813 for accounting. (Legacy ports 1645/1646 may also appear on the exam.)</li><li><strong>Password-only encryption</strong> — only the password field in the Access-Request packet is encrypted. All other data (username, attributes, accounting info) is sent in cleartext.</li><li><strong>Combines authentication and authorization</strong> — they are a single process that cannot be separated. The Access-Accept response includes both the authentication result AND authorization attributes (VLAN, ACL, etc.).</li><li><strong>Open standard (IETF)</strong> — supported by virtually every vendor: Cisco, Juniper, Aruba, Microsoft NPS, FreeRADIUS, Cisco ISE, etc.</li><li><strong>Attribute-value pairs</strong> — RADIUS communicates authorization data via standardized AV pairs (e.g., Tunnel-Type=VLAN, Tunnel-Private-Group-ID=100)</li></ul><p>RADIUS is preferred for <strong>network access</strong> because: 802.1X was designed to work with RADIUS (not TACACS+), it's multi-vendor (any switch/AP can use it), and it supports the specific AV pairs needed for VLAN assignment, ACL pushdown, and other network access policies.</p>",
    visual: { type: "shield", params: { items: ["UDP 1812/1813", "Password-Only Encryption", "Combines Auth + Authz", "Open Standard (RFC)", "Best for Network Access"], color: "#0ea5e9" } },
    hack: {
      memory: "RADIUS = UDP 1812/1813, password-only encryption, combines auth+authz (can't separate), open standard, multi-vendor. R = Remote access = RADIUS. Used for 802.1X, wireless, VPN.",
      practice: "Contrast with TACACS+: RADIUS = UDP + password encryption + combined auth/authz + open. TACACS+ = TCP + full encryption + separated AAA + Cisco. Drill until you can recite differences in 10 seconds.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 50. Wendell Odom OCG Chapter 10 + 27. Key exam facts: RADIUS = UDP, open, password-only encryption, combined auth/authz. TACACS+ = TCP, Cisco, full encryption, separated AAA.",
    },
    micro: [
      { id: "5.8.e.1", term: "RADIUS recap",                 def: "UDP 1812/1813. Open standard. Only password encrypted. Combines auth+authz. Network ACCESS.", weight: "high" },
      { id: "5.8.e.2", term: "TACACS+ recap",                def: "TCP 49. Cisco proprietary. FULL packet encryption. Separates AAA. Device ADMIN.", weight: "high" },
      { id: "5.8.e.3", term: "Protocol/port summary",        def: "RADIUS=UDP 1812/1813. TACACS+=TCP 49. This is tested every exam cycle.", weight: "high" }
    ]
  },

  "5.8.f": {
    info: "<p>Choosing between TACACS+ and RADIUS depends on the <strong>use case</strong>. In practice, many organizations deploy BOTH — each optimized for its strength.</p><p><strong>TACACS+ for device administration:</strong></p><ul><li>Managing routers, switches, and firewalls via SSH/console</li><li><strong>Per-command authorization</strong> — controlling exactly which CLI commands each admin can execute. Example: allow helpdesk to run <code>show</code> commands but deny <code>reload</code> and <code>configure terminal</code>.</li><li>Full encryption protects all command data sent between device and server</li><li>Separated AAA allows flexible policy — authenticate locally as a fallback if the TACACS+ server is unreachable</li></ul><p><strong>RADIUS for network access:</strong></p><ul><li><strong>802.1X wired and wireless authentication</strong> — the 802.1X standard was designed around RADIUS</li><li><strong>VPN authentication</strong> — remote access VPN users authenticate via RADIUS</li><li>Multi-vendor environments where the open standard ensures interoperability</li><li>Returns network-specific AV pairs: <strong>VLAN assignment</strong>, <strong>downloadable ACLs</strong>, <strong>QoS policies</strong></li></ul><p>The three 802.1X roles (which always use RADIUS):</p><ul><li><strong>Supplicant</strong> — the client device requesting access (laptop, phone)</li><li><strong>Authenticator</strong> — the network device controlling access (switch, AP)</li><li><strong>Authentication server</strong> — the RADIUS server verifying credentials (Cisco ISE, FreeRADIUS)</li></ul>",
    visual: { type: "comparison", params: { left: { label: "TACACS+ — Device Admin", items: ["Per-command authorization", "Full encryption", "Router/switch management", "Cisco proprietary"] }, right: { label: "RADIUS — Network Access", items: ["802.1X / wireless / VPN", "Open standard", "Multi-vendor support", "User network auth"] } } },
    hack: {
      memory: "TACACS+ = managing DEVICES (admin CLI, per-command control). RADIUS = managing USER ACCESS to the network (802.1X, wireless, VPN). Both can coexist: TACACS+ for admin, RADIUS for users.",
      practice: "Scenario drill: 'Control which show commands helpdesk can run' = TACACS+ (per-command auth). 'Authenticate wireless users via 802.1X' = RADIUS. 'VPN user login' = RADIUS. 'Router management login' = TACACS+.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 50. Wendell Odom OCG Chapter 10 + 27. Use case determines protocol. Device admin = TACACS+. Network access = RADIUS. The 802.1X trio: Supplicant, Authenticator, Authentication Server.",
    },
    micro: [
      { id: "5.8.f.1", term: "802.1X Supplicant",            def: "Client device requesting access. Runs 802.1X software. Provides credentials.", weight: "high" },
      { id: "5.8.f.2", term: "802.1X Authenticator",         def: "Switch/WLC. Intermediate device. Forwards credentials from supplicant to authentication server. Enforces decision.", weight: "high" },
      { id: "5.8.f.3", term: "802.1X Authentication Server", def: "Typically RADIUS server (Cisco ISE, Microsoft NPS). Validates credentials against user database.", weight: "high" },
      { id: "5.8.f.4", term: "EAP",                          def: "Extensible Authentication Protocol. Framework 802.1X uses. EAP-TLS, PEAP, EAP-FAST are common methods.", weight: "med" }
    ]
  },

  // ── 5.9 Describe wireless security protocols ──────────────────

  "5.9.a": {
    info: "<p><strong>WEP (Wired Equivalent Privacy)</strong> was the original 802.11 wireless security standard, intended to provide security 'equivalent' to a wired connection. It has been <strong>completely broken since 2001</strong> and should never be used.</p><p>Why WEP is broken:</p><ul><li>Uses the <strong>RC4 stream cipher</strong> with a <strong>24-bit initialization vector (IV)</strong></li><li>A 24-bit IV provides only ~16.7 million unique values. With high traffic, IVs <strong>repeat</strong> within hours or even minutes</li><li>When IVs repeat, an attacker can use statistical analysis to derive the encryption key</li><li>Tools like <strong>aircrack-ng</strong> can crack a WEP key in under 5 minutes by capturing enough packets with repeated IVs</li><li>WEP uses <strong>static keys</strong> — all users share the same key, which never changes automatically</li></ul><p>WEP was deprecated by the Wi-Fi Alliance in 2004 and replaced by WPA. Despite being broken for over 20 years, some legacy devices still use WEP, which is why it appears on the exam.</p><p>The wireless security evolution: <strong>WEP (broken)</strong> → <strong>WPA (TKIP, deprecated)</strong> → <strong>WPA2 (AES/CCMP, current standard)</strong> → <strong>WPA3 (SAE/OWE, newest)</strong>. Each generation fixes the flaws of the previous one.</p>",
    visual: { type: "gauge", params: { level: 10, label: "WEP Security Level: BROKEN", color: "#ef4444" } },
    hack: {
      memory: "WEP = Weak Encryption Protocol (unofficial but true). 24-bit IV = too short → repeats → cracked in minutes. RC4 cipher + static keys + short IV = completely broken. Never use WEP for anything.",
      practice: "Memorize the evolution timeline with cipher: WEP (RC4, 24-bit IV, broken) → WPA (TKIP/RC4, 48-bit IV, deprecated) → WPA2 (AES/CCMP, current) → WPA3 (SAE/OWE, newest). Know what each generation changes.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 56 (Wireless Security). Wendell Odom OCG Chapter 29. The exam tests WHY WEP is broken: 24-bit IV is too short and reuses keys. Don't just know 'WEP is bad' — know the mechanism.",
    },
    micro: [
      { id: "5.9.a.1", term: "WEP",                          def: "Original wireless encryption. RC4 cipher. COMPLETELY BROKEN. Never use.", weight: "high" },
      { id: "5.9.a.2", term: "WEP 24-bit IV problem",        def: "Initialization Vector is only 24 bits. Reuses within ~5000 frames. Attacker captures enough → cracks key in minutes.", weight: "high" },
      { id: "5.9.a.3", term: "WEP static keys",              def: "Same key across all devices. Never rotated. If one device compromised, entire network is exposed.", weight: "med" }
    ]
  },

  "5.9.b": {
    info: "<p><strong>WPA (Wi-Fi Protected Access)</strong> was developed as an emergency fix for WEP's broken security, designed to work on existing WEP hardware with a firmware update. It introduced <strong>TKIP (Temporal Key Integrity Protocol)</strong> as a wrapper around the existing RC4 cipher.</p><p>What TKIP added over WEP:</p><ul><li><strong>48-bit IV</strong> — extended from WEP's 24-bit, eliminating IV reuse for practical purposes (2^48 = ~281 trillion unique values)</li><li><strong>Per-packet key mixing</strong> — each packet gets a unique encryption key derived from the master key + sequence number + MAC address. Even if one packet key is compromised, others remain secure.</li><li><strong>MIC (Message Integrity Check)</strong> — also called 'Michael,' this detects packet tampering. If tampering is detected, the connection is temporarily shut down.</li><li><strong>Sequence counter</strong> — prevents replay attacks by rejecting packets received out of order</li></ul><p>However, WPA/TKIP is now <strong>deprecated</strong>. While it was a massive improvement over WEP, TKIP itself has known vulnerabilities (the Beck-Tews attack can inject packets). TKIP still uses the fundamentally weak RC4 cipher — TKIP was always meant to be a temporary bridge until hardware could support the stronger AES cipher.</p><p>WPA was replaced by <strong>WPA2</strong>, which uses <strong>AES/CCMP</strong> — a completely different (and much stronger) encryption system. Modern networks should never use WPA/TKIP.</p>",
    visual: { type: "state-machine", params: { states: ["WEP (RC4, 24-bit IV)", "WPA (TKIP, 48-bit IV)", "WPA2 (AES/CCMP)", "WPA3 (SAE/OWE)"], active: 1, transitions: true } },
    hack: {
      memory: "WPA = band-aid over WEP. TKIP = Temporary Key Integrity Protocol — the name literally says TEMPORARY. Still uses RC4 but adds 48-bit IV + per-packet keys + MIC. Better than WEP, but now deprecated.",
      practice: "Build a comparison: WEP vs WPA — both use RC4, but WPA adds TKIP (48-bit IV, per-packet keys, MIC). Then WPA vs WPA2 — WPA2 drops RC4/TKIP entirely and uses AES/CCMP. Track the cipher changes.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 56. Wendell Odom OCG Chapter 29. Know the cipher per generation: WEP = RC4 (broken). WPA = TKIP/RC4 (deprecated). WPA2 = AES/CCMP (current). WPA3 = SAE + AES/GCMP.",
    },
    micro: [
      { id: "5.9.b.1", term: "WPA (TKIP)",                   def: "Wi-Fi Protected Access v1. Uses TKIP (still RC4-based). Temporary fix between WEP and WPA2. Now deprecated.", weight: "high" },
      { id: "5.9.b.2", term: "TKIP purpose",                 def: "Made WEP-era hardware usable. Added per-packet keys and MIC. But TKIP itself has known weaknesses now.", weight: "med" },
      { id: "5.9.b.3", term: "Wireless cipher evolution",    def: "WEP (RC4) → WPA (TKIP/RC4) → WPA2 (AES/CCMP) → WPA3 (AES/GCMP + SAE).", weight: "high" }
    ]
  },

  "5.9.c": {
    info: "<p><strong>WPA2</strong> is the <strong>current wireless security standard</strong>, mandatory for Wi-Fi Alliance certification since 2006. It replaced the weak RC4/TKIP cipher suite with the far stronger <strong>AES (Advanced Encryption Standard)</strong> paired with <strong>CCMP (Counter Mode with CBC-MAC Protocol)</strong>.</p><p>WPA2 technical details:</p><ul><li><strong>AES</strong> — a symmetric block cipher using 128-bit keys (standard). AES is used worldwide for classified government communications and has no known practical attacks.</li><li><strong>CCMP</strong> — provides both confidentiality (encryption via Counter Mode) AND integrity (authentication via CBC-MAC). CCMP replaces both RC4 encryption and TKIP's MIC in a single, stronger protocol.</li><li><strong>Two operating modes:</strong><ul><li><strong>WPA2-Personal (PSK)</strong> — Pre-Shared Key. All users share one password. Simple, suitable for home/small office.</li><li><strong>WPA2-Enterprise (802.1X)</strong> — Individual credentials via RADIUS server. Each user has unique authentication. Required for corporate environments.</li></ul></li></ul><p>WPA2 vulnerability: the <strong>KRACK attack</strong> (Key Reinstallation Attack, 2017) demonstrated that WPA2's 4-way handshake could be manipulated to reinstall encryption keys, potentially allowing traffic decryption. This was patched in software updates and was one motivation for developing WPA3. Despite KRACK, WPA2 with current patches remains secure and is the minimum acceptable wireless security standard.</p>",
    visual: { type: "shield", params: { items: ["AES Encryption (128-bit)", "CCMP Integrity", "Current Standard", "Personal (PSK) + Enterprise (802.1X)"], color: "#10b981" } },
    hack: {
      memory: "WPA2 = AES + CCMP = current gold standard. AES = the cipher that hasn't been cracked. CCMP = does BOTH encryption and integrity in one protocol. Two modes: Personal (PSK, shared password) vs Enterprise (802.1X, individual creds).",
      practice: "Configure WPA2-PSK in Packet Tracer: create WLAN → Security → WPA2/AES → set PSK. Connect a laptop. Then reconfigure for WPA2-Enterprise: point to RADIUS server → test 802.1X authentication. Know both workflows.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 56. Wendell Odom OCG Chapter 29. WPA2 is the most heavily tested wireless security standard. Know: AES/CCMP, Personal vs Enterprise, and it's the minimum acceptable standard.",
    },
    micro: [
      { id: "5.9.c.1", term: "WPA2",                         def: "IEEE 802.11i standard. Current production baseline. Uses AES-CCMP encryption.", weight: "high" },
      { id: "5.9.c.2", term: "AES-CCMP",                     def: "WPA2's strong cipher. Counter Mode with CBC-MAC Protocol. Replaces TKIP.", weight: "high" },
      { id: "5.9.c.3", term: "WPA2 minimum standard",        def: "Any enterprise deploying today should at minimum use WPA2 + AES. TKIP only for legacy compat.", weight: "high" }
    ]
  },

  "5.9.d": {
    info: "<p><strong>WPA3</strong> is the newest wireless security standard (2018), designed to address WPA2's remaining weaknesses. It introduces three major improvements:</p><ul><li><strong>SAE (Simultaneous Authentication of Equals)</strong> — replaces WPA2's PSK 4-way handshake with a more secure 'Dragonfly' key exchange. SAE is resistant to <strong>offline dictionary attacks</strong>: even if an attacker captures the handshake, they cannot take it offline to brute-force the password. Each guess requires real-time interaction with the AP, making attacks exponentially slower. Also provides <strong>forward secrecy</strong> — compromising one session's key doesn't compromise past sessions.</li><li><strong>OWE (Opportunistic Wireless Encryption)</strong> — provides encryption on <strong>open networks</strong> (like coffee shops, airports) without requiring a password. Under WPA2, open networks transmit all data in cleartext. OWE encrypts individual sessions even without authentication, protecting against passive eavesdropping.</li><li><strong>192-bit cryptographic suite</strong> — WPA3-Enterprise optionally supports a 192-bit security suite (CNSA — Commercial National Security Algorithm) for government, military, and financial environments requiring the highest security.</li></ul><p>WPA3 modes:</p><ul><li><strong>WPA3-Personal</strong> — uses SAE instead of PSK. Same user experience (enter a password) but much stronger key exchange behind the scenes.</li><li><strong>WPA3-Enterprise</strong> — uses 802.1X/RADIUS with optional 192-bit security. Same framework as WPA2-Enterprise but with stronger crypto options.</li></ul>",
    visual: { type: "comparison", params: { left: { label: "WPA2", items: ["PSK (4-way handshake)", "Vulnerable to offline attacks", "Open networks = no encryption", "128-bit security"] }, right: { label: "WPA3", items: ["SAE (Dragonfly handshake)", "Resists offline dictionary attacks", "OWE encrypts open networks", "192-bit enterprise option"] } } },
    hack: {
      memory: "WPA3 adds three things: SAE (kills offline dictionary attacks on passwords), OWE (encrypts open/public WiFi), 192-bit (government-grade enterprise crypto). SAE = Simultaneous Authentication of Equals. OWE = Opportunistic Wireless Encryption.",
      practice: "List what WPA3 fixes vs WPA2: 1) SAE replaces PSK 4-way handshake (no more offline cracking). 2) OWE encrypts open networks (no more cleartext at Starbucks). 3) 192-bit enterprise option. Know what each SOLVES.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 56. Wendell Odom OCG Chapter 29. WPA3 is newer exam content (CCNA v1.1). Know SAE, OWE, and 192-bit. The exam tests what WPA3 ADDS over WPA2 — not deep protocol details.",
    },
    micro: [
      { id: "5.9.d.1", term: "WPA3",                         def: "Newest WPA standard. Adds SAE, OWE, forward secrecy. Drops WPS. Currently rolling out.", weight: "high" },
      { id: "5.9.d.2", term: "SAE (Dragonfly)",              def: "Simultaneous Authentication of Equals. Replaces PSK handshake. Forward secrecy; resists offline dictionary attacks.", weight: "high" },
      { id: "5.9.d.3", term: "OWE",                          def: "Opportunistic Wireless Encryption. Encrypts open networks (no password). Replaces plaintext on public WiFi.", weight: "high" },
      { id: "5.9.d.4", term: "WPA3-Enterprise 192-bit",      def: "Higher-security mode. 192-bit crypto for government/financial. Uses CNSA suite.", weight: "med" }
    ]
  },

  "5.9.e": {
    info: "<p><strong>Personal mode (WPA2-Personal or WPA3-Personal)</strong> uses a <strong>Pre-Shared Key (PSK)</strong> — a single shared password that all users must know to connect to the WLAN. The PSK is configured on the access point and entered by each connecting device.</p><p>How WPA2-Personal works:</p><ul><li>The PSK (passphrase of 8-63 characters) is converted into a <strong>Pairwise Master Key (PMK)</strong> using PBKDF2 hashing</li><li>The client and AP perform a <strong>4-way handshake</strong> to derive session-specific encryption keys from the PMK</li><li>Each client session uses unique encryption keys, but all keys are derived from the same PSK</li></ul><p>Strengths and weaknesses:</p><ul><li><strong>Simple to deploy</strong> — no server infrastructure needed, just set a password on the AP</li><li><strong>No individual accountability</strong> — all users share the same password, so you can't track who did what</li><li><strong>Single point of compromise</strong> — if one person shares the password (or a device is compromised), the entire network is exposed. Changing the PSK requires updating every device.</li><li><strong>Vulnerable to offline dictionary attacks (WPA2)</strong> — if an attacker captures the 4-way handshake, they can brute-force the password offline. Long, complex passwords mitigate this.</li></ul><p>Personal mode is appropriate for <strong>home and small office</strong> environments with few users. For corporate environments with many users, <strong>Enterprise mode</strong> is required for accountability and credential management.</p>",
    visual: { type: "handshake", params: { leftLabel: "Client", rightLabel: "AP", steps: ["I know the PSK ->", "<- I know the PSK too", "4-way handshake (derive keys) ->", "<- Encrypted session established"] } },
    hack: {
      memory: "Personal = PSK = one shared PASSWORD for everyone. Like a house key — if you give copies to 50 people, security depends on the most careless person. Simple but no accountability. Home/small office only.",
      practice: "Configure WPA2-Personal on a lab AP. Connect with the PSK. Change the PSK — verify old devices disconnect. Understand why managing 500 users with a shared password is impractical (that's what Enterprise solves).",
      effort: "low",
      meta: "Jeremy's IT Lab Day 56. Wendell Odom OCG Chapter 29. Personal = PSK = shared password = home/SOHO. Enterprise = 802.1X = individual credentials = corporate. The exam gives scenarios — pick the right mode.",
    },
    micro: [
      { id: "5.9.e.1", term: "Personal mode",                def: "PSK (pre-shared key). Everyone uses the same password. Simple. Used in home/SOHO.", weight: "high" },
      { id: "5.9.e.2", term: "Enterprise mode",              def: "802.1X. Each user has unique credentials. Requires RADIUS. Used in corporate networks.", weight: "high" },
      { id: "5.9.e.3", term: "Scenario decision",            def: "'Home network' → Personal. 'Corporate with AD' → Enterprise. 'Small office no RADIUS' → Personal.", weight: "high" }
    ]
  },

  "5.9.f": {
    info: "<p><strong>Enterprise mode (WPA2-Enterprise or WPA3-Enterprise)</strong> uses <strong>802.1X with a RADIUS server</strong> for individual user authentication. Each user has their own unique credentials — no shared password. This is the required security model for corporate and organizational wireless networks.</p><p>The <strong>802.1X authentication framework</strong> involves three parties:</p><ul><li><strong>Supplicant</strong> — the client device requesting network access (laptop, phone, tablet). Runs 802.1X supplicant software (built into most modern OS).</li><li><strong>Authenticator</strong> — the network device controlling access (wireless AP or wired switch). Acts as a middleman — passes authentication messages between supplicant and server but doesn't make the authentication decision itself.</li><li><strong>Authentication Server</strong> — the RADIUS server that verifies credentials and makes the permit/deny decision (Cisco ISE, FreeRADIUS, Microsoft NPS). Returns authorization attributes (VLAN, ACL) to the authenticator.</li></ul><p>Enterprise mode advantages:</p><ul><li><strong>Individual credentials</strong> — each user authenticates with unique username/password or certificate, enabling per-user accountability and audit trails</li><li><strong>Dynamic key assignment</strong> — each user session gets unique encryption keys, so compromising one user's session doesn't expose others</li><li><strong>Centralized management</strong> — add/remove/modify user access from the RADIUS server without touching any AP</li><li><strong>Revocation</strong> — disable one user's access instantly without affecting anyone else (impossible with PSK)</li></ul><p>Common EAP methods: <strong>PEAP</strong> (Protected EAP — username/password in a TLS tunnel), <strong>EAP-TLS</strong> (certificate-based, most secure), <strong>EAP-FAST</strong> (Cisco, uses PAC for fast reconnection).</p>",
    visual: { type: "packet-flow", params: { nodes: ["Supplicant (Client)", "Authenticator (AP)", "RADIUS Server"], color: "#6366f1" } },
    hack: {
      memory: "Enterprise = 802.1X = RADIUS = INDIVIDUAL credentials. Three players: Supplicant (client requesting access), Authenticator (AP/switch controlling access), Authentication Server (RADIUS making the decision). S-A-S.",
      practice: "Set up 802.1X in Packet Tracer or a lab: configure the AP as authenticator pointing to a RADIUS server. Test with a supplicant (laptop). Verify the three-party flow: supplicant → authenticator → RADIUS → back.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 56. Wendell Odom OCG Chapter 10 + 29. The 802.1X roles (supplicant, authenticator, authentication server) are heavily tested. Know all three and which device fills each role.",
    },
    micro: [
      { id: "5.9.f.1", term: "Wireless 802.1X supplicant",   def: "Client device (laptop, phone). Has 802.1X software configured with credentials or cert.", weight: "high" },
      { id: "5.9.f.2", term: "Wireless 802.1X authenticator", def: "The WLC or AP. Forwards EAP messages between supplicant and RADIUS server.", weight: "high" },
      { id: "5.9.f.3", term: "Wireless 802.1X auth server",  def: "RADIUS server (ISE/NPS). Validates credentials against AD or internal database. Returns Accept/Reject.", weight: "high" }
    ]
  },

  // ── 5.10 Configure WLAN with WPA2 PSK ────────────────────────

  "5.10.a": {
    info: "<p>The <strong>Wireless LAN Controller (WLC)</strong> provides centralized management of all access points and WLANs in the network. Administrators access the WLC through two primary methods:</p><ul><li><strong>HTTPS (Web GUI)</strong> — the primary day-to-day management method. Navigate to the WLC's management IP address in a browser (e.g., <code>https://10.0.0.2</code>). The GUI provides a dashboard for creating WLANs, managing APs, configuring security, monitoring clients, and troubleshooting wireless issues.</li><li><strong>Console cable</strong> — used for initial out-of-box setup when the WLC has no IP configuration. Connect via serial/USB console cable to configure the management IP, subnet, gateway, and admin credentials.</li><li><strong>SSH/CLI</strong> — available for advanced configuration and scripting, but the GUI is more commonly used for WLAN management tasks</li></ul><p>The WLC GUI is organized into major sections:</p><ul><li><strong>Monitor</strong> — dashboard showing AP status, client count, rogue AP detection, network health</li><li><strong>WLANs</strong> — create, modify, and delete WLANs (SSIDs, security, QoS)</li><li><strong>Controller</strong> — system settings, interfaces, ports, NTP, DHCP</li><li><strong>Wireless</strong> — AP management, RF groups, channels, power levels</li><li><strong>Security</strong> — AAA, ACLs, rogue AP policies, web authentication</li><li><strong>Management</strong> — SNMP, syslog, software updates, admin accounts</li></ul><p>For the CCNA exam, you need to know the general GUI navigation and the specific steps to create and configure a WLAN with WPA2-PSK security.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Admin Browser (HTTPS)", "WLC Management IP", "WLC Dashboard"], color: "#3b82f6" } },
    hack: {
      memory: "WLC access = HTTPS to the management IP (like any web-managed device). Console for initial setup. GUI tabs: Monitor, WLANs, Controller, Wireless, Security, Management.",
      practice: "If you have access to Cisco CML, Packet Tracer (3504 WLC), or the Cisco DevNet Sandbox WLC, navigate the GUI. Click through each tab. Know where WLANs, Security, and Interfaces live.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 57-58 (WLC Configuration). Wendell Odom OCG Chapter 29. The exam tests WLC management basics: HTTPS for GUI, console for initial setup, and general navigation.",
    },
    micro: [
      { id: "5.10.a.1", term: "WLC HTTPS GUI",               def: "Primary WLC management interface. Browser-based. Access via management IP on HTTPS.", weight: "high" },
      { id: "5.10.a.2", term: "WLC console initial setup",   def: "First-time WLC config via console. After IP assigned, switch to HTTPS GUI for full management.", weight: "med" },
      { id: "5.10.a.3", term: "WLC CLI limited",             def: "Some WLC CLI exists but most daily ops are GUI-driven. Different philosophy from IOS.", weight: "med" }
    ]
  },

  "5.10.b": {
    info: "<p>Creating a new WLAN on the WLC requires specifying three pieces of information:</p><ul><li><strong>Profile Name</strong> — an internal identifier used by administrators to manage the WLAN. This name appears in the WLC management interface but is NOT broadcast to clients. Example: <code>Corporate-WiFi-Employees</code></li><li><strong>SSID (Service Set Identifier)</strong> — the network name that wireless clients see when scanning for available networks. This is what users select on their devices to connect. Example: <code>CorpNet</code></li><li><strong>WLAN ID</strong> — a unique number (1-512) that internally identifies the WLAN on the WLC. Each WLAN must have a different ID.</li></ul><p>Important: the Profile Name and SSID <strong>can be different</strong>. The profile name is for admin purposes (internal), while the SSID is what's broadcast to clients (external). In practice, they're often set to the same value for simplicity, but the exam may test that they CAN be different.</p><p>WLC GUI workflow: <strong>WLANs tab → Create New → Type: WLAN → Enter Profile Name → Enter SSID → Select ID → Apply</strong>. After creation, the WLAN appears in the WLAN list but may be in a disabled state — you'll need to configure security settings (5.10.c), VLAN mapping (5.10.d), and enable it (5.10.e) before clients can connect.</p>",
    visual: { type: "hierarchy", params: { root: "Create WLAN", children: [{ name: "Profile Name (internal)", children: [{ name: "e.g., Corporate-WiFi" }] }, { name: "SSID (client-facing)", children: [{ name: "e.g., CorpNet" }] }, { name: "WLAN ID (1-512)", children: [{ name: "e.g., 1" }] }] } },
    hack: {
      memory: "Three fields to create a WLAN: Profile (admin name), SSID (what users see), ID (unique number 1-512). Mnemonic: PSI — Profile, SSID, ID. Profile and SSID CAN be different.",
      practice: "In a WLC simulator, create 3 WLANs with different profiles, SSIDs, and IDs. Make the profile name different from the SSID on at least one to prove they're independent. Verify each appears in the WLAN list.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 57-58. Wendell Odom OCG Chapter 29. The exam may show a WLC screenshot and ask to identify or create a WLAN. Know the three required fields and that profile name can differ from SSID.",
    },
    micro: [
      { id: "5.10.b.1", term: "Three WLAN identifiers",      def: "Profile Name (internal), SSID (broadcast), WLAN ID (1-512). All three required.", weight: "high" },
      { id: "5.10.b.2", term: "Profile Name ≠ SSID",         def: "Profile is internal label; SSID is what users see. Can match or differ.", weight: "high" },
      { id: "5.10.b.3", term: "WLAN must be enabled",        def: "Default after creation is DISABLED. Set Status = Enabled to start broadcasting.", weight: "high" }
    ]
  },

  "5.10.c": {
    info: "<p>Configuring <strong>WPA2-PSK security</strong> on a WLC WLAN involves navigating the Security tab and setting several options in the correct order:</p><ul><li><strong>Step 1:</strong> Navigate to the WLAN → <strong>Security tab</strong> → <strong>Layer 2</strong> subtab</li><li><strong>Step 2:</strong> Set Layer 2 Security to <strong>WPA+WPA2</strong> (this enables the WPA framework)</li><li><strong>Step 3:</strong> Under WPA+WPA2 Parameters → enable <strong>WPA2 Policy</strong> (check the WPA2 checkbox)</li><li><strong>Step 4:</strong> Set WPA2 Encryption to <strong>AES</strong> (this selects AES/CCMP — never select TKIP)</li><li><strong>Step 5:</strong> Under Auth Key Management → select <strong>PSK</strong> (Pre-Shared Key)</li><li><strong>Step 6:</strong> Enter the <strong>Pre-Shared Key</strong> (PSK format: ASCII, minimum 8 characters, recommended 12+ with complexity)</li><li><strong>Step 7:</strong> Click <strong>Apply</strong></li></ul><p>Critical settings:</p><ul><li><strong>AES is mandatory</strong> — TKIP is deprecated and provides weaker security. The exam expects AES/CCMP as the correct encryption choice.</li><li><strong>PSK complexity</strong> — the pre-shared key should be at least 12 characters mixing uppercase, lowercase, numbers, and symbols. Weak PSKs are vulnerable to offline dictionary attacks against the captured 4-way handshake.</li><li>If the exam shows a WPA2 config with TKIP selected, that's the error — change it to AES.</li></ul>",
    visual: { type: "layer-stack", params: { layers: ["Layer 2 Security: WPA+WPA2", "WPA2 Policy: Enabled", "Encryption: AES (CCMP)", "Auth Key Mgmt: PSK", "Pre-Shared Key: ************"], highlight: 2 } },
    hack: {
      memory: "Security config: WPA+WPA2 mode → enable WPA2 → AES encryption → PSK authentication → enter key. Mnemonic: MVCAK — Mode, Version, Cipher, Auth, Key. AES always, TKIP never.",
      practice: "Walk through the WLC security tab in a simulator: Layer 2 → WPA+WPA2 → WPA2 enabled → AES → PSK → enter a strong key. Connect a client and verify. Then deliberately set TKIP and note it's wrong for the exam.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 57-58. Wendell Odom OCG Chapter 29. The exam shows WLC security configs and asks what's wrong or what to set. Correct answer: WPA2 + AES + PSK. If you see TKIP, that's the error.",
    },
    micro: [
      { id: "5.10.c.1", term: "WPA2 + AES + PSK",            def: "Correct answer for WPA2-Personal scenarios. Layer 2 Security = WPA+WPA2, Encryption = AES, Auth = PSK.", weight: "high" },
      { id: "5.10.c.2", term: "TKIP = wrong answer",         def: "If WLC shows TKIP enabled, that's the security issue. Disable TKIP, enable only AES.", weight: "high" },
      { id: "5.10.c.3", term: "WPA2-PSK config",             def: "Security tab → Layer 2 Security → WPA+WPA2 → check AES → Auth Key Mgmt = PSK → enter key.", weight: "high" }
    ]
  },

  "5.10.d": {
    info: "<p>Each WLAN must be <strong>mapped to an interface</strong> on the WLC, which in turn is associated with a <strong>VLAN</strong>. This mapping determines which wired network segment wireless clients are placed on when they connect.</p><p>The mapping chain: <strong>WLAN (SSID) → WLC Interface → VLAN → Wired Network Segment</strong></p><p>Configuration: navigate to the WLAN's <strong>General tab</strong> and select an interface from the <strong>Interface/Interface Group dropdown</strong>. Each WLC interface is pre-configured with a VLAN tag, IP address, DHCP scope, and other network settings.</p><p>Example mapping:</p><ul><li><strong>WLAN 'Employee'</strong> → Interface 'corp-data' → <strong>VLAN 10</strong> → Corporate data network</li><li><strong>WLAN 'Guest'</strong> → Interface 'guest-net' → <strong>VLAN 100</strong> → Guest network (internet only)</li><li><strong>WLAN 'Voice'</strong> → Interface 'voice-vlan' → <strong>VLAN 50</strong> → Voice network (QoS priority)</li></ul><p>This architecture enables <strong>wireless traffic segmentation</strong> — different SSIDs can place clients on completely different VLANs with different security policies, just like assigning switch ports to VLANs for wired devices. A guest user on the 'Guest' SSID gets internet access only, while an employee on the 'Employee' SSID gets full corporate network access — determined entirely by the WLAN-to-VLAN mapping.</p>",
    visual: { type: "packet-flow", params: { nodes: ["WLAN (SSID: Guest)", "WLC Interface", "VLAN 100", "Guest Network"], color: "#f59e0b" } },
    hack: {
      memory: "WLAN → Interface → VLAN. The interface is the bridge from wireless to wired. Same concept as assigning a switch port to a VLAN, but for wireless. Different SSIDs → different VLANs → different security zones.",
      practice: "In a simulator, create two WLANs: 'Employee' mapped to VLAN 10 and 'Guest' mapped to VLAN 100. Connect clients to each and verify they get IPs from the correct DHCP scope and can't cross VLANs.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 57-58. Wendell Odom OCG Chapter 29. The exam tests that WLANs must be mapped to interfaces/VLANs. Interface mapping controls which VLAN wireless clients join.",
    },
    micro: [
      { id: "5.10.d.1", term: "WLAN → interface → VLAN",     def: "Each WLAN maps to a dynamic interface, which is tied to a VLAN. Clients get IPs from that VLAN's DHCP.", weight: "high" },
      { id: "5.10.d.2", term: "General tab interface mapping", def: "WLAN's General tab has 'Interface/Interface Group' dropdown. Select the dynamic interface.", weight: "high" },
      { id: "5.10.d.3", term: "Switch trunk must allow VLAN", def: "Trunk between switch and WLC must allow the VLANs mapped to WLANs. Common failure scenario.", weight: "high" }
    ]
  },

  "5.10.e": {
    info: "<p>After creating and configuring a WLAN, it must be <strong>explicitly enabled</strong> before clients can see and connect to it. Newly created WLANs are often <strong>disabled by default</strong> — this is a safety feature that prevents a misconfigured WLAN from being broadcast before security settings are properly applied.</p><p>Enable the WLAN: navigate to the WLAN's <strong>General tab</strong> → check <strong>'Status: Enabled'</strong> → click <strong>Apply</strong>.</p><p>Verification checklist after enabling:</p><ul><li><strong>WLAN list shows 'Enabled'</strong> — in the WLANs tab, the status column should show the WLAN as active</li><li><strong>APs are broadcasting the SSID</strong> — check the Wireless tab to verify associated APs are active and the SSID is being broadcast</li><li><strong>Clients can discover the SSID</strong> — scan for WiFi from a client device and verify the SSID appears</li><li><strong>Clients can authenticate</strong> — enter the PSK and verify the client successfully connects and receives an IP address via DHCP</li><li><strong>Traffic flows correctly</strong> — verify the client is on the correct VLAN, can reach expected resources, and is blocked from restricted resources</li></ul><p>The most common troubleshooting scenario: 'Users can't see the new SSID.' First thing to check: is the WLAN status enabled? This is the #1 missed step after creating a new WLAN.</p>",
    visual: { type: "state-machine", params: { states: ["Created (Disabled)", "Status: Enabled", "APs Broadcasting", "Clients Connecting"], active: 1, transitions: true } },
    hack: {
      memory: "Creating a WLAN does NOT enable it. You must flip the switch: General tab → Status: Enabled → Apply. The #1 mistake: perfect config, but WLAN is still disabled. Always check status last.",
      practice: "Lab: Create a WLAN, leave it disabled, scan from a client (SSID missing). Enable it, scan again (SSID appears). Connect with PSK. This sequence — disabled → enabled → connected — burns the lesson in.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 57-58. Wendell Odom OCG Chapter 29. Easy exam point: 'Users can't see the new SSID — what's wrong?' Check WLAN status is Enabled. It's the simplest fix for the most common problem.",
    },
    micro: [
      { id: "5.10.e.1", term: "WLAN status Disabled = no broadcast", def: "WLAN exists in config but SSID is NOT broadcast. APs don't announce it in beacons.", weight: "high" },
      { id: "5.10.e.2", term: "Enable the WLAN",             def: "WLANs tab → select WLAN → General → Status = Enabled → Apply. Now SSID appears.", weight: "high" },
      { id: "5.10.e.3", term: "Most common oversight",       def: "Lab sim scenario: 'new SSID invisible'. First check: WLAN status. Cheapest easy-point answer.", weight: "high" }
    ]
  },

  /* ══════════════════════════════════════════════════════════════
     DOMAIN 6 — AUTOMATION AND PROGRAMMABILITY (10%)
  ══════════════════════════════════════════════════════════════ */

  // ── 6.1 Explain how automation impacts network management ────

  "6.1.a": {
    info: "<p><strong>Network automation reduces human error</strong> — the single largest cause of network outages. Studies consistently show that <strong>40-80% of network outages are caused by human configuration mistakes</strong>: typos in IP addresses, copy-paste errors, inconsistent ACLs, forgetting to save configurations, or applying changes to the wrong device.</p><p>How automation addresses human error:</p><ul><li><strong>Repeatable execution</strong> — automated scripts and playbooks execute the exact same commands in the exact same order every time. A tested playbook that works on one switch works identically on 1,000 switches.</li><li><strong>Template-driven configurations</strong> — instead of typing commands from memory, configurations are generated from validated templates with variables (hostname, IP, VLAN). The template is tested once; variables change per device.</li><li><strong>Version control</strong> — automation configs (playbooks, templates) can be stored in Git, enabling change tracking, peer review, and rollback if a bad change is deployed</li><li><strong>Testing before deployment</strong> — automated changes can be validated in staging environments or dry-run modes before touching production</li></ul><p>Automation doesn't eliminate errors entirely — it <strong>shifts them to the template/script level</strong> where they can be caught once during review and testing, then fixed everywhere. A typo in a template affects every device (bad), but it's also caught and fixed in one place (good). Without automation, the same typo might be made differently on different devices, creating inconsistent and hard-to-troubleshoot configurations.</p>",
    visual: { type: "comparison", params: { left: { label: "Manual CLI", items: ["Typos in configs", "Inconsistent commands", "40-80% of outages", "One device at a time"] }, right: { label: "Automated", items: ["Exact same config every time", "Template-driven consistency", "Errors caught in testing", "Hundreds of devices at once"] } } },
    hack: {
      memory: "Humans make typos. Scripts don't. One bad <code>shut</code> on the wrong interface takes down a site. Automation runs the TESTED playbook every time. 40-80% of outages = human error. Automation directly fixes this.",
      practice: "Write a simple Ansible playbook that configures NTP on a switch. Now imagine typing <code>ntp server 10.0.0.1</code> manually on 200 switches via SSH. One typo, one wrong device, one missed switch — that's why automation exists.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60-62 (Network Automation). Wendell Odom OCG Chapter 19-20. The exam tests automation benefits conceptually. The key stat: 40-80% of outages = human error, and automation directly addresses this.",
    },
    micro: [
      {
        id: "6.1.a.1",
        term: "Automation reduces human error",
        weight: "high",
        info: "<p>Industry studies consistently show that <strong>40-80% of unplanned network outages are caused by human error</strong> — fat-finger typos, copy-paste mistakes, wrong device, forgotten <code>write memory</code>, or a command applied to the production VLAN instead of the lab VLAN. Even seasoned engineers make configuration mistakes under pressure, especially during late-night change windows. Automation attacks this problem directly by removing the keyboard from the critical path.</p><p><strong>How automation reduces errors:</strong> instead of typing commands on each device, engineers author a <strong>tested template</strong> once (Jinja2, YAML, Python) and let an automation engine render device-specific configs. The template goes through peer review and is validated in a lab against a real device. Once it is proven correct, the same rendered output is pushed to every matching device — 1 switch or 1,000 switches, the execution is identical.</p><p><strong>Error shifting, not error elimination:</strong> automation doesn't make humans infallible. If the template has a bug, that bug ships to every device. The difference is that the bug is caught <em>once</em> in code review, testing, or a pilot rollout, then fixed in a single place. Without automation the same typo would exist as 50 subtly different flavors across 50 devices — impossible to hunt down cleanly.</p><p><strong>Safety nets:</strong> modern automation stacks add <strong>dry-run / check mode</strong> (Ansible's <code>--check</code>), <strong>diff mode</strong> to show config deltas before commit, and <strong>pre/post validation tasks</strong> (ping, BGP neighbor up) that fail the playbook if the change breaks something measurable. NETCONF adds a native <strong>candidate datastore</strong> with commit-confirm rollback if the operator doesn't re-confirm within a timer.</p><p><strong>Exam angle:</strong> when the CCNA asks for the #1 benefit of automation, the intended answer is reduced human error and improved consistency, not raw speed. Speed is a side effect of not having to re-verify work a human did by hand.</p>",
        visual: { type: "comparison", params: { left: { label: "Manual CLI", items: ["40-80% of outages = human error", "Typos per device", "Inconsistent flavors", "No rollback plan"] }, right: { label: "Automated", items: ["Template tested once", "Rendered identically", "Peer reviewed in Git", "Commit-confirm rollback"] } } },
        hack: {
          memory: "Humans typo; scripts don't. Template once, test once, deploy everywhere. Errors get shifted from 'per-device random' to 'one place, caught in review'. 40-80% of outages are caused by humans — automation attacks that number directly.",
          practice: "Open Ansible in check mode (--check --diff) against a lab switch with an NTP playbook. Intentionally break the template (wrong server IP) and rerun — diff shows the mistake before anything hits the device. This is the human-error safety net in action.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62 (Network Automation). Wendell Odom OCG Chapter 19-20. Exam testing angle: memorize the 40-80% human-error stat and the concept of 'template reviewed in Git, rendered identically per device'."
        }
      },
      {
        id: "6.1.a.2",
        term: "Key automation benefits",
        weight: "high",
        info: "<p>The CCNA groups the benefits of automation under a short, memorizable list. Expect a multiple-choice question that asks 'Which of the following is NOT a benefit of network automation?' — if you know the canonical list, the odd one out is obvious.</p><p><strong>The core six:</strong></p><ul><li><strong>Speed</strong> — push a change to 1,000 devices in the time it used to take to SSH into one.</li><li><strong>Consistency</strong> — identical configs across all devices of the same role. No snowflakes.</li><li><strong>Scalability</strong> — work no longer grows linearly with device count. One engineer can own a fleet.</li><li><strong>Reduced human error</strong> — template-driven execution eliminates per-device typos.</li><li><strong>Auditability / compliance</strong> — every change is a Git commit with author, diff, and timestamp.</li><li><strong>Self-healing</strong> — event-driven remediation shrinks MTTR from hours to seconds.</li></ul><p><strong>Trade-offs worth knowing:</strong> automation requires <strong>up-front investment</strong> — tooling (Ansible, Python, CI/CD), skills (YAML, Jinja2, Git), and discipline (code review, testing). A bad playbook can break 1,000 devices as fast as it could have fixed them, so <strong>blast radius</strong> matters. Best practice is to roll changes out in waves: lab → 1 device → 10 devices → site → fleet.</p><p><strong>Exam-ready mental model:</strong> automation is about <em>shifting work left</em> — pay the cost once in engineering time to build the template, then reap the benefit every time a change is needed. The more devices you have, the bigger the ROI. This is why large enterprises, cloud providers, and service providers run almost entirely on automation today.</p>",
        visual: { type: "shield", params: { items: ["Speed", "Consistency", "Scalability", "Reduced human error", "Auditability", "Self-healing"], color: "#3b82f6" } },
        hack: {
          memory: "Six benefits: SCS-RAS — Speed, Consistency, Scalability, Reduced-error, Auditability, Self-healing. If a CCNA answer lists 'lower cost of hardware' or 'removes the need for routing protocols' — those are wrong. Automation changes HOW you operate, not the underlying protocols.",
          practice: "For a 500-device fleet, write out which benefit saves the most time/risk: speed (change windows), consistency (audits), scalability (hiring), reduced error (outages), auditability (compliance), self-healing (MTTR). Ranking forces deep familiarity.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Memorize the canonical benefits list — the exam almost always asks 'which is NOT a benefit'."
        }
      }
    ]
  },

  "6.1.b": {
    info: "<p><strong>Automation dramatically accelerates provisioning</strong> — the process of deploying new devices, services, or configurations into the network. Tasks that take hours manually can be completed in minutes or seconds with automation tools.</p><p>Manual vs automated provisioning:</p><ul><li><strong>Manual:</strong> SSH into each device → type configuration commands → verify → move to the next device. For 10 switches: hours. For a new site with 50+ devices: days or weeks.</li><li><strong>Automated:</strong> Write or select a playbook → run it against all target devices simultaneously → verify programmatically. For 10 switches: minutes. For a new site: hours.</li></ul><p><strong>Zero-Touch Provisioning (ZTP)</strong> is the ultimate automation example:</p><ul><li>A brand-new device is physically installed and cabled</li><li>On boot, it sends a DHCP request and receives its management IP plus a pointer to a configuration file (via DHCP options)</li><li>The device downloads its configuration from a TFTP/HTTP server automatically</li><li>The device applies the configuration and becomes operational — <strong>no engineer needs to SSH in or connect via console</strong></li></ul><p>ZTP is critical for organizations deploying devices at scale (data centers, branch offices, retail stores). It eliminates the need for a skilled engineer at every installation site and ensures consistent configurations from day one.</p>",
    visual: { type: "comparison", params: { left: { label: "Manual Provisioning", items: ["SSH to each device", "Type commands one by one", "Hours for 10 switches", "Days for a new site"] }, right: { label: "Automated Provisioning", items: ["Push config to all at once", "ZTP on boot", "Minutes for 10 switches", "Hours for a new site"] } } },
    hack: {
      memory: "Manual = hours per device. Automated = seconds per device. At 500 devices, the math is brutal. ZTP = Zero-Touch Provisioning: plug it in, boot it up, config appears automatically. No SSH, no console, no engineer on site.",
      practice: "Research Cisco ZTP and PnP (Plug and Play). Understand the flow: new device → DHCP → downloads config → operational. No human touch. This is the gold standard for provisioning at scale.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Speed and ZTP are key automation benefits. The exam frames it as 'what does automation improve for deployment?' = speed, consistency, scale.",
    },
    micro: [
      {
        id: "6.1.b.1",
        term: "Automation speed benefit",
        weight: "high",
        info: "<p><strong>Speed</strong> is the most visible benefit of automation. Manual provisioning is bounded by how fast a human can type, verify, and move to the next device. Automation removes both the typing and the context-switching — and critically, it runs <strong>in parallel</strong>, not serially.</p><p><strong>The math:</strong> assume a careful engineer can configure one switch in ~10 minutes (SSH, paste, verify, save, document). For 100 switches that's ~17 hours of continuous work, realistically a full week with breaks and context-switches. An Ansible playbook with a fork count of 50 can push the same change to all 100 switches in under 2 minutes. That is roughly a <strong>500x speedup</strong> on this single task.</p><p><strong>Where speed matters most:</strong></p><ul><li><strong>Emergency change windows</strong> — e.g., rapid ACL deployment in response to an active security incident.</li><li><strong>Site turn-up</strong> — bring a new branch or data hall online in hours instead of days.</li><li><strong>Fleet-wide updates</strong> — NTP servers, SNMPv3 keys, logging targets, certificate rotations.</li><li><strong>Failover / DR drills</strong> — flip routing policies across dozens of devices in seconds.</li></ul><p><strong>Speed is dangerous without discipline.</strong> The same engine that pushes a fix to 1,000 devices in 90 seconds will push a misconfiguration just as fast. Production-grade automation always pairs speed with <strong>staged rollouts</strong>, <strong>automated validation</strong>, and <strong>rollback-on-failure</strong> to keep blast radius under control.</p>",
        visual: { type: "comparison", params: { left: { label: "Manual", items: ["~10 min/device", "100 switches = ~17 hours", "Serial execution", "Context switches add time"] }, right: { label: "Automated", items: ["Parallel execution", "100 switches in ~2 min", "Template + inventory", "Repeatable, auditable"] } } },
        hack: {
          memory: "Manual = minutes per device (serial). Automated = minutes for the whole fleet (parallel). The speedup scales with device count — 10 devices saves a little time; 10,000 devices changes what's possible at all.",
          practice: "Run the same NTP config via Ansible against 5 lab switches, then imagine doing it to 500 manually. The time gap feels abstract on 5 devices — it's catastrophic on 500. Always extrapolate when reasoning about automation ROI.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam framing: 'which automation benefit lets you change the whole fleet at once?' = speed. Know that speed requires staged rollouts to be safe."
        }
      },
      {
        id: "6.1.b.2",
        term: "ZTP (Zero-Touch Provisioning)",
        weight: "high",
        info: "<p><strong>Zero-Touch Provisioning (ZTP)</strong> is the gold-standard automation workflow for new devices. A shipping-new, unconfigured switch or router arrives at a site, gets racked and cabled, powers on, and <strong>configures itself</strong> from a central server — no engineer ever opens a console cable.</p><p><strong>The standard ZTP boot sequence:</strong></p><ol><li>Device boots with factory defaults and sends a <strong>DHCP Discover</strong> on its management interface.</li><li>DHCP server replies with an IP, default gateway, and <strong>DHCP option 67 (bootfile name)</strong> or option 150 (TFTP server) pointing at a config or script URL.</li><li>Device downloads the indicated file from HTTP/HTTPS/TFTP (config, Python script, or Cisco PnP profile).</li><li>Device applies the config, saves it, and comes up in production state.</li></ol><p><strong>Cisco Plug and Play (PnP)</strong> is Cisco's implementation of ZTP, used by DNA Center / Catalyst Center. The device calls home to a PnP server, authenticates, and receives its site-appropriate config and IOS image. This enables <strong>ship-to-site workflows</strong> — Cisco sends the hardware directly to the remote location, a non-technical person plugs it in, and the device self-configures.</p><p><strong>Why it matters:</strong> at scale (hundreds of branch offices, retail stores, or data hall cabinets) the cost and delay of sending an engineer to console into each device is prohibitive. ZTP removes that bottleneck entirely. It also enforces <strong>consistency</strong> — every device gets the exact approved config, not whatever the on-site engineer happened to type.</p><p><strong>Security note:</strong> ZTP brings real security concerns (an attacker on the network could hijack the DHCP reply and feed the device a malicious config). Production ZTP uses signed images, TLS, and device certificate authentication to validate both the server and the device.</p>",
        visual: { type: "state-machine", params: { states: ["Box arrives", "Power on", "DHCP + option 67/150", "Download config", "Apply + operational"], active: 3, transitions: true } },
        hack: {
          memory: "ZTP = plug-and-pray-free. New device: power on → DHCP → options tell it where to fetch config → applies → operational. Cisco's flavor = Plug and Play (PnP), driven from DNA Center. No console cable, no on-site engineer.",
          practice: "Map the ZTP flow step-by-step on paper: DHCP discover → DHCP offer with option 67 → HTTP GET → config applied → device up. Cisco PnP uses the same shape but with a PnP agent calling the DNA Center PnP server.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: 'which feature allows a new switch to self-configure from boot?' = ZTP / PnP. Know DHCP options 67 / 150 are the hand-off."
        }
      }
    ]
  },

  "6.1.c": {
    info: "<p><strong>Automation enables scalability</strong> by decoupling the number of devices from the number of engineers needed to manage them. The effort to apply a configuration change to 10 devices vs 10,000 devices becomes <strong>nearly the same</strong> with proper automation.</p><p>Scalability without automation: adding more devices means hiring more network engineers proportionally. 100 devices might need 2 engineers. 10,000 devices needs 200 engineers. The cost and complexity grow linearly.</p><p>Scalability with automation: one engineer with well-designed playbooks, templates, and orchestration tools can manage thousands of devices. The work scales <strong>logarithmically</strong> rather than linearly — managing 10,000 devices isn't 1,000x the work of managing 10.</p><p>How automation enables scale:</p><ul><li><strong>Parallel execution</strong> — tools like Ansible can push configurations to hundreds of devices simultaneously, not sequentially</li><li><strong>Template reuse</strong> — the same template works for every device of a given role (access switch, distribution switch, WLC) with only variable changes</li><li><strong>Inventory management</strong> — device lists, groupings, and variables are maintained in structured inventory files, not in engineers' heads or spreadsheets</li><li><strong>Orchestration</strong> — complex multi-step workflows (firmware upgrade → config change → validation → rollback if failed) run automatically across the fleet</li></ul><p>This is particularly important for organizations experiencing rapid growth (new sites, cloud expansion, IoT deployments) where the network must scale faster than hiring can keep up.</p>",
    visual: { type: "gauge", params: { level: 90, label: "Scalability: 10 to 10,000 devices", color: "#10b981" } },
    hack: {
      memory: "Without automation: 10 devices = 1 engineer. 10,000 = need 1,000 engineers. With automation: 10,000 devices = 1 engineer + good playbooks. Automation makes the effort FLAT, not proportional to device count.",
      practice: "Thought experiment: change the NTP server on every network device in a 500-device network. Manually: ~50 hours (6+ min/device). Ansible: 5 minutes (one playbook, parallel execution). That's the scalability argument.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Scalability is automation's strongest argument for enterprise networks. The exam asks 'what benefit for growing networks?' = scalability.",
    },
    micro: [
      {
        id: "6.1.c.1",
        term: "Scalability benefit",
        weight: "high",
        info: "<p><strong>Scalability</strong> means the effort to manage the network does not grow proportionally to the number of devices. In a manual model, doubling the fleet doubles the work; in an automated model, doubling the fleet costs one extra line in an inventory file.</p><p><strong>The inventory is the scaling unit.</strong> In automation frameworks like Ansible, devices are listed in a structured <strong>inventory file</strong> with groups (access switches, WAN routers, firewalls) and per-device variables (hostname, mgmt IP, location). Playbooks target groups, not individual devices. Adding a new device to the fleet is a one-line change to inventory — the same playbooks immediately apply to it.</p><p><strong>Why one engineer can manage thousands:</strong></p><ul><li><strong>Parallel execution</strong> — fork count / thread pools run against many devices concurrently.</li><li><strong>Role-based templates</strong> — one template per device role covers the whole fleet of that role.</li><li><strong>Declarative intent</strong> — describe desired state; the engine handles the diff against current state.</li><li><strong>Self-service portals</strong> — routine requests (new VLAN, new ACL entry) flow through a ticket or form instead of an engineer's keyboard.</li></ul><p><strong>Real-world example:</strong> hyperscalers (CoreWeave, AWS, Azure, Meta) run networks of hundreds of thousands of devices with relatively small network-engineering teams. None of this is possible with CLI-per-device workflows — it only works because every device is provisioned and managed through automation from day one.</p>",
        visual: { type: "comparison", params: { left: { label: "Manual scaling", items: ["10 devices = 1 engineer", "100 = 10 engineers", "1000 = 100 engineers", "Linear cost growth"] }, right: { label: "Automation scaling", items: ["10 devices = 1 engineer", "1000 = still 1 engineer", "Inventory is just a list", "Sub-linear cost growth"] } } },
        hack: {
          memory: "Manual ops = LINEAR scaling (every new device adds work). Automation = SUB-LINEAR scaling (new devices are just inventory rows). The bigger the fleet, the bigger the automation ROI — this is why hyperscalers can't exist without it.",
          practice: "Write a mini-inventory in YAML with 3 device groups (access, dist, core). Add a new device — it's one line. Now imagine doing the same in a manual model: provision, document, update runbooks. Automation compresses all those steps into the one inventory edit.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: 'which benefit lets a small team manage a huge network?' = scalability. Distinct from speed — speed is per-change; scalability is about fleet size over time."
        }
      },
      {
        id: "6.1.c.2",
        term: "Linear vs sub-linear growth",
        weight: "med",
        info: "<p>This is the math behind the scalability argument. Understand it once and every other automation benefit makes intuitive sense.</p><p><strong>Linear growth (manual ops):</strong> effort is roughly proportional to device count. <code>Effort ≈ devices × minutes_per_device</code>. Double the fleet, double the work. Triple it, triple the work. This is why manual networking teams tend to hire linearly with the business.</p><p><strong>Sub-linear growth (automated ops):</strong> effort is dominated by <strong>designing the template / playbook</strong>, not by executing it. Executing it against 1 device or 1,000 devices is nearly the same operator effort — the engine handles the parallelism. The effort curve flattens out after initial investment.</p><p><strong>Break-even analysis:</strong> automation is <em>slower</em> for a one-off change on a single device (you spend time writing a playbook you'll never reuse). It becomes progressively faster as you reuse the template across more devices and more changes. In enterprise environments, break-even is almost always worth it — templates get reused dozens of times.</p><p><strong>The deeper shift:</strong> sub-linear growth is not just about cost — it unlocks <em>new operating models</em>. Things that were impossible to do manually (rotate all certs fleet-wide weekly, run a canary every deploy, shift traffic on demand) become trivial with automation. The scale question goes from 'can we afford it?' to 'should we?'.</p>",
        visual: { type: "layer-stack", params: { layers: ["Manual: effort scales ~linearly with devices", "Mixed: some templates, lots of manual", "Fully automated: effort ~flat regardless of device count"], highlight: 2 } },
        hack: {
          memory: "Manual ops = y = x (linear). Automated ops = y ≈ constant (flat after template is written). Break-even happens fast — even one reuse pays back the template effort. Hyperscalers survive because they never bought the linear curve.",
          practice: "Sketch a simple chart: x-axis = device count, y-axis = engineer hours. Draw the linear (manual) line and the nearly-flat (automated) line. Where they cross is the break-even point — usually very small for real networks.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: subtle framing — 'automation scales sub-linearly', 'the cost per device decreases with fleet size'. Both point to the same idea."
        }
      }
    ]
  },

  "6.1.d": {
    info: "<p><strong>Automation ensures consistency and compliance</strong> by deploying identical configurations from validated, version-controlled templates. Every device of the same role gets the exact same security policies, SNMP settings, NTP servers, ACLs, banners, and logging configuration.</p><p>How automation drives consistency:</p><ul><li><strong>Golden templates</strong> — a single, tested, approved template defines the desired configuration for each device role. All devices are configured from this template, eliminating the 'snowflake' problem (each device configured slightly differently by different engineers).</li><li><strong>Infrastructure as Code (IaC)</strong> — network configurations stored in version control (Git). Changes require code review and approval before deployment, just like software code.</li><li><strong>Drift detection</strong> — automated scripts periodically pull running configs and compare them against the approved golden template. Any differences (drift) are flagged for investigation or auto-remediated.</li></ul><p>How automation enables compliance:</p><ul><li><strong>Provable state</strong> — for regulatory compliance (PCI-DSS, HIPAA, SOX, GDPR), organizations must prove that all devices are configured to the required security standards. Automated audits provide this proof.</li><li><strong>Continuous validation</strong> — instead of annual audits, automation continuously verifies that every device matches the required configuration. Compliance becomes a continuous state, not a periodic checkpoint.</li><li><strong>Rapid remediation</strong> — if a device drifts from compliance (manual change, config corruption), automation can detect and fix it in minutes instead of waiting for the next audit cycle</li></ul>",
    visual: { type: "shield", params: { items: ["Identical configs everywhere", "Template-driven deployment", "Automated compliance audits", "Drift detection + remediation"], color: "#3b82f6" } },
    hack: {
      memory: "Consistency = every device configured identically (golden templates). Compliance = you can PROVE it (automated audits). Drift detection = catching unauthorized changes automatically. Templates + audits = consistency + compliance.",
      practice: "Design a compliance automation concept: 1) Define golden template. 2) Pull running-config from all devices nightly. 3) Compare against template. 4) Report differences. 5) Auto-remediate or alert. This is real-world network automation.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Consistency is a key exam benefit. Know: templates ensure identical configs, drift detection catches unauthorized changes, compliance is provable.",
    },
    micro: [
      {
        id: "6.1.d.1",
        term: "Consistency via templates",
        weight: "high",
        info: "<p><strong>Consistency</strong> means every device of the same role has the exact same baseline configuration — the same SNMP community, NTP servers, syslog targets, login banner, AAA servers, SSH timeouts, and QoS policy. Manual networks rarely achieve true consistency. Automated networks make it the default.</p><p><strong>The snowflake problem.</strong> In a manual environment, a switch configured by one engineer at 2 AM during an outage looks different from a switch configured by another engineer three years later during a refresh. Each device accumulates its own quirks — a manually added ACL, a forgotten debug command, a customized hostname pattern. These 'snowflakes' are nearly impossible to audit, hard to troubleshoot, and a nightmare during mass upgrades.</p><p><strong>Templates kill snowflakes.</strong> A golden template defines the config for each device role. The template is parameterized (hostname, IP, VLAN) but the structure is identical. Rendering the template against every device in the role group produces functionally identical configurations. If the template changes, the next playbook run re-aligns every device.</p><p><strong>Jinja2 in practice:</strong> most network automation uses <strong>Jinja2</strong> to render config snippets from variables. A loop over <code>{% for vlan in vlans %}</code> produces the VLAN block for each VLAN defined in inventory — no copy-paste, no missed entries. Templates are stored in Git so changes are reviewable.</p><p><strong>Consistency is a prerequisite for scaling.</strong> You can't troubleshoot a fleet you can't predict. You can't audit for compliance when every device is slightly different. Consistency is the foundation that speed, scalability, and auditability are built on.</p>",
        visual: { type: "comparison", params: { left: { label: "Snowflake devices", items: ["Each device subtly different", "Hidden drift", "Hard to audit", "Mass upgrades break"] }, right: { label: "Template-driven", items: ["Identical structure", "Variables per device", "Reviewable in Git", "Predictable at scale"] } } },
        hack: {
          memory: "Snowflake devices = each one unique, each one a problem. Templates = every device of the same role is a clone. Jinja2 + inventory variables = identical structure, parameterized per device. No drift, no surprises.",
          practice: "Take a lab switch's running config and split it into 'template body' (fixed per role) and 'variables' (hostname, IP, VLAN). That mental split is exactly how production automation works — template + inventory variables = rendered config.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: 'what eliminates configuration snowflakes?' = templates / Infrastructure-as-Code. Know the term 'golden template' or 'golden config'."
        }
      },
      {
        id: "6.1.d.2",
        term: "Configuration drift",
        weight: "high",
        info: "<p><strong>Configuration drift</strong> is the gap between the <strong>intended</strong> (template) config and the <strong>actual</strong> config running on a device. Every manual change that bypasses automation — a late-night hotfix, a 'temporary' debug command, a forgotten interface description — introduces drift.</p><p><strong>Why drift is dangerous:</strong></p><ul><li><strong>Security gaps</strong> — a disabled SNMPv3 group or an added ACL exception can open unauthorized access.</li><li><strong>Compliance failures</strong> — auditors expect every device to match the standard. Drifted devices fail audits.</li><li><strong>Troubleshooting hell</strong> — 'why does only this one switch behave differently?' Often the answer is drift no one documented.</li><li><strong>Broken automation</strong> — the next playbook run may collide with the out-of-band change and undo it, or fail.</li></ul><p><strong>Drift detection loops:</strong> production automation runs a periodic job (nightly, hourly) that pulls <code>running-config</code> from every device and compares it to the rendered template. Any delta is logged, alerted, and either <strong>remediated automatically</strong> (the template wins, device is reconfigured back) or <strong>flagged for review</strong> (someone has to own the diff).</p><p><strong>Tooling examples:</strong> Ansible + <code>--check --diff</code>, NAPALM <code>compare_config()</code>, Cisco DNA Center compliance checks, Nornir + Batfish. All follow the same 'intended vs actual' loop.</p><p><strong>Cultural rule:</strong> mature automation teams forbid 'fix it on the box, clean it up in the template later.' That rule sounds bureaucratic until the team experiences enough outages caused by out-of-band changes silently diverging from the template.</p>",
        visual: { type: "state-machine", params: { states: ["Template = Device (aligned)", "Manual change on device", "Drift detected", "Auto-remediate or alert"], active: 2, transitions: true } },
        hack: {
          memory: "Drift = 'template says X, device actually has Y'. Every out-of-band hotfix is a drift seed. The detection loop (intended vs actual) is the automation team's nightly heartbeat. Template wins: device gets re-aligned automatically.",
          practice: "Pull 'show running-config' from two identical-role switches. Diff them line by line. Every delta is drift. Now imagine that across 500 devices — drift detection tooling (DNA Center, NAPALM) is how you stay sane.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: 'what concept describes device configs gradually diverging from standard?' = configuration drift. Automation + periodic audit = solution."
        }
      },
      {
        id: "6.1.d.3",
        term: "Compliance provability",
        weight: "med",
        info: "<p><strong>Compliance provability</strong> is the ability to demonstrate — with evidence — that every device in the network meets a required security or configuration standard at a given point in time. Auditors don't accept 'trust me'; they require reports. Automation makes those reports trivial.</p><p><strong>Regulatory drivers:</strong> PCI-DSS (payment data), HIPAA (healthcare), SOX (financial controls), FedRAMP (US federal), GDPR (EU data protection), and customer contractual obligations all require proof that network controls are in place. Examples: 'SSH only, no Telnet', 'SNMPv3 with authPriv', 'logging to central SIEM', 'no default SNMP community'.</p><p><strong>How automation proves compliance:</strong></p><ul><li><strong>Single source of truth</strong> — the Git-tracked template IS the approved standard; history shows when/why it changed.</li><li><strong>Continuous validation</strong> — nightly jobs compare actual device state to template. Pass/fail report per device.</li><li><strong>Evidence collection</strong> — screenshots, JSON exports, Git commit IDs, and log entries are all automatically generated.</li><li><strong>Rapid remediation</strong> — failing devices are flagged and re-aligned in the next run, well before the next audit window.</li></ul><p><strong>Before vs after automation:</strong> a manual compliance audit was historically a multi-week fire drill — engineers SSHing into every device to run show commands, pasting output into spreadsheets, signing attestation forms. With automation, the same evidence is produced on demand, signed with timestamps, and stored with the playbook that generated it.</p><p><strong>Exam angle:</strong> the CCNA won't test specific regulatory frameworks, but it will test the <em>concept</em> — automation gives you auditability and provable compliance that manual workflows cannot match.</p>",
        visual: { type: "shield", params: { items: ["Git = single source of truth", "Nightly compliance scan", "Automated evidence export", "Rapid drift remediation"], color: "#10b981" } },
        hack: {
          memory: "Compliance = prove it on demand. Manual = two weeks of pasting show-output into spreadsheets. Automated = one report, one button, cryptographically timestamped. Auditors love automation because evidence is a byproduct of normal operations.",
          practice: "Pick a rule (e.g. 'no Telnet, SSH only'). Design the compliance check: pull 'show run | include transport input' from every device, fail any that include 'telnet'. That's the whole pattern — one rule, one query, every device.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: 'automation enables what compliance capability?' = continuous / provable / auditable compliance. Don't memorize PCI vs HIPAA details."
        }
      }
    ]
  },

  "6.1.e": {
    info: "<p><strong>Self-healing networks</strong> use event-driven automation to <strong>detect failures and automatically take corrective action</strong> without human intervention. The goal is to reduce <strong>MTTR (Mean Time To Repair)</strong> from hours (human response) to seconds (automated response).</p><p>How self-healing works:</p><ul><li><strong>Event detection</strong> — the network generates events via syslog messages, SNMP traps, streaming telemetry, or API webhooks when something goes wrong (link down, high CPU, authentication failure)</li><li><strong>Event correlation</strong> — automation tools correlate multiple events to identify the root cause. A single switch failure might generate 50+ individual alerts — correlation identifies them as one event.</li><li><strong>Automated remediation</strong> — pre-defined scripts execute corrective actions: reroute traffic around a failed link, restart a crashed service, roll back a bad configuration change, clear a stuck process, or isolate a compromised device</li><li><strong>Notification</strong> — even when auto-remediation succeeds, an alert is sent to the NOC team for awareness and post-incident review</li></ul><p>Self-healing examples:</p><ul><li>Link goes down → syslog trigger → script activates backup path → traffic rerouted in seconds</li><li>Interface error count spikes → streaming telemetry alert → script disables/re-enables the interface → problem cleared</li><li>Bad config pushed → validation fails → automatic rollback to previous known-good config</li></ul><p>Cisco DNA Center's <strong>network assurance</strong> with AI/ML-driven analytics is an enterprise example of self-healing capabilities — it can automatically identify and resolve common wireless client issues.</p>",
    visual: { type: "state-machine", params: { states: ["Normal Operation", "Failure Detected", "Auto-Remediation", "Service Restored"], active: 2, transitions: true } },
    hack: {
      memory: "Self-healing = the network is its own doctor. Detect (syslog/SNMP/telemetry) → Diagnose (correlate events) → Fix (run remediation script) → Notify (alert the team). Goal: MTTR from hours to seconds.",
      practice: "Design a self-healing scenario: link down → syslog EEM trigger → script activates backup path → notification to Slack/email. Map the complete automation chain from detection to resolution.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Self-healing is a newer exam concept. Know: event-driven automation detects and fixes issues without human intervention. MTTR reduction is the key metric.",
    },
    micro: [
      {
        id: "6.1.e.1",
        term: "Self-healing networks",
        weight: "high",
        info: "<p><strong>Self-healing networks</strong> use event-driven automation to <strong>detect a problem and fix it without waiting for a human</strong>. The network becomes its own first responder. This closes the gap between failure and recovery from 'human response time' (minutes to hours) to 'event handler time' (seconds).</p><p><strong>The closed-loop pattern:</strong></p><ol><li><strong>Detect</strong> — syslog, SNMP trap, streaming telemetry (gNMI), or webhook fires when something crosses a threshold.</li><li><strong>Correlate</strong> — the automation engine groups related events (one link failure often produces 40+ derivative alerts).</li><li><strong>Decide</strong> — a rule or ML model selects the appropriate remediation playbook.</li><li><strong>Act</strong> — the playbook executes (re-route traffic, clear an interface, restart a process, roll back a change).</li><li><strong>Notify</strong> — even on success, ticket + Slack message are generated so humans can review after the fact.</li></ol><p><strong>Concrete examples:</strong></p><ul><li>Cisco <strong>EEM (Embedded Event Manager)</strong> scripts on IOS devices — trigger on syslog pattern, execute CLI actions locally.</li><li>Ansible playbooks fired by webhooks from monitoring tools (Prometheus, Zabbix, Grafana).</li><li>DNA Center assurance rules that auto-disable interfaces with high error rates.</li><li>BGP route-manipulation scripts that shift traffic off a degraded uplink automatically.</li></ul><p><strong>Guardrails are mandatory.</strong> Self-healing with no limits becomes 'self-destroying' fast — a bug in a remediation script can cascade across the fleet. Production systems gate remediation with <strong>confidence thresholds</strong>, <strong>rate limits</strong> (max N auto-actions per hour), and <strong>human approval</strong> for high-risk playbooks.</p>",
        visual: { type: "state-machine", params: { states: ["Event detected", "Correlated", "Playbook selected", "Remediation executed", "Ticket + notify"], active: 3, transitions: true } },
        hack: {
          memory: "Self-healing = Detect → Correlate → Decide → Act → Notify. The human still sees the ticket; the human no longer has to be the first responder. EEM (Cisco), webhooks + Ansible, DNA Center assurance are the common implementations.",
          practice: "Sketch a self-healing scenario: interface Gi0/1 goes down → syslog → EEM script shuts the backup interface's standby state and brings it active → Slack notification. That is a minimal but complete closed loop.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: 'what lets the network react to failures without human intervention?' = self-healing / event-driven automation. Know EEM as the Cisco native example."
        }
      },
      {
        id: "6.1.e.2",
        term: "MTTR reduction",
        weight: "med",
        info: "<p><strong>MTTR (Mean Time To Repair / Recover)</strong> is the average elapsed time from when a failure occurs until the service is restored. It's one of the most common operational KPIs, second only to MTBF (Mean Time Between Failures). Every automation argument eventually circles back to reducing MTTR.</p><p><strong>Where MTTR is spent, manually:</strong></p><ul><li><strong>Detection</strong> — time until the NOC notices something broke (alert latency + human triage).</li><li><strong>Diagnosis</strong> — time to correlate symptoms and identify root cause.</li><li><strong>Decision</strong> — time to choose a fix, often including approval/escalation.</li><li><strong>Execution</strong> — time to log in, type the fix, verify, close ticket.</li></ul><p><strong>How automation compresses each phase:</strong> streaming telemetry cuts detection latency from minutes to seconds. AIOps / correlation cuts diagnosis. Pre-approved playbooks cut the decision. Automated execution replaces the keyboard. Total MTTR can drop from hours to seconds for common failures.</p><p><strong>Business impact:</strong> for revenue-generating networks (e-commerce, SaaS, carrier voice, cloud), MTTR directly translates to dollars. A 1-hour outage at $100k/hour vs a 2-minute outage saves ~$97k. For safety-critical networks (healthcare, utilities, public safety), MTTR has direct human consequences.</p><p><strong>Common trap:</strong> lowering MTTR on the wrong incident. Automating remediation for a rare, high-risk event is dangerous; automating remediation for common, low-risk events (link flap, process restart, BGP neighbor reset) is where the real MTTR savings come from. Start with boring, repetitive incidents.</p>",
        visual: { type: "layer-stack", params: { layers: ["Detection (telemetry cuts seconds)", "Diagnosis (AIOps cuts minutes)", "Decision (pre-approved playbook)", "Execution (automated action)"], highlight: 3 } },
        hack: {
          memory: "MTTR = Detection + Diagnosis + Decision + Execution. Automation shrinks every phase. Hours → seconds for common incidents is realistic. Start with boring high-frequency incidents, not rare exotic ones.",
          practice: "Time yourself fixing a lab link-flap manually (SSH, diagnose, re-enable, verify, close ticket). Imagine a playbook doing all of that on event. That delta = MTTR reduction. Multiply across all similar incidents per month.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: 'automation reduces what key operational metric?' = MTTR. Know MTBF vs MTTR (BF = between failures; TR = to recover)."
        }
      },
      {
        id: "6.1.e.3",
        term: "Example auto-remediation",
        weight: "med",
        info: "<p>The CCNA expects you to recognize concrete examples of auto-remediation — not to build them, but to identify 'yes, that's self-healing' when you see one. A few canonical examples:</p><ul><li><strong>Link down → backup path activation.</strong> Syslog <code>%LINK-3-UPDOWN</code> on a primary uplink triggers an EEM or Ansible playbook that unshuts the standby uplink and optionally adjusts routing weight. Traffic rerouted in seconds.</li><li><strong>High CPU → runaway process handling.</strong> SNMP trap or telemetry shows sustained 100% CPU → script identifies the top process and either restarts it or fails over to a redundant peer.</li><li><strong>Interface error spike → auto-isolation.</strong> CRC counter climbs on a port → playbook shuts the port and opens a ticket so an on-site tech can replace the transceiver.</li><li><strong>Bad config push → automatic rollback.</strong> NETCONF commit-confirm or Git revert triggered when post-change validation (BGP neighbor, ping, SLA probe) fails within N seconds.</li><li><strong>Security event → microsegmentation.</strong> SIEM detects an endpoint behaving like malware → calls the SDN controller's API to tag it into a quarantine group (e.g. DNA Center + ISE + SGT).</li></ul><p><strong>Pattern:</strong> every example has the same shape — <em>event source → rule / AI → remediation playbook → notification</em>. Memorize the shape and you can recognize any auto-remediation scenario the exam throws at you.</p><p><strong>Cisco specifics to know:</strong> <strong>EEM (Embedded Event Manager)</strong> runs locally on IOS devices and can react to syslog / SNMP / counter triggers without any external controller. DNA Center assurance adds cloud / controller-level correlation and remediation across the campus fabric.</p>",
        visual: { type: "packet-flow", params: { nodes: ["Syslog/SNMP/Telemetry", "Rule or AI", "Remediation playbook", "Action applied", "Ticket + Slack"], color: "#f59e0b" } },
        hack: {
          memory: "Every auto-remediation is: event source → decision → action → notify. Top 5 CCNA-flavor examples: link down → backup; high CPU → restart/failover; CRC errors → shut port; bad config → rollback; malware host → quarantine SGT.",
          practice: "Pick one scenario (link down) and write the complete chain: syslog pattern → EEM trigger → CLI commands → show-command verification → email notification. Building one end-to-end example cements the pattern for all the others.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 19-20. Exam: questions are usually 'which of these is an example of self-healing?' — look for the event → action → notify shape."
        }
      }
    ]
  },

  // ── 6.2 Compare traditional vs controller-based networking ────

  "6.2.a": {
    info: "<p><strong>Traditional networking</strong> uses a <strong>distributed control plane</strong> where every device independently makes its own forwarding decisions. Each router runs its own OSPF/EIGRP, each switch runs its own STP, and each device maintains its own routing tables, MAC tables, and security policies.</p><p>Characteristics of traditional networking:</p><ul><li><strong>Distributed intelligence</strong> — every device is autonomous; no single point of control or visibility</li><li><strong>CLI-based management</strong> — configuration is done device-by-device via SSH, Telnet, or console. Changing a VLAN on 50 switches means SSHing into 50 devices.</li><li><strong>Protocol-driven</strong> — network behavior is determined by distributed protocols (OSPF, STP, HSRP) that each device runs independently. Convergence depends on protocol timers, not central decisions.</li><li><strong>Configuration drift</strong> — over time, devices configured by different engineers at different times develop inconsistencies. No single source of truth exists.</li><li><strong>Limited programmability</strong> — traditional devices expose CLI and SNMP but lack modern APIs for programmatic control</li></ul><p>Traditional networking is <strong>well-understood and battle-tested</strong> — it has been the standard for decades. However, it doesn't scale well for large, dynamic environments because management effort grows linearly with device count. Every change requires touching each device individually, making large-scale changes slow, error-prone, and expensive.</p><p>This is the problem that <strong>controller-based (SDN) networking</strong> was designed to solve — centralizing control and configuration while keeping the proven data plane distributed on each device.</p>",
    visual: { type: "comparison", params: { left: { label: "Traditional", items: ["Distributed control plane", "CLI per device", "Each device decides independently", "Config changes = per device"] }, right: { label: "Controller-Based", items: ["Centralized control plane", "API-driven", "Controller decides for all", "Config changes = one place"] } } },
    hack: {
      memory: "Traditional = every device is its own boss. Like a company with no manager — each employee decides independently. Works for small teams, chaos at scale. Config changes = N devices = N SSH sessions.",
      practice: "List management challenges of a 500-device traditional network: config consistency across devices, change windows requiring per-device SSH, troubleshooting flows across 10 hops, no single source of truth. This motivates why SDN exists.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60 (SDN). Wendell Odom OCG Chapter 17. Traditional = distributed, CLI, per-device. SDN = centralized, API, controller-managed. The exam contrasts these two architectures heavily.",
    },
    micro: [
      {
        id: "6.2.a.1",
        term: "Traditional networking",
        weight: "high",
        info: "<p><strong>Traditional networking</strong> is the model you already know from the first two domains: every router and switch is <strong>autonomous</strong>. Each device runs its own routing protocols, its own spanning tree, its own ACLs. There is no central brain — the network's behavior emerges from each device's independent decisions and the protocols they speak to each other.</p><p><strong>Three planes, all local:</strong></p><ul><li><strong>Control plane</strong> — OSPF/EIGRP/BGP on routers, STP on switches, ARP everywhere. Runs on the device's CPU.</li><li><strong>Data plane</strong> — ASIC / forwarding engine that actually moves packets using tables the control plane built.</li><li><strong>Management plane</strong> — SSH, Telnet, SNMP, syslog, console. Operator access to configure and observe the device.</li></ul><p><strong>Strengths:</strong> no single point of failure for control decisions — if one device crashes, its neighbors react locally via OSPF/STP convergence. Decades of tooling, a massive ecosystem, and deep engineer familiarity. Excellent for small-to-medium networks where per-device management is tractable.</p><p><strong>Weaknesses at scale:</strong></p><ul><li><strong>CLI per device</strong> — change N devices = N SSH sessions (or one big automation layer bolted on, see 6.1).</li><li><strong>Limited global view</strong> — no device sees the whole network; troubleshooting requires stitching together per-hop views manually.</li><li><strong>Configuration drift</strong> — independent configs drift over time; no single source of truth.</li><li><strong>Slow to change policy</strong> — a new security rule requires touching every affected device.</li></ul><p><strong>Why it still dominates:</strong> it works, operators know it, and hardware is cheap and interoperable. SDN hasn't replaced it — instead, most modern enterprises run a hybrid: traditional protocols with layered automation (6.1) and/or a controller (6.2.b) on top.</p>",
        visual: { type: "hierarchy", params: { root: "Traditional network (autonomous devices)", children: [{ name: "Router A (OSPF + CLI)" }, { name: "Router B (OSPF + CLI)" }, { name: "Switch 1 (STP + CLI)" }, { name: "Switch 2 (STP + CLI)" }] } },
        hack: {
          memory: "Traditional = every device is its own boss. Control plane, data plane, management plane all run LOCALLY on each device. Config is CLI per device, change windows are per-SSH-session. Battle-tested but painful at scale.",
          practice: "On a pair of lab routers, configure OSPF manually on both. Notice that each router independently builds its LSDB and picks best paths — no outside brain. That autonomy is the defining feature of traditional networking.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60 (SDN). Wendell Odom OCG Chapter 17. Exam: 'traditional networking is characterized by?' = distributed control plane, per-device CLI config, device-level protocols (OSPF, STP)."
        }
      },
      {
        id: "6.2.a.2",
        term: "SDN / controller-based",
        weight: "high",
        info: "<p><strong>SDN (Software-Defined Networking)</strong> moves the <strong>control plane off the devices and onto a centralized controller</strong>. Devices become fast forwarding engines that do whatever the controller tells them. The controller has global visibility and makes network-wide decisions.</p><p><strong>Three layers of an SDN architecture:</strong></p><ul><li><strong>Application layer</strong> — business apps, orchestration tools, analytics platforms. Consumes northbound APIs.</li><li><strong>Control layer</strong> — the SDN controller. Runs control-plane logic for the entire fabric. Talks north to apps and south to devices.</li><li><strong>Infrastructure layer</strong> — physical / virtual switches and routers. Data plane only; follow controller rules.</li></ul><p><strong>Flavors of SDN on the CCNA:</strong></p><ul><li><strong>Open SDN</strong> — controller programs devices via <strong>OpenFlow</strong>. Pure model, academically clean.</li><li><strong>Cisco SD-Access</strong> — campus fabric driven by DNA / Catalyst Center (VXLAN + LISP + CTS).</li><li><strong>Cisco SD-WAN</strong> — driven by vManage / vSmart / vBond (Viptela). Controller-based overlay across WAN.</li><li><strong>Cisco ACI</strong> — data center fabric driven by APIC. Application-centric policy.</li><li><strong>Cloud SDN</strong> — Meraki dashboard (cloud-hosted controller) for wireless/switching/security.</li></ul><p><strong>Benefits vs traditional:</strong> single pane of glass, API-driven programmability, consistent policy, faster change rollouts, intent-based configuration. <strong>Trade-offs:</strong> the controller becomes critical infrastructure (HA and backup are mandatory), and the operational model is very different — engineers need programming skills, not just CLI muscle memory.</p>",
        visual: { type: "layer-stack", params: { layers: ["Applications (orchestration, analytics)", "SDN Controller (global brain)", "Devices (data plane only)"], highlight: 1 } },
        hack: {
          memory: "SDN = one brain, many hands. Controller owns control plane; devices own data plane only. Three layers: apps → controller → devices. Flavors to recognize: OpenFlow, SD-Access, SD-WAN, ACI, Meraki.",
          practice: "Draw the three-layer SDN pyramid from memory every day until it's automatic. Label north API (apps → controller) and south API (controller → devices). That diagram is the backbone of every 6.2/6.3 question.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'SDN centralizes which plane?' = control plane. 'Devices in SDN run which plane?' = data plane (forwarding) only."
        }
      },
      {
        id: "6.2.a.3",
        term: "Contrast table",
        weight: "high",
        info: "<p>The CCNA loves side-by-side comparison questions. Build this contrast table in your head — if you can recite it cold, you can answer almost any 6.2.a-flavored question instantly.</p><table><tr><th>Dimension</th><th>Traditional</th><th>Controller-based (SDN)</th></tr><tr><td>Control plane</td><td>Distributed — on every device</td><td>Centralized — on controller</td></tr><tr><td>Data plane</td><td>Distributed — on every device</td><td>Distributed — on every device</td></tr><tr><td>Config method</td><td>CLI per device</td><td>API / GUI on controller</td></tr><tr><td>Change scope</td><td>Per device</td><td>Fleet-wide from one place</td></tr><tr><td>Visibility</td><td>Per device (show commands)</td><td>Network-wide (single pane)</td></tr><tr><td>Policy</td><td>ACL/VLAN per device</td><td>Intent-based, rendered by controller</td></tr><tr><td>Programmability</td><td>SNMP, screen-scraping</td><td>REST + NETCONF/YANG native</td></tr><tr><td>Failure impact</td><td>Device failure = local</td><td>Controller failure = broad (needs HA)</td></tr></table><p><strong>The asymmetry to notice:</strong> <em>data plane is distributed in both models</em>. Devices always forward packets locally — that's how you get wire speed. What moves in SDN is the control plane (decisions) and the management plane (configuration). This is the most common exam trap: thinking SDN centralizes forwarding. It doesn't.</p><p><strong>Hybrid reality:</strong> most modern networks are a mix. Campus may run SD-Access; WAN may run SD-WAN; data center may run ACI; legacy branches may still be pure CLI. One enterprise can have all of these simultaneously.</p>",
        visual: { type: "comparison", params: { left: { label: "Traditional", items: ["Control plane: distributed", "CLI per device", "SNMP / screen-scrape", "Change scope: per device"] }, right: { label: "SDN / controller-based", items: ["Control plane: centralized", "REST / NETCONF APIs", "Intent-based policy", "Change scope: fleet-wide"] } } },
        hack: {
          memory: "Memorize the row that trips people up: DATA PLANE is distributed in BOTH models. What moves to the controller is CONTROL + MANAGEMENT. Traditional = every device a boss; SDN = one boss, many hands. Forwarding stays fast and local either way.",
          practice: "Recite the contrast table aloud: 'control plane — distributed vs centralized; config — CLI vs API; change scope — per-device vs fleet-wide; programmability — SNMP vs NETCONF/REST.' If it rolls off the tongue, the exam question is a gift.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: expect 'which of the following is true about SDN vs traditional?' style questions. The data-plane-stays-distributed fact is the usual trick."
        }
      }
    ]
  },

  "6.2.b": {
    info: "<p><strong>Controller-based networking (SDN)</strong> centralizes the network's intelligence into a <strong>software controller</strong> that has a global view of the entire network and makes forwarding/policy decisions for all devices. The individual devices retain their <strong>data plane</strong> (they still physically forward packets) but the <strong>control plane logic</strong> is managed centrally.</p><p>How controller-based networking works:</p><ul><li><strong>Centralized control plane</strong> — the SDN controller runs routing algorithms, computes optimal paths, defines security policies, and determines how traffic should flow across the entire network</li><li><strong>Distributed data plane</strong> — individual switches and routers still forward packets at wire speed, but they follow the rules pushed down by the controller rather than computing them independently</li><li><strong>API-driven management</strong> — configuration is done through the controller's REST APIs or GUI, not device-by-device CLI. Changes are applied to the controller, which pushes them to all relevant devices.</li><li><strong>Southbound communication</strong> — the controller communicates with network devices using protocols like OpenFlow, NETCONF, RESTCONF, or SNMP to push configurations and receive state information</li></ul><p>Key advantages over traditional networking:</p><ul><li><strong>Single point of management</strong> — one controller manages hundreds/thousands of devices</li><li><strong>Network-wide visibility</strong> — the controller sees the entire topology, all links, all traffic patterns</li><li><strong>Programmability</strong> — applications can interact with the network via APIs to request resources, implement policies, or respond to events automatically</li><li><strong>Consistency</strong> — policies are defined once on the controller and applied uniformly everywhere</li></ul>",
    visual: { type: "hierarchy", params: { root: "SDN Controller", children: [{ name: "Switch 1 (data plane)", children: [{ name: "Forwards per controller rules" }] }, { name: "Switch 2 (data plane)", children: [{ name: "Forwards per controller rules" }] }, { name: "Switch 3 (data plane)", children: [{ name: "Forwards per controller rules" }] }] } },
    hack: {
      memory: "SDN = the BRAIN (controller) tells the HANDS (switches) what to do. The brain sees the whole body (network-wide view); each hand only knows its own fingers (local forwarding). Centralized intelligence + distributed forwarding.",
      practice: "Draw the three-layer SDN architecture: Application Layer (top) → Controller Layer (middle) → Infrastructure Layer (bottom). Label northbound APIs (apps → controller) and southbound APIs (controller → devices). This diagram appears on the exam.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. The exam heavily tests SDN architecture. Controller = centralized control plane. Devices = data plane. Communication = southbound APIs (OpenFlow, NETCONF).",
    },
    micro: [
      {
        id: "6.2.b.1",
        term: "SDN controller",
        weight: "high",
        info: "<p>The <strong>SDN controller</strong> is the centralized software component that owns the control plane for an entire network fabric. It is simultaneously the brain (decides how the network behaves), the mouth (talks to devices and applications), and the memory (stores global topology, policy, and state).</p><p><strong>What lives inside the controller:</strong></p><ul><li><strong>Topology database</strong> — full graph of devices, links, and endpoints.</li><li><strong>Policy engine</strong> — rules that translate business intent (who can talk to whom) into device-level config.</li><li><strong>Path computation</strong> — replaces per-device OSPF/EIGRP with global shortest-path / traffic-engineered routing.</li><li><strong>API gateways</strong> — northbound REST to apps, southbound to devices.</li><li><strong>State store</strong> — current operational state of every managed device.</li></ul><p><strong>Examples of controllers tested on CCNA:</strong></p><ul><li><strong>Cisco DNA Center / Catalyst Center</strong> — campus SDN (SD-Access fabric).</li><li><strong>Cisco APIC</strong> — data center SDN (ACI fabric).</li><li><strong>Cisco vManage</strong> — SD-WAN controller (Viptela).</li><li><strong>Cisco Meraki dashboard</strong> — cloud-hosted controller for Meraki APs/switches/MX.</li><li><strong>Open source</strong> — OpenDaylight, ONOS — referenced for the OpenFlow ecosystem.</li></ul><p><strong>HA is non-negotiable.</strong> Because the controller is the control plane for the whole fabric, losing it means losing the ability to change the network (forwarding continues via cached rules, but nothing new can be provisioned). Production deployments cluster controllers for redundancy and replicate state between members.</p>",
        visual: { type: "hierarchy", params: { root: "SDN Controller", children: [{ name: "Topology DB" }, { name: "Policy engine" }, { name: "Path computation" }, { name: "API gateways" }, { name: "State store" }] } },
        hack: {
          memory: "The controller = brain + mouth + memory. Owns topology, policy, path computation, and APIs. Examples by domain: DNA Center (campus), APIC (DC/ACI), vManage (SD-WAN), Meraki (cloud). Cluster it for HA — it's now critical infra.",
          practice: "Log into the free Cisco DevNet Sandbox DNA Center or Meraki dashboard. Click around the inventory, topology, and policy tabs. You're seeing a real controller's view of a real fabric — exactly what the exam is describing in abstract.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'what component hosts the control plane in SDN?' = the SDN controller. Know which controller belongs to which fabric (DNA Center → SD-Access, APIC → ACI, vManage → SD-WAN)."
        }
      },
      {
        id: "6.2.b.2",
        term: "Data plane (devices)",
        weight: "high",
        info: "<p>In an SDN architecture, the network devices (switches, routers, APs) become <strong>data-plane-focused forwarding engines</strong>. They <em>do not</em> run the control-plane logic anymore — they follow the forwarding rules their controller gives them.</p><p><strong>What devices still do:</strong></p><ul><li>Parse incoming packets at wire speed in ASICs / NPUs.</li><li>Perform lookups in locally-installed tables (FIB, MAC, flow tables).</li><li>Rewrite L2/L3 headers, apply QoS marking, encapsulate/decapsulate VXLAN.</li><li>Emit telemetry (gNMI / streaming) and syslog to the controller.</li><li>Maintain physical link state and basic bring-up protocols.</li></ul><p><strong>What devices no longer do (in pure SDN):</strong></p><ul><li>Run global routing protocols like OSPF/BGP to peer with other devices for topology info.</li><li>Make autonomous forwarding decisions outside of what the controller provisioned.</li><li>Host per-device ACL / policy logic — that now comes from the controller's policy engine.</li></ul><p><strong>Hybrid reality:</strong> in practice (SD-Access, ACI, SD-WAN), devices still run <em>some</em> protocols locally — IS-IS in SD-Access underlay, BGP + OMP in SD-WAN, eVPN/BGP in some fabrics. The distinction is that those protocols now serve the controller's fabric rather than being configured device-by-device by a human. The controller is the user of those protocols; the human doesn't touch them directly.</p><p><strong>Performance implication:</strong> because data plane stays on device ASICs, SDN does NOT slow down forwarding. A 100G port still forwards at 100G. What's different is the <em>control</em> — rules come from the controller instead of from local OSPF/STP.</p>",
        visual: { type: "layer-stack", params: { layers: ["Controller (control plane)", "Device CPU (limited agent)", "Device ASIC (data plane, wire speed)"], highlight: 2 } },
        hack: {
          memory: "Devices = hands, not brains. They keep the ASIC-speed data plane (parse, lookup, rewrite, forward) but offload the control plane to the controller. Forwarding speed stays the same; what changes is who writes the forwarding rules.",
          practice: "On any Cisco device, 'show ip route' shows local control-plane decisions (OSPF, static, etc.). In an SDN fabric, those routes come from the controller, not from local OSPF. Same data plane operation, different source of truth.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'in SDN, network devices perform which plane's functions?' = data plane. Forwarding stays local and fast; control plane decisions move to the controller."
        }
      },
      {
        id: "6.2.b.3",
        term: "Southbound APIs",
        weight: "high",
        info: "<p><strong>Southbound APIs (SBIs)</strong> are the protocols the controller uses to program and monitor the devices below it. On the architecture diagram, 'southbound' literally means 'down toward the infrastructure layer.' Memorize the direction: <em>controller → devices = south</em>.</p><p><strong>The southbound protocols CCNA cares about:</strong></p><ul><li><strong>OpenFlow</strong> — the original pure-SDN protocol. Programs flow tables directly on switches. Great in academic and OpenDaylight / ONOS discussions; rare in production Cisco shops.</li><li><strong>NETCONF</strong> — XML over SSH (TCP 830). Uses <strong>YANG data models</strong>. Supports candidate / running / startup datastores and commit-confirm. Cisco's primary modern southbound protocol.</li><li><strong>RESTCONF</strong> — REST/HTTP version of NETCONF. Same YANG models, HTTP verbs + JSON. Easier to call from web apps.</li><li><strong>gNMI</strong> — gRPC Network Management Interface. Google-born, streaming telemetry + config. Increasingly popular.</li><li><strong>SNMP</strong> — legacy southbound for monitoring. Still everywhere; very limited for configuration.</li><li><strong>CLI over SSH</strong> — 'screen-scraping' bridge used by Ansible and older Cisco controllers to talk to IOS devices.</li></ul><p><strong>YANG matters.</strong> NETCONF / RESTCONF / gNMI all share YANG as the data modeling language — a schema that describes what can be configured and what state can be read. This makes device configuration <strong>structured</strong> (not free-form CLI), which is what enables reliable programmatic control.</p><p><strong>Ports to know:</strong> NETCONF over SSH = <strong>TCP 830</strong>. RESTCONF = HTTPS <strong>TCP 443</strong>. gNMI = gRPC typically on <strong>TCP 50051</strong> or TLS. SNMP = UDP 161 (get/set), UDP 162 (traps).</p>",
        visual: { type: "layer-stack", params: { layers: ["Controller (south egress)", "NETCONF / RESTCONF / gNMI / OpenFlow", "Devices (agents)"], highlight: 1 } },
        hack: {
          memory: "SBI = Southbound = Down to devices. The four you must recognize: OpenFlow (pure SDN), NETCONF (XML/SSH/830), RESTCONF (HTTP/JSON), gNMI (gRPC/streaming). YANG is the schema language behind NETCONF/RESTCONF/gNMI.",
          practice: "Pick NETCONF in a lab: 'netconf-yang' on a Cisco device, then connect with ncclient from Python. Seeing an XML config pulled via TCP 830 makes the abstraction concrete in a way that never fades.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 17-18. Exam: 'which is a southbound protocol?' = OpenFlow / NETCONF / RESTCONF / gNMI. Pair with ports: NETCONF = TCP 830."
        }
      }
    ]
  },

  "6.2.c": {
    info: "<p>Controller-based networking provides a <strong>'single pane of glass'</strong> — one unified dashboard through which administrators can view and manage the entire network. This is one of the most significant operational advantages of SDN over traditional per-device management.</p><p>Key benefits of the centralized controller approach:</p><ul><li><strong>Network-wide visibility</strong> — see ALL devices, links, traffic patterns, and health metrics from one dashboard. No more piecing together information from individual device CLIs.</li><li><strong>End-to-end troubleshooting</strong> — trace a packet's path across the entire network from one interface. In traditional networking, you'd need to SSH into each hop and check routing/switching tables manually.</li><li><strong>Consistent policy enforcement</strong> — define a security policy once on the controller (e.g., 'block guest traffic from reaching the server VLAN') and it's applied uniformly across every relevant device.</li><li><strong>Rapid provisioning</strong> — deploy new devices, VLANs, or services from the controller in minutes. No device-by-device CLI configuration.</li><li><strong>API programmability</strong> — third-party applications can interact with the network via the controller's northbound REST APIs. This enables custom automation, integration with ITSM tools, and dynamic network adaptation.</li><li><strong>Intent-based operations</strong> — tell the controller WHAT you want ('isolate IoT devices from corporate traffic'), and it translates your intent into device-level configurations automatically</li></ul><p>The controller's <strong>global network view</strong> enables optimizations that are impossible with distributed control — like traffic engineering across the entire network rather than hop-by-hop shortest-path routing.</p>",
    visual: { type: "shield", params: { items: ["Single pane of glass", "Network-wide visibility", "API programmability", "Consistent policy", "Rapid provisioning"], color: "#10b981" } },
    hack: {
      memory: "Single pane of glass = one window to see EVERYTHING. No more SSH into 50 devices to troubleshoot one flow. The controller sees the whole network at once. Intent-based = tell it WHAT you want, not HOW.",
      practice: "If you've used Meraki, DNA Center, or UniFi, recall the dashboard experience — full network visibility from one screen. Compare to logging into 20 individual switches via SSH to track one packet flow. That contrast IS the exam answer.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. 'Single pane of glass' is THE SDN buzzword. Know it. Also know: intent-based networking = describe desired state, controller implements it.",
    },
    micro: [
      {
        id: "6.2.c.1",
        term: "Single pane of glass",
        weight: "high",
        info: "<p><strong>Single pane of glass</strong> is SDN's most-marketed, most-tested phrase. It means: <em>one dashboard that shows the entire network and lets you manage all of it.</em> Replaces the traditional workflow of opening 10 SSH tabs and stitching together <code>show</code> output in your head.</p><p><strong>What a good SPG dashboard shows:</strong></p><ul><li><strong>Topology</strong> — every device and link, click-to-drill-in.</li><li><strong>Health</strong> — device-level and fabric-level status, color-coded.</li><li><strong>Traffic / flow</strong> — who's talking to whom, bandwidth hot spots, top talkers.</li><li><strong>Policy view</strong> — what ACLs / SGTs / segmentation rules are active where.</li><li><strong>Change / config</strong> — deploy changes, view diffs, roll back.</li><li><strong>Alerts / assurance</strong> — issues detected + suggested remediations.</li></ul><p><strong>Why it's transformative for ops:</strong> troubleshooting time collapses. A wireless user complaint that used to require checking the AP, the WLC, the switch uplink, the distribution layer, and the DHCP server now surfaces in the controller as one correlated event with 'likely root cause: AP-21's radar detection is flapping the 5 GHz channel.'</p><p><strong>Examples you'll see referenced:</strong> Cisco DNA / Catalyst Center, Cisco Meraki dashboard, Cisco vManage, APIC, and non-Cisco analogs (Juniper Mist, Arista CloudVision, Aruba Central). All implement the same single-pane concept at different scales.</p><p><strong>Exam tip:</strong> if a question mentions 'unified management', 'one dashboard', or 'centralized visibility', the expected answer is SDN / controller-based networking / single pane of glass.</p>",
        visual: { type: "shield", params: { items: ["Topology", "Health", "Flows", "Policy", "Change", "Assurance"], color: "#3b82f6" } },
        hack: {
          memory: "Single pane of glass = one dashboard to rule them all. Replaces 10 SSH tabs with one unified view. If a question mentions 'centralized visibility' or 'unified management', the answer is always SDN / controller-based.",
          practice: "Open the Meraki dashboard demo (dashboard.meraki.com, public sandbox) or DNA Center sandbox. Count how many screens you'd need in a CLI world to see the same information. That delta IS the benefit.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: buzzword-level. 'Single pane of glass' appears verbatim in multiple-choice stems."
        }
      },
      {
        id: "6.2.c.2",
        term: "Intent-Based Networking (IBN)",
        weight: "high",
        info: "<p><strong>Intent-Based Networking (IBN)</strong> is the natural evolution of SDN: instead of telling the controller HOW to configure the fabric, you tell it WHAT outcome you want — your <em>intent</em> — and the controller figures out all the device-level details.</p><p><strong>The four IBN pillars:</strong></p><ol><li><strong>Translation</strong> — capture business intent in plain-ish language ('Guest users cannot reach HR servers').</li><li><strong>Activation</strong> — controller renders the intent into device-specific configs and deploys them.</li><li><strong>Assurance</strong> — controller continuously verifies that the deployed network actually delivers the intent (monitors traffic, counts policy violations, checks health).</li><li><strong>Optimization</strong> — controller adapts to changing conditions (new devices, traffic shifts, failures) while preserving the intent.</li></ol><p><strong>Declarative vs procedural:</strong> traditional CLI is <em>procedural</em> — you write the exact steps in the exact order. IBN is <em>declarative</em> — you describe the end state; the system picks the steps. This is the same shift that happened in infrastructure-as-code (Terraform declarative vs bash scripts procedural) and in Kubernetes (declarative manifests vs procedural 'run this container').</p><p><strong>Cisco DNA / Catalyst Center</strong> is the reference IBN platform on the CCNA. You define sites, user groups, and policies in the GUI; DNA Center translates that intent into IS-IS underlay configs, VXLAN overlays, LISP mappings, and CTS group-tag policies across every fabric device.</p><p><strong>Why it's exam-relevant:</strong> IBN ties together SDN, assurance, automation, and AI/ML topics. Expect questions that describe an outcome ('configure the fabric so these users can only reach these apps') and ask what Cisco feature enables that declarative workflow = IBN / DNA Center.</p>",
        visual: { type: "state-machine", params: { states: ["Translation", "Activation", "Assurance", "Optimization"], active: 1, transitions: true } },
        hack: {
          memory: "IBN = declare WHAT, not HOW. Four pillars: Translation → Activation → Assurance → Optimization (TAAO). Same shift as Terraform or Kubernetes. Cisco's reference platform = DNA / Catalyst Center.",
          practice: "Frame a policy in intent language: 'IoT devices can reach the internet but nothing inside.' Then imagine the device-level work it takes (VLANs, ACLs, SGTs across 20 switches). IBN abstracts all that away.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'what style of networking lets admins declare desired outcomes?' = intent-based networking / IBN. Know it pairs with DNA Center."
        }
      }
    ]
  },

  "6.2.d": {
    info: "<p><strong>Cisco DNA Center</strong> (rebranded as <strong>Cisco Catalyst Center</strong> in 2023) is Cisco's enterprise SDN controller for campus networks. It implements <strong>intent-based networking (IBN)</strong> — you define the desired network state (your intent), and DNA Center automatically translates it into device-specific configurations.</p><p>DNA Center's four workflow pillars (DPPA):</p><ul><li><strong>Design</strong> — define the network hierarchy (sites, buildings, floors), create IP address pools, configure network settings (DNS, NTP, DHCP), and define network profiles</li><li><strong>Policy</strong> — create and manage access policies using <strong>SD-Access</strong> (Software-Defined Access) for group-based segmentation. Uses <strong>Cisco TrustSec</strong> with Scalable Group Tags (SGTs) to enforce who can talk to whom, regardless of IP addressing.</li><li><strong>Provision</strong> — deploy devices using Plug and Play (PnP), assign devices to sites, push configurations, and manage software images (<strong>SWIM — Software Image Management</strong>)</li><li><strong>Assurance</strong> — AI/ML-driven network analytics that continuously monitors network health, identifies issues before users report them, and provides guided remediation. Correlates wireless client issues (auth, DHCP, association) to root causes automatically.</li></ul><p>DNA Center uses <strong>NETCONF/YANG</strong> for device communication (southbound) and exposes <strong>REST APIs</strong> (northbound) for integration with third-party tools and custom automation. It integrates with <strong>Cisco ISE (Identity Services Engine)</strong> for 802.1X authentication and policy enforcement.</p><p>The CCNA exam tests DNA Center conceptually — you won't configure it, but you must know what it does, its four pillars, and that it's intent-based.</p>",
    visual: { type: "hierarchy", params: { root: "Cisco DNA Center", children: [{ name: "Design", children: [{ name: "Network hierarchy" }] }, { name: "Policy", children: [{ name: "SD-Access segmentation" }] }, { name: "Provision", children: [{ name: "Device deployment" }] }, { name: "Assurance", children: [{ name: "AI/ML analytics" }] }] } },
    hack: {
      memory: "DNA Center = Cisco's SDN controller for campus. Four pillars: DPPA — Design, Policy, Provision, Assurance. Intent-based = tell it WHAT you want, not HOW. Uses REST APIs + NETCONF/YANG. Integrates with ISE.",
      practice: "Access the free Cisco DevNet Sandbox (devnetsandbox.cisco.com) to explore DNA Center's GUI. Navigate the four workflow areas: Design, Policy, Provision, Assurance. Know what lives in each section.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. DNA Center is testable on the CCNA. Know: intent-based, four pillars (DPPA), REST APIs, SD-Access for segmentation. Conceptual knowledge only — no configuration.",
    },
    micro: [
      {
        id: "6.2.d.1",
        term: "DNA Center / Catalyst Center",
        weight: "high",
        info: "<p><strong>Cisco DNA Center</strong> — renamed to <strong>Cisco Catalyst Center</strong> in 2023 — is Cisco's on-prem SDN / IBN controller for <strong>campus</strong> wired and wireless networks. Think of it as the brain of Cisco's SD-Access fabric.</p><p><strong>Scope of what it manages:</strong></p><ul><li>Catalyst 9000 series switches (access, distribution, core).</li><li>Cisco access points and wireless LAN controllers (Catalyst 9800, older 3504 / 5520).</li><li>Cisco ISR / ASR routers when they're in campus / WAN roles.</li><li>Integration with Cisco ISE for identity, Stealthwatch for security analytics, ThousandEyes for WAN visibility.</li></ul><p><strong>What it is NOT:</strong> it is not the data-center controller (APIC / ACI owns that), it is not the SD-WAN controller (vManage), and it is not the cloud-managed option (Meraki dashboard). Matching the right controller to the right domain is a common CCNA trap — DNA Center = campus.</p><p><strong>Deployment model:</strong> on-prem appliance (dedicated UCS-based server). HA cluster for production. Communicates southbound to devices with <strong>NETCONF/YANG</strong>, <strong>SNMP</strong>, and <strong>SSH/CLI</strong> for legacy commands. Exposes <strong>northbound REST APIs</strong> to external tools (ITSM integration, custom automation).</p><p><strong>Why the rename?</strong> Cisco consolidated its campus management product line under the Catalyst brand (Catalyst 9000, Catalyst 9800 WLC, Catalyst SD-WAN, Catalyst Center). CCNA 200-301 v1.1 accepts either 'DNA Center' or 'Catalyst Center' — same product, same functionality.</p>",
        visual: { type: "hierarchy", params: { root: "Cisco DNA / Catalyst Center", children: [{ name: "Design" }, { name: "Policy" }, { name: "Provision" }, { name: "Assurance" }, { name: "Platform / APIs" }] } },
        hack: {
          memory: "DNA Center = Catalyst Center = Cisco's campus SDN controller. NOT data center (APIC), NOT SD-WAN (vManage), NOT cloud (Meraki). Southbound = NETCONF/SNMP/SSH. Northbound = REST API. On-prem appliance, clustered for HA.",
          practice: "In the free DevNet DNA Center sandbox (devnetsandbox.cisco.com) walk the four workflow pillars — Design, Policy, Provision, Assurance. Note which tasks live in each pillar so the DPPA acronym in 6.2.d.2 feels natural.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'which controller manages campus SD-Access?' = DNA Center / Catalyst Center. If the stem says 'data center' → APIC; 'WAN' → vManage; 'cloud-managed APs' → Meraki."
        }
      },
      {
        id: "6.2.d.2",
        term: "DPPA four pillars",
        weight: "high",
        info: "<p>DNA / Catalyst Center organizes its workflows into <strong>four pillars</strong>. Memorize the acronym <strong>DPPA</strong> (Design, Policy, Provision, Assurance) — the CCNA uses it as the skeleton for most controller questions.</p><ul><li><strong>Design</strong> — network hierarchy (sites, buildings, floors), IP address pools, DNS/NTP/DHCP settings, device credentials, network profiles. This is 'describe the network you want to have.'</li><li><strong>Policy</strong> — security and segmentation rules. Virtual networks, group-based access control, SGTs via Cisco TrustSec, integration with ISE for user/device identity. This is 'who can talk to whom.'</li><li><strong>Provision</strong> — deploy devices (Plug and Play / ZTP), assign them to sites, push configurations, manage software images with <strong>SWIM (Software Image Management)</strong>, onboard access points. This is 'make the design real.'</li><li><strong>Assurance</strong> — AI/ML-driven monitoring. Health scores per device and per client, root-cause analysis for wireless issues, guided remediation. This is 'prove it's working and fix it when it's not.'</li></ul><p><strong>Order matters conceptually:</strong> you Design first (define the structure), then define Policy (what rules apply), then Provision (push it to devices), then continuously run Assurance (watch and heal). DPPA is the lifecycle of an SDN campus.</p><p><strong>Exam trap:</strong> if a question describes an activity, you should be able to map it to the correct pillar. 'Onboarding a new switch via PnP' = Provision. 'Creating an SGT for guest users' = Policy. 'AI-based wireless issue root-cause analysis' = Assurance. 'Defining the Chicago site hierarchy' = Design.</p>",
        visual: { type: "layer-stack", params: { layers: ["Design (structure)", "Policy (segmentation)", "Provision (deploy)", "Assurance (monitor + heal)"], highlight: 3 } },
        hack: {
          memory: "DPPA = Design, Policy, Provision, Assurance. Lifecycle: design first, define policy, provision devices, assure forever. If you see PnP or SWIM → Provision. SGT / TrustSec → Policy. Hierarchy / sites → Design. AI / health → Assurance.",
          practice: "Map 8 lab activities to DPPA without peeking: create site → Design; add guest SGT → Policy; push new IOS to 10 switches (SWIM) → Provision; AI root-cause for wireless drop → Assurance. Do this until it's reflexive.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: DPPA is a guaranteed recall question. Also know SWIM = Software Image Management (under Provision) and PnP = under Provision."
        }
      },
      {
        id: "6.2.d.3",
        term: "REST APIs",
        weight: "med",
        info: "<p>DNA / Catalyst Center exposes a full <strong>REST API</strong> northbound so other systems can drive it programmatically. The UI is just one consumer of the same API — everything clickable in the UI is also callable over HTTPS.</p><p><strong>API fundamentals to know:</strong></p><ul><li><strong>HTTPS transport</strong> — TLS-secured, TCP 443. No plain HTTP.</li><li><strong>JSON payloads</strong> — request and response bodies are JSON, not XML.</li><li><strong>Token-based auth</strong> — POST to <code>/api/system/v1/auth/token</code> with basic auth, receive a bearer token, include it in subsequent requests via the <code>X-Auth-Token</code> header.</li><li><strong>Standard HTTP verbs</strong> — GET for reads, POST for creates, PUT/PATCH for updates, DELETE for removals (CRUD mapping).</li><li><strong>Pagination and filtering</strong> — large collections use query parameters like <code>?limit=50&offset=100</code>.</li></ul><p><strong>What you use the API for:</strong></p><ul><li>ITSM integration — ServiceNow pulls inventory and health from DNA Center; pushes tickets based on assurance events.</li><li>Custom automation — Python scripts that orchestrate DNA Center + ISE + external systems.</li><li>Reporting — export inventory, compliance, and assurance data into BI tools.</li><li>Self-service portals — internal apps that let app owners request VLANs / ACL changes via REST calls instead of tickets.</li></ul><p><strong>Exam framing:</strong> CCNA won't have you write Python, but expects you to know DNA Center has a REST API, uses JSON, uses HTTPS, and is authenticated by bearer token. Same shape as Meraki, APIC, vManage APIs — once you understand one, the rest are familiar.</p>",
        visual: { type: "handshake", params: { leftLabel: "Client script", rightLabel: "DNA Center REST API", steps: ["POST /auth/token ->", "<- 200 + X-Auth-Token", "GET /network-device (header: X-Auth-Token) ->", "<- 200 + JSON device list", "POST /sda/fabric (JSON body) ->", "<- 202 Accepted"] } },
        hack: {
          memory: "DNA Center REST API = HTTPS + JSON + bearer token (X-Auth-Token) + CRUD verbs. Same shape as Meraki / APIC / vManage APIs. UI is just another API consumer — everything the UI does is callable.",
          practice: "In Postman (or curl), follow the DevNet DNA Center sandbox walkthrough: POST /auth/token → receive token → GET /dna/intent/api/v1/network-device with the token in X-Auth-Token. Seeing JSON devices come back makes the API concrete.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. Exam: 'DNA Center uses what style of API for external integration?' = REST (HTTPS + JSON). Authentication by bearer token. No Python needed — concept only."
        }
      }
    ]
  },

  // ── 6.3 Describe SDN architecture ─────────────────────────────

  "6.3.a": {
    info: "<p>SDN fundamentally <strong>separates the control plane from the data plane</strong> — this is THE foundational concept of Software-Defined Networking. Understanding this separation is critical for the CCNA exam.</p><p>The three planes in networking:</p><ul><li><strong>Control Plane</strong> — the decision-making intelligence. It determines HOW packets should be forwarded: running routing protocols (OSPF, EIGRP, BGP), computing shortest paths, building routing/forwarding tables, and processing ARP/STP/DHCP. In SDN, the control plane is <strong>extracted from individual devices and centralized</strong> in the SDN controller.</li><li><strong>Data Plane (Forwarding Plane)</strong> — the packet forwarding engine. It does the actual work of moving packets from input to output based on the tables built by the control plane. This includes L2 switching (MAC table lookup), L3 routing (FIB lookup), QoS queuing, encapsulation/decapsulation, and applying ACLs. In SDN, the data plane <strong>remains distributed</strong> on each device — switches still forward packets at wire speed.</li><li><strong>Management Plane</strong> — provides administrative access to devices: SSH, SNMP, syslog, HTTPS GUI. Often considered part of the control plane but worth noting separately.</li></ul><p>In <strong>traditional networking</strong>, all three planes exist on every device — each router makes its own routing decisions AND forwards packets AND provides management access. In <strong>SDN</strong>, the control plane is centralized on the controller, while the data plane stays distributed on the devices. The devices become simpler 'forwarding engines' that just follow the rules the controller provides.</p>",
    visual: { type: "layer-stack", params: { layers: ["Application Plane (business apps)", "Control Plane (SDN Controller)", "Data Plane (switches/routers — forwarding only)"], highlight: 1 } },
    hack: {
      memory: "Control plane = the GPS telling you where to turn (decisions). Data plane = your legs doing the walking (forwarding). Management plane = the settings app on the GPS. SDN = one centralized GPS for everyone.",
      practice: "List 3 functions per plane: Control = routing decisions, path computation, ACL logic. Data = packet forwarding, encapsulation, QoS queuing. Management = SSH, SNMP, syslog. Know which plane owns each function.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. THE foundational SDN concept. Exam question: 'What does SDN separate?' = control plane from data plane. Control = centralized. Data = distributed.",
    },
    micro: [
      {
        id: "6.3.a.1",
        term: "Control plane vs data plane",
        weight: "high",
        info: "<p>The <strong>control plane / data plane</strong> distinction is the single most important mental model in SDN. Every other concept in 6.2 and 6.3 is a variation on this theme. Learn it once, cold, and the rest follows.</p><p><strong>Control plane = decisions.</strong> This is where the network figures out <em>how</em> to forward traffic. Examples: OSPF / EIGRP / BGP choosing best paths, STP electing root bridges and blocking redundant links, ARP resolving IP to MAC, DHCP assigning IPs, MAC learning, CEF table construction. The output of the control plane is the <em>forwarding state</em> (routing table, FIB, MAC table, ACL programs).</p><p><strong>Data plane = execution.</strong> This is where individual packets are inspected and actually moved. Reads the destination IP / MAC, looks up the FIB / MAC table that the control plane built, rewrites headers, decrements TTL, applies QoS, and ships the frame out the correct egress. Runs at wire speed in hardware (ASICs, NPUs, FPGAs) on modern gear.</p><p><strong>Why the split matters:</strong></p><ul><li><strong>Speed vs flexibility</strong> — control plane is complex and changes infrequently (routing converges, policies update); data plane must be blazing fast and simple. Separating them lets each optimize independently.</li><li><strong>Centralization is possible</strong> — because decisions and forwarding are already decoupled architecturally, moving decisions to a controller (SDN) doesn't break anything.</li></ul><p><strong>Everyday Cisco devices already split these planes internally.</strong> CEF is literally the data plane's consumer of the control plane's routing table. SDN just takes the next step: move the control plane <em>off the box</em> entirely.</p>",
        visual: { type: "layer-stack", params: { layers: ["Control plane — decisions (OSPF, STP, ARP, policy)", "Data plane — execution (lookup, rewrite, forward)"], highlight: 0 } },
        hack: {
          memory: "Control plane = decisions (who to send where). Data plane = action (actually move the packet). Control runs on CPU; data runs on ASIC. SDN moves DECISIONS to a controller; FORWARDING stays on the device.",
          practice: "On a lab router: 'show ip route' (control plane output) vs 'show ip cef' (data plane's consumption of it). Watch a route change in routing table → CEF updates shortly after. That is the internal control → data plane handoff in action.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: core question. 'Which plane does OSPF live in?' = control. 'Which plane forwards packets?' = data. Memorize cold."
        }
      },
      {
        id: "6.3.a.2",
        term: "SDN separation",
        weight: "high",
        info: "<p>In <strong>SDN</strong>, the control plane is <strong>lifted off the individual devices and centralized on a controller</strong>. The data plane stays exactly where it always was — distributed on the forwarding hardware. This asymmetric move is what defines SDN as an architecture.</p><p><strong>Why centralize control?</strong></p><ul><li><strong>Global view</strong> — the controller sees the whole fabric; it can compute optimal paths that no single device could.</li><li><strong>Consistent policy</strong> — one place to define who can talk to whom; the controller renders it into device configs.</li><li><strong>Faster change</strong> — one API call replaces N SSH sessions.</li><li><strong>Programmability</strong> — applications can drive the network through APIs instead of scripts screen-scraping CLI.</li></ul><p><strong>Why keep data distributed?</strong></p><ul><li><strong>Performance</strong> — centralized forwarding doesn't scale; you cannot route a 100 Gbps flow through a controller.</li><li><strong>Latency</strong> — forwarding decisions on every packet must happen in nanoseconds; only local ASICs can deliver that.</li><li><strong>Survivability</strong> — cached rules in devices keep traffic flowing even during brief controller outages.</li></ul><p><strong>Exam trap:</strong> do NOT say 'SDN moves forwarding to the controller.' That's wrong. Forwarding stays local and fast. Control (the decisions about how to forward) is what moves. The more precisely you can state this, the more SDN questions you answer correctly.</p><p><strong>Variations:</strong> pure SDN (OpenFlow) removes almost all control-plane logic from the device. Cisco's SD-Access and ACI take a hybrid approach — devices still run IS-IS / BGP locally, but those protocols are driven and coordinated by the controller. Same net effect: humans manage the fabric from the controller, not device-by-device.</p>",
        visual: { type: "comparison", params: { left: { label: "Traditional", items: ["Control = local on device", "Data = local on device", "Autonomous decisions"] }, right: { label: "SDN", items: ["Control = centralized on controller", "Data = still local on device", "Devices obey the controller"] } } },
        hack: {
          memory: "SDN separation: MOVE control plane to the controller; KEEP data plane on the device. Forwarding stays local & fast. Control gets a global brain. Every SDN benefit flows from this asymmetric split.",
          practice: "Re-answer the contrast table from 6.2.a.3 emphasizing the data-plane-stays-distributed row. Quiz yourself: 'does SDN centralize forwarding?' Answer: NO. Only control. This is the most common miss on this topic.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'SDN separates ___ from ___.' = control plane from data plane. And the controller centralizes ONLY the control plane."
        }
      },
      {
        id: "6.3.a.3",
        term: "Management plane",
        weight: "med",
        info: "<p>The <strong>management plane</strong> is the third, often-forgotten plane in networking. It's where administrators <strong>interact with the device</strong> — not forwarding packets, not running protocols, just configuring and observing.</p><p><strong>Protocols that live in the management plane:</strong></p><ul><li><strong>SSH / Telnet</strong> — interactive CLI access (Telnet is clear text; SSH is the modern standard).</li><li><strong>HTTPS GUI</strong> — web UI on routers, switches, firewalls, WLCs.</li><li><strong>SNMP</strong> — monitoring and limited configuration (UDP 161 / 162).</li><li><strong>Syslog</strong> — log export (UDP 514 typically).</li><li><strong>NetFlow / IPFIX / sFlow</strong> — flow-based traffic export.</li><li><strong>NETCONF / RESTCONF / gNMI</strong> — modern programmatic config + telemetry.</li><li><strong>TACACS+ / RADIUS</strong> — authentication, authorization, accounting (AAA).</li></ul><p><strong>Why it matters:</strong> the management plane is often the <strong>attack surface</strong> of a network device. A compromised SSH session, an SNMP community string on the internet, or a misconfigured syslog target can lead to reconnaissance, configuration theft, or outright device takeover. Security best practice is to lock the management plane to a <strong>dedicated out-of-band (OOB) management network</strong> reachable only by trusted admin workstations.</p><p><strong>In SDN:</strong> the controller owns the management plane for the fabric. Admins log into the controller (UI or API) instead of individual devices. Devices still have an emergency CLI for break-glass, but day-to-day operations happen on the controller. This is a security win — a smaller attack surface, centralized logging, centralized RBAC.</p><p><strong>Exam note:</strong> some Cisco materials roll the management plane into the control plane (treating it as two planes rather than three). Either framing is acceptable; what matters is knowing SSH / SNMP / syslog are admin-access protocols and do not forward customer traffic.</p>",
        visual: { type: "shield", params: { items: ["SSH / Telnet", "SNMP", "Syslog", "NETCONF / gNMI", "HTTPS GUI", "TACACS+ / RADIUS"], color: "#6366f1" } },
        hack: {
          memory: "Management plane = admin-only plane. SSH / HTTPS / SNMP / syslog / NETCONF / AAA live here. Not forwarding, not deciding — just configuring and observing. Lock it to an OOB network — it's the attack surface.",
          practice: "On a lab switch, 'transport input ssh' restricts the management plane to SSH only. 'access-class' ACLs limit which source IPs can reach it. Harden one device; recognize you just protected the management plane.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'which plane does SSH live in?' = management. Some sources fold it into control plane — either is acceptable. Know SSH/SNMP/syslog examples cold."
        }
      }
    ]
  },

  "6.3.b": {
    info: "<p><strong>Northbound and Southbound APIs</strong> are the two communication interfaces of an SDN controller. Understanding their direction and purpose is a guaranteed exam question.</p><p><strong>Northbound APIs (NBIs)</strong> — connect the controller to applications and management tools <strong>above</strong> it:</p><ul><li>Allow software applications to <strong>program the network</strong> through the controller</li><li>Typically implemented as <strong>REST APIs</strong> using HTTP verbs (GET, POST, PUT, DELETE) with JSON or XML data</li><li>Examples: a security application requesting the controller to block a compromised IP, a monitoring app pulling network topology data, or a provisioning portal requesting a new VLAN</li><li>Think: North = UP = toward applications</li></ul><p><strong>Southbound APIs (SBIs)</strong> — connect the controller to network devices <strong>below</strong> it:</p><ul><li>Allow the controller to <strong>push configurations and pull state information</strong> from/to network devices</li><li>Common southbound protocols:<ul><li><strong>OpenFlow</strong> — programs the forwarding table directly (pure SDN approach)</li><li><strong>NETCONF</strong> — configures devices using YANG data models over SSH (TCP 830)</li><li><strong>RESTCONF</strong> — REST-based alternative to NETCONF, uses HTTP/HTTPS</li><li><strong>SNMP</strong> — legacy monitoring and limited configuration</li><li><strong>CLI (SSH)</strong> — controller SSHes into devices and pushes commands (used by Ansible, DNA Center for IOS devices)</li></ul></li><li>Think: South = DOWN = toward infrastructure devices</li></ul>",
    visual: { type: "layer-stack", params: { layers: ["Applications (Northbound APIs — REST)", "SDN Controller", "Network Devices (Southbound APIs — OpenFlow, NETCONF)"], highlight: 1 } },
    hack: {
      memory: "Northbound = UP to APPS (North on a map = up). Southbound = DOWN to DEVICES (South = down). NBIs = REST APIs (JSON). SBIs = OpenFlow, NETCONF, RESTCONF. Draw the sandwich: Apps | Controller | Devices.",
      practice: "Draw the three-layer SDN diagram from memory. Top: Application Layer (business apps). Middle: Controller (SDN brain). Bottom: Infrastructure (switches/routers). Label NBI (REST, up arrow) and SBI (OpenFlow/NETCONF, down arrow).",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Northbound vs Southbound is a guaranteed exam question. North = apps (REST APIs). South = devices (OpenFlow, NETCONF, RESTCONF). Draw the diagram once.",
    },
    micro: [
      {
        id: "6.3.b.1",
        term: "Northbound APIs",
        weight: "high",
        info: "<p><strong>Northbound APIs (NBIs)</strong> are how applications talk <em>up to</em> the SDN controller. The controller is in the middle; apps are 'above it' in the architecture diagram, hence north. NBIs are the interface that makes the network programmable from business applications, dashboards, and orchestration tools.</p><p><strong>Characteristics:</strong></p><ul><li>Almost always <strong>REST over HTTPS</strong> with <strong>JSON</strong> payloads.</li><li>Authenticated with <strong>bearer tokens</strong> (OAuth, X-Auth-Token, API keys).</li><li>Stateless, resource-oriented, CRUD-mapped (GET/POST/PUT/DELETE).</li><li>Return HTTP status codes (200, 201, 400, 401, 404, 500) that align with REST conventions.</li></ul><p><strong>Who consumes NBIs:</strong></p><ul><li><strong>ITSM tools</strong> — ServiceNow, Jira Service Desk — sync inventory, update tickets, trigger changes.</li><li><strong>Automation platforms</strong> — Ansible, Python scripts, Terraform providers.</li><li><strong>Analytics / SIEM</strong> — Splunk, ELK, Grafana pull flow and health data.</li><li><strong>Security tools</strong> — Cisco ISE, Stealthwatch push context into the controller.</li><li><strong>Custom portals</strong> — internal self-service apps that request VLANs or ACL changes.</li></ul><p><strong>Exam trap:</strong> north = UP = toward APPLICATIONS. Don't confuse with southbound (down to devices). When a question mentions a script / portal / ITSM system talking to the controller, that's the northbound side.</p>",
        visual: { type: "handshake", params: { leftLabel: "Application", rightLabel: "SDN Controller (northbound)", steps: ["POST /auth/token ->", "<- 200 + token", "GET /api/v1/devices ->", "<- 200 + JSON list", "POST /api/v1/policy (JSON) ->", "<- 201 Created"] } },
        hack: {
          memory: "Northbound = UP to apps. REST + JSON + HTTPS + bearer token. Consumers: ServiceNow, Ansible, Python scripts, security tools. If a script is hitting the controller, that traffic is northbound.",
          practice: "Draw the SDN three-layer stack and label the arrows: app → controller = NORTHBOUND, controller → device = SOUTHBOUND. Practice every day until the directions are reflex.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17-18. Exam: 'which API is northbound?' = REST. NETCONF / OpenFlow / RESTCONF are southbound on CCNA framing (even though RESTCONF is technically also REST-style)."
        }
      },
      {
        id: "6.3.b.2",
        term: "Southbound APIs",
        weight: "high",
        info: "<p><strong>Southbound APIs (SBIs)</strong> are how the SDN controller talks <em>down to</em> the network devices it manages. The controller pushes config, programs flow tables, and pulls telemetry. On the architecture diagram, devices sit below the controller — hence south.</p><p><strong>The southbound protocol zoo (must recognize):</strong></p><ul><li><strong>OpenFlow</strong> — programs flow tables on switches directly. Classic 'pure SDN' protocol. Common in academic / service provider contexts; less common in Cisco enterprise.</li><li><strong>NETCONF</strong> — XML over SSH, TCP 830. Candidate / running / startup datastores. Commit-confirm rollback.</li><li><strong>RESTCONF</strong> — REST/HTTP version of NETCONF. Same YANG data models, HTTP verbs, JSON payloads.</li><li><strong>gNMI</strong> — gRPC Network Management Interface. Streaming telemetry + config. The modern choice.</li><li><strong>SNMP</strong> — legacy, heavily used for monitoring. Limited config.</li><li><strong>CLI over SSH</strong> — fallback when a device has no API. Ansible and older controllers use this.</li></ul><p><strong>YANG is the schema:</strong> NETCONF / RESTCONF / gNMI all use <strong>YANG</strong> data models to describe what can be configured and read on a device. YANG is to network config what JSON Schema is to REST bodies — a contract that makes programmatic access reliable.</p><p><strong>Direction of travel:</strong> north = from apps down <em>to</em> controller; south = from controller down <em>to</em> devices. That's why the controller has BOTH northbound and southbound APIs — it's the middle layer talking to both sides.</p>",
        visual: { type: "layer-stack", params: { layers: ["Controller (south egress)", "SBI: OpenFlow / NETCONF / RESTCONF / gNMI / SNMP", "Network devices"], highlight: 1 } },
        hack: {
          memory: "Southbound = DOWN to devices. Four exam-level protocols: OpenFlow (pure SDN), NETCONF (XML/SSH/830), RESTCONF (HTTP/JSON), gNMI (gRPC streaming). All three modern ones share YANG as schema. SNMP is legacy southbound.",
          practice: "Pick one device and try NETCONF over ncclient (Python). Port 830, XML capability exchange, then 'get-config' of the running datastore. Seeing real YANG-shaped XML come back burns the concept in permanently.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60-62. Wendell Odom OCG Chapter 17. Exam: expect direct recall — 'identify a southbound protocol', 'NETCONF port', 'YANG is used by which protocols'."
        }
      },
      {
        id: "6.3.b.3",
        term: "North vs South memory trick",
        weight: "high",
        info: "<p>The north/south naming trips up almost everyone on first contact. Anchor it with one mental image and never worry about it again.</p><p><strong>Picture the SDN architecture as a map, north up:</strong></p><ul><li><strong>North</strong> (top of the map) = Applications. Business logic, dashboards, orchestration.</li><li><strong>Middle</strong> = SDN Controller. The bridge.</li><li><strong>South</strong> (bottom of the map) = Infrastructure devices. Switches, routers, APs.</li></ul><p>Now the APIs are obvious:</p><ul><li><strong>Northbound APIs</strong> = controller ↔ applications. Up. REST + JSON.</li><li><strong>Southbound APIs</strong> = controller ↔ devices. Down. OpenFlow / NETCONF / RESTCONF / gNMI.</li></ul><p><strong>East / West (bonus):</strong> you may also see 'east/west APIs' referenced in vendor literature — these are controller-to-controller communications when multiple SDN controllers cluster or federate. CCNA doesn't test east/west, but recognizing the term prevents confusion.</p><p><strong>One-sentence rule:</strong> <em>Apps are up (North, REST); devices are down (South, NETCONF/OpenFlow); controller sits in the middle and speaks both directions.</em> Repeat until reflex.</p><p><strong>Exam trap to avoid:</strong> don't let 'RESTCONF' fool you. It's REST-style, but on CCNA framing it's considered a southbound protocol because it's used controller-to-device. Plain 'REST APIs' (no CONF suffix) typically refers to northbound APIs from apps to the controller.</p>",
        visual: { type: "layer-stack", params: { layers: ["North — Applications (REST)", "Controller", "South — Devices (NETCONF / OpenFlow)"], highlight: 1 } },
        hack: {
          memory: "Map rule: North = up = apps = REST. South = down = devices = NETCONF/OpenFlow/gNMI. East/West = controller cluster peers. Controller sits in the middle and speaks both. Apps up, devices down — never flip.",
          practice: "Draw this stack on scratch paper at the start of every SDN study block. Label north arrow + south arrow + protocol families. Muscle memory solves this entire subtopic.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: direction confusion is the #1 SDN mistake. Memorize the map image."
        }
      }
    ]
  },

  "6.3.c": {
    info: "<p>The <strong>underlay</strong> is the <strong>physical network infrastructure</strong> — the actual routers, switches, cables, and physical connections that provide IP reachability between all network devices. The underlay is the foundation upon which everything else is built.</p><p>Underlay characteristics:</p><ul><li>Consists of physical devices (routers, switches), physical links (fiber, copper), and physical protocols (IS-IS, OSPF for routing)</li><li>Provides basic <strong>IP connectivity</strong> between all points in the network — any device can reach any other device at L3</li><li>Must be <strong>properly designed and fully operational</strong> before any overlay can function — if the underlay has connectivity issues, overlays built on top will also fail</li><li>In Cisco SD-Access deployments, the underlay typically uses <strong>IS-IS</strong> as the routing protocol (chosen for its fast convergence and independence from IP addressing)</li></ul><p>Analogy: the underlay is like the <strong>physical road system</strong> — asphalt, bridges, intersections. It provides the raw transportation infrastructure. The overlay is like the <strong>lane markings, HOV rules, and toll systems</strong> painted on top of the roads — they create virtual traffic management without changing the physical roads.</p><p>The underlay doesn't need to know anything about the overlay networks running on top of it. It just provides IP reachability. This <strong>separation of concerns</strong> is a key design principle — the physical network is simple and stable, while complexity lives in the overlay.</p>",
    visual: { type: "layer-stack", params: { layers: ["Overlay (virtual networks)", "Underlay (physical network)"], highlight: 1 } },
    hack: {
      memory: "Underlay = the ROAD (physical infrastructure — switches, cables, IP routing). Overlay = the LANES painted on the road (virtual networks). You need the road (underlay) before you can paint lanes (overlay).",
      practice: "Think of your home network: physical switches and cables = underlay. VLANs running on top = simple overlay. VXLAN in a data center = advanced overlay. The concept scales from small to massive networks.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Underlay = physical. Overlay = virtual. Fabric = both together. Three definitions to memorize for the exam.",
    },
    micro: [
      {
        id: "6.3.c.1",
        term: "Underlay network",
        weight: "high",
        info: "<p>The <strong>underlay</strong> is the <strong>physical IP-routed network</strong> that provides basic any-to-any reachability between all nodes in an SDN fabric. It's the concrete road system. Every overlay feature sits on top of the underlay and fails if the underlay fails.</p><p><strong>Properties of a good underlay:</strong></p><ul><li><strong>Pure Layer 3</strong> — every link between fabric nodes is routed, not switched. No big flat VLANs to span.</li><li><strong>Fast convergence</strong> — when a link or device fails, the underlay's IGP re-converges in sub-second time.</li><li><strong>Loop-free</strong> — routing metrics + proper design prevent the underlay itself from looping.</li><li><strong>ECMP capable</strong> — multiple equal-cost paths between nodes for bandwidth and resilience.</li><li><strong>Simple</strong> — boring /31 links, IS-IS or OSPF, no policy. Complexity lives in the overlay.</li></ul><p><strong>Common underlay IGPs:</strong></p><ul><li><strong>IS-IS</strong> — Cisco's default in SD-Access. Fast convergence, IP-addressing independent, widely used by service providers.</li><li><strong>OSPF</strong> — acceptable alternative. More familiar to enterprise operators.</li><li><strong>eBGP as underlay</strong> — popular in modern data centers (EVPN-VXLAN spine-leaf) for its scale and explicit path control.</li></ul><p><strong>Design principle:</strong> keep the underlay dumb. No ACLs, no QoS policy, no segmentation. The underlay's only job is 'any leaf can reach any other leaf at L3 with sub-second convergence.' All richness (users, policy, segmentation) happens in the overlay. Separating concerns this way makes both layers simpler and more reliable.</p><p><strong>Underlay = the foundation</strong>. Overlay = the city built on the foundation. Break the foundation and the city falls.</p>",
        visual: { type: "layer-stack", params: { layers: ["Overlay (policy, users, VXLAN)", "Underlay (IS-IS or OSPF IP reachability)"], highlight: 1 } },
        hack: {
          memory: "Underlay = physical L3 network. Its job: any node reaches any node at IP. Keep it dumb, fast, loop-free, ECMP-capable. Cisco SD-Access default = IS-IS. Data center fabrics = often eBGP as underlay.",
          practice: "On a leaf-spine topology in CML/Packet Tracer, configure IS-IS on /31 links between every leaf and spine. Verify full mesh L3 reachability with ping. Congrats — you just built an underlay. Now an overlay could run on top.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'what's the role of the underlay?' = provide IP reachability between fabric nodes. Know IS-IS is the Cisco SD-Access default."
        }
      },
      {
        id: "6.3.c.2",
        term: "Overlay network",
        weight: "high",
        info: "<p>The <strong>overlay</strong> is a <strong>virtual network built on top of the underlay using encapsulation</strong>. Overlay traffic rides inside outer IP packets of the underlay — tunnels, in effect. This lets the overlay have its own addressing, segmentation, and policy while the underlay only knows 'forward this outer IP'.</p><p><strong>Why overlays exist:</strong></p><ul><li><strong>Segmentation at scale</strong> — 16 million VXLAN VNIs beats 4,094 VLANs.</li><li><strong>Stretched L2 over L3</strong> — two hosts appear on the same L2 segment even if they're in different buildings or data centers.</li><li><strong>Policy independence</strong> — the overlay can move or change without re-addressing the underlay.</li><li><strong>Multi-tenancy</strong> — thousands of isolated virtual networks on one physical fabric.</li></ul><p><strong>Common overlay encapsulations:</strong></p><ul><li><strong>VXLAN</strong> — dominant, L2 frames in UDP 4789. The one to memorize.</li><li><strong>GRE</strong> — generic IP-in-IP tunneling. Older.</li><li><strong>LISP</strong> — separates endpoint ID from location; pairs with VXLAN in SD-Access.</li><li><strong>IPsec</strong> — encrypted overlay commonly used in SD-WAN.</li><li><strong>MPLS</strong> — service provider overlay technology (older but still huge).</li></ul><p><strong>The mental model:</strong> an overlay packet has an OUTER header (underlay routing) and an INNER header (overlay payload). Devices along the underlay path only read the outer header. Only the overlay endpoints (VTEPs, in VXLAN) understand and decapsulate the inner payload. The underlay is blissfully unaware of everything happening inside the tunnel.</p>",
        visual: { type: "encapsulation", params: { layers: [{ label: "Outer IP (underlay routing)", color: "#6366f1" }, { label: "VXLAN header (VNI)", color: "#f59e0b" }, { label: "Inner frame (overlay payload)", color: "#3b82f6" }] } },
        hack: {
          memory: "Overlay = virtual network riding on top of the physical network using encapsulation. Outer header = underlay routing; inner = overlay payload. Underlay nodes only read the outer. VXLAN is the dominant overlay on the CCNA.",
          practice: "Trace a VXLAN packet on paper: Outer Ethernet → Outer IP → Outer UDP (4789) → VXLAN header (VNI) → Inner Ethernet → Inner IP → payload. Being able to draw this proves you own the overlay concept.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'overlay network is?' = virtual network using encapsulation over the underlay. VXLAN is the canonical example."
        }
      },
      {
        id: "6.3.c.3",
        term: "Fabric",
        weight: "high",
        info: "<p><strong>Fabric</strong> = underlay + overlay + control-plane glue, all managed as one integrated system by a controller. The word 'fabric' is intentional — like cloth, it is woven together out of many devices but behaves as a single entity.</p><p><strong>What separates a 'fabric' from a 'network':</strong></p><ul><li><strong>Controller-managed</strong> — one brain provisions and monitors all nodes.</li><li><strong>Automated underlay + overlay</strong> — engineers don't hand-roll IS-IS or VXLAN; the controller does.</li><li><strong>Policy abstraction</strong> — security, segmentation, and QoS expressed as fabric-wide intent, not per-device ACLs.</li><li><strong>Unified assurance</strong> — health, telemetry, and troubleshooting all surface through the controller UI.</li></ul><p><strong>The Cisco fabric family:</strong></p><ul><li><strong>SD-Access (SDA)</strong> — campus fabric. DNA/Catalyst Center + IS-IS underlay + VXLAN overlay + LISP control plane + CTS/SGT policy.</li><li><strong>ACI</strong> — data center fabric. APIC + VXLAN + contracts between EPGs.</li><li><strong>SD-WAN</strong> — WAN fabric. vManage / vSmart / vBond + IPsec overlay + OMP control plane.</li></ul><p><strong>Mental summary:</strong> network = a pile of devices; fabric = a single orchestrated system. Underlay is the substrate, overlay is the logic, controller is the conductor. You've turned discrete routers and switches into one programmable entity.</p><p><strong>Exam tip:</strong> the CCNA uses 'fabric' and 'SDN network' interchangeably in most questions. When you see 'fabric', think 'controller-managed underlay+overlay'.</p>",
        visual: { type: "hierarchy", params: { root: "Fabric (controller-managed)", children: [{ name: "Underlay (IS-IS)" }, { name: "Overlay (VXLAN)" }, { name: "Control (LISP / eBGP / OMP)" }, { name: "Policy (CTS/SGT or contracts)" }] } },
        hack: {
          memory: "Fabric = underlay + overlay + control + policy, woven into one managed whole by a controller. 'Network' = discrete devices; 'fabric' = a single orchestrated system. Three Cisco flavors: SD-Access (campus), ACI (DC), SD-WAN (WAN).",
          practice: "Label each Cisco fabric with its underlay, overlay, control plane, and policy engine. SD-Access = IS-IS / VXLAN / LISP / CTS. ACI = iBGP-EVPN underlay / VXLAN / COOP / EPG contracts. SD-WAN = IPsec / OMP / vSmart / policies.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: fabric = underlay + overlay combined, managed by a controller. Know the three Cisco fabric products and their domains."
        }
      }
    ]
  },

  "6.3.d": {
    info: "<p>The <strong>overlay</strong> is a <strong>virtual network</strong> built on top of the physical underlay using <strong>encapsulation protocols</strong>. Overlay networks create logical topologies that are completely independent of the physical topology — devices can appear to be on the same Layer 2 segment even if they're physically in different buildings, cities, or data centers.</p><p><strong>VXLAN (Virtual Extensible LAN)</strong> is the dominant overlay technology:</p><ul><li>Encapsulates original Ethernet frames inside <strong>UDP packets</strong> for transport across the L3 underlay</li><li>Uses a <strong>24-bit VXLAN Network Identifier (VNI)</strong> supporting up to <strong>16,777,216 virtual networks</strong> — compared to VLAN's 12-bit ID limit of only 4,096</li><li>Provides <strong>Layer 2 connectivity over a Layer 3 network</strong> (L2 over L3) — meaning two hosts on the same virtual network can communicate at L2 even if they're separated by routers</li><li>VXLAN endpoints are called <strong>VTEPs (VXLAN Tunnel Endpoints)</strong> — typically switches that encapsulate/decapsulate frames</li></ul><p>VXLAN packet structure: <strong>Outer Ethernet → Outer IP → Outer UDP (port 4789) → VXLAN Header (VNI) → Original Ethernet Frame</strong></p><p>Why overlays matter: traditional VLANs are limited to 4,096 IDs and require L2 adjacency (same broadcast domain). In modern data centers and multi-site deployments, you need far more network segments and the ability to stretch them across L3 boundaries. VXLAN solves both problems — 16 million segments that work across any IP-routable network.</p>",
    visual: { type: "encapsulation", params: { layers: [{ label: "Outer IP/UDP Header (Underlay)", color: "#6366f1" }, { label: "VXLAN Header (VNI)", color: "#f59e0b" }, { label: "Original Frame (Overlay)", color: "#3b82f6" }] } },
    hack: {
      memory: "VXLAN = VLAN eXtended. 24-bit VNI = 16 million virtual networks (vs VLAN's 4,096 with 12-bit ID). L2 over L3 encapsulation. UDP port 4789. VTEPs = tunnel endpoints.",
      practice: "Compare: VLAN (12-bit ID → 4,096 max, L2 only, same broadcast domain required) vs VXLAN (24-bit VNI → 16M max, L2 over L3, works across routers). Know why data centers need VXLAN's scale.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. VXLAN is THE overlay technology for the exam. Know: 24-bit VNI, 16 million segments, L2 over L3, UDP 4789. Compare to VLAN's 4,096.",
    },
    micro: [
      {
        id: "6.3.d.1",
        term: "VXLAN",
        weight: "high",
        info: "<p><strong>VXLAN (Virtual Extensible LAN)</strong> is the dominant overlay encapsulation in modern data centers and SDN fabrics. It <strong>tunnels Ethernet frames inside UDP packets</strong> so that L2 segments can stretch across any IP-routed network. Think of it as 'VLANs that scale to 16 million and work across routers'.</p><p><strong>Full packet structure (outer to inner):</strong></p><ol><li><strong>Outer Ethernet</strong> — hop-by-hop MAC addresses on the underlay.</li><li><strong>Outer IP</strong> — source VTEP IP, destination VTEP IP.</li><li><strong>Outer UDP</strong> — destination port <strong>4789</strong>, source port often derived from inner flow hash for ECMP.</li><li><strong>VXLAN header</strong> — 8 bytes total, carries the <strong>24-bit VNI</strong> (network identifier).</li><li><strong>Inner Ethernet frame</strong> — the original L2 frame being tunneled, with its own MAC addresses and payload.</li></ol><p><strong>Key roles:</strong></p><ul><li><strong>VTEP (VXLAN Tunnel Endpoint)</strong> — the device (usually a leaf switch) that encapsulates frames entering the overlay and decapsulates them on exit. Hosts connect to VTEPs without knowing VXLAN exists.</li><li><strong>VNI (VXLAN Network Identifier)</strong> — 24-bit segment ID. Each virtual network = one VNI.</li><li><strong>Underlay</strong> — the L3 network between VTEPs that carries the outer IP/UDP traffic.</li></ul><p><strong>VXLAN control planes (how VTEPs learn who's where):</strong></p><ul><li><strong>Multicast (flood-and-learn)</strong> — older, simpler, doesn't scale well.</li><li><strong>BGP EVPN</strong> — the modern standard. Distributes MAC/IP-to-VTEP mappings as BGP routes. Used in ACI and most enterprise fabrics.</li><li><strong>LISP</strong> — used in Cisco SD-Access to map endpoint IDs to VTEP locations.</li></ul><p><strong>Why it matters:</strong> in a multi-tenant data center, VXLAN lets you have thousands of isolated virtual networks on the same physical spine-leaf fabric. Tenants can reuse IP ranges; VMs can move between hosts without changing their IP; two data centers can appear as one broadcast domain over a WAN.</p>",
        visual: { type: "encapsulation", params: { layers: [{ label: "Outer Ethernet", color: "#475569" }, { label: "Outer IP (VTEP src/dst)", color: "#6366f1" }, { label: "Outer UDP 4789", color: "#8b5cf6" }, { label: "VXLAN header (24-bit VNI)", color: "#f59e0b" }, { label: "Inner Ethernet + payload", color: "#3b82f6" }] } },
        hack: {
          memory: "VXLAN = Ethernet-in-UDP tunneling for overlays. Outer Eth → Outer IP → UDP 4789 → VXLAN header (VNI) → Inner Eth. VTEPs encap/decap at the edges. BGP EVPN is the modern control plane; LISP is the SD-Access control plane.",
          practice: "Draw the VXLAN packet from scratch, labeling each header and its size. If you can reproduce the 5-layer stack with UDP 4789 and a 24-bit VNI, you've earned this topic.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: guaranteed VXLAN header question. Know UDP 4789 + 24-bit VNI + VTEP role + 'Ethernet over IP' cold."
        }
      },
      {
        id: "6.3.d.2",
        term: "VNI (VXLAN Network ID)",
        weight: "high",
        info: "<p>The <strong>VXLAN Network Identifier (VNI)</strong> is the 24-bit segment number carried inside the VXLAN header. It plays the same role for VXLAN that the VLAN ID plays for 802.1Q — but at massive scale.</p><p><strong>The scale argument:</strong></p><ul><li><strong>VLAN</strong> (802.1Q) — 12-bit ID → <strong>4,094 usable segments</strong> (0 and 4095 reserved). Fine for a campus, insufficient for a multi-tenant data center.</li><li><strong>VXLAN</strong> — 24-bit VNI → <strong>16,777,214 usable segments</strong>. Enough to give every tenant in a cloud provider their own virtual networks.</li></ul><p><strong>Encapsulation and decapsulation:</strong> when a host sends a frame into the fabric, the ingress VTEP looks up which VNI that host belongs to (mapped from its VLAN or SGT), stamps the VNI into the VXLAN header, and encapsulates the frame. The egress VTEP strips the outer headers, reads the VNI, and delivers the frame into the correct virtual network on the far side. The VNI is how VTEPs keep tenants isolated.</p><p><strong>VLAN-to-VNI mapping:</strong> on Cisco fabrics you typically map a local VLAN to a VNI. VLAN 10 on leaf 1 and VLAN 10 on leaf 2 might both map to VNI 10010 — hosts in those VLANs appear on the same L2 segment even though they're on different physical switches. The VLAN is the local access identifier; the VNI is the fabric-wide identifier.</p><p><strong>Exam shortcut:</strong> 24 bits → 16M. 12 bits → 4K. Factor of ~4,000x more virtual networks. That's the single highest-yield VNI fact.</p>",
        visual: { type: "comparison", params: { left: { label: "VLAN (802.1Q)", items: ["12-bit ID", "4,094 usable", "L2 only", "Same broadcast domain required"] }, right: { label: "VXLAN VNI", items: ["24-bit ID", "16,777,214 usable", "L2 over L3", "Works across routed networks"] } } },
        hack: {
          memory: "VNI = 24-bit = 16,777,214 segments. VLAN = 12-bit = 4,094 segments. VLAN is local per switch; VNI is fabric-wide. Often VLANs are MAPPED to VNIs at the VTEP to bridge local access into the overlay.",
          practice: "Math: 2^24 = 16,777,216. Subtract 2 reserved values = 16,777,214. 2^12 = 4,096. Subtract 2 = 4,094. If a question asks 'how many VXLAN segments?', the answer is always '~16 million' or '2^24'.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'VNI is how many bits?' = 24. 'How many segments?' = 16M. Compare to VLAN's 12 bits / 4,094."
        }
      },
      {
        id: "6.3.d.3",
        term: "VXLAN UDP 4789",
        weight: "high",
        info: "<p>VXLAN rides over <strong>UDP destination port 4789</strong> — the one number every CCNA candidate must have ready for recall. Knowing this port, and why UDP, is classic exam fodder.</p><p><strong>Why UDP, not TCP:</strong></p><ul><li><strong>No per-flow state</strong> — VTEPs may handle millions of tunneled flows; TCP would require a socket per flow. UDP is stateless, lightweight, and scales.</li><li><strong>Speed</strong> — no handshake, no retransmission overhead. The inner protocol (TCP / IP / etc.) already handles reliability end to end; VXLAN doesn't duplicate that work.</li><li><strong>ECMP friendliness</strong> — VTEPs set the outer UDP source port to a hash of the inner flow, so equal-cost paths in the underlay get flow-level load balancing from standard 5-tuple ECMP.</li></ul><p><strong>Why destination port 4789:</strong> IANA assigned UDP 4789 to VXLAN. Earlier implementations used 8472 (the Linux default before the RFC). Cisco and modern vendors use 4789 per the RFC. Always 4789 for the exam.</p><p><strong>Implications for firewalls and path MTU:</strong></p><ul><li>Firewalls between VTEPs must permit UDP/4789 both directions. This is a common lab / production bug.</li><li>VXLAN adds 50 bytes of overhead (outer Ethernet 14 + outer IP 20 + UDP 8 + VXLAN 8). Path MTU between VTEPs must be at least 1550 (ideally jumbo 9000+) to avoid fragmentation of tunneled full-size Ethernet frames.</li></ul><p><strong>Remember the triple:</strong> VXLAN = UDP 4789 = 24-bit VNI. Almost every CCNA question about VXLAN can be answered from this triple plus the encap layer stack.</p>",
        visual: { type: "packet-flow", params: { nodes: ["Src VTEP", "UDP dport 4789", "Underlay IP transport", "Dst VTEP", "Decap to inner frame"], color: "#8b5cf6" } },
        hack: {
          memory: "VXLAN = UDP 4789. UDP because stateless + ECMP hashing. 4789 per IANA (older Linux used 8472 — ignore it for exam). Allow UDP 4789 on firewalls between VTEPs; raise MTU to 1550+ to avoid fragmentation of tunneled frames.",
          practice: "Memorize the triple: VXLAN = UDP 4789 = 24-bit VNI. Recite it until automatic. Then layer on: 50-byte overhead, BGP EVPN control plane, VTEP endpoints.",
          effort: "low",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'VXLAN uses which protocol and port?' = UDP 4789. This is a direct-recall question; do not overthink it."
        }
      }
    ]
  },

  "6.3.e": {
    info: "<p>A <strong>network fabric</strong> is the combination of the underlay and overlay working together as an <strong>integrated, programmable system</strong>. The term 'fabric' implies that the network operates as a single unified entity rather than a collection of individual devices — like threads woven together into cloth.</p><p>Cisco <strong>SD-Access (Software-Defined Access)</strong> is the specific fabric architecture tested on the CCNA. It consists of four integrated components:</p><ul><li><strong>IS-IS (underlay routing)</strong> — provides IP reachability between all fabric nodes. IS-IS was chosen for its fast convergence, loop-free topology, and independence from IP addressing.</li><li><strong>VXLAN (overlay encapsulation)</strong> — creates virtual networks (VNs) on top of the physical infrastructure, enabling L2 over L3 communication and 16M+ network segments</li><li><strong>LISP (Locator/ID Separation Protocol)</strong> — separates device identity (endpoint ID) from location (network locator), enabling dynamic endpoint discovery and mobility. When a device moves, LISP updates the mapping without changing routing.</li><li><strong>CTS/SGT (Cisco TrustSec / Scalable Group Tags)</strong> — provides group-based micro-segmentation. Instead of ACLs based on IP addresses, traffic is tagged with SGTs (e.g., 'Employee,' 'Guest,' 'IoT') and policies are defined between groups.</li></ul><p>The fabric is managed by <strong>Cisco DNA Center</strong>, which automates provisioning, policy definition, and monitoring. Administrators define intent ('guest devices cannot reach employee servers'), and DNA Center translates it into fabric configurations across IS-IS, VXLAN, LISP, and CTS.</p>",
    visual: { type: "layer-stack", params: { layers: ["Policy & Segmentation (CTS/SGT)", "Virtual Networks (VXLAN Overlay)", "Endpoint Mapping (LISP)", "Physical Network (IS-IS Underlay)"], highlight: 1 } },
    hack: {
      memory: "Fabric = Underlay + Overlay = the WHOLE system. Like fabric in clothing — threads (underlay) woven into unified cloth (fabric). SD-Access = IS-IS (routing) + VXLAN (overlay) + LISP (mapping) + CTS/SGT (segmentation).",
      practice: "Name the four SD-Access components and their roles: IS-IS (underlay routing), VXLAN (overlay encapsulation), LISP (endpoint discovery/mobility), CTS/SGT (group-based segmentation). Know each role.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Fabric = underlay + overlay combined. SD-Access = IS-IS + VXLAN + LISP + CTS. You won't configure it but must describe the components and their roles.",
    },
    micro: [
      {
        id: "6.3.e.1",
        term: "SD-Access fabric",
        weight: "high",
        info: "<p><strong>Cisco SD-Access (SDA)</strong> is Cisco's <strong>software-defined campus fabric</strong>. It combines a simple L3 underlay with a VXLAN overlay and group-based policy, all orchestrated by DNA / Catalyst Center. This is the fabric the CCNA tests most heavily.</p><p><strong>The four technology pillars of SD-Access:</strong></p><ul><li><strong>Underlay: IS-IS</strong> — every link between fabric nodes is L3-routed; IS-IS provides fast, loop-free reachability. Automatically provisioned by DNA Center via LAN Automation.</li><li><strong>Overlay: VXLAN</strong> — L2 frames encapsulated over UDP 4789 across the underlay. Users appear on the same logical segment regardless of physical location.</li><li><strong>Control plane: LISP</strong> — Locator / ID Separation Protocol. Maps endpoint IDs (EIDs) to VTEP locations (RLOCs) via a central Control Plane Node.</li><li><strong>Policy: Cisco TrustSec + Scalable Group Tags (SGTs)</strong> — users/devices tagged by identity (from ISE); policy enforced between tags, not IPs. Enables true microsegmentation.</li></ul><p><strong>Node roles in SD-Access:</strong></p><ul><li><strong>Edge nodes</strong> — fabric-attached switches where endpoints plug in. Act as VTEPs.</li><li><strong>Border nodes</strong> — connect the fabric to the outside world (internet, data center, legacy network).</li><li><strong>Control plane nodes</strong> — host the LISP mapping database.</li><li><strong>Intermediate nodes</strong> — underlay-only transit, no fabric role beyond forwarding.</li></ul><p><strong>What you give up / what you gain:</strong> operators lose per-device CLI control (changes flow through DNA Center) but gain a unified fabric, automatic segmentation, consistent policy, and AI-driven assurance. The complexity moves from 'many devices each complex' to 'one controller, many simple devices'.</p>",
        visual: { type: "hierarchy", params: { root: "SD-Access fabric", children: [{ name: "Underlay: IS-IS" }, { name: "Overlay: VXLAN" }, { name: "Control plane: LISP" }, { name: "Policy: CTS / SGT" }, { name: "Controller: DNA / Catalyst Center" }] } },
        hack: {
          memory: "SD-Access = Cisco's campus fabric. Four pillars: IS-IS underlay, VXLAN overlay, LISP control plane, CTS/SGT policy. Managed by DNA / Catalyst Center. Node roles: edge, border, control-plane, intermediate.",
          practice: "Recite the four pillars cold: IS-IS / VXLAN / LISP / CTS-SGT. Then map the four node roles to their jobs: edge = VTEP for users, border = outside world, control plane = LISP database, intermediate = underlay transit.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'what's SD-Access?' = Cisco campus fabric built on IS-IS + VXLAN + LISP + CTS/SGT, orchestrated by DNA Center."
        }
      },
      {
        id: "6.3.e.2",
        term: "LISP (in SDA)",
        weight: "med",
        info: "<p><strong>LISP (Locator / ID Separation Protocol)</strong> is the control plane of Cisco SD-Access. It solves a problem the traditional Internet can't: <em>how do you let an endpoint move around without changing its IP?</em></p><p><strong>The core idea:</strong> LISP separates two concepts that are normally fused in a single IP address:</p><ul><li><strong>Endpoint ID (EID)</strong> — the endpoint's identity. In SDA, this is the user/device IP as seen inside the overlay.</li><li><strong>Routing Locator (RLOC)</strong> — the IP of the fabric VTEP where the endpoint currently lives.</li></ul><p><strong>How it works in SDA:</strong></p><ol><li>An endpoint connects to edge node A. The edge node registers the EID → RLOC (A) mapping with the LISP <strong>Control Plane Node</strong>.</li><li>When another endpoint on edge node B wants to send traffic, edge node B queries the Control Plane Node: 'where is EID X?'</li><li>Control Plane Node answers: 'EID X is at RLOC A.'</li><li>Edge node B encapsulates the frame in VXLAN to RLOC A.</li><li>When the endpoint moves to edge node C, the registration updates and new lookups return RLOC C. No IP change required for the endpoint.</li></ol><p><strong>Why it matters for mobility:</strong> wireless roams, VM migrations, and plugged-in-somewhere-new events all work without re-addressing. The endpoint keeps its IP (its EID) forever; only its location (RLOC) changes. The fabric handles the mapping transparently.</p><p><strong>CCNA expectation:</strong> know that SDA's control plane is LISP, that LISP separates ID (EID) from location (RLOC), and that Control Plane Nodes host the EID→RLOC mapping database. No need to configure LISP on the exam.</p>",
        visual: { type: "state-machine", params: { states: ["Edge registers EID → RLOC", "CP Node stores mapping", "Remote edge queries CP Node", "Gets EID → RLOC answer", "Encapsulates via VXLAN"], active: 2, transitions: true } },
        hack: {
          memory: "LISP = Locator / ID Separation. EID = who (endpoint IP). RLOC = where (VTEP IP). Control Plane Node = the phonebook mapping EID→RLOC. Edge nodes register endpoints; remote edges query. Endpoint can roam without re-addressing.",
          practice: "Draw a three-node SDA: edge A, edge B, control plane node. Walk an endpoint from A to B on paper: registration updates at CP Node, future lookups return RLOC B, old RLOC A becomes stale. That's mobility without IP change.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'what protocol provides SD-Access control plane?' = LISP. Know EID vs RLOC and that Control Plane Nodes host the mapping database."
        }
      },
      {
        id: "6.3.e.3",
        term: "CTS / SGTs",
        weight: "med",
        info: "<p><strong>Cisco TrustSec (CTS)</strong> with <strong>Scalable Group Tags (SGTs)</strong> is SD-Access's policy and segmentation engine. Instead of writing ACLs that say 'src 10.1.1.0/24 → dst 10.2.2.0/24', you tag users/devices by role and write policies between roles. This is called <strong>group-based</strong> or <strong>intent-based</strong> segmentation.</p><p><strong>How SGTs work:</strong></p><ol><li>An endpoint authenticates via 802.1X / MAB / WebAuth to <strong>Cisco ISE</strong>.</li><li>ISE evaluates the user/device context (identity, posture, location) and returns an SGT — a 16-bit tag like 'Employee' (10), 'Guest' (20), 'IoT' (30), 'Contractor' (40).</li><li>The edge node applies the SGT to all traffic from that endpoint — carrying it across the fabric in VXLAN's SGT field.</li><li>At the egress (or any policy enforcement point), the device consults a <strong>Security Group ACL (SGACL)</strong> matrix: 'SGT 20 → SGT 30 = deny'. Traffic is permitted or dropped based on the <em>tag pair</em>, not IP addresses.</li></ol><p><strong>Why it's better than IP-based ACLs:</strong></p><ul><li><strong>Independent of IP</strong> — policy survives re-addressing, DHCP changes, VM moves.</li><li><strong>Scales to microsegmentation</strong> — one policy row per group pair, not per host pair.</li><li><strong>Identity-aware</strong> — same user on a corporate laptop vs a personal phone can get different tags and policies.</li><li><strong>Centrally managed</strong> — policies live on DNA Center / ISE, not scattered in per-device ACLs.</li></ul><p><strong>Exam expectation:</strong> know that SDA policy is SGT-based (not IP-based), that SGTs come from ISE after authentication, and that SGACLs define what tag pairs can communicate. You won't configure SGTs on the CCNA; recognizing the model is enough.</p>",
        visual: { type: "shield", params: { items: ["Identity from ISE", "SGT assigned", "Carried in VXLAN", "SGACL at enforcement", "Policy by group, not IP"], color: "#10b981" } },
        hack: {
          memory: "CTS + SGT = identity-based policy. User authenticates to ISE → ISE returns an SGT → fabric tags traffic → SGACL at egress allows/denies based on tag pairs. Policy independent of IP. This is microsegmentation done right.",
          practice: "Design a 4-tag policy matrix: Employee, Guest, IoT, Server. Fill in allow/deny between each pair. Realize the matrix has 16 cells, vs IP-based ACLs that would need hundreds of rules. That density advantage is why CTS exists.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: 'SD-Access policy is based on?' = SGT / group / TrustSec. Know the ISE → SGT → SGACL flow at a conceptual level."
        }
      }
    ]
  },

  // ── 6.4 Explain AI and ML in network operations ───────────────

  "6.4.a": {
    info: "<p><strong>Generative AI in networking</strong> creates new content — configurations, documentation, troubleshooting steps, and code — from natural language input. An engineer describes their intent in plain English, and the AI generates the corresponding technical output.</p><p>Applications of generative AI in network operations:</p><ul><li><strong>Configuration generation</strong> — describe 'block HTTP from guest VLAN to server VLAN' and the AI generates the complete ACL configuration with correct syntax, wildcard masks, and placement recommendation</li><li><strong>Documentation</strong> — generate network diagrams, change request documents, runbooks, and compliance reports from existing configuration data</li><li><strong>Troubleshooting</strong> — describe symptoms ('users in VLAN 10 can't reach the internet') and receive step-by-step diagnostic procedures</li><li><strong>Code generation</strong> — generate Python scripts, Ansible playbooks, or API calls for network automation tasks</li><li><strong>Translation</strong> — convert configurations between platforms (IOS to NX-OS) or between formats (CLI to YAML)</li></ul><p>Important limitations: generative AI can produce <strong>plausible but incorrect output</strong> (hallucinations). Network engineers must <strong>validate all AI-generated configurations</strong> before deploying them. AI is a productivity tool, not a replacement for network knowledge.</p><p>This is a newer CCNA topic (added in v1.1) reflecting the industry's adoption of AI assistants like Cisco AI Assistant, ChatGPT, and Claude for network operations. The exam tests conceptual understanding — what generative AI does for networking, not how to build AI models.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Natural Language Intent", "Generative AI", "Network Configuration"], color: "#8b5cf6" } },
    hack: {
      memory: "Generative AI = tell it WHAT you want in plain English, it GENERATES the config. Like a translator between human intent and CLI syntax. But always VERIFY the output — AI can hallucinate wrong configs.",
      practice: "Try describing an ACL requirement to an AI assistant: 'Block Telnet from 10.0.0.0/24 to server 192.168.1.100 but allow SSH.' Compare the generated config to what you'd write manually. Note any errors.",
      effort: "low",
      meta: "Jeremy's IT Lab may cover this in updated content. Wendell Odom OCG Chapter 17 (updated editions). New CCNA v1.1 topic. Conceptual understanding only — what generative AI does for networking, not how AI works internally.",
    },
    micro: [
      {
        id: "6.4.a.1",
        term: "Generative AI in networking",
        weight: "high",
        info: "<p><strong>Generative AI</strong> creates <em>new</em> content — text, configs, code, diagrams — from natural-language input. In networking, it's the class of AI that lets an engineer type 'give me an ACL blocking HTTP from guest VLAN to server VLAN' and get back valid IOS config.</p><p><strong>Common generative AI use cases in network ops:</strong></p><ul><li><strong>Configuration drafting</strong> — generate VLAN, ACL, QoS, or BGP snippets from plain-language requirements.</li><li><strong>Log and alert summarization</strong> — condense thousands of syslog lines into 'your core router had a BGP flap at 02:41'.</li><li><strong>Troubleshooting assistants</strong> — walk through suspected root causes step by step when given symptoms.</li><li><strong>Documentation and diagrams</strong> — produce runbooks, change records, topology maps from live state.</li><li><strong>Code generation</strong> — Python scripts, Ansible playbooks, Postman collections for specific tasks.</li><li><strong>Platform translation</strong> — convert IOS config to NX-OS, or CLI to YANG JSON.</li></ul><p><strong>Key constraint — hallucinations.</strong> Generative AI produces <em>plausible-sounding</em> output, which may or may not be correct. It can invent wildcard masks, reference commands that don't exist, or miss edge cases. Every AI-generated config must be reviewed, tested in a lab, and validated before being pushed to production. This is exam-testable: AI augments engineers, it doesn't replace validation.</p><p><strong>Cisco context:</strong> Cisco AI Assistant (part of Catalyst Center and Security Cloud) is the vendor's generative offering. General-purpose tools — ChatGPT, Claude, GitHub Copilot — are heavily used across the industry for the same tasks.</p>",
        visual: { type: "packet-flow", params: { nodes: ["Natural language intent", "Gen-AI model", "Draft config / script", "Human review", "Validated deploy"], color: "#8b5cf6" } },
        hack: {
          memory: "Generative AI = CREATES output. In networking: configs, docs, scripts, troubleshooting steps from plain-English input. Always hallucinates occasionally — humans must validate. Cisco flavor = Cisco AI Assistant.",
          practice: "Ask a chat AI to write an IOS ACL for a specific policy. Compare its output to what you'd write by hand. Note at least one subtle mistake it makes — that's why validation is non-negotiable.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17 (newer printings). New CCNA 200-301 v1.1 topic. Exam: 'what does generative AI do?' = creates content from natural language input."
        }
      },
      {
        id: "6.4.a.2",
        term: "Gen-AI use cases",
        weight: "med",
        info: "<p>The CCNA 200-301 v1.1 adds generative AI as a knowledge-level topic. You don't have to know how models work — you have to recognize <strong>what generative AI can and should do in a NOC or engineering context</strong>. Memorize the common use cases so you can identify them in a multiple-choice stem.</p><p><strong>High-confidence use cases (safe to recognize on the exam):</strong></p><ul><li><strong>Config generation from natural language</strong> — 'block telnet from guest VLAN' → ACL lines.</li><li><strong>Log/alert summarization</strong> — hundreds of syslog messages compressed into a human-readable incident summary.</li><li><strong>Runbook and documentation drafting</strong> — turn a change request into a procedure document.</li><li><strong>Troubleshooting step-by-step suggestions</strong> — symptom-driven diagnostic walkthroughs.</li><li><strong>Code generation</strong> — Python / Ansible for a specific repeatable task.</li><li><strong>Explaining output</strong> — 'what does this show ip ospf database output tell me?'</li></ul><p><strong>Lower-confidence use cases (still valid but need more oversight):</strong></p><ul><li>Writing production-critical security policy from scratch.</li><li>Generating large, multi-device configuration changes end-to-end without human review.</li><li>Making runtime decisions (AIOps / predictive) — that's predictive AI's job, not generative AI.</li></ul><p><strong>Key contrast with predictive AI (6.4.b):</strong> generative <em>creates</em>; predictive <em>forecasts</em>. The exam may list both and ask which is which — if the answer talks about writing a config or summarizing a log, it's generative. If it talks about forecasting a failure or detecting anomalies, it's predictive.</p><p><strong>Career framing:</strong> in NOC and DC-ops work, generative AI is already part of the job — writing playbooks, summarizing tickets, drafting change docs. Knowing how to use it responsibly (validate everything) is the real skill.</p>",
        visual: { type: "shield", params: { items: ["Config gen", "Log summary", "Runbook draft", "Troubleshooting walk", "Code gen", "Output explain"], color: "#a855f7" } },
        hack: {
          memory: "Gen-AI creates text/config/code from natural language. Top 6 use cases: configs, summaries, runbooks, troubleshooting steps, scripts, explain-this-output. Always validate — models hallucinate. Gen = creates; predictive = forecasts.",
          practice: "Match these to 'generative' or 'predictive': summarize 500 syslogs (gen), detect hardware failure before it happens (pred), write an ACL from a sentence (gen), flag unusual traffic at 3 AM (pred). Build the instinct.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. Exam: 'which of these is a generative AI use case?' — pick the one about CREATING output from natural language."
        }
      }
    ]
  },

  "6.4.b": {
    info: "<p><strong>Predictive AI</strong> uses machine learning models trained on historical network data to <strong>forecast future events</strong> before they occur. This shifts network operations from <strong>reactive</strong> (fix after failure) to <strong>proactive</strong> (fix before failure).</p><p>Key predictive AI capabilities:</p><ul><li><strong>Anomaly detection</strong> — ML models learn normal network behavior patterns and flag deviations that may indicate security breaches, hardware failures, or configuration problems. Example: a sudden spike in DNS queries from one host might indicate malware communicating with a C2 server.</li><li><strong>Capacity forecasting</strong> — analyzes bandwidth utilization trends to predict when links, storage, or compute resources will reach capacity, enabling proactive upgrades before users experience degradation</li><li><strong>Failure prediction</strong> — identifies patterns that precede hardware failures. Example: rising CRC errors and increasing interface resets on a port historically correlate with a failing transceiver — predict and replace before it fails.</li><li><strong>Performance degradation detection</strong> — detects slow degradation that static thresholds miss. A link slowly climbing from 40% to 70% utilization over 6 months won't trigger an 80% alert but predictive AI forecasts the capacity breach date.</li></ul><p>Key distinction: <strong>Generative AI creates new content</strong> (configs, docs, code). <strong>Predictive AI forecasts future events</strong> (anomalies, failures, capacity). They solve different problems. Generative = creation. Predictive = forecasting.</p>",
    visual: { type: "state-machine", params: { states: ["Collect Data", "Train ML Model", "Detect Anomalies", "Predict Failures", "Proactive Action"], active: 3, transitions: true } },
    hack: {
      memory: "Predictive AI = a weather forecast for your network. It looks at historical patterns and predicts storms (failures) before they hit. Reactive = fix after crash. Proactive = fix before crash. Generative = creates. Predictive = forecasts.",
      practice: "List 3 things predictive AI could detect: 1) Switch CPU slowly climbing toward 100% (capacity). 2) Interface CRC errors increasing weekly (hardware failing). 3) Unusual DNS traffic from one host at 3 AM (security breach). All caught before they become outages.",
      effort: "low",
      meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. The exam tests: predictive AI = forecasting + anomaly detection + proactive operations. Know the difference: predictive (forecasts) vs generative (creates).",
    },
    micro: [
      {
        id: "6.4.b.1",
        term: "Predictive AI",
        weight: "high",
        info: "<p><strong>Predictive AI</strong> uses machine-learning models trained on historical network data to <strong>forecast future events</strong>. Where generative AI creates content, predictive AI spots trends, flags anomalies, and predicts failures before they happen.</p><p><strong>Core capabilities:</strong></p><ul><li><strong>Anomaly detection</strong> — baseline normal behavior, alert when today deviates. Catches zero-day malware, brute force attempts, and misconfigurations that static rules miss.</li><li><strong>Failure prediction</strong> — optical transceivers, fans, PSUs, and storage all emit early warning signs (rising CRC, temperature drift, SMART counters) before they fail. ML models learn these patterns and raise flags.</li><li><strong>Capacity forecasting</strong> — trends in link utilization, CPU, and memory are extrapolated into 'link X will hit 80% in 6 weeks'. Enables planned upgrades instead of emergency ones.</li><li><strong>User-experience prediction</strong> — predict that a given Wi-Fi client will have poor voice-call quality based on RSSI, retries, and neighboring APs; preemptively roam the client or adjust the radio.</li></ul><p><strong>Data fuel:</strong> predictive AI needs historical data — NetFlow, streaming telemetry, syslog, SNMP counters, wireless RRM data, device inventory. The more structured, long-lived telemetry you have, the better the models.</p><p><strong>Why it matters operationally:</strong> predictive AI shifts operations from <strong>reactive</strong> (fix after it breaks) to <strong>proactive</strong> (fix before it breaks). Paired with self-healing automation (6.1.e), this can almost eliminate certain classes of outages from the incident log.</p><p><strong>Cisco examples:</strong> Catalyst Center Assurance, Cisco AI Endpoint Analytics, Meraki AI-driven wireless optimization, ThousandEyes AI Insights for Internet/cloud path analytics.</p>",
        visual: { type: "state-machine", params: { states: ["Ingest telemetry", "Train ML baseline", "Detect deviation", "Predict failure / capacity", "Alert or auto-remediate"], active: 3, transitions: true } },
        hack: {
          memory: "Predictive AI = FORECASTS future events. Top uses: anomaly detection, failure prediction, capacity forecasting, user-experience prediction. Shifts ops from reactive → proactive. Cisco's flavor = Catalyst Center Assurance.",
          practice: "For each example, identify what historical data would feed the model: CRC errors → optical failure prediction. BW utilization over 6 months → capacity forecast. Wi-Fi retries + RSSI → client experience. Data → model → prediction.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. Exam: 'predictive AI does what?' = forecasts events / detects anomalies / predicts failures. Goal: proactive ops."
        }
      },
      {
        id: "6.4.b.2",
        term: "Generative vs Predictive",
        weight: "high",
        info: "<p>The CCNA tests the distinction between generative and predictive AI directly. Get the contrast clean and you unlock every 6.4 question.</p><table><tr><th>Dimension</th><th>Generative AI</th><th>Predictive AI</th></tr><tr><td>Primary output</td><td>New content (text, config, code)</td><td>Forecasts, scores, classifications</td></tr><tr><td>Input</td><td>Natural language prompt</td><td>Structured telemetry / historical data</td></tr><tr><td>Model class</td><td>LLMs, diffusion models</td><td>Classifiers, regressors, time-series ML</td></tr><tr><td>Examples</td><td>Write an ACL, summarize logs, generate a runbook</td><td>Anomaly detection, failure prediction, capacity planning</td></tr><tr><td>Risk</td><td>Hallucinations — fabricated but plausible output</td><td>False positives, model drift, bias</td></tr><tr><td>Guardrail</td><td>Human validation of every output before deploy</td><td>Calibrated thresholds + human review of high-impact actions</td></tr></table><p><strong>Shortcut:</strong> if the exam's verb is 'create / write / draft / generate / summarize', the answer is generative. If it's 'forecast / predict / detect anomaly / baseline', the answer is predictive. They aren't opposites — they're complementary tools addressing different problems.</p><p><strong>In real ops:</strong> they layer together. Predictive AI spots an anomaly; generative AI then drafts the troubleshooting runbook or the fix script. Both operate under human supervision in mature teams.</p><p><strong>Cisco split:</strong> Catalyst Center Assurance is mostly predictive (AIOps, anomaly detection). Cisco AI Assistant is generative (natural-language config, query, summarize). Same platform, two different AI classes.</p>",
        visual: { type: "comparison", params: { left: { label: "Generative AI", items: ["Creates content", "Input: NL prompt", "Example: write ACL from English", "Risk: hallucination"] }, right: { label: "Predictive AI", items: ["Forecasts events", "Input: historical telemetry", "Example: predict HDD failure", "Risk: false positives / drift"] } } },
        hack: {
          memory: "Gen = CREATE. Predictive = FORECAST. Verb test: 'write / summarize / draft' → gen. 'Predict / detect / baseline' → predictive. Both are real, both layer together, both need humans. Cisco: Assistant (gen), Assurance (predictive).",
          practice: "Rapid-fire 10 mini scenarios: 'draft a BGP config from English' (gen), 'predict next month's link saturation' (pred), 'summarize last night's syslog' (gen), 'detect anomalous DNS traffic' (pred). Repeat until instant.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. Exam: 'which AI class does X?' — gen creates, pred forecasts. This is the single most-tested distinction in 6.4."
        }
      },
      {
        id: "6.4.b.3",
        term: "Anomaly detection",
        weight: "med",
        info: "<p><strong>Anomaly detection</strong> is predictive AI's marquee use case. ML models learn what 'normal' looks like for a given metric, device, or user and raise alerts when observed behavior deviates from that baseline in a statistically significant way.</p><p><strong>Why anomaly detection beats static thresholds:</strong></p><ul><li><strong>Context aware</strong> — 90% CPU at 9 AM Monday might be normal; 90% at 3 AM Sunday is almost certainly a problem. Static 'CPU > 80' alerts can't tell the difference.</li><li><strong>Slow drift</strong> — metrics that creep up over weeks never trip a static threshold until the day they do. ML spots the trend line long before impact.</li><li><strong>Multivariate</strong> — a single metric may look fine, but ML can correlate 'high CPU AND low throughput AND high retransmits' to flag the pattern a human would miss.</li></ul><p><strong>Common security anomaly-detection targets:</strong></p><ul><li>DNS tunneling — unusual query volumes or uncommon record types.</li><li>Data exfiltration — outbound flows from an endpoint suddenly 10x normal.</li><li>Lateral movement — one host suddenly probing many internal hosts on unusual ports.</li><li>Compromised credentials — a user logging in from two continents within an hour.</li></ul><p><strong>Common infrastructure anomaly-detection targets:</strong></p><ul><li>Interface errors rising on a specific port (failing transceiver).</li><li>Wireless client drop rate spiking on one AP (environmental interference).</li><li>Storage read latency climbing on one array (aging disk).</li></ul><p><strong>Trade-offs:</strong> ML baselines can also <em>miss</em> anomalies that mimic normal patterns (evasion techniques), and produce <em>false positives</em> when legitimate behavior changes (marketing campaign traffic spike). Mature deployments combine ML anomaly detection with human-in-the-loop review and explicit allow-lists.</p>",
        visual: { type: "state-machine", params: { states: ["Ingest metrics", "Learn baseline", "Score current value", "Flag significant deviation", "Human or auto triage"], active: 3, transitions: true } },
        hack: {
          memory: "Anomaly detection = 'what is normal?' → 'is now abnormal?' Beats static thresholds because it's context-aware and catches slow drift. Security catches (DNS tunneling, exfil, lateral movement) + infra catches (optic failures, AP issues, storage latency).",
          practice: "For one metric (interface CRC errors), sketch a baseline (near zero normally) and a 10-day rising trend. Static threshold at '>1000/min' triggers too late. ML picks up the upward slope on day 3. That's the value.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. Exam: 'what's anomaly detection?' = ML-based identification of deviations from learned normal patterns. Used in both security and infra ops."
        }
      }
    ]
  },

  "6.4.c": {
    info: "<p><strong>ML-driven monitoring</strong> learns the <strong>normal baseline behavior</strong> of your network and automatically detects deviations that may indicate problems. This is fundamentally different from traditional static threshold monitoring.</p><p>Static thresholds vs ML baselines:</p><ul><li><strong>Static thresholds</strong> — 'alert when CPU > 80%.' The same threshold applies 24/7, regardless of context. Problem: 85% CPU might be normal during Monday business hours (everyone logging in) but highly abnormal at 3 AM Sunday. Static thresholds generate many <strong>false positives</strong> during peak times and <strong>miss real issues</strong> during off-hours.</li><li><strong>ML baselines</strong> — the system learns that 'CPU is normally 75-85% at 9 AM Monday, 20-30% at 3 AM Sunday, 50-60% on Saturday.' It alerts when actual values deviate significantly from the <strong>expected pattern for that specific time and context</strong>. 85% at 9 AM = normal, no alert. 60% at 9 AM = unusual, investigate. 50% at 3 AM = normal, no alert. 80% at 3 AM = critical, alert immediately.</li></ul><p>ML baselines also detect <strong>slow drift</strong> that static thresholds completely miss. If a link's utilization slowly creeps from 40% to 75% over 6 months, it never triggers an 80% alert but ML recognizes the upward trend and forecasts when capacity will be exceeded.</p><p><strong>Cisco DNA Center Assurance</strong> uses ML-driven baselines for network health scoring, comparing real-time metrics against learned normal patterns to identify issues before users report them.</p>",
    visual: { type: "comparison", params: { left: { label: "Static Thresholds", items: ["CPU > 80% = alert", "Same threshold 24/7", "Many false positives", "Misses slow degradation"] }, right: { label: "ML Baseline", items: ["Learns normal patterns", "Context-aware alerts", "Fewer false positives", "Detects slow drift"] } } },
    hack: {
      memory: "ML monitoring LEARNS what 'normal' looks like for each device at each time. Static thresholds are dumb — same number 24/7. ML says 'this is unusual FOR THIS TIME.' 85% at 9 AM = fine. 85% at 3 AM = problem.",
      practice: "Thought experiment: web server CPU. Static alert at 80%. Result: alerts every Monday at 9 AM (normal login spike) but misses a 50% anomaly at midnight (unusual). ML baseline catches the midnight anomaly and ignores the Monday spike.",
      effort: "low",
      meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. The exam tests ML baselines vs static thresholds. ML adapts to patterns and detects contextual anomalies. Cisco DNA Center Assurance is the concrete example.",
    },
    micro: [
      {
        id: "6.4.c.1",
        term: "ML-based baselines",
        weight: "high",
        info: "<p><strong>ML-based baselines</strong> are the engine behind modern anomaly detection. Instead of an operator typing 'CPU > 80%' into a monitoring tool, the ML model <strong>learns</strong> what CPU (and hundreds of other metrics) look like normally for each device, at each time, in each context — then uses those learned patterns as the threshold.</p><p><strong>What the model actually learns:</strong></p><ul><li><strong>Time-of-day and day-of-week patterns</strong> — Monday 9 AM ≠ Sunday 3 AM.</li><li><strong>Seasonality</strong> — end-of-quarter spikes, holiday lulls, school-year patterns.</li><li><strong>Per-device baselines</strong> — a data-hall spine switch vs a branch office router have very different normals.</li><li><strong>Per-application baselines</strong> — VoIP, video, backup, and SaaS each have their own traffic signatures.</li><li><strong>Multivariate baselines</strong> — relationships between metrics (CPU + throughput + retransmits) as a set.</li></ul><p><strong>Static thresholds vs ML baselines — head to head:</strong></p><table><tr><th>Scenario</th><th>Static</th><th>ML baseline</th></tr><tr><td>85% CPU at 9 AM Monday</td><td>Alerts (false positive)</td><td>Normal, no alert</td></tr><tr><td>85% CPU at 3 AM Sunday</td><td>Alerts (correct)</td><td>Alert — highly abnormal</td></tr><tr><td>CPU creeping 50→75% over 6 months</td><td>Silent</td><td>Trend alert weeks in advance</td></tr><tr><td>60% CPU but throughput dropped 40%</td><td>Silent</td><td>Multivariate anomaly alert</td></tr></table><p><strong>Operational impact:</strong> ML baselines drastically reduce alert fatigue (fewer false positives during normal busy periods) and catch classes of problems (slow drift, multivariate issues) that humans miss entirely.</p><p><strong>Where they live:</strong> Catalyst Center Assurance, Meraki AI, ThousandEyes, Splunk ITSI, Dynatrace. All use the same underlying idea — learn what normal looks like, alert on deviation.</p>",
        visual: { type: "comparison", params: { left: { label: "Static thresholds", items: ["One number, 24/7", "Many false positives", "Misses slow drift", "No multivariate sense"] }, right: { label: "ML baselines", items: ["Time + context aware", "Fewer false positives", "Catches slow drift", "Multivariate patterns"] } } },
        hack: {
          memory: "ML baseline LEARNS normal — per device, per time, per variable combo. Static thresholds are dumb: one number all day. ML alerts catch context (85% at 3 AM) and slow drift (50→75% over months). Alert fatigue drops; real issues rise.",
          practice: "Pick one metric on a lab device (interface utilization). Define a static threshold AND imagine the ML baseline learning hour-by-hour patterns over a week. Identify 2 scenarios where the static threshold is wrong and ML wins.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. Exam: 'ML-based monitoring differs from static thresholds how?' = learns context-aware patterns + catches slow drift + multivariate."
        }
      },
      {
        id: "6.4.c.2",
        term: "Cisco DNA Center Assurance",
        weight: "high",
        info: "<p><strong>Cisco DNA / Catalyst Center Assurance</strong> is the concrete, CCNA-testable example of ML-driven monitoring and AIOps. It's one of the four DPPA pillars (6.2.d.2) and the most AI-heavy part of the controller.</p><p><strong>What Assurance does, in practice:</strong></p><ul><li><strong>Health scores</strong> — every device and every client gets a numeric health score (0-10) computed from dozens of underlying metrics. Green/yellow/red surfaces at the site, building, and floor level.</li><li><strong>Issue correlation</strong> — an AP flapping, Radius authentication failing, and a client stuck in association all correlate into ONE issue: 'wireless clients cannot onboard to AP-21'. Hundreds of raw events → one actionable issue.</li><li><strong>Root-cause analysis</strong> — ML models suggest the most likely cause. 'Radar interference on 5 GHz is triggering DFS channel changes, causing client disconnects.'</li><li><strong>Guided remediation</strong> — step-by-step fix instructions surfaced directly in the UI, often with one-click actions that the controller can execute.</li><li><strong>Path trace</strong> — visualize the L2/L3 path a packet takes between two endpoints and see per-hop health / policy in one view. No more SSH-ing into five switches.</li></ul><p><strong>Under the hood:</strong> Assurance ingests NetFlow, streaming telemetry (gNMI), SNMP, syslog, wireless RRM data, ISE context, and running configs. ML models build per-metric baselines, run anomaly detection, and cross-correlate events. Results surface as a prioritized issue list.</p><p><strong>Why the CCNA tests it:</strong> it's the single best concrete example of predictive AI, ML baselines, and AIOps in Cisco's product line. Expect questions like 'which DNA Center pillar uses AI/ML?' (Assurance) or 'what provides root-cause analysis for wireless issues?' (Catalyst Center Assurance).</p>",
        visual: { type: "hierarchy", params: { root: "DNA / Catalyst Center Assurance", children: [{ name: "Health scores" }, { name: "Issue correlation" }, { name: "Root-cause analysis" }, { name: "Guided remediation" }, { name: "Path trace" }] } },
        hack: {
          memory: "DNA / Catalyst Center Assurance = Cisco's ML / AIOps pillar. Health scores + issue correlation + root-cause analysis + guided remediation + path trace. Ingests NetFlow + telemetry + syslog + ISE context. Fourth DPPA pillar.",
          practice: "In the free DNA Center sandbox, open Assurance. Pick a site with issues; click through to root-cause analysis and guided remediation. Seeing it live makes the AIOps concept concrete in a way no diagram can.",
          effort: "medium",
          meta: "Jeremy's IT Lab Day 60. Wendell Odom OCG Chapter 17. Exam: Assurance = the ML/AIOps arm of DNA / Catalyst Center. Pairs with DPPA and with 6.4.b predictive AI."
        }
      }
    ]
  },

  "6.4.d": {
    info: "<p><strong>AIOps (Artificial Intelligence for IT Operations)</strong> applies AI/ML to automate the most time-consuming parts of network operations: <strong>event correlation, root cause analysis, and remediation</strong>.</p><p>How AIOps works in practice:</p><ul><li><strong>Event correlation</strong> — when a network incident occurs, it may generate hundreds or thousands of individual alerts across multiple devices and monitoring systems. A single switch failure might trigger alerts for STP recalculation, OSPF neighbor loss, client disconnections, application timeouts, and throughput drops. AIOps correlates all these events into a <strong>single root cause</strong>, reducing alert noise from 1,000 events to 1 actionable issue.</li><li><strong>Automated root cause analysis</strong> — ML models analyze the correlated events, compare against historical incident patterns, and identify the most likely root cause. 'These 47 alerts match the pattern of a failed uplink on a distribution switch.' This analysis that might take a human team hours happens in seconds.</li><li><strong>Guided/automated remediation</strong> — once the root cause is identified, AIOps can suggest specific fix steps or automatically execute pre-approved remediation actions (restart a process, failover to backup path, isolate a compromised device).</li></ul><p><strong>Cisco DNA Center Assurance</strong> is a practical AIOps example in networking. It continuously monitors network health using ML, correlates wireless client issues (authentication failures, DHCP problems, roaming issues) to their root causes, and provides guided remediation steps directly in the dashboard.</p><p>AIOps represents the convergence of automation (doing things automatically), analytics (understanding what happened), and AI (predicting and deciding what to do). It's the next evolution beyond simple monitoring and alerting.</p>",
    visual: { type: "hierarchy", params: { root: "AIOps", children: [{ name: "Event Correlation", children: [{ name: "1000s of alerts -> 1 root cause" }] }, { name: "Root Cause Analysis", children: [{ name: "ML identifies source" }] }, { name: "Auto-Remediation", children: [{ name: "Suggested/automated fixes" }] }] } },
    hack: {
      memory: "AIOps = AI that does what a NOC team does: correlate 1000 alerts → find 1 root cause → suggest the fix. Turns hours of troubleshooting into seconds of analysis. Cisco DNA Center Assurance is the real-world example.",
      practice: "Scenario: switch fails → 200 SNMP traps + 50 syslog messages + 30 client complaints. Manual NOC: 2 hours to identify root cause. AIOps: 30 seconds to correlate, identify failed switch, and suggest failover. That's the value.",
      effort: "low",
      meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. AIOps = event correlation + automated root cause analysis + remediation. Know: Cisco DNA Center Assurance as the concrete example.",
    },
    micro: [
      {
        id: "6.4.d.1",
        term: "AIOps",
        weight: "high",
        info: "<p><strong>AIOps (Artificial Intelligence for IT Operations)</strong> is the umbrella term for applying AI/ML to operational data — events, metrics, logs, traces — to automate the toughest parts of running infrastructure: <strong>correlation, root-cause analysis, and remediation</strong>.</p><p><strong>AIOps loop (read this as one picture):</strong></p><ol><li><strong>Ingest</strong> — pull events from many sources (syslog, SNMP, streaming telemetry, ticketing, APM, NetFlow).</li><li><strong>Deduplicate + correlate</strong> — hundreds or thousands of raw events per incident → a single grouped incident.</li><li><strong>Root-cause</strong> — ML identifies the most likely origin ('uplink down on dist-sw-3').</li><li><strong>Recommend / act</strong> — suggest a runbook or execute an approved playbook automatically.</li><li><strong>Learn</strong> — feed back post-incident review so future correlations and recommendations improve.</li></ol><p><strong>Alert volume reduction is the big win.</strong> Without AIOps, a single major incident can produce 1,000+ alerts across monitoring tools — overwhelming on-call engineers and burying the real signal. With AIOps, those 1,000 alerts collapse into 1 incident with a rank-ordered list of likely causes. Operators spend time fixing, not triaging.</p><p><strong>AIOps vs predictive AI vs generative AI:</strong></p><ul><li><strong>Predictive AI</strong> — detects / forecasts (component of AIOps).</li><li><strong>Generative AI</strong> — writes / summarizes (can augment AIOps with natural-language output).</li><li><strong>AIOps</strong> — the operational umbrella that glues correlation + prediction + automation together.</li></ul><p><strong>Cisco example:</strong> DNA / Catalyst Center Assurance is an AIOps platform. Non-Cisco examples: Moogsoft, BigPanda, Dynatrace Davis, Splunk ITSI, New Relic AI. All solve the same correlation + root-cause + remediation problem.</p>",
        visual: { type: "state-machine", params: { states: ["Ingest", "Dedupe + correlate", "Root-cause (ML)", "Recommend or auto-remediate", "Learn / feedback"], active: 2, transitions: true } },
        hack: {
          memory: "AIOps = the AI loop for ops. Ingest → correlate → root-cause → act → learn. Main win: collapses 1,000 alerts into 1 incident. Includes predictive AI + automation + (sometimes) generative summaries. Cisco example = DNA / Catalyst Center Assurance.",
          practice: "Map a real NOC incident to the AIOps loop: 50 SNMP traps + 20 syslog + 5 tickets → dedupe/correlate → root-cause (core switch uplink) → runbook (failover) → post-incident review. Five steps, one picture.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. Exam: 'AIOps does what?' = correlates events + automates root-cause analysis + drives remediation. Cisco concrete example = DNA Center Assurance."
        }
      },
      {
        id: "6.4.d.2",
        term: "Event correlation",
        weight: "high",
        info: "<p><strong>Event correlation</strong> is the process of grouping many low-level events into a smaller set of meaningful incidents. It's the engine that makes AIOps usable — without correlation, an operator drowns in raw alerts.</p><p><strong>Correlation types:</strong></p><ul><li><strong>Temporal</strong> — events arriving within a short window are likely related.</li><li><strong>Topological</strong> — events from devices close together in the network graph are likely related (shared uplink).</li><li><strong>Causal</strong> — known relationships like 'when this uplink fails, these downstream OSPF neighbors will drop'.</li><li><strong>Pattern-based / ML</strong> — learned signatures of past incidents matched against current event streams.</li></ul><p><strong>Concrete example.</strong> A core switch power supply fails at 02:14. Within 30 seconds:</p><ul><li>40 OSPF adjacency-down syslog messages from neighboring routers.</li><li>150 STP topology change events from access switches.</li><li>300 interface-down SNMP traps.</li><li>20 application timeouts from APM tools.</li><li>12 user tickets from the help desk.</li></ul><p>Without correlation, the on-call engineer sees 500+ alerts and has to piece together the story manually. With AIOps correlation, all 500+ events roll up into ONE incident: 'core-sw-A power failure at 02:14, impact: 200 downstream devices'. Triage goes from 30 minutes to 30 seconds.</p><p><strong>Why correlation reduces MTTR more than raw alert routing:</strong> the biggest operational time sink isn't acting — it's figuring out which of a thousand alerts is the real one. Correlation removes that bottleneck so engineers jump straight to 'what do we do?' instead of 'what's actually broken?'</p>",
        visual: { type: "hierarchy", params: { root: "Event correlation", children: [{ name: "Temporal" }, { name: "Topological" }, { name: "Causal" }, { name: "ML pattern" }] } },
        hack: {
          memory: "Event correlation = turn 1,000 alerts into 1 incident. Four modes: temporal (same time), topological (same region), causal (known dependency), ML (learned signature). Reduces triage time way more than 'faster acting' ever could.",
          practice: "Walk through a core-switch PSU failure scenario on paper: list every event it would produce across OSPF, STP, SNMP, APM, tickets. Count them. Now collapse into one incident. That collapse = correlation's ROI.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. Exam: 'event correlation does what?' = groups related events into incidents. Key benefit = reduces alert noise / triage time."
        }
      },
      {
        id: "6.4.d.3",
        term: "Automated root cause",
        weight: "med",
        info: "<p><strong>Automated root-cause analysis (RCA)</strong> is the step after correlation: once events are grouped, the AIOps engine tries to identify <em>why</em> the incident happened. It goes from 'something is wrong in this region' to 'uplink Gi1/0/24 on dist-sw-3 is flapping, causing the downstream OSPF adjacencies to drop, which is the cause of 47 correlated alerts'.</p><p><strong>How the engine decides:</strong></p><ul><li><strong>Topology awareness</strong> — the system knows which device depends on which. A failure at the top of the dependency graph is a more likely root cause than a downstream symptom.</li><li><strong>Temporal ordering</strong> — the first event in a correlated group is often closer to the root cause than later ones.</li><li><strong>Historical pattern match</strong> — 'this event mix is 90% similar to incident #12847 from 3 months ago, root cause was X'.</li><li><strong>Change correlation</strong> — did someone push a config in the last hour? A recent change is a strong root-cause candidate.</li><li><strong>Telemetry corroboration</strong> — counters (errors, drops, temperature) on the suspected device confirming the hypothesis.</li></ul><p><strong>Why 'automated' matters:</strong> humans can do root-cause analysis — but it takes training, time, and composure under pressure. A Level 1 NOC engineer at 3 AM trying to troubleshoot a fabric-wide outage is unlikely to arrive at the right cause quickly. Automated RCA surfaces a prioritized candidate list in seconds, letting humans focus on validating and fixing.</p><p><strong>Limits and cautions:</strong> RCA is probabilistic. It can be confidently wrong, especially in rare or novel incidents. Mature operations treat automated RCA as a <strong>starting point</strong>, not a final verdict. Operators still validate before acting on high-risk remediations.</p><p><strong>Cisco concrete example:</strong> Catalyst Center Assurance root-cause analysis for wireless issues is the most CCNA-relevant reference — it will name the failing AP, the contributing channel condition, and the impacted clients, with a confidence score.</p>",
        visual: { type: "packet-flow", params: { nodes: ["Correlated incident", "Topology + timing analysis", "Pattern + change match", "Ranked root-cause candidates", "Operator validates + acts"], color: "#ef4444" } },
        hack: {
          memory: "Automated root-cause = engine picks the most likely WHY after correlation groups the WHAT. Uses topology + timing + history + change data + telemetry. Probabilistic — humans still validate before acting. Cisco concrete = Catalyst Center Assurance RCA.",
          practice: "Chain the three AIOps steps: 500 alerts → correlate into 1 incident → RCA points at dist-sw-3 uplink as likely cause → human confirms and runs failover playbook. End-to-end this flow on paper until it's second nature.",
          effort: "low",
          meta: "Jeremy's IT Lab (updated content). Wendell Odom OCG Chapter 17. Exam: 'automated root cause analysis does what?' = identifies the most likely cause of a correlated incident, with confidence scoring. Cisco example = DNA Center Assurance."
        }
      }
    ]
  },

  // ── 6.5 Describe REST-based APIs ──────────────────────────────

  "6.5.a": {
    info: "<p><strong>REST (Representational State Transfer)</strong> is an architectural style for building APIs that uses standard HTTP protocols. REST APIs are the dominant interface for network automation, SDN controllers (DNA Center, Meraki), and cloud services.</p><p>Core REST principles:</p><ul><li><strong>Stateless</strong> — each request contains ALL information the server needs to process it. The server does not remember previous requests or maintain session state between calls. Every request is independent.</li><li><strong>Client-Server</strong> — separation of concerns between the client (makes requests) and server (processes requests and returns responses). They evolve independently.</li><li><strong>Uniform Interface</strong> — resources are identified by standardized <strong>URIs (Uniform Resource Identifiers)</strong>. Example: <code>/api/v1/devices/123</code> identifies device 123. The URI structure is consistent and predictable.</li><li><strong>Cacheable</strong> — responses can be cached by the client for performance. The server indicates whether a response can be cached and for how long.</li><li><strong>Layered System</strong> — clients don't need to know if they're talking to the actual server or an intermediary (load balancer, proxy, cache)</li></ul><p>REST uses standard <strong>HTTP methods</strong> (GET, POST, PUT, PATCH, DELETE) to perform operations on resources identified by URIs. Data is typically exchanged in <strong>JSON format</strong>. This standardization makes REST APIs intuitive and widely supported across programming languages and tools.</p><p>In network automation, REST APIs are used to interact with SDN controllers (DNA Center REST API), cloud platforms (AWS, Azure), and network management tools (Meraki Dashboard API, Cisco vManage).</p>",
    visual: { type: "handshake", params: { leftLabel: "Client", rightLabel: "REST API Server", steps: ["GET /api/v1/devices ->", "<- 200 OK + JSON data", "POST /api/v1/devices (new device) ->", "<- 201 Created + device JSON", "DELETE /api/v1/devices/5 ->", "<- 204 No Content"] } },
    hack: {
      memory: "REST = Stateless + URI + HTTP verbs + JSON. Each request is INDEPENDENT — the server has amnesia between requests. Everything the server needs is in the request itself. URIs identify resources, HTTP verbs define actions.",
      practice: "Use curl or Postman to hit a public REST API (jsonplaceholder.typicode.com). Make GET, POST, PUT, DELETE requests. See how URIs identify resources and verbs define operations.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 62 (REST APIs). Wendell Odom OCG Chapter 18. REST principles are heavily tested. Know: stateless, client-server, URI-based resources, HTTP verbs. Exam question: 'what makes an API RESTful?'",
    },
    micro: [
      { id: "6.5.a.1", term: "REST",                         def: "REpresentational State Transfer. Architecture style for APIs. Uses HTTP methods + URIs + JSON/XML.", weight: "high" },
      { id: "6.5.a.2", term: "Stateless",                    def: "Each request contains all info needed. Server doesn't store client session between requests.", weight: "high" },
      { id: "6.5.a.3", term: "Client-server",                def: "Separation of concerns. Client handles UI; server handles data and logic. Independent evolution.", weight: "high" },
      { id: "6.5.a.4", term: "URI-based resources",          def: "Each resource has a unique URI. /api/v1/devices/42 represents device ID 42. Nouns, not verbs.", weight: "high" }
    ]
  },

  "6.5.b": {
    info: "<p><strong>HTTP verbs (methods)</strong> tell the server what action to perform on the resource identified by the URI. They map directly to <strong>CRUD (Create, Read, Update, Delete)</strong> operations — the four fundamental data operations.</p><p>HTTP verb to CRUD mapping:</p><ul><li><strong>POST → Create</strong> — creates a new resource. Send data in the request body. Example: <code>POST /api/devices</code> with a JSON body creates a new device. NOT idempotent — calling it twice creates two resources.</li><li><strong>GET → Read</strong> — retrieves data. No request body needed. Example: <code>GET /api/devices/5</code> returns device 5's data. <strong>Safe</strong> (doesn't modify anything) and <strong>idempotent</strong> (calling it 10 times returns the same result).</li><li><strong>PUT → Update (Replace)</strong> — replaces an entire resource with new data. Example: <code>PUT /api/devices/5</code> with a complete JSON body replaces ALL of device 5's data. Idempotent — calling it twice with the same data has the same result.</li><li><strong>PATCH → Update (Modify)</strong> — modifies specific fields of a resource without replacing the entire thing. Example: <code>PATCH /api/devices/5</code> with <code>{\"hostname\": \"SW5-new\"}</code> changes only the hostname.</li><li><strong>DELETE → Delete</strong> — removes a resource. Example: <code>DELETE /api/devices/5</code> removes device 5. Idempotent — deleting an already-deleted resource returns the same result (gone).</li></ul><p>The URI identifies WHICH resource, the HTTP verb defines WHAT to do with it. This consistent pattern is what makes REST APIs intuitive and predictable.</p>",
    visual: { type: "comparison", params: { left: { label: "HTTP Verb", items: ["GET", "POST", "PUT / PATCH", "DELETE"] }, right: { label: "CRUD Operation", items: ["Read", "Create", "Update", "Delete"] } } },
    hack: {
      memory: "CRUD = Create-Read-Update-Delete. HTTP = POST-GET-PUT-DELETE. Mnemonic: POST Creates, GET Reads, PUT Updates, DELETE Deletes. PGPD matches CRUD. GET is safe (read-only). POST is not idempotent (creates duplicates).",
      practice: "Build a mental table: <code>GET /devices</code> = list all. <code>GET /devices/5</code> = read one. <code>POST /devices</code> = create new. <code>PUT /devices/5</code> = replace entirely. <code>PATCH /devices/5</code> = modify fields. <code>DELETE /devices/5</code> = remove.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. The CRUD-to-HTTP mapping is GUARANTEED on the exam. Memorize cold: POST=Create, GET=Read, PUT/PATCH=Update, DELETE=Delete.",
    },
    micro: [
      { id: "6.5.b.1", term: "POST = Create",                def: "Add a new resource. 'POST /devices' creates a device. Server assigns ID.", weight: "high" },
      { id: "6.5.b.2", term: "GET = Read",                   def: "Retrieve resource(s). 'GET /devices' lists all. 'GET /devices/42' gets one. Read-only, no side effects.", weight: "high" },
      { id: "6.5.b.3", term: "PUT = Update (full)",          def: "Replace an existing resource entirely. 'PUT /devices/42' overwrites device 42.", weight: "high" },
      { id: "6.5.b.4", term: "PATCH = Update (partial)",     def: "Modify specific fields. 'PATCH /devices/42' updates only the specified attributes.", weight: "high" },
      { id: "6.5.b.5", term: "DELETE = Delete",              def: "Remove resource. 'DELETE /devices/42' destroys the device entry.", weight: "high" }
    ]
  },

  "6.5.c": {
    info: "<p><strong>HTTP status codes</strong> are three-digit numbers returned by the server indicating the result of an API request. They follow a consistent pattern by category:</p><ul><li><strong>2xx — Success:</strong><ul><li><strong>200 OK</strong> — request succeeded. For GET: data returned. For PUT/PATCH: resource updated.</li><li><strong>201 Created</strong> — new resource successfully created (typical POST response)</li><li><strong>204 No Content</strong> — request succeeded but no data to return (typical DELETE response)</li></ul></li><li><strong>3xx — Redirection:</strong><ul><li><strong>301 Moved Permanently</strong> — resource has a new URI</li><li><strong>304 Not Modified</strong> — cached version is still valid</li></ul></li><li><strong>4xx — Client Error</strong> (YOUR fault):<ul><li><strong>400 Bad Request</strong> — malformed request, invalid JSON, missing required fields</li><li><strong>401 Unauthorized</strong> — authentication required or credentials are wrong</li><li><strong>403 Forbidden</strong> — authenticated but NOT authorized for this resource (you proved who you are, but you don't have permission)</li><li><strong>404 Not Found</strong> — the requested resource doesn't exist</li><li><strong>405 Method Not Allowed</strong> — the HTTP verb isn't supported for this URI</li></ul></li><li><strong>5xx — Server Error</strong> (THEIR fault):<ul><li><strong>500 Internal Server Error</strong> — generic server-side failure</li><li><strong>503 Service Unavailable</strong> — server is temporarily down (maintenance, overloaded)</li></ul></li></ul><p>Pattern to memorize: <strong>2xx = success, 4xx = client error, 5xx = server error</strong>. The first digit tells you the category immediately.</p>",
    visual: { type: "layer-stack", params: { layers: ["2xx: Success (200 OK, 201 Created)", "3xx: Redirection", "4xx: Client Error (400, 401, 403, 404)", "5xx: Server Error (500)"], highlight: 0 } },
    hack: {
      memory: "2xx = success (Too good). 4xx = YOUR fault (client error — FOURbidden). 5xx = THEIR fault (server error — FIVE alarm fire). Key codes: 200=OK, 201=Created, 400=Bad Request, 401=Unauthorized, 403=Forbidden, 404=Not Found, 500=Server Error.",
      practice: "Use Postman to intentionally trigger different codes: valid GET (200), POST to create (201), malformed JSON body (400), wrong API key (401), non-existent resource (404). See each code in practice.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. The exam tests specific codes: 200, 201, 400, 401, 403, 404, 500. Know what each means and whether it's client (4xx) or server (5xx) error.",
    },
    micro: [
      { id: "6.5.c.1", term: "200 OK",                       def: "Success. GET/PUT/DELETE returned what was expected.", weight: "high" },
      { id: "6.5.c.2", term: "201 Created",                  def: "POST succeeded. New resource was created. Response often includes location header with new URI.", weight: "high" },
      { id: "6.5.c.3", term: "400 Bad Request",              def: "Client sent malformed request. Syntax or validation error.", weight: "high" },
      { id: "6.5.c.4", term: "401 Unauthorized",             def: "Missing or invalid credentials. Authentication required.", weight: "high" },
      { id: "6.5.c.5", term: "403 Forbidden",                def: "Authenticated but NOT authorized. Valid creds but not allowed to access this resource.", weight: "high" },
      { id: "6.5.c.6", term: "404 Not Found",                def: "Resource doesn't exist at that URI.", weight: "high" },
      { id: "6.5.c.7", term: "500 Internal Server Error",    def: "Server-side failure. Not client's fault — server bug or crash.", weight: "high" },
      { id: "6.5.c.8", term: "4xx vs 5xx",                   def: "4xx = CLIENT error (you sent bad request). 5xx = SERVER error (server failed to handle valid request).", weight: "high" }
    ]
  },

  "6.5.d": {
    info: "<p><strong>REST API authentication</strong> verifies the identity of the client making the API request. Three common methods, increasing in complexity and security:</p><ul><li><strong>API Key</strong> — a simple unique string (token) that identifies the caller. Sent as a header (<code>X-API-Key: abc123</code>) or query parameter (<code>?api_key=abc123</code>). Simple to implement but provides identification only — no authorization framework. If the key is leaked, it must be regenerated. Common for public APIs with basic access control.</li><li><strong>Bearer Token</strong> — an access token (often a <strong>JWT — JSON Web Token</strong>) sent in the Authorization header: <code>Authorization: Bearer eyJhbGc...</code>. The client first authenticates (username/password) to receive the token, then includes it with every subsequent request. Tokens typically expire after a set period. More secure than API keys because tokens are time-limited.</li><li><strong>OAuth 2.0</strong> — a full authorization framework where users <strong>grant limited access to their resources without sharing their credentials</strong>. Uses a flow of authorization codes, access tokens, and refresh tokens. The client never sees the user's password — it receives a scoped, time-limited access token. Most secure and flexible, commonly used by enterprise APIs (Google, Microsoft, Cisco).</li></ul><p>For the CCNA exam, understand what each method is and when to use it: API key = simplest (basic identification). Bearer token = common for session-based access. OAuth 2.0 = enterprise delegated authorization.</p>",
    visual: { type: "hierarchy", params: { root: "API Authentication", children: [{ name: "API Key", children: [{ name: "Simple header/query param" }] }, { name: "Bearer Token", children: [{ name: "Authorization: Bearer <token>" }] }, { name: "OAuth 2.0", children: [{ name: "Token exchange flow" }] }] } },
    hack: {
      memory: "API Key = library card (simple ID). Bearer Token = security badge (time-limited access). OAuth 2.0 = company SSO (delegated authorization, no password sharing). Complexity and security increase left to right.",
      practice: "Get a free API key from a public API (OpenWeatherMap, GitHub). Make requests with it in the header. Then try a Bearer token with the GitHub API after authenticating. See both in action.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. Know what each method is and when to use it. API key = simplest. Bearer token = session-based. OAuth 2.0 = enterprise delegated access.",
    },
    micro: [
      { id: "6.5.d.1", term: "API key",                      def: "Static secret string sent in header. Simplest auth. Fine for internal or low-sensitivity APIs.", weight: "high" },
      { id: "6.5.d.2", term: "Bearer token",                 def: "Token obtained after login, sent in 'Authorization: Bearer [token]' header. Session-based.", weight: "high" },
      { id: "6.5.d.3", term: "OAuth 2.0",                    def: "Delegated access. User grants limited permissions to third-party apps without sharing password.", weight: "high" },
      { id: "6.5.d.4", term: "Basic Auth",                   def: "Base64-encoded username:password in header. Not encryption — must be combined with HTTPS. Legacy.", weight: "med" }
    ]
  },

  "6.5.e": {
    info: "<p>REST APIs exchange data in structured formats. The CCNA tests three formats, with <strong>JSON being the primary focus</strong>:</p><ul><li><strong>JSON (JavaScript Object Notation)</strong> — lightweight, human-readable, and the most common format for REST APIs and network automation. Uses curly braces <code>{ }</code> for objects and square brackets <code>[ ]</code> for arrays. Example: <code>{\"hostname\": \"SW1\", \"vlans\": [10, 20]}</code>. JSON is the default for nearly all modern REST APIs, SDN controllers, and cloud platforms.</li><li><strong>XML (eXtensible Markup Language)</strong> — more verbose, uses angle-bracket tags like HTML. Example: <code>&lt;hostname&gt;SW1&lt;/hostname&gt;</code>. Common in older SOAP APIs and <strong>NETCONF</strong> (which uses XML with YANG data models). XML supports schemas for strict validation but is harder to read than JSON.</li><li><strong>YAML (YAML Ain't Markup Language)</strong> — the most human-readable format, uses indentation instead of brackets or tags. Example: <code>hostname: SW1</code>. Common in <strong>Ansible playbooks</strong>, Docker Compose files, Kubernetes manifests, and configuration files. YAML is a superset of JSON — valid JSON is valid YAML.</li></ul><p>Quick identification: <strong>JSON = curly braces { }</strong>. <strong>XML = angle brackets < ></strong>. <strong>YAML = indentation (no brackets)</strong>. The exam may show a snippet and ask you to identify the format.</p><p>Format-to-tool mapping: JSON → REST APIs, SDN controllers. XML → NETCONF, SOAP. YAML → Ansible, Docker, Kubernetes.</p>",
    visual: { type: "comparison", params: { left: { label: "JSON", items: ["{ \"key\": \"value\" }", "Lightweight, most common", "REST APIs standard"] }, right: { label: "XML / YAML", items: ["<key>value</key>", "key: value (YAML)", "NETCONF / Ansible"] } } },
    hack: {
      memory: "JSON = curly braces { }. XML = angle brackets < >. YAML = indentation (no brackets). JSON → REST APIs. XML → NETCONF. YAML → Ansible. See brackets? JSON. See tags? XML. See indentation? YAML.",
      practice: "Write the same data (hostname: SW1, IP: 10.0.0.1, VLANs: 10, 20) in all three formats. JSON: <code>{\"hostname\":\"SW1\"}</code>. XML: <code>&lt;hostname&gt;SW1&lt;/hostname&gt;</code>. YAML: <code>hostname: SW1</code>. Practice identifying format at a glance.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. The exam focuses heavily on JSON. Also know: XML = NETCONF, YAML = Ansible. Be able to identify format by sight.",
    },
    micro: [
      { id: "6.5.e.1", term: "JSON",                         def: "JavaScript Object Notation. Most common REST data format. { } for objects, [ ] for arrays.", weight: "high" },
      { id: "6.5.e.2", term: "XML",                          def: "Extensible Markup Language. Used by NETCONF. Tag-based: <tag>value</tag>. More verbose than JSON.", weight: "high" },
      { id: "6.5.e.3", term: "YAML",                         def: "YAML Ain't Markup Language. Human-friendly. Used by Ansible playbooks. Indentation-based.", weight: "high" },
      { id: "6.5.e.4", term: "Identify by sight",            def: "{ } JSON. < /> XML. --- + indented lines = YAML. Exam may show a snippet and ask format.", weight: "high" }
    ]
  },

  "6.5.f": {
    info: "<p><strong>CRUD (Create, Read, Update, Delete)</strong> represents the four fundamental operations you can perform on data. REST APIs implement CRUD through HTTP verbs, creating a consistent and predictable pattern for interacting with network resources.</p><p>The complete CRUD-to-REST mapping with examples:</p><ul><li><strong>Create = POST</strong> — <code>POST /api/devices</code> with body <code>{\"hostname\": \"SW5\", \"ip\": \"10.0.0.5\"}</code> → creates a new device. Returns <code>201 Created</code> with the new resource.</li><li><strong>Read = GET</strong> — <code>GET /api/devices</code> returns all devices. <code>GET /api/devices/5</code> returns just device 5. No request body needed. Returns <code>200 OK</code> with JSON data.</li><li><strong>Update = PUT or PATCH</strong> — <code>PUT /api/devices/5</code> with a complete JSON body <strong>replaces the entire resource</strong>. <code>PATCH /api/devices/5</code> with partial data <strong>modifies only the specified fields</strong>. Returns <code>200 OK</code>.</li><li><strong>Delete = DELETE</strong> — <code>DELETE /api/devices/5</code> removes device 5. Returns <code>204 No Content</code>.</li></ul><p>The pattern is always: the <strong>URI identifies WHICH resource</strong> (<code>/api/devices/5</code>), and the <strong>HTTP verb defines WHAT to do</strong> (GET it, DELETE it, PUT to replace it). This consistent mapping is what makes REST APIs intuitive — once you understand the pattern, you can predict how any REST API works.</p><p>In network automation, CRUD maps to real operations: Create a VLAN (POST), Read the routing table (GET), Update an interface description (PATCH), Delete an ACL (DELETE).</p>",
    visual: { type: "comparison", params: { left: { label: "CRUD", items: ["Create", "Read", "Update (full)", "Update (partial)", "Delete"] }, right: { label: "REST", items: ["POST /api/devices", "GET /api/devices/1", "PUT /api/devices/1", "PATCH /api/devices/1", "DELETE /api/devices/1"] } } },
    hack: {
      memory: "CRUD = the four things you do with data (Create, Read, Update, Delete). REST = how you do them over HTTP (POST, GET, PUT/PATCH, DELETE). Same concept, different vocabulary. URI = which resource. Verb = what action.",
      practice: "Use Postman or curl against jsonplaceholder.typicode.com: <code>POST /posts</code> (create), <code>GET /posts/1</code> (read), <code>PUT /posts/1</code> (update), <code>DELETE /posts/1</code> (delete). See all four CRUD operations in action.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. The exam shows API calls and asks 'what CRUD operation is this?' or vice versa. Practice translating both directions instantly.",
    },
    micro: [
      { id: "6.5.f.1", term: "API call format",              def: "HTTP verb + URI + headers + body. Example: 'POST /api/devices' + JSON body = create a device.", weight: "high" },
      { id: "6.5.f.2", term: "CRUD ↔ HTTP mapping drill",    def: "Given 'PUT /devices/42' → Update. Given 'Create a device' → POST /devices. Practice both directions.", weight: "high" }
    ]
  },

  // ── 6.6 Recognize configuration management ────────────────────

  "6.6.a": {
    info: "<p><strong>Ansible</strong> is an agentless automation tool developed by Red Hat that has become the <strong>most popular tool for network automation</strong>. It connects to devices via <strong>SSH</strong> (or NETCONF for modern network devices) and <strong>pushes</strong> configurations — no software needs to be installed on managed devices.</p><p>Ansible key characteristics:</p><ul><li><strong>Agentless</strong> — no agent software needs to be installed on managed devices. Ansible connects via SSH (which is already available on all network devices). This is a massive advantage over Puppet/Chef which require agents.</li><li><strong>Push model</strong> — the Ansible control node initiates connections and pushes configurations to devices. The admin decides when changes happen (on-demand), unlike pull-based tools where devices periodically check in.</li><li><strong>YAML playbooks</strong> — configurations are defined in human-readable YAML files called playbooks. Even non-programmers can read and understand what a playbook does.</li><li><strong>Idempotent</strong> — running a playbook twice produces the same result. If a device already has the desired configuration, Ansible makes no changes. This is a critical safety feature — you can safely re-run playbooks without fear of breaking things.</li><li><strong>Python-based</strong> — written in Python, uses Python libraries for network connectivity (Paramiko for SSH, NAPALM for multi-vendor abstraction)</li></ul><p>Ansible is preferred for network automation because: network devices already have SSH (no agent install needed), network engineers can read YAML (low learning curve), and the push model gives explicit control over when changes are applied.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Ansible Control Node", "SSH Push", "Network Devices"], color: "#ef4444" } },
    hack: {
      memory: "Ansible = Agentless + SSH + YAML + Push + Idempotent. No agents on devices, just SSH in and configure. YAML playbooks = recipes anyone can read. Run twice = same result (idempotent).",
      practice: "Use the Cisco DevNet Sandbox or install Ansible locally. Write a playbook that sets the hostname on a network device. Run it twice and verify idempotency — second run shows 'ok' (no changes).",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 61-62 (Ansible). Wendell Odom OCG Chapter 19-20. Ansible is THE most tested config management tool. Know: agentless, SSH, YAML, push model, idempotent. Compare against Puppet/Chef (agent-based, pull).",
    },
    micro: [
      { id: "6.6.a.1", term: "Ansible",                      def: "Config management tool. AGENTLESS (uses SSH). YAML playbooks. PUSH model. Idempotent.", weight: "high" },
      { id: "6.6.a.2", term: "Agentless",                    def: "No software installed on managed devices. Uses SSH/WinRM. Network-friendly.", weight: "high" },
      { id: "6.6.a.3", term: "Idempotent",                   def: "Running a playbook multiple times produces the same result. Safe to re-run.", weight: "high" },
      { id: "6.6.a.4", term: "Ansible vs Puppet/Chef",       def: "Ansible = agentless, push, YAML. Puppet/Chef = agent-based, pull, Ruby DSL.", weight: "high" }
    ]
  },

  "6.6.b": {
    info: "<p><strong>Ansible's key components</strong> work together as a structured automation framework:</p><ul><li><strong>Inventory</strong> — a file listing all managed devices (hosts), organized into groups. Can be in INI or YAML format. Groups allow you to target subsets of devices (e.g., 'all_switches,' 'core_routers,' 'site_A'). Example:<br><code>[access_switches]</code><br><code>sw1 ansible_host=10.0.0.1</code><br><code>sw2 ansible_host=10.0.0.2</code></li><li><strong>Playbooks</strong> — YAML files containing ordered lists of tasks (called plays) to execute against inventory hosts. Each task calls a module with specific parameters. Playbooks are the 'recipes' that define what changes to make. They run top-to-bottom, task by task.</li><li><strong>Modules</strong> — pre-built units of code for specific tasks. Cisco network modules include: <code>ios_config</code> (push configuration lines), <code>ios_command</code> (run show commands), <code>ios_facts</code> (gather device facts), <code>nxos_config</code> (for Nexus). Modules abstract the complexity of device interaction.</li><li><strong>Roles</strong> — reusable, organized collections of playbooks, templates, variables, and handlers packaged for sharing. Example: a 'base_hardening' role that applies security baseline to any switch.</li><li><strong>Templates (Jinja2)</strong> — files with variables that generate device-specific configurations. One template + different variables = unique config per device.</li></ul><p>The workflow: <strong>Inventory</strong> (who to configure) + <strong>Playbook</strong> (what to do) + <strong>Modules</strong> (how to do it) + <strong>Templates</strong> (device-specific configs) = complete automation.</p>",
    visual: { type: "hierarchy", params: { root: "Ansible", children: [{ name: "Inventory", children: [{ name: "Hosts & groups" }] }, { name: "Playbooks", children: [{ name: "YAML task lists" }] }, { name: "Modules", children: [{ name: "ios_config, nxos_config" }] }, { name: "Roles", children: [{ name: "Reusable packages" }] }] } },
    hack: {
      memory: "IPMRT: Inventory (who to configure), Playbooks (what to do), Modules (how to do it), Roles (reusable packages), Templates (device-specific configs). Inventory = guest list. Playbooks = instructions. Modules = tools.",
      practice: "Create an Ansible directory: inventory file (3 devices in 2 groups), a playbook with 2 tasks using <code>ios_command</code> module. Run <code>ansible-playbook -i inventory playbook.yml</code>. See how components connect.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 61-62. Wendell Odom OCG Chapter 19-20. The exam tests component definitions: inventory = device list, playbook = task sequence, module = action unit, role = reusable bundle.",
    },
    micro: [
      { id: "6.6.b.1", term: "Inventory",                    def: "List of managed devices. Can be static file or dynamic (cloud API). Groups devices by role/env.", weight: "high" },
      { id: "6.6.b.2", term: "Playbook",                     def: "YAML file defining a sequence of tasks to run against devices. The 'what to do'.", weight: "high" },
      { id: "6.6.b.3", term: "Module",                       def: "Individual action (install package, copy file, run command). Playbooks call modules.", weight: "high" },
      { id: "6.6.b.4", term: "Role",                         def: "Reusable bundle of tasks/files/templates. 'role: webserver' applies all web-related config.", weight: "med" }
    ]
  },

  "6.6.c": {
    info: "<p><strong>Terraform</strong> is an <strong>Infrastructure as Code (IaC)</strong> tool by HashiCorp that provisions and manages infrastructure through declarative configuration files written in <strong>HCL (HashiCorp Configuration Language)</strong>.</p><p>Key distinction from Ansible: Ansible is best for <strong>configuring existing devices</strong> (setting hostnames, pushing ACLs). Terraform is best for <strong>creating and destroying infrastructure</strong> (spinning up VMs, creating VPCs, provisioning cloud networks). They solve different problems and are often used together.</p><p>Terraform characteristics:</p><ul><li><strong>Declarative</strong> — you define the desired end state ('I want 3 VMs with these specs'), and Terraform figures out what actions are needed to reach that state. Contrast with Ansible's procedural approach ('do step 1, then step 2, then step 3').</li><li><strong>State file</strong> — Terraform maintains a <code>terraform.tfstate</code> file that tracks the current state of all managed resources. This is how Terraform knows what exists, what needs to be created, and what needs to be destroyed.</li><li><strong>Provider-based</strong> — providers are plugins that interface with specific platforms (AWS, Azure, GCP, Cisco ACI, VMware). Each provider exposes resources that Terraform can manage.</li><li><strong>Plan before apply</strong> — <code>terraform plan</code> shows exactly what changes will be made before <code>terraform apply</code> executes them. This preview step is a critical safety feature.</li><li><strong>Lifecycle management</strong> — Terraform manages the full lifecycle: create → update → destroy. It can tear down entire environments as easily as it creates them.</li></ul>",
    visual: { type: "comparison", params: { left: { label: "Ansible", items: ["Configuration management", "Push model, agentless", "YAML playbooks", "Procedural approach"] }, right: { label: "Terraform", items: ["Infrastructure provisioning", "Declarative state model", "HCL language", "State file tracking"] } } },
    hack: {
      memory: "Terraform = the ARCHITECT that builds the building (infrastructure). Ansible = the ELECTRICIAN that wires it (configuration). Terraform CREATES resources; Ansible CONFIGURES them. Complementary, not competing.",
      practice: "Read a simple Terraform HCL file that creates a cloud VM. Compare to an Ansible playbook that configures a switch. See the difference: provisioning (Terraform) vs configuration (Ansible).",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 19-20. Terraform vs Ansible: Terraform = create/destroy infrastructure (IaC, declarative, state file). Ansible = configure existing devices (agentless, procedural, SSH).",
    },
    micro: [
      { id: "6.6.c.1", term: "Terraform",                    def: "Infrastructure as Code tool. CREATES/DESTROYS cloud resources. HCL language. Declarative. Maintains state file.", weight: "high" },
      { id: "6.6.c.2", term: "IaC (Infrastructure as Code)", def: "Define infrastructure in code files. Version-controlled, repeatable, reviewable. Terraform is the canonical IaC tool.", weight: "high" },
      { id: "6.6.c.3", term: "State file (Terraform)",       def: "Tracks what resources Terraform manages. Required to plan and apply changes correctly.", weight: "med" },
      { id: "6.6.c.4", term: "Declarative vs procedural",    def: "Declarative (Terraform): describe end state. Procedural (Ansible): describe steps to reach state.", weight: "high" }
    ]
  },

  "6.6.d": {
    info: "<p>The <strong>Terraform workflow</strong> has four stages that execute in a specific order. Understanding this workflow is testable on the CCNA:</p><ul><li><strong>1. terraform init</strong> — initializes the working directory. Downloads required provider plugins (AWS, Azure, Cisco) and sets up the backend for state storage. Run once when starting a new project or after adding a new provider.</li><li><strong>2. terraform plan</strong> — creates an execution plan (dry run). Shows EXACTLY what changes Terraform will make: resources to create (+), modify (~), or destroy (-). <strong>No actual changes are made.</strong> This preview is the critical safety step — review the plan before proceeding.</li><li><strong>3. terraform apply</strong> — executes the planned changes. Creates, modifies, or destroys resources to match the desired state in the configuration files. Updates the state file to reflect the new reality. Requires confirmation (yes/no) unless <code>-auto-approve</code> is used.</li><li><strong>4. terraform destroy</strong> — tears down ALL resources managed by the Terraform configuration. Used for cleanup, decommissioning environments, or starting over. Also supports targeted destroy of individual resources.</li></ul><p>The <code>plan</code> stage is what makes Terraform safe for infrastructure management — you always see exactly what will happen before committing. In a team environment, the plan output is often shared for peer review before anyone runs <code>apply</code>.</p>",
    visual: { type: "state-machine", params: { states: ["terraform init", "terraform plan", "terraform apply", "terraform destroy"], active: 2, transitions: true } },
    hack: {
      memory: "Init, Plan, Apply, Destroy = IPAD. Init = download plugins. Plan = dry run (preview). Apply = execute changes. Destroy = tear it all down. ALWAYS plan before apply — the plan is your safety net.",
      practice: "Walk through the workflow: <code>terraform init</code> (setup) → <code>terraform plan</code> (see what will change) → <code>terraform apply</code> (make it happen) → <code>terraform destroy</code> (clean up). Know the four stages in order.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 19-20. The four Terraform stages in order: init → plan → apply → destroy. Plan is the safety net — always review before apply.",
    },
    micro: [
      { id: "6.6.d.1", term: "terraform init",               def: "Initialize working directory. Downloads provider plugins. First command you run.", weight: "high" },
      { id: "6.6.d.2", term: "terraform plan",               def: "Preview changes. Shows what Terraform WOULD do. Safety net — always review before apply.", weight: "high" },
      { id: "6.6.d.3", term: "terraform apply",              def: "Execute the plan. Creates/modifies/destroys resources to match config.", weight: "high" },
      { id: "6.6.d.4", term: "terraform destroy",            def: "Tear down all managed resources. Useful for test environments. Irreversible.", weight: "high" }
    ]
  },

  "6.6.e": {
    info: "<p><strong>Ansible vs Terraform</strong> — they solve different problems and are often used together in the same workflow:</p><ul><li><strong>Ansible = Configuration Management</strong><ul><li>Best for: configuring existing devices (hostnames, ACLs, VLANs, NTP, banners)</li><li>Model: <strong>procedural</strong> ('do step 1, then step 2, then step 3') and <strong>push</strong> (control node pushes to devices)</li><li>Connection: <strong>agentless via SSH</strong></li><li>Language: <strong>YAML playbooks</strong></li><li>Example: 'Configure OSPF on all core routers'</li></ul></li><li><strong>Terraform = Infrastructure Provisioning</strong><ul><li>Best for: creating and destroying infrastructure (VMs, cloud networks, load balancers, VPCs)</li><li>Model: <strong>declarative</strong> ('I want this end state') and <strong>state-based</strong> (tracks what exists via state file)</li><li>Connection: <strong>provider plugins</strong> (API-based)</li><li>Language: <strong>HCL (HashiCorp Configuration Language)</strong></li><li>Example: 'Create 50 VMs in AWS with these specs'</li></ul></li></ul><p>Many organizations use <strong>both tools together</strong>: Terraform creates the infrastructure (VMs, networks, cloud resources), then Ansible configures the devices running on that infrastructure (OS settings, applications, network configs). Terraform BUILDS the house; Ansible FURNISHES it.</p><p>Exam scenarios: 'Deploy 50 VMs in AWS' = Terraform. 'Configure OSPF on 50 routers' = Ansible. 'Create a VPC with subnets' = Terraform. 'Push ACLs to all switches' = Ansible.</p>",
    visual: { type: "comparison", params: { left: { label: "Ansible", items: ["Config management", "Procedural (do this, then this)", "Agentless / SSH", "YAML playbooks", "Best for: network device config"] }, right: { label: "Terraform", items: ["Infrastructure provisioning", "Declarative (desired state)", "State file tracking", "HCL language", "Best for: cloud infra creation"] } } },
    hack: {
      memory: "Terraform BUILDS the house (infrastructure). Ansible FURNISHES it (configuration). Use both together: Terraform creates VMs → Ansible configures them. Infrastructure creation = Terraform. Device config = Ansible.",
      practice: "Scenario drill: 'Deploy 50 VMs in AWS' = Terraform. 'Configure OSPF on 50 routers' = Ansible. 'Create a VPC with subnets' = Terraform. 'Push ACLs to switches' = Ansible. 'Destroy a test environment' = Terraform.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 19-20. The exam gives scenarios and asks which tool. Infrastructure creation = Terraform. Device configuration = Ansible. They're complementary, not competing.",
    },
    micro: [
      { id: "6.6.e.1", term: "Tool selection scenario",      def: "Create VMs, VPCs, cloud infra → Terraform. Configure existing routers/servers → Ansible.", weight: "high" },
      { id: "6.6.e.2", term: "Complementary use",            def: "Real workflow: Terraform creates VMs → Ansible configures them. Each tool's strength.", weight: "high" }
    ]
  },

  "6.6.f": {
    info: "<p><strong>Puppet and Chef</strong> are agent-based configuration management tools that use a <strong>pull model</strong>. They represent a fundamentally different architecture from Ansible's agentless push model.</p><p>How Puppet/Chef work:</p><ul><li>An <strong>agent</strong> is installed on each managed device</li><li>The agent <strong>periodically checks in</strong> with a central server (Puppet Master / Chef Server) — typically every 30 minutes</li><li>The server provides the <strong>desired configuration</strong> for that device</li><li>The agent <strong>applies any differences</strong> between the current state and desired state locally</li><li>If someone manually changes a configuration, the agent <strong>automatically reverts it</strong> on the next check-in — enforcing desired state continuously</li></ul><p>Puppet vs Chef differences:</p><ul><li><strong>Puppet</strong> — uses a <strong>declarative DSL</strong> (Puppet language) to define desired state. You describe WHAT the system should look like, not HOW to get there.</li><li><strong>Chef</strong> — uses <strong>Ruby-based recipes</strong> with a more procedural approach. Recipes describe the steps to reach the desired state.</li></ul><p>Comparison with Ansible:</p><ul><li><strong>Ansible (Push, Agentless)</strong> — admin pushes configs on-demand via SSH. No agent needed. YAML playbooks. Most popular for network automation.</li><li><strong>Puppet/Chef (Pull, Agent-based)</strong> — devices pull configs periodically from a server. Agent required on each device. More common for server/OS management than network devices.</li></ul><p>Puppet and Chef are less common for network automation because installing agents on network devices (routers, switches) is often impractical or impossible. Ansible's agentless SSH approach is better suited for networking.</p>",
    visual: { type: "comparison", params: { left: { label: "Ansible (Push)", items: ["Agentless — SSH", "Runs on demand", "YAML playbooks", "Most popular for networking"] }, right: { label: "Puppet/Chef (Pull)", items: ["Agent installed on device", "Periodic pull from server", "DSL / Ruby recipes", "More common for servers"] } } },
    hack: {
      memory: "Push (Ansible) = YOU send the config on-demand via SSH. Pull (Puppet/Chef) = DEVICE asks for config periodically from server. Ansible = agentless. Puppet/Chef = agent required. Ansible = YAML. Puppet = DSL. Chef = Ruby.",
      practice: "Build a 3-column comparison table from memory: Ansible (agentless, push, SSH, YAML, network-friendly) vs Puppet (agent, pull, DSL, server-focused) vs Chef (agent, pull, Ruby, server-focused). Drill until instant recall.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 61-62. Wendell Odom OCG Chapter 19-20. The exam tests: push vs pull, agent vs agentless. Ansible = agentless push (most popular for networking). Puppet/Chef = agent-based pull (servers).",
    },
    micro: [
      { id: "6.6.f.1", term: "Push model",                   def: "Server pushes config to managed devices on demand. Ansible uses push. Immediate application.", weight: "high" },
      { id: "6.6.f.2", term: "Pull model",                   def: "Agent on device periodically pulls config from master. Puppet/Chef use pull. Continuous reconciliation.", weight: "high" },
      { id: "6.6.f.3", term: "Agent vs agentless",           def: "Agent = software running on device. Agentless = remote connection (SSH/WinRM). Ansible is agentless.", weight: "high" }
    ]
  },

  // ── 6.7 Recognize JSON-encoded data ───────────────────────────

  "6.7.a": {
    info: "<p>A <strong>JSON object</strong> is enclosed in <strong>curly braces <code>{ }</code></strong> and contains <strong>key-value pairs</strong> separated by commas. It is the fundamental building block of JSON data — similar to a Python dictionary or JavaScript object.</p><p>JSON object rules:</p><ul><li><strong>Keys must be strings in double quotes</strong> — <code>\"hostname\"</code> is valid, <code>hostname</code> without quotes is NOT</li><li><strong>Values can be any JSON type</strong> — strings, numbers, booleans, null, arrays, or nested objects</li><li><strong>Pairs are separated by commas</strong> — but no trailing comma after the last pair</li><li><strong>Key-value pairs are separated by colons</strong> — <code>\"key\": \"value\"</code></li></ul><p>Example JSON object representing a network device:</p><p><code>{<br>&nbsp;&nbsp;\"hostname\": \"SW1\",<br>&nbsp;&nbsp;\"ip\": \"10.0.0.1\",<br>&nbsp;&nbsp;\"model\": \"C9300\",<br>&nbsp;&nbsp;\"vlans\": [10, 20, 30],<br>&nbsp;&nbsp;\"managed\": true<br>}</code></p><p>This object contains five key-value pairs: a string (<code>hostname</code>), another string (<code>ip</code>), a string (<code>model</code>), an array (<code>vlans</code>), and a boolean (<code>managed</code>). Note how different value types coexist in the same object.</p><p>JSON objects are used everywhere in network automation: REST API responses from DNA Center, device inventory data, configuration templates, and SDN controller communications. Being able to read JSON fluently is essential for the CCNA.</p>",
    visual: { type: "encapsulation", params: { layers: [{ label: "{ } JSON Object (curly braces)", color: "#f59e0b" }, { label: "\"key\": \"value\" pairs", color: "#3b82f6" }, { label: "Separated by commas", color: "#10b981" }] } },
    hack: {
      memory: "Curly braces { } = Object = key:value pairs. Think of { } as a labeled container. Every key MUST be a string in double quotes. Values can be any JSON type. Commas between pairs, colon between key and value.",
      practice: "Write a JSON object representing a network device: hostname, management IP, model, serial number, VLAN list, enabled status. Validate it at jsonlint.com. Get comfortable with the syntax.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. The exam shows JSON snippets and asks you to identify objects vs arrays or extract specific values. Practice reading JSON until natural.",
    },
    micro: [
      { id: "6.7.a.1", term: "JSON structure",               def: "Key-value pairs. Keys in quotes. Values: string, number, boolean, null, object, array.", weight: "high" },
      { id: "6.7.a.2", term: "JSON object",                  def: "{ } braces. Key-value pairs inside. Example: {\"name\": \"R1\", \"type\": \"router\"}.", weight: "high" },
      { id: "6.7.a.3", term: "JSON array",                   def: "[ ] brackets. Ordered list of values. Example: [\"R1\", \"R2\", \"R3\"].", weight: "high" }
    ]
  },

  "6.7.b": {
    info: "<p>A <strong>JSON array</strong> is enclosed in <strong>square brackets <code>[ ]</code></strong> and contains an <strong>ordered list of values</strong> separated by commas. Arrays represent collections, lists, and sequences of data.</p><p>JSON array rules:</p><ul><li><strong>Values are ordered</strong> — position matters. The first element is at index 0.</li><li><strong>Values can be any JSON type</strong> — strings, numbers, objects, other arrays, booleans, or null</li><li><strong>Values are separated by commas</strong> — no trailing comma after the last element</li><li><strong>Arrays can contain mixed types</strong> — though in practice they usually hold items of the same type</li></ul><p>Common array patterns in network data:</p><ul><li>Simple value array: <code>\"vlans\": [10, 20, 30, 40]</code></li><li>Array of objects: <code>\"devices\": [{\"name\": \"SW1\", \"ip\": \"10.0.0.1\"}, {\"name\": \"SW2\", \"ip\": \"10.0.0.2\"}]</code></li><li>Nested arrays: <code>\"topology\": [[\"SW1\", \"SW2\"], [\"R1\", \"R2\"]]</code></li></ul><p>Quick identification: <strong><code>{ }</code> = object</strong> (one thing with properties). <strong><code>[ ]</code> = array</strong> (a list of things). This visual distinction is tested on the exam — see curly braces, it's an object; see square brackets, it's an array.</p><p>In API responses, arrays are extremely common: a list of devices, a list of interfaces on a device, a list of VLANs, a list of routes. Any time the API returns multiple items of the same type, they'll be in an array.</p>",
    visual: { type: "encapsulation", params: { layers: [{ label: "[ ] JSON Array (square brackets)", color: "#8b5cf6" }, { label: "Ordered list of values", color: "#3b82f6" }, { label: "Separated by commas", color: "#10b981" }] } },
    hack: {
      memory: "Square brackets [ ] = Array = ordered list. [ ] looks like a shelf holding items in a row. { } = one thing with properties (object). [ ] = many things in a list (array). See brackets? Identify instantly.",
      practice: "Write a JSON array of 3 device objects: <code>[{\"name\":\"SW1\"},{\"name\":\"SW2\"},{\"name\":\"SW3\"}]</code>. Then a simple VLAN array: <code>[10, 20, 30]</code>. Practice both patterns — arrays of objects are the most common API response format.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. Instant ID: { } = object, [ ] = array. If the exam shows curly braces, it's an object. Square brackets = array. Know the visual difference cold.",
    },
    micro: [
      { id: "6.7.b.1", term: "{ } = object",                 def: "Curly braces. Collection of unordered key-value pairs. Access by key.", weight: "high" },
      { id: "6.7.b.2", term: "[ ] = array",                  def: "Square brackets. Ordered list. Access by numeric index (0-based).", weight: "high" },
      { id: "6.7.b.3", term: "Visual identification",        def: "{ → object. [ → array. First character of snippet tells you the type.", weight: "high" }
    ]
  },

  "6.7.c": {
    info: "<p>JSON supports <strong>six data types</strong>. Knowing them is essential for reading API responses and identifying valid vs invalid JSON on the exam.</p><ul><li><strong>String</strong> — text enclosed in <strong>double quotes</strong>. Examples: <code>\"hello\"</code>, <code>\"10.0.0.1\"</code>, <code>\"GigabitEthernet0/1\"</code>. Note: single quotes are NOT valid in JSON.</li><li><strong>Number</strong> — integer or decimal, <strong>no quotes</strong>. Examples: <code>42</code>, <code>3.14</code>, <code>-7</code>, <code>1.5e10</code>. JSON makes no distinction between integer and float — all numbers are the same type.</li><li><strong>Boolean</strong> — <code>true</code> or <code>false</code>, <strong>lowercase only, no quotes</strong>. <code>True</code> (capitalized) is NOT valid JSON. <code>\"true\"</code> (quoted) is a string, not a boolean.</li><li><strong>Null</strong> — represents empty or no value: <code>null</code>, <strong>lowercase, no quotes</strong>. Used when a field exists but has no value assigned.</li><li><strong>Object</strong> — nested <code>{ }</code> with key-value pairs. Allows hierarchical data structures.</li><li><strong>Array</strong> — nested <code>[ ]</code> with ordered values. Allows lists and collections.</li></ul><p>What JSON does NOT have:</p><ul><li><strong>No date/time type</strong> — dates are stored as strings (e.g., <code>\"2024-03-15\"</code>)</li><li><strong>No comments</strong> — <code>//</code> and <code>/* */</code> are NOT allowed in JSON</li><li><strong>No undefined</strong> — that's JavaScript, not JSON. JSON uses <code>null</code> instead.</li><li><strong>No single quotes</strong> — all strings must use double quotes</li></ul>",
    visual: { type: "layer-stack", params: { layers: ["String: \"hello\"", "Number: 42 or 3.14", "Boolean: true / false", "Null: null", "Object: { } (nested)", "Array: [ ] (nested)"], highlight: 0 } },
    hack: {
      memory: "6 types: String (quotes), Number (no quotes), Boolean (true/false, lowercase), Null (null, lowercase), Object ({ }), Array ([ ]). No date type, no comments, no single quotes, no capitalized True/False.",
      practice: "Write one JSON document using all 6 types: <code>{\"name\":\"SW1\", \"vlans\":[10,20], \"uptime\":99.5, \"managed\":true, \"location\":null, \"config\":{\"hostname\":\"SW1\"}}</code>. Validate at jsonlint.com.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. The exam asks 'which is a valid JSON data type?' Know all six. Traps: no 'date' type, no 'undefined,' capitalized True is invalid.",
    },
    micro: [
      { id: "6.7.c.1", term: "6 valid JSON types",           def: "string, number, boolean, null, object, array. Memorize all six.", weight: "high" },
      { id: "6.7.c.2", term: "JSON booleans lowercase",      def: "'true' / 'false' only. 'True' or 'FALSE' = INVALID JSON. Must be lowercase.", weight: "high" },
      { id: "6.7.c.3", term: "No date/undefined in JSON",    def: "JSON has no 'date' type (use string). No 'undefined' (use null). Common exam traps.", weight: "high" }
    ]
  },

  "6.7.d": {
    info: "<p><strong>JSON nesting</strong> allows objects to contain other objects or arrays, and arrays to contain objects or other arrays, to <strong>any depth</strong>. This enables complex hierarchical data structures that represent real-world relationships.</p><p>Common nesting pattern in network automation — a device with nested interfaces:</p><p><code>{<br>&nbsp;&nbsp;\"hostname\": \"SW1\",<br>&nbsp;&nbsp;\"model\": \"C9300\",<br>&nbsp;&nbsp;\"interfaces\": [<br>&nbsp;&nbsp;&nbsp;&nbsp;{<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"name\": \"GigabitEthernet0/1\",<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"ip\": \"10.0.0.1\",<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"status\": \"up\",<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"vlan\": 10<br>&nbsp;&nbsp;&nbsp;&nbsp;},<br>&nbsp;&nbsp;&nbsp;&nbsp;{<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"name\": \"GigabitEthernet0/2\",<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"ip\": \"10.0.0.2\",<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"status\": \"down\",<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;\"vlan\": 20<br>&nbsp;&nbsp;&nbsp;&nbsp;}<br>&nbsp;&nbsp;]<br>}</code></p><p>Accessing nested data using path notation:</p><ul><li><code>device.hostname</code> → <code>\"SW1\"</code></li><li><code>device.interfaces</code> → the entire array of interface objects</li><li><code>device.interfaces[0]</code> → the first interface object (index 0)</li><li><code>device.interfaces[0].name</code> → <code>\"GigabitEthernet0/1\"</code></li><li><code>device.interfaces[1].status</code> → <code>\"down\"</code></li></ul><p>The exam shows nested JSON and asks you to extract a specific value by following the path through the nesting levels. Practice reading nested JSON as following a path: start at the top, go through each level, arrive at the value.</p>",
    visual: { type: "hierarchy", params: { root: "{ device }", children: [{ name: "hostname: \"SW1\"" }, { name: "interfaces: [...]", children: [{ name: "{ name: \"Gi0/1\", ip: \"10.0.0.1\" }" }, { name: "{ name: \"Gi0/2\", ip: \"10.0.0.2\" }" }] }] } },
    hack: {
      memory: "Nesting = Russian dolls. Objects inside objects inside arrays. Read nested JSON by following the path: device → interfaces → [0] → ip. Each arrow goes one level deeper. Arrays use index numbers [0], [1].",
      practice: "Given the nested JSON above, extract: 'What is the VLAN of the second interface?' Path: device → interfaces → [1] → vlan → answer: 20. Practice 5 extractions until the path-following is automatic.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. The exam shows nested JSON and asks for a specific value. Practice tracing paths: object key → array index → nested key → value.",
    },
    micro: [
      { id: "6.7.d.1", term: "Nested JSON",                  def: "Objects inside objects, arrays inside objects, etc. Exam shows nested structure and asks for a value.", weight: "high" },
      { id: "6.7.d.2", term: "Access path",                  def: "Dot notation + brackets. data.devices[0].name = first device's name field.", weight: "high" },
      { id: "6.7.d.3", term: "Tracing strategy",             def: "Read outer keys first → drill down. Object key → array index [N] → nested key → value.", weight: "high" }
    ]
  },

  "6.7.e": {
    info: "<p><strong>Valid JSON</strong> has strict syntax rules. The exam tests your ability to distinguish valid from invalid JSON by spotting common errors.</p><p>JSON syntax rules:</p><ul><li><strong>All keys must be in double quotes</strong> — <code>\"name\"</code> is valid. <code>name</code> (no quotes) or <code>'name'</code> (single quotes) are INVALID.</li><li><strong>Strings use double quotes only</strong> — <code>\"hello\"</code> is valid. <code>'hello'</code> (single quotes) is INVALID.</li><li><strong>No trailing commas</strong> — a comma after the last item is invalid. <code>{\"a\": 1, \"b\": 2,}</code> is INVALID (trailing comma after 2).</li><li><strong>No comments</strong> — <code>//</code> and <code>/* */</code> are NOT allowed in JSON. This is a common mistake for programmers used to JavaScript or Python.</li><li><strong>Booleans are lowercase</strong> — <code>true</code> and <code>false</code> only. <code>True</code>, <code>False</code>, <code>TRUE</code> are all INVALID.</li><li><strong>Null is lowercase</strong> — <code>null</code> only. <code>Null</code>, <code>NULL</code>, <code>None</code> are all INVALID.</li><li><strong>No leading zeros in numbers</strong> — <code>01</code> is INVALID. <code>0.1</code> and <code>1</code> are valid.</li><li><strong>No functions or expressions</strong> — JSON is data only, not code</li></ul><p>Common exam traps (spot the errors):</p><ul><li><code>{'name': 'SW1'}</code> — INVALID (single quotes on both key and value)</li><li><code>{\"vlans\": [10, 20,]}</code> — INVALID (trailing comma in array)</li><li><code>{\"enabled\": True}</code> — INVALID (capitalized True)</li><li><code>{\"hostname\": \"SW1\" // main switch}</code> — INVALID (comment)</li><li><code>{\"count\": 01}</code> — INVALID (leading zero)</li></ul>",
    visual: { type: "comparison", params: { left: { label: "VALID JSON", items: ["{\"name\": \"SW1\"}", "{\"vlans\": [10, 20]}", "{\"enabled\": true}"] }, right: { label: "INVALID JSON", items: ["{'name': 'SW1'} (single quotes)", "{\"vlans\": [10, 20,]} (trailing comma)", "{\"enabled\": True} (capital T)"] } } },
    hack: {
      memory: "JSON validation checklist: DOUBLE quotes only (not single), NO trailing commas, NO comments, lowercase true/false/null (not True/False/Null), no leading zeros on numbers. Check these five things first when debugging.",
      practice: "Write 5 invalid JSON examples and fix each: 1) Single quotes → double. 2) Trailing comma → remove. 3) Comment → remove. 4) Capitalized True → lowercase true. 5) Leading zero 01 → 1. Validate each at jsonlint.com.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 62. Wendell Odom OCG Chapter 18. The exam shows JSON and asks 'which is valid?' Traps: single quotes, trailing commas, comments, capitalized booleans. Know what makes JSON INVALID.",
    },
    micro: [
      { id: "6.7.e.1", term: "JSON uses double quotes",      def: "Strings + keys MUST use double quotes \" \". Single quotes ' ' are INVALID JSON.", weight: "high" },
      { id: "6.7.e.2", term: "No trailing commas",           def: "[1, 2, 3,] is INVALID. Final element cannot have trailing comma. JavaScript allows it; JSON does NOT.", weight: "high" },
      { id: "6.7.e.3", term: "No comments",                  def: "JSON does NOT support // or /* */ comments. Any comment = invalid JSON.", weight: "high" },
      { id: "6.7.e.4", term: "No single quotes / capitalized booleans", def: "'text' INVALID. True/FALSE INVALID. Must be: \"text\", true, false.", weight: "high" },
      { id: "6.7.e.5", term: "Exam JSON validation",         def: "Scan for: double quotes only, no trailing commas, no comments, lowercase booleans. Any violation = invalid.", weight: "high" }
    ]
  }

};
