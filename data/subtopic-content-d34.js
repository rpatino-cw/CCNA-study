/**
 * subtopic-content-d34.js — Expanded study content for CCNA 200-301
 * Domains 3 (IP Connectivity, 25%) and 4 (IP Services, 10%).
 *
 * Each entry: info, visual (animation config), hack (memory/practice/effort/meta).
 * Loaded via <script> tag; attaches to window.subtopicContentD34.
 */
window.subtopicContentD34 = {

  /* ══════════════════════════════════════════════════════════════
     DOMAIN 3 — IP CONNECTIVITY (25%)
  ══════════════════════════════════════════════════════════════ */

  // ── 3.1  Interpret the components of routing table ──────────

  "3.1.a": {
    info: "<p><strong>Routing protocol codes</strong> appear at the beginning of every route entry in the <code>show ip route</code> output and identify the source that taught the router about that network. The most common codes on the CCNA are: <strong>C</strong> (directly connected — the router has an interface in that subnet), <strong>L</strong> (local — the /32 host route for the router's own interface IP), <strong>S</strong> (static — manually configured by an administrator), <strong>O</strong> (OSPF — learned via OSPF), <strong>D</strong> (EIGRP — learned via EIGRP's DUAL algorithm), <strong>R</strong> (RIP — learned via RIP), and <strong>B</strong> (BGP — learned via the Border Gateway Protocol). Additional codes you may encounter include <strong>O IA</strong> (OSPF inter-area), <strong>O E1/E2</strong> (OSPF external type 1 or 2), and <strong>S*</strong> (candidate default static route).</p><p>When you run <code>show ip route</code>, the router prints a legend at the top listing every code present in the current table. A route entry like <code>O 10.1.1.0/24 [110/20] via 192.168.1.2, 00:05:32, GigabitEthernet0/1</code> tells you the route was learned via OSPF (O), the destination network is 10.1.1.0/24, the AD is 110, the metric (cost) is 20, the next-hop address is 192.168.1.2, the route has been in the table for 5 minutes and 32 seconds, and the exit interface is Gi0/1. Reading each component quickly is essential for troubleshooting.</p><p>The <strong>C</strong> and <strong>L</strong> codes appear automatically whenever you configure an IP address on an interface and bring it up. You do not need any routing protocol for these — they are generated by the router itself. The C route covers the entire subnet (e.g., 10.1.1.0/24), while the L route is a /32 for just the interface IP (e.g., 10.1.1.1/32). The L route exists so the router knows that traffic destined for its own IP should be processed locally rather than forwarded.</p><p><strong>Key commands:</strong> <code>show ip route</code> (full table with codes), <code>show ip route connected</code> (only C/L routes), <code>show ip route static</code> (only S routes), <code>show ip route ospf</code> (only O routes). You can filter the table by protocol to isolate specific route sources during troubleshooting.</p>",
    visual: { type: "comparison", params: { left: { label: "IGP Codes", items: ["C — Connected", "S — Static", "O — OSPF", "R — RIP", "D — EIGRP"] }, right: { label: "EGP Codes", items: ["B — BGP", "i — IS-IS", "* — Candidate default", "L — Local", "ia — OSPF inter-area"] } } },
    hack: {
      memory: "CSORD-B: 'Cisco Students Often Ride Dirty Buses' — Connected, Static, OSPF, RIP, EIGRP (D for DUAL), BGP. Also remember L for Local (/32 self-routes) and S* for candidate default.",
      practice: "In Packet Tracer, build a 3-router triangle: R1-R2 use OSPF, R2-R3 use a static route, R1-R3 are directly connected. Run 'show ip route' on R2 and identify every code letter (C, L, O, S). Then add a default route and verify S* appears. Practice reading route entries until you can parse AD, metric, next-hop, and age in under 5 seconds per line.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) and Day 24 (OSPF Part 1) both show routing table output with codes. Wendell Odom OCG Chapters 15-16 cover routing table interpretation in depth. The exam will show you a routing table and ask 'how did the router learn this route?' — instant code recognition is required."
    }
  },

  "3.1.b": {
    info: "<p>The <strong>prefix</strong> (also called the network address or destination network) is the IP network that a routing table entry describes. In the route <code>O 10.1.1.0/24 [110/20] via 192.168.1.2</code>, the prefix is <strong>10.1.1.0</strong>. This is the network portion of the address — all host bits are zeroed out. The prefix tells the router 'this entry covers all IP addresses that fall within this network.'</p><p>When a packet arrives at a router, the router extracts the destination IP address from the packet header and compares it against all prefixes in the routing table. The comparison uses the associated mask (prefix length) to determine which bits of the destination IP must match the prefix. For example, a route to 10.1.1.0/24 matches any destination from 10.1.1.0 through 10.1.1.255 because the first 24 bits must match and the last 8 bits are 'don't care.'</p><p>Prefixes can be more or less specific depending on the mask. A <strong>/8 prefix</strong> like 10.0.0.0/8 covers over 16 million addresses (the entire 10.x.x.x range). A <strong>/24 prefix</strong> like 10.1.1.0/24 covers 256 addresses. A <strong>/32 prefix</strong> like 10.1.1.1/32 covers exactly one address (a host route). This specificity directly impacts the <strong>longest prefix match</strong> algorithm — more specific prefixes always win when multiple routes match a destination.</p><p>In Cisco IOS, the <code>show ip route</code> output groups routes by their parent network. For example, all /24 subnets of 10.0.0.0/8 appear under a parent entry for 10.0.0.0/8. This is cosmetic — the router still evaluates each subnet independently during the lookup process. The prefix and mask together define the exact scope of each route entry.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Packet: dst 10.1.1.50", "Route Table Lookup", "Match: 10.1.1.0/24", "Forward via next-hop"], color: "#3b82f6" } },
    hack: {
      memory: "The prefix is the 'street name' — the network address with all host bits zeroed. 10.1.1.0 is the street, /24 defines how long the street is (256 addresses). The router checks if the destination house number falls on that street.",
      practice: "Pull up 'show ip route' on any router in Packet Tracer. For each route entry, identify the prefix and convert it: write the prefix in binary, circle the network bits (defined by the mask), and verify the host bits are all zeros. Then take 5 random IPs and determine which prefix in the table would match each one.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) and Day 13 (Subnetting) cover prefix interpretation. Wendell Odom OCG Chapters 15-16 explain the routing table structure. The prefix itself is rarely a standalone exam question — it's tested indirectly through 'which route will the router use?' scenarios that require understanding prefix matching."
    }
  },

  "3.1.c": {
    info: "<p>The <strong>network mask</strong> (subnet mask or prefix length) defines the boundary between the network portion and the host portion of an IP address. In routing tables, Cisco IOS displays the mask as a <strong>prefix length</strong> using CIDR notation — for example, /24 instead of 255.255.255.0. The mask tells the router exactly how many leading bits of the destination IP must match the prefix for the route to be considered a match.</p><p>A <strong>/24 mask</strong> means the first 24 bits are network bits (must match the prefix) and the last 8 bits are host bits (can be anything). This covers 256 total addresses (2^8), of which 254 are usable for hosts (subtract network and broadcast). A <strong>/30 mask</strong> has 30 network bits and 2 host bits, covering only 4 addresses (2 usable) — ideal for point-to-point links between routers. A <strong>/16 mask</strong> covers 65,536 addresses.</p><p>The mask is critical for <strong>longest prefix match</strong>. If the routing table contains both 10.0.0.0/8 and 10.1.1.0/24, and a packet arrives for 10.1.1.50, both routes technically match. But /24 has more network bits (is more specific) than /8, so the /24 route wins. The mask determines how specific each route is — longer masks are more specific and always preferred.</p><p><strong>Subnet math shortcuts:</strong> The 'magic number' method works well for the exam. Subtract the interesting octet of the mask from 256 to get the block size. For 255.255.255.240 (/28), the block size is 256-240 = 16, so subnets increment by 16 (0, 16, 32, 48...). For a destination of 10.1.1.50, the subnet is 10.1.1.48/28 (because 48 is the highest multiple of 16 that doesn't exceed 50). Master this technique — it appears on nearly every subnetting question.</p><p><strong>Common masks to memorize:</strong> /8 = 255.0.0.0 (Class A), /16 = 255.255.0.0 (Class B), /24 = 255.255.255.0 (Class C), /25 = 255.255.255.128 (128 hosts), /26 = 255.255.255.192 (64 hosts), /27 = 255.255.255.224 (32 hosts), /28 = 255.255.255.240 (16 hosts), /29 = 255.255.255.248 (8 hosts), /30 = 255.255.255.252 (4 hosts, 2 usable — point-to-point).</p>",
    visual: { type: "binary-breakdown", params: { bits: "11111111.11111111.11111111.00000000", label: "/24 = 255.255.255.0", highlight: [24, 25, 26, 27] } },
    hack: {
      memory: "Mask = stencil laid over the address. Network bits (1s) = 'must match exactly.' Host bits (0s) = 'don't care.' A /28 stencil is more precise than a /24 stencil, which is more precise than a /16 stencil. More 1s = more specific = wins the longest prefix match.",
      practice: "Create a flashcard deck with every prefix length from /16 through /30. Each card: front = prefix length, back = dotted-decimal mask, number of total addresses, number of usable hosts, and block size. Drill until you achieve sub-2-second recall per card. Then practice the magic number method: given an IP and mask, find the network address, broadcast address, and valid host range in under 30 seconds.",
      effort: "medium",
      meta: "Jeremy's IT Lab Days 13-14 (Subnetting) are the definitive video lessons for this. Wendell Odom OCG Chapters 11-14 provide extensive subnetting practice. Subnetting speed is the #1 skill that separates passing from failing — Cisco allocates roughly 10 questions that require fast subnet math. Practice the magic number method daily until the exam."
    }
  },

  "3.1.d": {
    info: "<p>The <strong>next hop</strong> is the IP address of the next router in the path toward the destination network. In the route entry <code>O 10.1.1.0/24 [110/20] via 192.168.1.2, GigabitEthernet0/1</code>, the next hop is <strong>192.168.1.2</strong>. This tells the router: 'to reach the 10.1.1.0/24 network, forward the packet to 192.168.1.2 out interface Gi0/1.' The next-hop IP must be on a directly connected subnet — the router must be able to ARP for the next hop.</p><p>For <strong>directly connected routes</strong> (code C), there is no next hop because the destination network is directly attached to one of the router's interfaces. The router simply ARPs for the destination host's MAC address and delivers the frame directly. Similarly, <strong>local routes</strong> (code L) are /32 routes for the router's own interface IPs — traffic to these addresses is processed locally, not forwarded.</p><p>When a static route is configured with a next-hop IP (e.g., <code>ip route 10.1.1.0 255.255.255.0 192.168.1.2</code>), the router performs a <strong>recursive lookup</strong>: it first looks up 192.168.1.2 in the routing table to find which interface can reach that next hop, then forwards the packet out that interface. This is why the next-hop address must be reachable — if the recursive lookup fails (no route to the next hop), the route is removed from the table.</p><p>Some routes show only an <strong>exit interface</strong> instead of a next-hop IP (e.g., <code>S 10.1.1.0/24 is directly connected, Serial0/0/0</code>). This is common on point-to-point serial links where there is only one possible next hop. On multi-access networks (Ethernet), using only an exit interface causes the router to ARP for every destination IP individually, which is inefficient — always prefer specifying a next-hop IP on Ethernet links.</p>",
    visual: { type: "packet-flow", params: { nodes: ["R1 (10.0.0.1)", "Next-hop: 10.0.0.2", "R2 (10.0.0.2)", "Destination: 192.168.1.0/24"], color: "#8b5cf6" } },
    hack: {
      memory: "Next hop = the relay runner you hand the baton to. You don't run the whole race — you pass the packet to the next router closer to the finish line. The next hop MUST be on a subnet you're directly connected to (you can only hand off to someone within arm's reach).",
      practice: "Build a 4-router linear topology in Packet Tracer (R1-R2-R3-R4) with static routes. Run 'show ip route' on each router and trace a packet from R1 to R4's LAN step by step: write down each next-hop IP at each router. Then use 'traceroute' from R1 to R4's LAN and verify the hops match your manual trace. Practice reading 'via X.X.X.X' entries until you can map them to a topology diagram instantly.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) demonstrates next-hop configuration and recursive lookups. Wendell Odom OCG Chapters 15-16 detail the forwarding process. The exam shows topology diagrams alongside routing tables — you must map 'via X.X.X.X' entries to the correct interface/router on the diagram. Practice this visual mapping skill."
    }
  },

  "3.1.e": {
    info: "<p><strong>Administrative distance (AD)</strong> is a numeric value from 0 to 255 assigned to every route source, indicating how trustworthy that source is. The lower the AD, the more the router trusts that source. AD is used as a <strong>tiebreaker between different routing protocols</strong> — if OSPF (AD 110) and RIP (AD 120) both advertise a route to 10.1.1.0/24 with the same prefix length, the OSPF route is installed because 110 < 120. The RIP route is kept by the RIP process as a backup but does not appear in the routing table.</p><p><strong>Default AD values you must memorize:</strong> Directly Connected = <strong>0</strong>, Static = <strong>1</strong>, eBGP = <strong>20</strong>, EIGRP (internal) = <strong>90</strong>, OSPF = <strong>110</strong>, IS-IS = <strong>115</strong>, RIP = <strong>120</strong>, EIGRP (external) = <strong>170</strong>, iBGP = <strong>200</strong>, Unknown/Unbelievable = <strong>255</strong> (route is never used). Connected has AD 0 because the router physically sees the network — maximum trust. Static has AD 1 because an administrator explicitly configured it.</p><p>AD only comes into play when <strong>multiple sources advertise the same prefix length</strong>. If OSPF has a /24 route and RIP has a /16 route to an overlapping destination, longest prefix match picks the /24 first — AD is never consulted because the prefix lengths differ. AD is the <em>second</em> step in the forwarding decision, after longest prefix match.</p><p>You can <strong>modify the AD</strong> of a static route by appending the AD value to the command: <code>ip route 10.1.1.0 255.255.255.0 192.168.1.2 210</code> sets the AD to 210. This is how <strong>floating static routes</strong> work — by setting the AD higher than the primary routing protocol's default, the static route stays out of the table unless the primary route disappears.</p><p>In <code>show ip route</code> output, the AD appears in square brackets: <code>[110/20]</code> means AD 110, metric 20. Always read both numbers — the first is AD, the second is metric.</p>",
    visual: { type: "layer-stack", params: { layers: ["Connected (0)", "Static (1)", "eBGP (20)", "EIGRP (90)", "OSPF (110)", "RIP (120)"], highlight: 0 } },
    hack: {
      memory: "AD = trust score. 0 = 'I see it with my own eyes' (connected). 1 = 'my boss manually told me' (static). 20 = trusted external partner (eBGP). 90 = close colleague (EIGRP). 110 = acquaintance (OSPF). 120 = stranger at a party (RIP). 255 = 'I don't believe you at all.' Chant the numbers: 0-1-20-90-110-115-120-200.",
      practice: "Write the AD table from memory 10 times: Connected=0, Static=1, eBGP=20, EIGRP=90, OSPF=110, IS-IS=115, RIP=120, EIGRP ext=170, iBGP=200. Then in Packet Tracer, configure both OSPF and a static route to the same 10.1.1.0/24 network. Run 'show ip route 10.1.1.0' and verify the static route (AD 1) wins. Shut the static route's next-hop interface and confirm OSPF (AD 110) takes over.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) introduces AD, and Day 24 (OSPF) reinforces it. Wendell Odom OCG Chapters 15-16 have the complete AD table. This is a GUARANTEED exam question — Boson and Pearson practice exams always include at least one 'which route is installed?' question that requires comparing AD values. Write the AD table on your whiteboard as part of your exam brain dump."
    }
  },

  "3.1.f": {
    info: "<p>The <strong>metric</strong> is the cost or distance value that a routing protocol uses to compare multiple routes <em>from the same protocol</em> to the same destination. While administrative distance compares routes across different protocols, the metric compares routes within a single protocol. Each routing protocol defines its own metric differently:</p><p><strong>OSPF metric = cost</strong>, calculated as reference bandwidth / interface bandwidth. Default reference is 100 Mbps, so a 100 Mbps link has cost 1 and a 10 Mbps link has cost 10. The total OSPF cost to a destination is the sum of all link costs along the path. <strong>RIP metric = hop count</strong> — simply the number of routers between source and destination. Maximum is 15 hops; 16 means unreachable. <strong>EIGRP metric</strong> uses a composite formula based primarily on bandwidth and delay (by default; load and reliability can be included but rarely are). <strong>BGP</strong> uses a complex set of path attributes including AS-path length, MED, local preference, and origin type.</p><p>The metric appears in <code>show ip route</code> output as the second number in the brackets: <code>[110/20]</code> means AD 110, metric 20. For OSPF, a metric of 20 means the total cost to reach that network is 20. For RIP, <code>[120/3]</code> means the destination is 3 hops away. Lower metric is <strong>always</strong> preferred within the same protocol.</p><p>When two paths from the same protocol have <strong>equal metrics</strong>, the router performs <strong>equal-cost multi-path (ECMP) load balancing</strong>. OSPF supports ECMP with up to 4 equal-cost paths by default (configurable up to 32 with <code>maximum-paths</code>). RIP also supports ECMP with up to 4 paths. EIGRP supports both equal-cost and unequal-cost load balancing (using the <code>variance</code> command) — a unique EIGRP feature not tested deeply on CCNA but good to know.</p>",
    visual: { type: "comparison", params: { left: { label: "Protocol", items: ["OSPF", "RIP", "EIGRP", "BGP"] }, right: { label: "Metric", items: ["Cost (bandwidth)", "Hop count (max 15)", "Composite (BW + delay)", "AS-path, MED, etc."] } } },
    hack: {
      memory: "Metric = mileage within the same GPS app. Two Google Maps routes — you pick the shorter one. AD picks WHICH app to use (Google Maps vs Waze); metric picks which route within that app. OSPF = cost, RIP = hops, EIGRP = bandwidth+delay. Lower always wins.",
      practice: "In Packet Tracer, build a triangle topology with OSPF. Make one path use 100 Mbps links (cost 1 each, total 2) and the alternate path use 10 Mbps links (cost 10 each, total 20). Run 'show ip route ospf' and verify the lower-cost path is selected. Then make both paths equal cost and confirm both appear in the routing table (ECMP). Check with 'show ip ospf interface brief' to see individual link costs.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 24-26 (OSPF) cover cost calculation extensively. Wendell Odom OCG Chapters 19-21 detail OSPF metric math. For the exam: know that OSPF cost = ref BW / interface BW, RIP counts hops (max 15), and ECMP load-balances across equal-cost paths. The exam will ask you to calculate OSPF cost given bandwidth values."
    }
  },

  "3.1.g": {
    info: "<p>The <strong>gateway of last resort</strong> is the default route — <code>0.0.0.0/0</code> for IPv4 or <code>::/0</code> for IPv6. It matches every possible destination because a /0 mask means zero bits need to match. When no more specific route exists in the routing table for a packet's destination, the router forwards the packet to the gateway of last resort. If no default route is configured, the router drops the packet and may send an <strong>ICMP Destination Unreachable</strong> message back to the source.</p><p>The gateway of last resort appears at the very top of the <code>show ip route</code> output. If set, it shows the next-hop IP and the routing source — for example: <code>Gateway of last resort is 203.0.113.1 to network 0.0.0.0</code>. If not set, it reads: <code>Gateway of last resort is not set</code>. This line is one of the first things to check when troubleshooting connectivity to the internet or external networks.</p><p>A default route can be configured <strong>statically</strong> with <code>ip route 0.0.0.0 0.0.0.0 [next-hop-ip]</code>, or it can be <strong>learned dynamically</strong> via a routing protocol. In OSPF, you inject a default route with <code>default-information originate</code> on the edge router. In RIP, <code>default-information originate</code> also works. When learned via OSPF, the default route appears as <code>O*E2 0.0.0.0/0</code> (OSPF external type 2 candidate default).</p><p>In the routing table, the default route has a /0 prefix length — the shortest possible mask. This means it always loses to any more specific route via longest prefix match. A packet to 10.1.1.50 will match a route to 10.1.1.0/24 before it ever considers 0.0.0.0/0. The default route is truly the 'last resort' — only used when nothing else matches.</p><p><strong>Best practice:</strong> Every network should have a default route pointing toward the internet/WAN exit point. Branch office routers typically have a single default route pointing to the headquarters or ISP router. Core routers in large networks may learn the default route via OSPF or BGP.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Packet: dst 8.8.8.8", "No specific match", "Gateway of last resort", "Forward to ISP"], color: "#f59e0b" } },
    hack: {
      memory: "Gateway of last resort = 'when in doubt, send it out.' It is the catch-all exit for unknown destinations. 0.0.0.0/0 = match everything with zero specificity. It always loses to any more specific route (longest prefix match), so it truly is the LAST resort.",
      practice: "In Packet Tracer: (1) Run 'show ip route' and note 'Gateway of last resort is not set.' (2) Configure 'ip route 0.0.0.0 0.0.0.0 [ISP-next-hop]'. (3) Run 'show ip route' again and verify the gateway is now set. (4) Ping 8.8.8.8 (simulated internet) and confirm it works. (5) Remove the default route and verify pings fail with 'destination unreachable.' Then practice injecting the default via OSPF with 'default-information originate' on the edge router.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) covers default route configuration. Day 24-26 (OSPF) cover 'default-information originate.' Wendell Odom OCG Chapters 15-16 (routing table) and Chapter 19 (OSPF default route). The exam will test both the static command and the OSPF injection method. 'Gateway of last resort is not set' is a classic troubleshooting clue — if internet access is broken, check this first."
    }
  },

  // ── 3.2  Determine how a router makes a forwarding decision ──

  "3.2.a": {
    info: "<p><strong>Longest prefix match</strong> is the single most important rule in IP routing. When a packet arrives and multiple routes in the table match the destination IP, the router selects the route with the <strong>longest subnet mask</strong> (most specific match). A /28 beats a /24, which beats a /16, which beats a /8, which beats 0.0.0.0/0. The number of matching bits in the mask determines specificity — more matching bits means a more precise route to the destination.</p><p><strong>Example:</strong> A router has three routes: 10.0.0.0/8 via R2, 10.1.0.0/16 via R3, and 10.1.1.0/24 via R4. A packet arrives for destination 10.1.1.50. All three routes match (the destination falls within all three ranges), but the /24 has the longest mask — 24 bits match vs. 16 or 8. The router forwards to R4. If a fourth route existed for 10.1.1.48/28, that /28 would win instead, because 28 > 24.</p><p>Longest prefix match takes <strong>absolute priority</strong> over administrative distance and metric. Even if the /24 route came from RIP (AD 120) and the /8 route is a directly connected network (AD 0), the /24 wins because it is more specific. AD and metric only come into play when two routes have the <em>same</em> prefix length. This hierarchy is non-negotiable: specificity first, trust second, cost third.</p><p>A <strong>/32 host route</strong> is the most specific route possible — it matches exactly one IP address. Local routes (L) in the routing table are /32 entries for the router's own interfaces. If someone sends a packet to the router's own IP, the /32 local route always wins against any network route, ensuring the packet is processed locally. Conversely, 0.0.0.0/0 (the default route) is the least specific — it only matches when nothing else does.</p><p><strong>Practical implication:</strong> When troubleshooting 'traffic is going the wrong way,' check if a more specific route exists that is overriding the expected path. A rogue /32 host route can hijack traffic that should follow a /24 network route. Use <code>show ip route [destination-ip]</code> to see exactly which route the router selects for a specific destination.</p>",
    visual: { type: "hierarchy", params: { root: "Dst: 10.1.1.50", children: [{ name: "10.0.0.0/8 (match)", children: [{ name: "10.1.0.0/16 (better)" , children: [{ name: "10.1.1.0/24 (BEST)" }] }] }] } },
    hack: {
      memory: "Longest prefix = most specific address wins, ALWAYS, regardless of AD or metric. Like mail delivery: 'USA' matches, 'Illinois' is better, '123 Main St, Apt 4B, Chicago IL 60601' wins — because it's the most specific. /32 > /28 > /24 > /16 > /8 > /0.",
      practice: "Write 10 practice scenarios: create a routing table with 4-5 overlapping routes at different prefix lengths (/8, /16, /24, /28, /32), then pick 5 destination IPs and determine which route wins for each. Time yourself — aim for under 10 seconds per lookup. Verify in Packet Tracer using 'show ip route [destination]' which shows the exact match. Then use traceroute to confirm the path.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) and Day 15 (Routing Fundamentals) cover longest prefix match with examples. Wendell Odom OCG Chapters 15-16 detail the forwarding decision. This is the SINGLE most tested routing concept — every 'which route will the router use?' question starts with longest prefix match. If you master nothing else in Domain 3, master this."
    }
  },

  "3.2.b": {
    info: "<p>When two or more routes have the <strong>same prefix length</strong> but were learned from <strong>different routing sources</strong>, the router uses <strong>administrative distance (AD)</strong> as the tiebreaker. The route with the lower AD is installed in the routing table; the higher-AD route is kept by its routing protocol process as a backup but does not appear in the active routing table.</p><p><strong>Example:</strong> Both OSPF and RIP advertise a route to 10.1.1.0/24. OSPF has AD 110, RIP has AD 120. Since both routes have the same /24 prefix length (longest prefix match cannot decide), the router compares AD values: 110 < 120, so the OSPF route wins. The RIP route remains in RIP's internal database but is not installed in the routing table. If the OSPF route later disappears (link failure, neighbor down), the RIP route immediately becomes eligible and gets installed.</p><p>This mechanism enables <strong>routing protocol redundancy</strong>. A common design uses OSPF as the primary routing protocol with a static route (AD 1) or floating static route (AD > 110) as backup. If the OSPF adjacency goes down, the static route can take over automatically. The key insight: AD comparison only happens between routes with identical prefix lengths — it is NEVER consulted when prefix lengths differ (longest prefix match always wins first).</p><p>In <code>show ip route</code>, you can see the AD in the brackets: <code>O 10.1.1.0/24 [110/20]</code> shows AD 110. To see routes that are NOT in the table (kept as backups by their protocols), use protocol-specific commands like <code>show ip rip database</code> or <code>show ip ospf database</code>. The routing table only shows the winning route for each prefix.</p>",
    visual: { type: "comparison", params: { left: { label: "OSPF Route", items: ["10.1.1.0/24", "AD: 110", "Installed? YES"] }, right: { label: "RIP Route", items: ["10.1.1.0/24", "AD: 120", "Installed? NO"] } } },
    hack: {
      memory: "AD is the second judge in a three-judge panel. Judge 1 (longest prefix) picks the most specific route. If Judge 1 declares a tie (same prefix length), Judge 2 (lowest AD) picks the most trusted source. Connected (0) beats Static (1) beats OSPF (110) beats RIP (120).",
      practice: "Build a lab in Packet Tracer: configure both OSPF (AD 110) and a static route (AD 1) to the same 10.1.1.0/24 network. Run 'show ip route 10.1.1.0' and confirm the static route (S) wins. Then shut down the static route's exit interface or next-hop and run 'show ip route 10.1.1.0' again — the OSPF route (O) should now appear. Bring the interface back up and watch the static reclaim the slot.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) introduces AD, Day 24 (OSPF) reinforces it. Wendell Odom OCG Chapters 15-16 cover route selection in detail. The exam commonly shows two protocols learning the same network and asks 'which route appears in the table?' Always compare AD values when prefix lengths are equal."
    }
  },

  "3.2.c": {
    info: "<p>The <strong>default administrative distance values</strong> are assigned by Cisco IOS to each route source and determine route preference when multiple sources advertise the same prefix. These values are a Cisco convention (not an RFC standard) and must be memorized for the CCNA exam:</p><ul><li><strong>Directly Connected: 0</strong> — Maximum trust. The router has a physical interface in this network.</li><li><strong>Static: 1</strong> — Manually configured by an administrator. Highly trusted.</li><li><strong>eBGP: 20</strong> — External BGP routes learned from a different autonomous system.</li><li><strong>EIGRP (internal): 90</strong> — Routes learned within the same EIGRP autonomous system.</li><li><strong>OSPF: 110</strong> — All OSPF route types (intra-area, inter-area, external) share this AD.</li><li><strong>IS-IS: 115</strong> — Intermediate System to Intermediate System (less common on CCNA).</li><li><strong>RIP: 120</strong> — The highest AD of the common IGPs, reflecting RIP's simplicity and limitations.</li><li><strong>EIGRP (external): 170</strong> — Routes redistributed into EIGRP from another protocol.</li><li><strong>iBGP: 200</strong> — Internal BGP routes (within the same AS). Low trust because iBGP routes may be far away.</li><li><strong>Unknown/Unusable: 255</strong> — Route is never installed. Used to effectively disable a route.</li></ul><p>The logic behind the values: connected routes (AD 0) are the most trustworthy because you can physically verify the network exists. Static routes (AD 1) are trusted because a human configured them deliberately. Dynamic protocols get progressively higher AD values based on their sophistication and scalability — EIGRP (90) is considered more reliable than OSPF (110) in Cisco's view, though both are widely deployed.</p><p>AD values <strong>can be modified</strong>. The <code>distance</code> command under a routing protocol changes its AD globally. Individual static routes can have their AD set per-route by appending the value to the <code>ip route</code> command. This flexibility enables floating static routes and other failover designs.</p>",
    visual: { type: "layer-stack", params: { layers: ["Connected — 0", "Static — 1", "eBGP — 20", "EIGRP — 90", "OSPF — 110", "IS-IS — 115", "RIP — 120", "iBGP — 200"], highlight: 0 } },
    hack: {
      memory: "Chant: 'Connected 0, Static 1, External BGP 20, EIGRP 90, OSPF 110, IS-IS 115, RIP 120, Internal BGP 200.' Write this on your exam whiteboard FIRST. Mnemonic: 'Cool Students Earn Every Opportunity In Real Internships Before' → C=0, S=1, E=20, E=90, O=110, I=115, R=120, I=200.",
      practice: "Create physical flashcards: protocol on front, AD value on back. Shuffle and drill until you can recite all values in under 10 seconds. Then test yourself backwards: given AD 110, which protocol? (OSPF). Given AD 90? (EIGRP). Given AD 120? (RIP). Both directions must be instant.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) presents the AD table. Wendell Odom OCG Chapter 15 has the complete default AD chart. This is 100% memorization and is tested on EVERY exam. Boson, Pearson, and the real exam all include AD comparison questions. Write the table on your whiteboard during the exam tutorial period as part of your brain dump."
    }
  },

  "3.2.d": {
    info: "<p>When two routes have the <strong>same prefix length</strong> AND the <strong>same administrative distance</strong> (which means they came from the same routing protocol), the router uses the protocol's <strong>metric</strong> as the final tiebreaker. The route with the lower metric is installed in the routing table. Each protocol defines its metric differently: OSPF uses <strong>cost</strong> (reference bandwidth / interface bandwidth), RIP uses <strong>hop count</strong> (number of routers to the destination, max 15), and EIGRP uses a <strong>composite metric</strong> based on bandwidth and delay.</p><p><strong>Example:</strong> Two OSPF paths exist to 10.1.1.0/24. Path A goes through two GigabitEthernet links with total cost 2. Path B goes through a FastEthernet link and a GigabitEthernet link with total cost 11. Both are OSPF (same AD 110), both are /24 (same prefix length), so metric decides: cost 2 < cost 11, Path A wins.</p><p>When metrics are <strong>exactly equal</strong>, the router performs <strong>equal-cost multi-path (ECMP) load balancing</strong> — both routes are installed in the routing table, and traffic is distributed across both paths. OSPF supports up to <strong>4 equal-cost paths</strong> by default (configurable with <code>maximum-paths [1-32]</code>). RIP also supports up to 4 equal-cost paths. In <code>show ip route</code>, ECMP routes appear as multiple next-hop entries for the same prefix.</p><p>ECMP load balancing operates on a <strong>per-destination</strong> or <strong>per-packet</strong> basis depending on the switching method. CEF (Cisco Express Forwarding), which is the default on modern Cisco routers, uses per-destination load balancing based on source-destination IP pairs. This ensures all packets in a single flow follow the same path, preventing out-of-order delivery.</p>",
    visual: { type: "comparison", params: { left: { label: "OSPF Path A", items: ["Cost: 10", "Via Gi0/0", "WINNER"] }, right: { label: "OSPF Path B", items: ["Cost: 20", "Via Gi0/1", "Backup"] } } },
    hack: {
      memory: "Metric = the FINAL tiebreaker, only consulted when prefix length AND AD are both tied. Two OSPF routes? Lower cost wins. Two RIP routes? Fewer hops wins. Equal metric? Both go in the table — that's ECMP load balancing. Think of it as two equally-rated GPS routes — the shorter one wins, or if tied, you use both lanes.",
      practice: "In Packet Tracer: build a triangle topology (R1-R2-R3, all running OSPF). Create two paths from R1 to R3's LAN — one through fast links (cost 2) and one through slow links (cost 20). Run 'show ip route ospf' on R1 and verify only the low-cost path appears. Then change both paths to equal cost and verify BOTH paths appear in the routing table. Use 'show ip route 10.1.1.0' to see both next hops listed.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 24-26 (OSPF) cover cost calculation and ECMP. Wendell Odom OCG Chapters 19-21 detail metric comparison. ECMP is a favorite exam topic — know that OSPF load-balances across equal-cost paths by default (up to 4). The exam may ask 'how many paths will appear in the routing table?' when costs are equal."
    }
  },

  "3.2.e": {
    info: "<p>The <strong>complete router forwarding decision</strong> follows a strict three-step hierarchy that is the foundation of all IP routing. Every 'which route will the router use?' question on the CCNA exam is solved by applying these steps in order:</p><p><strong>Step 1 — Longest Prefix Match:</strong> The router compares the destination IP against all routes and selects the one(s) with the longest matching prefix (most specific mask). A /28 beats a /24, a /24 beats a /16. This step ALWAYS comes first and overrides everything else. If only one route has the longest prefix, the decision is made — AD and metric are never consulted.</p><p><strong>Step 2 — Lowest Administrative Distance:</strong> If multiple routes tie on prefix length (same mask) but come from different sources, the router picks the one with the lowest AD. OSPF (110) beats RIP (120). Static (1) beats OSPF (110). Connected (0) beats everything. This step is only reached when Step 1 produces a tie.</p><p><strong>Step 3 — Lowest Metric:</strong> If routes tie on both prefix length AND AD (meaning they came from the same routing protocol), the router picks the one with the lowest metric. OSPF uses cost, RIP uses hop count. If metrics are also equal, the router installs ALL tied routes and performs ECMP load balancing.</p><p>The mnemonic <strong>PAM</strong> (Prefix, AD, Metric) captures this hierarchy. A common exam trap is presenting a connected route (/24, AD 0) alongside a more specific static route (/28, AD 1). Students who jump to AD comparison without checking prefix length first will incorrectly pick the connected route. The /28 static wins because Step 1 (longest prefix) comes before Step 2 (AD). Always work the steps in order.</p>",
    visual: { type: "state-machine", params: { states: ["1. Longest Prefix Match", "2. Lowest AD", "3. Lowest Metric"], active: 0, transitions: true } },
    hack: {
      memory: "PAM: Prefix first, AD second, Metric third. 'Please Ask Mom.' NEVER skip a step. The exam will try to trick you into jumping to AD before checking prefix length. A /32 from RIP (AD 120) still beats a /24 from a connected route (AD 0) because /32 > /24 in specificity. Prefix is king.",
      practice: "Create 10 multi-step scenarios on paper. Each scenario: routing table with 4-5 routes from mixed sources (connected, static, OSPF, RIP) with different prefix lengths and metrics. For each destination IP, work through PAM step by step and identify the winning route. Then verify in Packet Tracer with 'show ip route [destination]' and 'traceroute.' Aim for 100% accuracy before moving on.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) and Day 15 (Routing Fundamentals) walk through the full decision process. Wendell Odom OCG Chapters 15-16 formalize the three-step hierarchy. This is tested in multi-step questions: 'Given this routing table, which path does a packet to X.X.X.X take?' Work each step methodically. Boson practice exams have excellent forwarding-decision questions."
    }
  },

  // ── 3.3  Configure and verify IPv4 and IPv6 static routing ──

  "3.3.a": {
    info: "<p>A <strong>default static route</strong> uses the special destination <code>0.0.0.0 0.0.0.0</code> (IPv4) or <code>::/0</code> (IPv6) to match ALL destination addresses. The /0 mask means zero bits need to match — every possible IP is a match. This route is only used when no more specific route exists in the routing table (it has the shortest possible prefix length). It is the <strong>gateway of last resort</strong>.</p><p><strong>IPv4 command:</strong> <code>ip route 0.0.0.0 0.0.0.0 203.0.113.1</code> — this tells the router 'for any destination I don't have a specific route for, send the packet to 203.0.113.1.' <strong>IPv6 command:</strong> <code>ipv6 route ::/0 2001:DB8:FEED::1</code>. You can also specify an exit interface instead of a next hop, or both: <code>ip route 0.0.0.0 0.0.0.0 GigabitEthernet0/0 203.0.113.1</code>.</p><p>Default routes are typically configured on <strong>edge/border routers</strong> that connect the enterprise network to the ISP or internet. Branch office routers commonly use a single default route pointing upstream because they don't need to know every internet route — they just send everything toward the ISP. Core routers in large networks may learn the default route dynamically via OSPF (<code>default-information originate</code>) or BGP.</p><p>When a default route exists, <code>show ip route</code> displays <code>Gateway of last resort is 203.0.113.1 to network 0.0.0.0</code> at the top. The route itself appears as <code>S* 0.0.0.0/0 [1/0] via 203.0.113.1</code> — the asterisk (*) marks it as a <strong>candidate default route</strong>. Without a default route, the line reads 'Gateway of last resort is not set,' and traffic to unknown destinations is dropped with an ICMP Destination Unreachable response.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Branch Router", "Default Route 0.0.0.0/0", "ISP Router", "Internet"], color: "#10b981" } },
    hack: {
      memory: "Default route = 'I don't know where this goes, so send it to the exit.' 0.0.0.0/0 matches everything but loses to any more specific route. It's the catch-all — like the 'miscellaneous' drawer for unknown destinations. S* in the routing table = static candidate default.",
      practice: "In Packet Tracer: (1) Build R1 (branch) connected to R2 (ISP). (2) Configure 'ip route 0.0.0.0 0.0.0.0 [R2-IP]' on R1. (3) Verify 'show ip route' shows the gateway of last resort. (4) Ping from R1 to a simulated internet address through R2. (5) Remove the default route and verify pings fail. (6) For IPv6: configure 'ipv6 route ::/0 [R2-IPv6]' and verify with 'show ipv6 route.'",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) covers default route configuration. Day 26 (OSPF Part 3) covers OSPF default route injection. Wendell Odom OCG Chapters 15-16. Default route configuration is a near-guaranteed lab sim on the CCNA — practice the exact syntax for both IPv4 and IPv6 until it's muscle memory."
    }
  },

  "3.3.b": {
    info: "<p>A <strong>network static route</strong> specifies the path to a particular destination network by defining the network address, subnet mask, and either a next-hop IP or exit interface. The command syntax is: <code>ip route [network] [mask] [next-hop-ip | exit-interface]</code>. For example, <code>ip route 10.1.1.0 255.255.255.0 192.168.1.2</code> tells the router: 'to reach the 10.1.1.0/24 network, forward packets to 192.168.1.2.'</p><p>Static network routes are <strong>manually configured by an administrator</strong> and do not adapt to topology changes. If the path to the next hop goes down, the static route simply becomes invalid and is removed from the routing table (assuming the directly connected interface goes down). This is both a strength (simple, predictable, no routing protocol overhead) and a weakness (no automatic failover unless paired with a floating static).</p><p><strong>When to use network static routes:</strong> (1) Small networks with few routes and stable topologies. (2) Stub networks with only one path in/out — no benefit from dynamic routing. (3) Routes to specific networks that should never change (e.g., a management network). (4) As a backup via floating static routes. In large networks, dynamic routing protocols (OSPF, EIGRP) replace static routes because manually maintaining hundreds of routes is impractical and error-prone.</p><p>Static routes appear in the routing table with code <strong>S</strong> and AD <strong>1</strong> by default: <code>S 10.1.1.0/24 [1/0] via 192.168.1.2</code>. The metric is always 0 for static routes because there is no protocol calculating cost. Remember: you must configure static routes in <strong>both directions</strong> — R1 needs a route to R2's LAN, and R2 needs a route back to R1's LAN. Forgetting the return route is the most common static routing mistake.</p><p><strong>IPv6 equivalent:</strong> <code>ipv6 route 2001:DB8:1::/64 2001:DB8:2::1</code>. The syntax is similar but uses IPv6 addresses and prefix notation instead of dotted-decimal masks.</p>",
    visual: { type: "packet-flow", params: { nodes: ["R1", "Static: 10.1.1.0/24 via R2", "R2", "10.1.1.0/24 Network"], color: "#6366f1" } },
    hack: {
      memory: "Network static route = manual GPS waypoint. You hard-code: 'To reach THAT network, go through THIS next hop.' Simple, predictable, but dumb — it doesn't detect road closures. Always remember to configure the RETURN route too, or traffic will flow one way and die on the way back.",
      practice: "Build a 3-router chain (R1-R2-R3) in Packet Tracer. R1's LAN is 10.1.0.0/24, R3's LAN is 10.3.0.0/24. Configure static routes so all networks are reachable: R1 needs routes to R2's transit and R3's LAN, R3 needs routes back. Verify end-to-end connectivity with ping and traceroute from R1's LAN to R3's LAN. Then purposely omit the return route on R3 and observe pings fail (request times out, not destination unreachable).",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) is the core lesson. Wendell Odom OCG Chapters 15-16. The exam tests both the command syntax and the concept of bidirectional routing. A classic exam trap: routing works in one direction but not the other because the return static route is missing."
    }
  },

  "3.3.c": {
    info: "<p>A <strong>host route</strong> uses a <strong>/32 prefix length</strong> (mask 255.255.255.255) to create a route to exactly one IP address. With all 32 bits in the mask, every bit of the destination must match — there are zero host bits, so the route covers precisely one address. A /32 is the most specific route possible and always wins longest prefix match against any broader route.</p><p>Host routes appear in the routing table in two ways. First, the router <strong>automatically creates /32 local routes</strong> (code <strong>L</strong>) for every IP address configured on its own interfaces. For example, if Gi0/0 has IP 10.1.1.1/24, the routing table will contain both <code>C 10.1.1.0/24</code> (connected network) and <code>L 10.1.1.1/32</code> (local host route). The L route ensures traffic destined for the router's own IP is processed locally rather than forwarded.</p><p>Second, you can <strong>manually create a /32 static host route</strong>: <code>ip route 10.1.1.100 255.255.255.255 192.168.1.2</code>. This forces all traffic to the specific host 10.1.1.100 through a particular next hop, regardless of what broader routes exist. Use cases include directing traffic to a specific server through a particular path, or creating policy-based routing workarounds.</p><p>Because /32 is the longest possible prefix, a host route <strong>always wins</strong> against /24, /16, or any other route during longest prefix match. This makes host routes powerful but potentially dangerous — a misconfigured /32 can hijack traffic that should follow a different path. In troubleshooting, always check for unexpected /32 entries if traffic is being routed incorrectly.</p>",
    visual: { type: "binary-breakdown", params: { bits: "11111111.11111111.11111111.11111111", label: "/32 = Single host", highlight: [0, 8, 16, 24, 31] } },
    hack: {
      memory: "/32 = one and only one address. All 32 bits are network bits — zero flexibility, zero host bits. Like a GPS pin on a single house, not a neighborhood. L routes in 'show ip route' are automatic /32s for the router's own IPs. /32 ALWAYS wins longest prefix match — nothing is more specific.",
      practice: "Run 'show ip route' on any router in Packet Tracer and identify every L (local) entry — these are automatic /32 host routes. Note how each interface IP has both a C (connected network) and L (local host) entry. Then configure a manual host route: 'ip route 10.1.1.100 255.255.255.255 192.168.1.2'. Verify it appears as 'S 10.1.1.100/32' in the table and that traffic to 10.1.1.100 follows this route even if a broader /24 route exists.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) shows host routes. Wendell Odom OCG Chapters 15-16. Host routes are tested indirectly — the exam won't ask 'what is a host route?' but will present scenarios where a /32 wins longest prefix match. Know that L codes are automatic /32s and that /32 always beats any other prefix length."
    }
  },

  "3.3.d": {
    info: "<p>A <strong>floating static route</strong> is a static route configured with a <strong>higher administrative distance</strong> than the primary route, causing it to stay out of the routing table during normal operation. It 'floats' above the active routing table, only dropping in when the primary route disappears. This provides a backup path without requiring a dynamic routing protocol for redundancy.</p><p>The AD is set by appending it to the <code>ip route</code> command: <code>ip route 10.1.1.0 255.255.255.0 192.168.2.2 210</code>. Here, the static route has AD 210 instead of the default 1. If the primary route to 10.1.1.0/24 is via OSPF (AD 110), the OSPF route wins (110 < 210) and the floating static stays hidden. If the OSPF route disappears (neighbor goes down, link fails), the floating static (AD 210) becomes the best remaining route and gets installed in the routing table.</p><p><strong>Critical rule:</strong> The floating static's AD must be <strong>higher than the primary route's AD</strong>. If the primary is OSPF (AD 110), set the floating static to at least 111 — though convention is to use 200-250 to leave a safety margin. Common choices: 200, 210, or 250. If you forget to set a custom AD, the static route defaults to AD 1 and will ALWAYS win over the dynamic protocol, defeating the purpose entirely.</p><p>Floating static routes work for both <strong>IPv4 and IPv6</strong>. The IPv6 equivalent: <code>ipv6 route 2001:DB8:1::/64 2001:DB8:BACKUP::1 210</code>. The AD value is appended the same way. Verification: when the primary route is active, <code>show ip route static</code> will NOT show the floating static (it's not in the table). When the primary fails, running the same command will show it. You can also check <code>show ip route 10.1.1.0</code> to see which route is currently active for that destination.</p><p><strong>Use case:</strong> A branch office connects to HQ via a primary WAN link (OSPF) and a backup ISP link (floating static). During normal operation, OSPF handles routing. If the primary WAN goes down, the floating static route through the backup ISP automatically activates. When the primary WAN recovers, OSPF re-establishes the route (AD 110 < 210) and the floating static floats back up out of the table.</p>",
    visual: { type: "state-machine", params: { states: ["Primary (OSPF AD 110)", "Link Failure", "Floating Static (AD 210) Activates"], active: 0, transitions: true } },
    hack: {
      memory: "Floating static = the backup generator sitting idle in the basement. Its higher AD (e.g., 210) keeps it out of the routing table during normal operation. When the main power (primary route) fails, the generator kicks in automatically. When power returns, the generator shuts off. The key: you MUST set the AD number at the end of the ip route command — forget it and the static route (AD 1) will always win.",
      practice: "Lab this in Packet Tracer: (1) Configure OSPF between R1 and R2 for network 10.1.1.0/24 (primary path). (2) Configure a floating static route on R1: 'ip route 10.1.1.0 255.255.255.0 [backup-next-hop] 210'. (3) Verify with 'show ip route' that only the OSPF route appears. (4) Shut down the OSPF interface on R2. (5) Verify the floating static now appears in 'show ip route'. (6) Bring the OSPF link back up and verify OSPF reclaims the route.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) covers floating static routes. Wendell Odom OCG Chapters 15-16. This is a favorite exam scenario — both conceptual and lab sim. The most common student mistake: forgetting to append the AD value, leaving the static at AD 1 where it always overrides the dynamic protocol. The second most common mistake: setting the AD lower than the primary protocol's AD."
    }
  },

  "3.3.e": {
    info: "<p><strong>IPv6 static routes</strong> follow the same logic as IPv4 static routes but use IPv6 addresses and CIDR prefix notation. The command syntax is: <code>ipv6 route [prefix/length] [next-hop | exit-interface]</code>. For example: <code>ipv6 route 2001:DB8:1::/64 2001:DB8:2::1</code> tells the router to reach the 2001:DB8:1::/64 network via next-hop 2001:DB8:2::1.</p><p>IPv6 static routes support the same types as IPv4: <strong>directly attached</strong> (exit interface only — <code>ipv6 route 2001:DB8:1::/64 GigabitEthernet0/0</code>), <strong>recursive</strong> (next-hop only — <code>ipv6 route 2001:DB8:1::/64 2001:DB8:2::1</code>), and <strong>fully specified</strong> (both — <code>ipv6 route 2001:DB8:1::/64 GigabitEthernet0/0 2001:DB8:2::1</code>). Default routes use <code>::/0</code> as the prefix: <code>ipv6 route ::/0 2001:DB8:FEED::1</code>.</p><p><strong>Critical gotcha — link-local next hops:</strong> When using a <strong>link-local address</strong> (fe80::) as the next hop, you <strong>MUST also specify the exit interface</strong>. This is because link-local addresses are only unique per-interface — the same fe80::1 address could exist on multiple interfaces, so the router can't determine which interface to use without being told. Example: <code>ipv6 route ::/0 GigabitEthernet0/0 fe80::1</code>. This is the most common IPv6 static routing mistake on the exam.</p><p><strong>IPv6 routing must be enabled</strong> with the global command <code>ipv6 unicast-routing</code> before the router will forward IPv6 packets. Without this command, the router processes IPv6 on its own interfaces but does not route between them. This is analogous to <code>ip routing</code> for IPv4 (which is enabled by default on routers but not on L3 switches).</p><p><strong>Verification:</strong> <code>show ipv6 route</code> displays the full IPv6 routing table. <code>show ipv6 route static</code> filters to static routes only. IPv6 routes use the same code letters: S for static, C for connected, L for local, O for OSPF. The route entry format is similar: <code>S 2001:DB8:1::/64 [1/0] via 2001:DB8:2::1</code>.</p>",
    visual: { type: "packet-flow", params: { nodes: ["R1", "ipv6 route 2001:DB8:1::/64", "via 2001:DB8:2::1", "R2"], color: "#06b6d4" } },
    hack: {
      memory: "IPv6 static = same concept as IPv4, longer addresses, CIDR notation. The ONE big gotcha: link-local next-hop (fe80::) REQUIRES the exit interface because fe80:: is ambiguous across interfaces. Command: 'ipv6 route ::/0 Gi0/0 fe80::1' — interface THEN link-local. Also: 'ipv6 unicast-routing' must be enabled or nothing gets routed.",
      practice: "Build an IPv6-only 3-router lab in Packet Tracer. (1) Enable 'ipv6 unicast-routing' on all routers. (2) Configure global unicast addresses on all interfaces. (3) Create static routes using global unicast next-hops and verify with 'show ipv6 route'. (4) Replace one route with a link-local next-hop WITHOUT specifying the exit interface — observe the error. (5) Fix it by adding the exit interface and verify it works. (6) Configure a default route: 'ipv6 route ::/0 Gi0/0 fe80::1'.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 31-33 (IPv6) cover IPv6 static routing. Wendell Odom OCG Chapters 23-25 detail IPv6 addressing and routing. IPv6 static routing appears every exam cycle. The link-local + exit-interface requirement is the #1 tested concept — if you remember nothing else about IPv6 static routes, remember this rule."
    }
  },

  "3.3.f": {
    info: "<p>Static routes can specify the next hop in three ways, each with different behavior and trade-offs:</p><p><strong>1. Next-hop IP only (recursive static route):</strong> <code>ip route 10.0.0.0 255.0.0.0 192.168.1.2</code>. The router must perform a <strong>recursive lookup</strong> — it looks up 192.168.1.2 in the routing table to find which exit interface can reach that next hop. This adds a lookup step but works on all network types. This is the <strong>preferred method on multi-access networks</strong> (Ethernet) because the router sends a single ARP for the next-hop IP and caches the result.</p><p><strong>2. Exit interface only (directly attached static route):</strong> <code>ip route 10.0.0.0 255.0.0.0 GigabitEthernet0/0</code>. No recursive lookup needed — the router knows the exit interface immediately. However, on <strong>multi-access networks (Ethernet)</strong>, this causes a serious problem: the router ARPs for EVERY destination IP individually (proxy ARP), filling the ARP table and creating significant CPU overhead. On <strong>point-to-point links</strong> (serial, GRE tunnel), this is fine because there's only one possible next hop.</p><p><strong>3. Both (fully specified static route):</strong> <code>ip route 10.0.0.0 255.0.0.0 GigabitEthernet0/0 192.168.1.2</code>. Combines the best of both — no recursive lookup AND no ARP flooding. The router knows both the exit interface and the next-hop IP. This is the most complete specification and avoids all the drawbacks of the other two methods.</p><p><strong>Cisco recommendation:</strong> Always use a next-hop IP (option 1) or fully specified route (option 3) on Ethernet/multi-access networks. Use exit interface only (option 2) on point-to-point serial links. The exam specifically tests whether you understand that exit-interface-only on Ethernet causes excessive ARP traffic.</p>",
    visual: { type: "comparison", params: { left: { label: "Next-hop IP", items: ["Recursive lookup", "Works on all link types", "Preferred on Ethernet"] }, right: { label: "Exit Interface", items: ["No recursive lookup", "Best on point-to-point", "Causes ARP issues on Ethernet"] } } },
    hack: {
      memory: "Next-hop IP = 'deliver to that specific person at the front desk' (single ARP, efficient). Exit interface only = 'throw it out that door and ARP for every person individually' (works on P2P, disaster on Ethernet). Fully specified = 'deliver to that person via that specific door' (best of both worlds).",
      practice: "In Packet Tracer, configure a next-hop-only static route and an exit-interface-only static route to different networks on the same router (Ethernet segment). Use 'debug arp' or 'show arp' to observe the difference in ARP behavior. The exit-interface route will generate ARP requests for every destination IP, while the next-hop route generates a single ARP for the next hop.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) compares all three methods. Wendell Odom OCG Chapters 15-16 explain the recursive lookup and ARP implications. The exam tests this as: 'which static route type causes excessive ARP on Ethernet?' Answer: exit-interface-only. Cisco recommends next-hop IP or fully specified on multi-access networks."
    }
  },

  "3.3.g": {
    info: "<p><strong>Static route verification</strong> requires checking three things: (1) Is the route configured? (2) Is it in the routing table? (3) Does traffic actually reach the destination? These are three different checks — a route can be configured but not in the table (if the exit interface is down or the next hop is unreachable), or in the table but traffic still fails (if the return route is missing).</p><p><strong>Configuration check:</strong> <code>show running-config | include ip route</code> shows all configured static routes. For IPv6: <code>show running-config | include ipv6 route</code>. This confirms the commands were entered but doesn't guarantee they're active. A static route with a down exit interface or unreachable next hop is configured but NOT installed in the routing table.</p><p><strong>Routing table check:</strong> <code>show ip route static</code> shows only static routes currently in the routing table. <code>show ip route</code> shows the full table — static routes are marked with <strong>S</strong> (standard static) or <strong>S*</strong> (candidate default static). For IPv6: <code>show ipv6 route static</code>. If a configured static route doesn't appear here, the exit interface is down or the next hop is unreachable. Use <code>show ip route [destination-network]</code> to check a specific route.</p><p><strong>Connectivity check:</strong> <code>ping [destination]</code> tests basic reachability. <code>traceroute [destination]</code> shows the path taken and identifies where packets get lost. Always test from both ends — traffic may flow one direction but fail on the return path due to a missing return route. An extended ping (<code>ping [destination] source [source-interface]</code>) lets you test connectivity from a specific interface, which is important when verifying routing from a particular subnet.</p><p><strong>Common static routing issues:</strong> (1) Missing return route — R1 can route to R3's LAN, but R3 has no route back to R1's LAN. (2) Next-hop unreachable — the next-hop IP is configured but the link to it is down. (3) Wrong mask — configured /16 instead of /24, causing the route to cover too many addresses. (4) Asymmetric routing — forward path uses one link, return path uses another, causing stateful devices (firewalls) to drop traffic.</p>",
    visual: { type: "comparison", params: { left: { label: "IPv4 Verify", items: ["show ip route static", "show ip route", "show run | inc ip route"] }, right: { label: "IPv6 Verify", items: ["show ipv6 route static", "show ipv6 route", "show run | inc ipv6 route"] } } },
    hack: {
      memory: "Three-step verification: CONFIG (show run | inc ip route) — is it typed? TABLE (show ip route static) — is it active? CONNECTIVITY (ping + traceroute) — does it actually work? A route can pass check 1 but fail check 2 (interface down). It can pass checks 1 and 2 but fail check 3 (missing return route). Always run all three.",
      practice: "After every static route you configure in Packet Tracer, run all three checks: (1) 'show run | inc ip route' to confirm the command. (2) 'show ip route static' to confirm it's in the table. (3) Ping AND traceroute end-to-end to confirm connectivity. Build this three-step habit — it's what real network engineers do and what the exam expects you to demonstrate in lab sims.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 11 (Static Routing) walks through verification. Wendell Odom OCG Chapters 15-16. Exam sims show you routing table output and ask 'what's wrong?' The most common answers: missing return route, wrong next-hop IP, exit interface in down state. Know what S, S*, and the [1/0] brackets mean in route output."
    }
  },

  // ── 3.4  Configure and verify single area OSPFv2 ───────────

  "3.4.a": {
    info: "<p><strong>OSPF neighbor adjacencies</strong> are the foundation of OSPF operation. Before two OSPF routers can exchange routing information, they must first discover each other and form a neighbor relationship. Routers discover neighbors by sending <strong>Hello packets</strong> out every OSPF-enabled interface at regular intervals. For an adjacency to form, several parameters must match between the two routers on the shared link.</p><p><strong>Parameters that MUST match for OSPF neighbors to form:</strong></p><ul><li><strong>Hello and Dead intervals</strong> — Default Hello is 10 seconds on broadcast and point-to-point networks (30 seconds on NBMA). Dead interval defaults to 4x Hello (40 seconds on broadcast/P2P, 120 seconds on NBMA). If these don't match, the routers never see each other as valid neighbors.</li><li><strong>Area ID</strong> — Both interfaces must be in the same OSPF area. A router in area 0 cannot form a neighbor relationship with a router in area 1 on the same link.</li><li><strong>Subnet mask</strong> — Both interfaces must use the same subnet mask. If R1 has 10.1.1.1/24 and R2 has 10.1.1.2/25, the adjacency fails because the masks differ.</li><li><strong>Authentication type and credentials</strong> — If OSPF authentication is configured, both sides must use the same type (null, plaintext, or MD5) and password.</li><li><strong>Stub area flags</strong> — Both routers must agree on whether the area is a stub, NSSA, or regular area.</li></ul><p><strong>OSPF neighbor states progress through a sequence:</strong> Down -> Init (Hello received but own router ID not listed) -> 2-Way (both routers see each other in Hellos — DR/BDR election happens here on broadcast networks) -> ExStart (master/slave negotiation for DB exchange) -> Exchange (database description packets exchanged) -> Loading (LSA requests and updates) -> <strong>Full</strong> (databases synchronized, adjacency complete). On broadcast networks, routers that are not the DR or BDR stop at <strong>2-Way</strong> with each other — only DR/BDR relationships reach Full state.</p><p><strong>Troubleshooting stuck states:</strong> Stuck in <strong>Init</strong> = the neighbor's Hellos aren't reaching you (ACL blocking OSPF multicast 224.0.0.5/224.0.0.6, or interface issue). Stuck in <strong>ExStart/Exchange</strong> = MTU mismatch between the two interfaces (this is a classic hard-to-diagnose issue). Stuck at <strong>2-Way</strong> = normal on broadcast networks for non-DR/BDR pairs. If expected to be Full but stuck at 2-Way, check if DR/BDR election is occurring.</p>",
    visual: { type: "handshake", params: { leftLabel: "R1", rightLabel: "R2", steps: ["Hello (area 0, timer 10) ->", "<- Hello (area 0, timer 10)", "2-Way State", "DB Exchange ->", "<- DB Exchange", "Full Adjacency"] } },
    hack: {
      memory: "HAMSS: Hello/Dead timers, Area ID, MTU (for ExStart), Subnet mask, Stub flag — all must match. If neighbors are stuck in Init, check Hello packet delivery. If stuck in ExStart, check MTU. If stuck in 2-Way, check DR/BDR (2-Way is normal for DROther-to-DROther). The mnemonic 'HAM SubS' covers the match requirements.",
      practice: "In Packet Tracer: (1) Configure OSPF between two routers and verify Full adjacency. (2) Change the Hello timer on one side: 'ip ospf hello-interval 15'. Watch the adjacency break. (3) Fix it. (4) Change the area ID on one side and observe. (5) Change the subnet mask on one side and observe. For each mismatch, run 'show ip ospf neighbor' and note the stuck state. Build a troubleshooting table: symptom -> cause -> fix.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 24-26 (OSPF) cover neighbor formation in detail. Wendell Odom OCG Chapters 19-21 explain the state machine and match requirements. 'Why are my OSPF neighbors not forming?' is the #1 OSPF troubleshooting question on the CCNA. Memorize the match requirements and check each one systematically. The exam will present you with mismatched parameters and ask you to identify the problem."
    }
  },

  "3.4.b": {
    info: "<p>On <strong>point-to-point network types</strong>, OSPF operates without a DR/BDR election because there are exactly two routers on the link — there is no multi-access contention to manage. Serial links, GRE tunnels, and point-to-point subinterfaces use this network type by default. The adjacency progresses directly from 2-Way to ExStart/Exchange to Full without waiting for a DR/BDR election timer.</p><p>The OSPF <strong>Hello interval on point-to-point</strong> networks defaults to <strong>10 seconds</strong> (same as broadcast), with a Dead interval of <strong>40 seconds</strong>. OSPF multicast Hellos are sent to <strong>224.0.0.5</strong> (AllSPFRouters) since there is no DR to address specifically. Both routers form a Full adjacency directly — you will see <code>FULL/ -</code> in the <code>show ip ospf neighbor</code> output, where the dash indicates no DR/BDR role applies.</p><p>A critical exam topic: you can <strong>manually change the OSPF network type</strong> on an Ethernet interface to point-to-point using <code>ip ospf network point-to-point</code>. This is commonly done on links where only two routers share a subnet (e.g., a /30 or /31 link between two routers connected via Ethernet). Benefits: no DR/BDR election overhead, faster convergence (no 40-second wait for DR election), and simpler operation. The downside: if more than two routers share the segment, point-to-point won't work properly because OSPF assumes only one neighbor.</p><p>On point-to-point links, <strong>OSPF neighbor discovery is automatic</strong> via multicast Hellos — you don't need to manually specify neighbors (unlike NBMA networks). This makes P2P the simplest OSPF network type to configure. Verify with <code>show ip ospf interface [interface]</code> — the output will show <code>Network Type POINT_TO_POINT</code> and <code>No designated router on this network</code>.</p>",
    visual: { type: "handshake", params: { leftLabel: "R1", rightLabel: "R2", steps: ["Hello (P2P) ->", "<- Hello (P2P)", "ExStart/Exchange", "Full (No DR/BDR)"] } },
    hack: {
      memory: "Point-to-point = two-person conversation. No need to elect a moderator (DR) when only two people are talking. Adjacency goes straight to Full. You can FORCE Ethernet to act as P2P with 'ip ospf network point-to-point' — useful on /30 links between two routers to skip DR election and converge faster.",
      practice: "In Packet Tracer: (1) Configure OSPF on a serial link between two routers. (2) Run 'show ip ospf interface [serial-int]' and verify Network Type POINT_TO_POINT and 'No designated router.' (3) Run 'show ip ospf neighbor' and verify FULL/ - state. (4) Now configure two routers on an Ethernet /30 link, run 'ip ospf network point-to-point' on both interfaces, and verify the same P2P behavior on Ethernet.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 25 (OSPF Part 2) covers network types including P2P. Wendell Odom OCG Chapters 19-21. The exam tests: (1) P2P networks have no DR/BDR. (2) You can change Ethernet to P2P with 'ip ospf network point-to-point.' (3) P2P Hello default is 10 seconds. Know that 'FULL/ -' in the neighbor table means P2P (the dash = no DR role)."
    }
  },

  "3.4.c": {
    info: "<p>On <strong>broadcast network types</strong> (Ethernet LANs), OSPF elects a <strong>Designated Router (DR)</strong> and <strong>Backup Designated Router (BDR)</strong> to reduce flooding overhead. Without a DR, every router would form a full adjacency with every other router on the segment (n*(n-1)/2 adjacencies), creating an explosion of LSA flooding. With a DR, all routers send their LSAs to the DR, and the DR reflood them to everyone else — reducing the number of full adjacencies dramatically.</p><p><strong>DR/BDR election process:</strong> (1) The router with the <strong>highest OSPF priority</strong> becomes DR. Default priority is 1 (range 0-255). (2) If priorities are tied, the router with the <strong>highest Router ID</strong> wins. (3) Priority <strong>0</strong> means 'I refuse to participate in the election' — that router will never become DR or BDR. The second-highest priority/RID becomes the BDR. All other routers are <strong>DROther</strong>.</p><p><strong>Non-preemptive election — this is critical:</strong> Once a DR is elected, it remains the DR until it fails, even if a router with a higher priority joins the segment later. Changing the OSPF priority on a router does NOT trigger a new election. To force a new election, you must either restart the OSPF process (<code>clear ip ospf process</code>) or shut down the DR's interface. The exam specifically tests this 'non-preemptive' behavior as a trap question.</p><p><strong>Adjacency behavior on broadcast networks:</strong> DROther routers form <strong>Full adjacency only with the DR and BDR</strong>. DROther-to-DROther relationships remain in <strong>2-Way state</strong> — this is normal and expected. Hellos are sent to <strong>224.0.0.5</strong> (AllSPFRouters), and LSA updates are sent to the DR/BDR at <strong>224.0.0.6</strong> (AllDRouters). The DR refloods updates to 224.0.0.5.</p><p><strong>Configuration:</strong> Set OSPF priority per interface: <code>ip ospf priority [0-255]</code>. Verify DR/BDR roles with <code>show ip ospf neighbor</code> (look for DR, BDR, or DROTHER in the State column) or <code>show ip ospf interface [int]</code> (shows 'Designated Router' and 'Backup Designated Router' IPs).</p>",
    visual: { type: "hierarchy", params: { root: "DR Election", children: [{ name: "Highest Priority Wins", children: [{ name: "Tie? Highest Router ID" }, { name: "Priority 0 = Never DR" }] }, { name: "Non-preemptive", children: [{ name: "DR stays until failure" }] }] } },
    hack: {
      memory: "DR election = class president election: highest priority wins, ties broken by student ID (router ID). Priority 0 = 'I'm not running.' Non-preemptive = once elected, you're president until you leave school — a transfer student with higher grades (priority) does NOT trigger a re-election. DROther-to-DROther stays 2-Way (normal, not a problem).",
      practice: "In Packet Tracer: (1) Connect 3 routers to a common switch. (2) Set priorities: R1=100, R2=50, R3=0. (3) Enable OSPF and verify DR/BDR with 'show ip ospf neighbor.' R1 should be DR, R2 should be BDR, R3 should be DROther. (4) Change R3's priority to 200 — verify with 'show ip ospf neighbor' that R1 is STILL DR (non-preemptive). (5) Run 'clear ip ospf process' on all routers — now R3 should become DR.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 25 (OSPF Part 2) covers DR/BDR election in depth. Wendell Odom OCG Chapters 19-21. Non-preemptive behavior is the #1 DR election trap question. Students who assume 'higher priority always becomes DR immediately' will get the question wrong. The correct answer: changing priority has NO immediate effect — DR stays until it fails or OSPF restarts."
    }
  },

  "3.4.d": {
    info: "<p>The <strong>OSPF Router ID (RID)</strong> is a 32-bit value (formatted like an IP address) that uniquely identifies each router within the OSPF domain. Every OSPF router must have a unique RID — duplicate RIDs cause serious problems including database corruption and routing loops. The RID appears in Hello packets, LSAs, and the output of <code>show ip ospf</code>.</p><p><strong>Router ID selection follows a strict priority:</strong></p><ol><li><strong>Manually configured:</strong> <code>router-id X.X.X.X</code> under the OSPF process. This is always the best practice because it's explicit and predictable. Example: <code>router-id 1.1.1.1</code>.</li><li><strong>Highest loopback IP:</strong> If no manual RID is set, the router uses the highest IP address configured on any loopback interface, even if that loopback is not running OSPF. Loopback IPs are preferred because they never go down (no physical link to fail).</li><li><strong>Highest active physical IP:</strong> If no loopback exists and no manual RID is set, the router uses the highest IP on any active (up/up) physical interface at the time the OSPF process starts.</li></ol><p><strong>Critical behavior:</strong> The Router ID is determined <strong>at OSPF process startup</strong> and does NOT change automatically afterward, even if you add a higher loopback IP or change the <code>router-id</code> command. To apply a new Router ID, you must restart the OSPF process with <code>clear ip ospf process</code> (which temporarily drops all OSPF neighbor adjacencies). This restart requirement is frequently tested.</p><p><strong>Verification:</strong> <code>show ip ospf</code> displays the current Router ID at the top of the output. <code>show ip protocols</code> also shows the RID under the OSPF section. If you see unexpected DR election results, check the RID — it may have been auto-selected from an unexpected interface and might be influencing election outcomes.</p>",
    visual: { type: "state-machine", params: { states: ["1. Manual router-id", "2. Highest Loopback IP", "3. Highest Physical IP"], active: 0, transitions: true } },
    hack: {
      memory: "Router ID priority = MLP: Manual, Loopback, Physical. 'My Loopback's Perfect.' Manual always wins. If not set, highest loopback. If no loopback, highest active physical IP. IMPORTANT: the RID is locked at OSPF startup — changing it requires 'clear ip ospf process' to take effect. This restart drops ALL adjacencies temporarily.",
      practice: "In Packet Tracer: (1) Configure a router with Gi0/0 = 10.0.0.1 and Loopback0 = 192.168.1.1. (2) Start OSPF without a manual router-id. (3) Run 'show ip ospf' — RID should be 192.168.1.1 (highest loopback). (4) Add 'router-id 1.1.1.1' under OSPF. (5) Run 'show ip ospf' again — RID is STILL 192.168.1.1 (hasn't restarted). (6) Run 'clear ip ospf process' and confirm — now RID is 1.1.1.1.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 24 (OSPF Part 1) covers Router ID selection. Wendell Odom OCG Chapters 19-20. The exam tests two things: (1) the MLP priority order, and (2) the fact that changing the RID requires 'clear ip ospf process.' Always manually set router-id in production for predictability."
    }
  },

  "3.4.e": {
    info: "<p><strong>Single-area OSPFv2 configuration</strong> involves enabling the OSPF process, setting the router ID, and activating OSPF on the correct interfaces. There are two methods to enable OSPF on interfaces: the <strong>network command</strong> (traditional) and the <strong>interface-level command</strong> (modern, preferred).</p><p><strong>Method 1 — Network command (traditional):</strong><br><code>router ospf 1</code> — enters OSPF config mode. The process ID (1) is <strong>locally significant only</strong> — it does NOT need to match between routers. Multiple OSPF processes can run on the same router with different process IDs.<br><code>router-id 1.1.1.1</code> — sets the RID (best practice).<br><code>network 10.0.0.0 0.0.0.255 area 0</code> — enables OSPF on all interfaces whose IP falls within the 10.0.0.x range, placing them in area 0. The <strong>wildcard mask</strong> (0.0.0.255) is the inverse of the subnet mask — it specifies which bits must match and which are 'don't care.'</p><p><strong>Method 2 — Interface-level command (modern):</strong><br><code>interface GigabitEthernet0/0</code><br><code>ip ospf 1 area 0</code> — enables OSPF process 1 in area 0 directly on this interface. This method avoids wildcard mask confusion and is more explicit about which interfaces participate in OSPF. Cisco recommends this for clarity.</p><p><strong>Wildcard mask tips:</strong> The wildcard is the inverse of the subnet mask. /24 mask (255.255.255.0) = wildcard 0.0.0.255. /30 mask (255.255.255.252) = wildcard 0.0.0.3. /32 (exact match on one IP) = wildcard 0.0.0.0. You can also use <code>network 10.0.0.1 0.0.0.0 area 0</code> to match a single interface by its exact IP — this is the safest approach with the network command.</p><p><strong>Single-area OSPF</strong> places all interfaces in <strong>area 0</strong> (the backbone area). This is the simplest OSPF design and is all that's tested on the CCNA. In production, multi-area OSPF is used for large networks, but the CCNA only covers single-area concepts. After configuration, verify with <code>show ip ospf neighbor</code> (adjacencies formed), <code>show ip ospf interface brief</code> (all OSPF-enabled interfaces), and <code>show ip route ospf</code> (learned routes).</p>",
    visual: { type: "layer-stack", params: { layers: ["router ospf 1", "router-id 1.1.1.1", "network 10.0.0.0 0.0.0.255 area 0", "network 192.168.1.0 0.0.0.3 area 0", "passive-interface default"], highlight: 2 } },
    hack: {
      memory: "OSPF config recipe: 'router ospf [#]' (# is local only, doesn't need to match), 'router-id X.X.X.X', then either 'network [IP] [wildcard] area 0' or 'ip ospf [#] area 0' on each interface. Wildcard = inverse of subnet mask: /24 -> 0.0.0.255, /30 -> 0.0.0.3, /32 -> 0.0.0.0. The interface-level command ('ip ospf 1 area 0') avoids wildcard confusion entirely.",
      practice: "In Packet Tracer: (1) Build a 3-router triangle (all Ethernet). (2) Configure OSPF using the network command on R1: 'router ospf 1', 'router-id 1.1.1.1', 'network 10.0.0.0 0.0.0.255 area 0'. (3) Configure OSPF using the interface command on R2: 'ip ospf 1 area 0' under each interface. (4) Configure R3 either way. (5) Verify all neighbors reach Full state with 'show ip ospf neighbor'. (6) Verify all networks are learned with 'show ip route ospf'. (7) Practice calculating wildcard masks: given 192.168.1.0/24, what wildcard? (0.0.0.255). Given 10.0.0.0/30? (0.0.0.3).",
      effort: "high",
      meta: "Jeremy's IT Lab Day 24-26 (OSPF Parts 1-3) cover configuration start to finish. Wendell Odom OCG Chapters 19-21. The exam expects you to configure OSPF in a lab sim. Both methods (network and interface-level) may be tested. Wildcard masks are the #1 student stumbling block — practice converting subnet masks to wildcards until it's instant. The process ID is locally significant (common trick question)."
    }
  },

  "3.4.f": {
    info: "<p>A <strong>passive interface</strong> in OSPF is an interface that has its network advertised into OSPF but does <strong>not send or receive Hello packets</strong>. This means the passive interface's subnet is included in the OSPF database and shared with all OSPF neighbors, but the router will never attempt to form an OSPF adjacency on that interface. Passive interfaces are used on segments facing end devices (PCs, servers, printers) where no OSPF router exists.</p><p><strong>Why use passive interfaces?</strong> (1) <strong>Security:</strong> Prevents unauthorized routers connected to user-facing ports from forming OSPF adjacencies and injecting rogue routes. (2) <strong>Efficiency:</strong> Eliminates unnecessary Hello packet overhead on links where no OSPF neighbor can exist. (3) <strong>Best practice:</strong> Every interface not facing another OSPF router should be passive.</p><p><strong>Configuration — two approaches:</strong></p><ul><li><strong>Selective passive:</strong> <code>passive-interface GigabitEthernet0/2</code> — makes only Gi0/2 passive. Use when most interfaces need to form adjacencies.</li><li><strong>Default passive (recommended):</strong> <code>passive-interface default</code> — makes ALL interfaces passive, then <code>no passive-interface GigabitEthernet0/0</code> and <code>no passive-interface GigabitEthernet0/1</code> to selectively enable adjacency formation only on interfaces facing other routers. This is the <strong>Cisco-recommended best practice</strong> because it's safer — any new interface added is automatically passive until explicitly enabled.</li></ul><p><strong>Verification:</strong> <code>show ip ospf interface [interface]</code> displays whether an interface is passive: a passive interface shows <code>No Hellos (Passive interface)</code>. <code>show ip protocols</code> under the OSPF section lists all passive interfaces. If an OSPF adjacency isn't forming and the configuration looks correct, check if the interface is accidentally set to passive — this is a common troubleshooting scenario.</p>",
    visual: { type: "comparison", params: { left: { label: "Active Interface", items: ["Sends Hellos", "Forms adjacencies", "Advertises network", "Facing other routers"] }, right: { label: "Passive Interface", items: ["No Hellos sent", "No adjacencies", "Still advertises network", "Facing end devices"] } } },
    hack: {
      memory: "Passive = 'I'll tell everyone about my subnet, but I won't talk on that interface.' Like posting a sign outside your door (network advertised) but never opening it (no Hellos). Best practice: 'passive-interface default' then 'no passive-interface' only on router-facing interfaces. If OSPF neighbors won't form, check if the interface is accidentally passive.",
      practice: "In Packet Tracer: (1) Configure OSPF with 'passive-interface default'. (2) Run 'show ip ospf neighbor' — no neighbors should form. (3) Add 'no passive-interface Gi0/0' and 'no passive-interface Gi0/1' on router-facing interfaces. (4) Verify neighbors form. (5) Verify the user-facing interface's subnet still appears in 'show ip route ospf' on other routers even though it's passive. (6) Run 'show ip protocols' and confirm the passive interface list.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 25 (OSPF Part 2) covers passive interfaces. Wendell Odom OCG Chapter 20. The exam tests: (1) passive interfaces still advertise their networks, (2) 'passive-interface default' is best practice, (3) accidentally passive interfaces prevent adjacencies. A common exam trap: 'OSPF is configured correctly but neighbors won't form' — check passive-interface settings first."
    }
  },

  "3.4.g": {
    info: "<p><strong>OSPF cost</strong> is the metric OSPF uses to determine the best path to a destination. The cost of a single interface is calculated as: <code>cost = reference bandwidth / interface bandwidth</code>. The total cost of a path is the <strong>sum of all interface costs</strong> along the route from source to destination (only outgoing/outbound interface costs are counted).</p><p><strong>Default reference bandwidth is 100 Mbps</strong>, which creates a significant problem: 100 Mbps / 100 Mbps (FastEthernet) = cost 1, and 100 Mbps / 1000 Mbps (GigabitEthernet) = cost 0.1, rounded to <strong>1</strong>. Both FastEthernet and GigabitEthernet have the same OSPF cost! This means OSPF cannot differentiate between 100 Mbps and 1 Gbps links (or 10 Gbps, 40 Gbps, etc.) using the default reference. The minimum OSPF cost is always 1.</p><p><strong>Fix: Change the reference bandwidth.</strong> Under the OSPF process, configure <code>auto-cost reference-bandwidth 10000</code> (10 Gbps in Mbps). Now: 10 Mbps = cost 1000, 100 Mbps = cost 100, 1 Gbps = cost 10, 10 Gbps = cost 1. This lets OSPF properly differentiate between modern link speeds. <strong>Critical:</strong> This must be configured on <strong>every OSPF router in the domain</strong> — inconsistent reference bandwidth across routers causes suboptimal routing because routers disagree on link costs.</p><p><strong>Manual cost override:</strong> You can set the cost on a specific interface directly with <code>ip ospf cost [value]</code> under the interface. This overrides the calculated cost. Use this when you need fine-grained control over path selection. The manually set cost always takes precedence over the auto-calculated cost.</p><p><strong>Cost calculation practice:</strong> Given reference 10000: What is the cost of a 100 Mbps link? 10000/100 = 100. What is the cost of a 1 Gbps link? 10000/1000 = 10. If a path traverses a 1 Gbps link (cost 10) and a 100 Mbps link (cost 100), the total path cost is 110. Verify with <code>show ip ospf interface brief</code> (shows cost per interface) or <code>show ip ospf interface [int]</code> (detailed, including timers and cost).</p>",
    visual: { type: "comparison", params: { left: { label: "Default Ref (100 Mbps)", items: ["10 Mbps = cost 10", "100 Mbps = cost 1", "1 Gbps = cost 1", "10 Gbps = cost 1"] }, right: { label: "Ref 10000 Mbps", items: ["10 Mbps = cost 1000", "100 Mbps = cost 100", "1 Gbps = cost 10", "10 Gbps = cost 1"] } } },
    hack: {
      memory: "Cost = reference / bandwidth. Default reference 100 Mbps makes FastEthernet AND GigE both cost 1 (bad). Fix: 'auto-cost reference-bandwidth 10000' on EVERY router. Then: 10G=1, 1G=10, 100M=100, 10M=1000. Manual override: 'ip ospf cost [value]' on the interface. The exam WILL ask you to calculate cost given a reference bandwidth.",
      practice: "In Packet Tracer: (1) Build a 3-router topology with mixed link speeds. (2) Run 'show ip ospf interface brief' and note all links show cost 1 (default reference problem). (3) Configure 'auto-cost reference-bandwidth 10000' on all routers. (4) Verify costs now differ with 'show ip ospf interface brief'. (5) Calculate the total path cost by summing interface costs along a route and verify it matches 'show ip route ospf' metric. (6) Set 'ip ospf cost 5' on one interface and verify the override.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 25 (OSPF Part 2) covers cost calculation and reference bandwidth. Wendell Odom OCG Chapters 19-21. The exam expects you to: (1) calculate cost given reference and bandwidth, (2) know the default reference is 100 Mbps, (3) know 'auto-cost reference-bandwidth' must be consistent across all routers, (4) know 'ip ospf cost' overrides the calculated value."
    }
  },

  "3.4.h": {
    info: "<p>To advertise a <strong>default route into OSPF</strong>, use the command <code>default-information originate</code> under the OSPF router process on the edge router (the router connected to the ISP or internet). This causes the router to create an <strong>OSPF External Type 2 (E2) LSA</strong> for 0.0.0.0/0 and flood it to all OSPF neighbors, giving every router in the OSPF domain a path to the internet through this edge router.</p><p><strong>Without the 'always' keyword:</strong> <code>default-information originate</code> only advertises the default route if the router <strong>already has a default route in its own routing table</strong>. This means you must first configure a static default route (e.g., <code>ip route 0.0.0.0 0.0.0.0 [ISP-next-hop]</code>) before OSPF will advertise it. If the static default route is removed or the ISP link goes down, OSPF stops advertising the default — which is actually the desired behavior (don't advertise a path that doesn't work).</p><p><strong>With the 'always' keyword:</strong> <code>default-information originate always</code> advertises the default route <strong>regardless of whether the router has a default route in its table</strong>. Use this when the router will always be the internet exit point, even if its own default route comes and goes. This is less safe because it can advertise a default route when the internet path is actually down.</p><p>On receiving routers, the default route appears as <code>O*E2 0.0.0.0/0 [110/1] via [edge-router-IP]</code> — the <strong>O*</strong> indicates an OSPF-learned candidate default, and <strong>E2</strong> means it's an external type 2 route. The metric for E2 routes does not increment as they traverse the OSPF domain (the cost stays at 1 regardless of how many hops away the edge router is). Verify with <code>show ip route</code> and check that 'Gateway of last resort' is set on all internal routers.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Edge Router", "default-information originate", "OSPF Area 0", "All Routers Learn 0.0.0.0/0"], color: "#f43f5e" } },
    hack: {
      memory: "'default-information originate' = 'I know the way to the internet — follow me.' Without 'always', the router must actually HAVE a default route first (honest advertising). With 'always', it advertises regardless (potentially lying if the internet path is down). The result on other routers: O*E2 0.0.0.0/0 — OSPF external type 2 candidate default.",
      practice: "In Packet Tracer: (1) Configure a static default route on the edge router: 'ip route 0.0.0.0 0.0.0.0 [ISP-IP]'. (2) Under OSPF, add 'default-information originate'. (3) On internal routers, verify 'show ip route' shows 'O*E2 0.0.0.0/0' and 'Gateway of last resort is set.' (4) Remove the static default route on the edge router — verify internal routers LOSE the default. (5) Add 'default-information originate always' — verify internal routers regain the default even without the static route on the edge.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 26 (OSPF Part 3) covers default route injection. Wendell Odom OCG Chapter 21. The 'always' keyword is a common exam trap. Without it: needs a default route in the local table first. With it: advertises unconditionally. The exam will test this distinction directly."
    }
  },

  "3.4.i": {
    info: "<p><strong>OSPF verification</strong> is essential for confirming that the protocol is operating correctly. The key show commands every CCNA candidate must know:</p><p><strong><code>show ip ospf neighbor</code></strong> — The most important OSPF verification command. Shows all OSPF neighbors, their state (Full, 2-Way, Init, etc.), their Router ID, priority, DR/BDR role, dead timer countdown, and the interface through which they're reached. <strong>What to look for:</strong> All expected neighbors should show <strong>FULL</strong> state (except DROther-to-DROther pairs which show 2-Way on broadcast networks). If a neighbor is stuck in <strong>Init</strong>, Hellos aren't being received properly. Stuck in <strong>ExStart</strong> usually means MTU mismatch.</p><p><strong><code>show ip ospf interface [interface]</code></strong> — Shows detailed OSPF information for an interface: area, cost, Hello/Dead intervals, network type (broadcast/point-to-point), DR/BDR IPs, neighbor count, and whether the interface is passive. Use this to verify cost calculations, check timer settings, confirm the network type, and diagnose passive-interface issues. <code>show ip ospf interface brief</code> provides a compact summary of all OSPF-enabled interfaces with their area, cost, and state.</p><p><strong><code>show ip ospf</code></strong> — Shows the OSPF process information: Router ID, process ID, area configuration, SPF calculation statistics, and reference bandwidth. Use this to verify the Router ID is correct and the reference bandwidth is set properly.</p><p><strong><code>show ip route ospf</code></strong> — Filters the routing table to show only OSPF-learned routes. Routes are marked with O (intra-area), O IA (inter-area), O E1/E2 (external type 1/2). Each entry shows the AD (110), metric (cost), next hop, and exit interface. Use this to verify that expected remote networks are being learned.</p><p><strong><code>show ip protocols</code></strong> — Shows all running routing protocols, including OSPF process ID, Router ID, advertised networks, passive interfaces, and the routing source for learned routes. Useful for a quick overview of the entire OSPF configuration.</p>",
    visual: { type: "comparison", params: { left: { label: "Neighbor Verification", items: ["show ip ospf neighbor", "Look for FULL state", "Check DR/BDR roles"] }, right: { label: "Interface Verification", items: ["show ip ospf interface", "Check cost, timers", "Verify area, network type"] } } },
    hack: {
      memory: "The OSPF 'Big 5' show commands: (1) 'show ip ospf neighbor' = who are my friends and what state? (2) 'show ip ospf interface' = what are my settings per interface? (3) 'show ip ospf' = what's my Router ID and process config? (4) 'show ip route ospf' = what routes did OSPF teach me? (5) 'show ip protocols' = overview of all routing config. Memorize these — the exam shows you their output and asks you to diagnose problems.",
      practice: "After every OSPF lab, run ALL five commands and interpret every field: (1) In 'show ip ospf neighbor', identify Full vs 2-Way vs stuck states. (2) In 'show ip ospf interface', verify cost, timers, network type, and passive status. (3) In 'show ip ospf', confirm Router ID and reference bandwidth. (4) In 'show ip route ospf', trace learned routes back to their source. (5) In 'show ip protocols', verify all expected networks are listed. Practice until you can read each output in under 10 seconds.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 24-26 (OSPF) use these commands extensively. Wendell Odom OCG Chapters 19-21 show sample outputs for each. The exam frequently presents OSPF show command output and asks you to identify the problem — mismatched timers, wrong area, incorrect cost, stuck neighbor state, or missing passive-interface. Practice reading the output until you can spot issues at a glance."
    }
  },

  // ── 3.5  Describe first hop redundancy protocols ────────────

  "3.5.a": {
    info: "<p><strong>First Hop Redundancy Protocols (FHRPs)</strong> solve a critical problem in network design: hosts typically configure a single default gateway IP address. If that gateway router fails, all hosts on the subnet lose connectivity to remote networks — even if a second router is available on the same segment. FHRPs create a <strong>virtual IP address (VIP)</strong> shared between two or more routers, so hosts point to the VIP as their default gateway. If the active gateway router fails, another router seamlessly takes over the VIP, and hosts continue communicating without any configuration change.</p><p><strong>How it works:</strong> Two (or more) routers on the same subnet participate in the FHRP group. They share a <strong>virtual IP address</strong> and a <strong>virtual MAC address</strong>. One router is elected as the active/primary gateway — it responds to ARP requests for the virtual IP with the virtual MAC. The other router(s) stand by. When the active router fails (detected via missed Hello/advertisement messages), the standby router assumes both the virtual IP and virtual MAC, and hosts continue sending traffic to the same gateway addresses without interruption.</p><p><strong>Three FHRP protocols to know for the CCNA:</strong></p><ul><li><strong>HSRP (Hot Standby Router Protocol)</strong> — Cisco proprietary. Uses Active/Standby terminology.</li><li><strong>VRRP (Virtual Router Redundancy Protocol)</strong> — Open standard (RFC 5798). Uses Master/Backup terminology.</li><li><strong>GLBP (Gateway Load Balancing Protocol)</strong> — Cisco proprietary. Provides redundancy AND load balancing.</li></ul><p>The key concept: hosts configure the <strong>virtual IP</strong> as their default gateway, NOT any router's physical IP. This abstraction is what enables transparent failover. Without FHRPs, the only options would be manually reconfiguring every host's gateway after a failure or running a dynamic routing protocol on hosts (impractical).</p>",
    visual: { type: "hierarchy", params: { root: "Virtual IP: 10.0.0.1", children: [{ name: "Active Router (R1)", children: [{ name: "Physical: 10.0.0.2" }] }, { name: "Standby Router (R2)", children: [{ name: "Physical: 10.0.0.3" }] }] } },
    hack: {
      memory: "FHRP = two pilots in the cockpit sharing ONE call sign. Passengers (hosts) always radio 'the pilot' (virtual IP). If pilot 1 passes out, pilot 2 answers the same call sign — passengers never notice the switch. The virtual IP is the call sign. Hosts NEVER point to a physical router IP as their gateway in an FHRP setup.",
      practice: "Draw a diagram: two routers (R1: 10.0.0.2, R2: 10.0.0.3) sharing virtual IP 10.0.0.1 on a common switch. PCs use 10.0.0.1 as their gateway. Walk through: (1) Normal operation — R1 (active) responds to ARP for 10.0.0.1. (2) R1 fails — R2 takes over 10.0.0.1. (3) PCs don't change anything. This conceptual understanding is more important than configuration details for the exam.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 29 (First Hop Redundancy Protocols) covers all FHRPs. Wendell Odom OCG Chapter 18. The exam asks: 'which technology prevents a single point of failure at the default gateway?' (FHRP), 'what IP do hosts configure as their gateway?' (the virtual IP, NOT a physical router IP). Understand the WHY before memorizing protocol-specific details."
    }
  },

  "3.5.b": {
    info: "<p><strong>HSRP (Hot Standby Router Protocol)</strong> is a Cisco proprietary FHRP that provides gateway redundancy. In an HSRP group, one router is elected <strong>Active</strong> (forwards traffic and responds to ARP for the virtual IP), one is <strong>Standby</strong> (ready to take over if Active fails), and any additional routers are in <strong>Listen</strong> state (monitoring but not eligible to take over immediately).</p><p><strong>Key HSRP details:</strong></p><ul><li><strong>Virtual MAC address:</strong> <code>0000.0c07.acXX</code> where XX is the HSRP group number in hexadecimal. Group 1 = 0000.0c07.ac01, Group 10 = 0000.0c07.ac0a.</li><li><strong>Transport:</strong> UDP port <strong>1985</strong>, multicast to <strong>224.0.0.2</strong> (HSRPv1) or <strong>224.0.0.102</strong> (HSRPv2).</li><li><strong>Timers:</strong> Hello interval = <strong>3 seconds</strong>, Hold (dead) timer = <strong>10 seconds</strong>. If the Active router misses 3 Hellos (10 seconds), the Standby assumes it's dead and takes over.</li><li><strong>Priority:</strong> Range 0-255, default <strong>100</strong>. Highest priority becomes Active. Ties broken by highest IP address on the HSRP interface.</li><li><strong>Preemption:</strong> <strong>OFF by default</strong>. Even if a higher-priority router joins or returns, it does NOT become Active unless preemption is enabled.</li></ul><p><strong>Configuration:</strong> Under the interface: <code>standby [group] ip [virtual-ip]</code> enables HSRP. <code>standby [group] priority [value]</code> sets priority. <code>standby [group] preempt</code> enables preemption. Example: <code>standby 1 ip 10.0.0.1</code>, <code>standby 1 priority 110</code>, <code>standby 1 preempt</code>.</p><p><strong>Verification:</strong> <code>show standby</code> shows the HSRP group, state (Active/Standby), virtual IP, virtual MAC, priority, timers, and preemption status. <code>show standby brief</code> provides a compact summary across all groups and interfaces.</p>",
    visual: { type: "handshake", params: { leftLabel: "Active (R1)", rightLabel: "Standby (R2)", steps: ["Hello (priority 110) ->", "<- Hello (priority 100)", "R1 is Active", "R1 fails ->", "<- R2 becomes Active", "Hosts see no change"] } },
    hack: {
      memory: "HSRP = Cisco proprietary. Active/Standby terminology. Virtual MAC = 0000.0c07.acXX (group in hex). UDP 1985. Hello 3s, Hold 10s. Priority default 100 (higher wins). Preempt OFF by default. Mnemonic: 'Hot Standby = Hollywood Stunt Person' — the stunt double (Standby) waits, then takes the star's (Active's) place. Cisco-only, like Hollywood.",
      practice: "In Packet Tracer: (1) Connect R1 and R2 to a switch with PCs. (2) Configure HSRP group 1: R1 with priority 110, R2 with priority 100, virtual IP 10.0.0.1. (3) Run 'show standby brief' — R1 should be Active. (4) Shut R1's interface — R2 becomes Active. (5) Bring R1 back — R2 STAYS Active (preempt is off). (6) Add 'standby 1 preempt' on R1, shut/unshut R1's interface — now R1 reclaims Active.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 29 (FHRP) covers HSRP configuration and verification. Wendell Odom OCG Chapter 18. The exam differentiates HSRP from VRRP by terminology — 'Active/Standby' = HSRP, 'Master/Backup' = VRRP. Also know: virtual MAC format, UDP 1985, preempt OFF by default, timers 3/10."
    }
  },

  "3.5.c": {
    info: "<p><strong>HSRPv2</strong> is the updated version of HSRP that addresses several limitations of version 1. While HSRPv1 is sufficient for most deployments, v2 adds important capabilities:</p><p><strong>Key differences between HSRPv1 and HSRPv2:</strong></p><ul><li><strong>Group numbers:</strong> v1 supports groups <strong>0-255</strong>. v2 expands to <strong>0-4095</strong> — important for large-scale deployments with many VLANs.</li><li><strong>Virtual MAC format:</strong> v1 uses <code>0000.0c07.acXX</code>. v2 uses <code>0000.0c9f.fXXX</code> — the last three hex digits encode the group number, accommodating the larger group range.</li><li><strong>Multicast address:</strong> v1 uses <strong>224.0.0.2</strong>. v2 uses <strong>224.0.0.102</strong> (IPv4) or <strong>ff02::66</strong> (IPv6). The dedicated v2 multicast prevents interference with other protocols using 224.0.0.2.</li><li><strong>IPv6 support:</strong> v1 is IPv4 only. v2 supports both <strong>IPv4 and IPv6</strong>.</li><li><strong>Compatibility:</strong> v1 and v2 are <strong>NOT compatible</strong> — all routers in an HSRP group must run the same version. Mixing versions causes the adjacency to fail.</li></ul><p><strong>Configuration:</strong> <code>standby version 2</code> under the interface switches to HSRPv2. The remaining HSRP commands are the same. Verify with <code>show standby</code> — the output will show 'Version 2' and the updated MAC address format.</p><p>In practice, most modern deployments use HSRPv2 because of IPv6 support and the larger group number range. The exam expects you to know the comparison points between v1 and v2, not deep configuration details of v2.</p>",
    visual: { type: "comparison", params: { left: { label: "HSRPv1", items: ["Groups 0-255", "MAC: 0000.0c07.acXX", "Multicast: 224.0.0.2", "IPv4 only"] }, right: { label: "HSRPv2", items: ["Groups 0-4095", "MAC: 0000.0c9f.fXXX", "Multicast: 224.0.0.102", "IPv4 + IPv6"] } } },
    hack: {
      memory: "HSRPv2 = 'More of everything.' More groups (4095 vs 255), more addressing (IPv6), new MAC (0000.0c9f.fXXX), dedicated multicast (224.0.0.102). NOT backward compatible — all routers in the group must match versions. Command to switch: 'standby version 2' under the interface.",
      practice: "Make a comparison flashcard: HSRPv1 vs HSRPv2 with group range, MAC format, multicast, and IPv6 support. This single card covers 90% of what the exam asks about v2. No deep lab needed — configure 'standby version 2' in Packet Tracer and compare 'show standby' output between versions.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 29 (FHRP) mentions v2 differences. Wendell Odom OCG Chapter 18. The exam rarely deep-dives HSRPv2, but you must know: (1) IPv6 support, (2) different MAC/multicast, (3) larger group range, (4) not compatible with v1. One comparison flashcard is sufficient."
    }
  },

  "3.5.d": {
    info: "<p><strong>VRRP (Virtual Router Redundancy Protocol)</strong> is an open-standard FHRP defined in RFC 5798 that provides the same gateway redundancy as HSRP but is vendor-neutral. VRRP uses different terminology and has several behavioral differences from HSRP that the exam specifically tests.</p><p><strong>Key VRRP characteristics:</strong></p><ul><li><strong>Terminology:</strong> <strong>Master</strong> (active gateway, equivalent to HSRP Active) and <strong>Backup</strong> (standby, equivalent to HSRP Standby). If you see 'Master/Backup' on the exam, it's VRRP.</li><li><strong>Virtual IP can match a physical IP:</strong> Unlike HSRP, the virtual IP CAN be the same as one router's actual interface IP. The router whose physical IP matches the VIP automatically becomes the 'IP address owner' and has priority 255.</li><li><strong>Transport:</strong> IP protocol <strong>112</strong> (not UDP — VRRP uses its own IP protocol number), multicast to <strong>224.0.0.18</strong>.</li><li><strong>Timers:</strong> Master Advertisement interval = <strong>1 second</strong> (faster than HSRP's 3-second Hello). Dead interval is typically 3x advertisement (3 seconds).</li><li><strong>Preemption:</strong> <strong>ON by default</strong>. This is a critical difference from HSRP (which has preempt OFF by default). A higher-priority router will automatically reclaim the Master role when it comes back online.</li><li><strong>Priority:</strong> Range 1-254, default <strong>100</strong>. Priority 255 is reserved for the IP address owner.</li></ul><p>The most common exam question compares HSRP and VRRP side by side: terminology (Active/Standby vs Master/Backup), preemption default (off vs on), virtual IP rules (cannot match physical vs can match physical), and transport (UDP 1985 vs IP protocol 112).</p>",
    visual: { type: "comparison", params: { left: { label: "HSRP (Cisco)", items: ["Active / Standby", "Virtual IP != physical", "Preempt off by default", "UDP 1985"] }, right: { label: "VRRP (Open)", items: ["Master / Backup", "Virtual IP can = physical", "Preempt on by default", "IP protocol 112"] } } },
    hack: {
      memory: "VRRP = open standard (RFC 5798). Master/Backup terminology. Preempt ON by default (assertive). Virtual IP CAN equal a physical IP (IP owner gets priority 255). IP protocol 112, multicast 224.0.0.18. Advertisement 1 second (faster than HSRP's 3s). The exam differentiates by terminology: see 'Master/Backup'? It's VRRP. See 'Active/Standby'? It's HSRP.",
      practice: "Create a detailed comparison table: HSRP vs VRRP covering roles, preemption default, virtual IP rules, transport, multicast address, timers, priority range/default, and vendor. This table is your primary study tool for FHRP exam questions. The exam tests differences, not individual protocol details.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 29 (FHRP) compares HSRP and VRRP. Wendell Odom OCG Chapter 18. The exam almost always includes one HSRP-vs-VRRP comparison question. Guaranteed comparison points: terminology, preemption default, and virtual IP rules. Know that VRRP allows the virtual IP to match a router's physical IP — this is unique to VRRP."
    }
  },

  "3.5.e": {
    info: "<p><strong>GLBP (Gateway Load Balancing Protocol)</strong> is a Cisco proprietary FHRP that is unique because it provides both <strong>gateway redundancy AND load balancing</strong> across multiple routers simultaneously. HSRP and VRRP only provide redundancy — one router handles all traffic while others sit idle. GLBP distributes traffic across all participating routers, utilizing the full bandwidth of every gateway.</p><p><strong>How GLBP works:</strong> One router is elected as the <strong>AVG (Active Virtual Gateway)</strong> — this router answers ARP requests for the virtual IP address. However, instead of always responding with the same virtual MAC, the AVG cycles through <strong>different virtual MAC addresses</strong>, each assigned to a different router called an <strong>AVF (Active Virtual Forwarder)</strong>. Up to 4 AVFs can share the load.</p><p><strong>Example:</strong> Host A ARPs for the virtual IP 10.0.0.1. The AVG responds with MAC-A (belonging to R1/AVF1). Host B ARPs for 10.0.0.1. The AVG responds with MAC-B (belonging to R2/AVF2). Host C gets MAC-C (R3/AVF3). Now three different routers are forwarding traffic, all behind the same virtual IP. The load balancing is automatic and transparent to hosts.</p><p>GLBP uses <strong>round-robin</strong> (default), <strong>weighted</strong>, or <strong>host-dependent</strong> load-balancing algorithms. The virtual MAC format is <code>0007.b400.XXYY</code> where XX is the group and YY is the forwarder number. GLBP uses <strong>UDP port 3222</strong> and multicast <strong>224.0.0.102</strong>.</p><p>GLBP is tested at a conceptual level on the CCNA — you won't configure it, but you must know that it's the <strong>only FHRP that provides active-active load balancing</strong>, it uses AVG/AVF terminology, and it's Cisco proprietary.</p>",
    visual: { type: "hierarchy", params: { root: "Virtual IP: 10.0.0.1", children: [{ name: "AVG (R1) assigns MACs", children: [{ name: "AVF1: MAC-A (R1)" }, { name: "AVF2: MAC-B (R2)" }, { name: "AVF3: MAC-C (R3)" }] }] } },
    hack: {
      memory: "GLBP = only FHRP that load-balances. AVG = the dispatcher who assigns different virtual MACs to different hosts via ARP responses. AVFs = the workers who forward traffic. Same virtual IP, different MACs per host = traffic distributed across multiple routers. Cisco proprietary. 'GLBP = Gateway Load Balancing Protocol' — the name tells you what makes it special.",
      practice: "Draw the GLBP concept: virtual IP 10.0.0.1 with 3 routers. Host A asks 'who has 10.0.0.1?' and gets MAC-A (R1). Host B asks and gets MAC-B (R2). Both hosts use the same gateway IP but reach different routers. Understand this concept — GLBP configuration is not tested on the CCNA.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 29 (FHRP) covers GLBP conceptually. Wendell Odom OCG Chapter 18. GLBP appears as: 'which FHRP provides both redundancy and load balancing?' Answer: GLBP. 'What are the roles in GLBP?' Answer: AVG and AVF. Know it's Cisco-only. This is a 1-2 question topic at most — one flashcard covers it."
    }
  },

  "3.5.f": {
    info: "<p><strong>Preemption</strong> determines whether a higher-priority router can automatically reclaim the active/master role when it comes back online after a failure. This behavior differs between HSRP and VRRP, and the difference is one of the most tested FHRP concepts.</p><p><strong>HSRP — Preemption OFF by default:</strong> When the original Active router (high priority) fails, the Standby router takes over as Active. When the original router recovers and comes back online, it does NOT automatically reclaim the Active role — the current Active stays Active, and the returning router becomes Standby. To enable preemption: <code>standby [group] preempt</code>. With preemption enabled, the higher-priority router will immediately reclaim Active status upon return.</p><p><strong>VRRP — Preemption ON by default:</strong> When the Master fails, a Backup takes over. When the original Master recovers, it automatically reclaims the Master role because preemption is enabled by default. You would need to explicitly disable preemption with <code>no vrrp [group] preempt</code> to prevent this behavior (rarely done).</p><p><strong>Why preemption matters:</strong> In a well-designed network, the Active/Master router is typically chosen because it has the best path to the WAN/internet (e.g., it's directly connected to the ISP). After a failover, you want that router to reclaim its role when it recovers, because the backup router may have a suboptimal path. Without preemption, the network stays on the suboptimal path until an administrator manually intervenes.</p><p><strong>The exam trap:</strong> The question presents a scenario where HSRP is configured, the Active fails, the Standby takes over, and then the original Active recovers. 'Which router is now Active?' If preempt is NOT configured (the default for HSRP), the former Standby remains Active. Students who don't know the default preemption behavior will answer incorrectly.</p>",
    visual: { type: "state-machine", params: { states: ["R1 Active (pri 110)", "R1 Fails -> R2 Active", "R1 Returns", "Preempt ON: R1 reclaims", "Preempt OFF: R2 stays"], active: 3, transitions: true } },
    hack: {
      memory: "HSRP preempt = OFF by default (polite — 'you took over while I was gone, I won't take it back'). VRRP preempt = ON by default (assertive — 'I'm back and I'm in charge again'). Enable HSRP preempt: 'standby [group] preempt.' Without it, the original Active does NOT reclaim its role after recovery. This default difference is a classic exam trap.",
      practice: "In Packet Tracer: (1) Configure HSRP with R1 priority 110 (Active) and R2 priority 100 (Standby). No preempt configured. (2) Verify R1 is Active. (3) Shut R1's HSRP interface — R2 becomes Active. (4) Bring R1 back up — R2 STAYS Active (no preempt). (5) Now add 'standby 1 preempt' on R1. (6) Shut/unshut R1 again — this time R1 reclaims Active. (7) Verify with 'show standby brief' after each step.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 29 (FHRP) demonstrates preemption behavior. Wendell Odom OCG Chapter 18. The preemption default difference (HSRP off, VRRP on) is tested every exam cycle. Also know: in GLBP, preemption controls exist per-AVF role. For the exam, focus on HSRP preemption — that's where the trap questions live."
    }
  },

  /* ══════════════════════════════════════════════════════════════
     DOMAIN 4 — IP SERVICES (10%)
  ══════════════════════════════════════════════════════════════ */

  // ── 4.1  Configure and verify NAT ───────────────────────────

  "4.1.a": {
    info: "<p><strong>NAT (Network Address Translation)</strong> modifies IP addresses in packet headers as traffic passes through a router, enabling private networks to communicate with the public internet. NAT defines <strong>four address types</strong> that describe addresses from different perspectives:</p><ul><li><strong>Inside Local:</strong> The private IP address of the host on the inside (private) network. Example: 192.168.1.10. This is the 'real' address the host is configured with.</li><li><strong>Inside Global:</strong> The public IP address that represents the inside host to the outside world. Example: 203.0.113.5. This is the translated address that external hosts see as the source.</li><li><strong>Outside Local:</strong> How an outside host appears to the inside network. Usually the same as Outside Global (unless outside NAT is configured, which is rare).</li><li><strong>Outside Global:</strong> The real public IP of the external host. Example: 8.8.8.8 (Google DNS).</li></ul><p><strong>The most important pair for the exam:</strong> Inside Local vs Inside Global. When a host with private IP 192.168.1.10 sends a packet through NAT, the source IP is translated from the Inside Local (192.168.1.10) to the Inside Global (203.0.113.5). When the response comes back, the destination is translated from Inside Global back to Inside Local. The Outside addresses typically remain unchanged because we rarely NAT the external server's address.</p><p>In <code>show ip nat translations</code>, all four addresses appear in the output columns: <code>Inside local</code>, <code>Inside global</code>, <code>Outside local</code>, <code>Outside global</code>. The exam presents this output and asks you to identify which address is which. The key: Inside Local is ALWAYS a private (RFC 1918) address, and Inside Global is ALWAYS a public address.</p>",
    visual: { type: "comparison", params: { left: { label: "Inside Perspective", items: ["Inside Local: 192.168.1.10", "Inside Global: 203.0.113.5"] }, right: { label: "Outside Perspective", items: ["Outside Local: 8.8.8.8", "Outside Global: 8.8.8.8"] } } },
    hack: {
      memory: "Inside Local = the host's REAL private IP (what IT configured). Inside Global = the host's PUBLIC disguise (what the internet sees). Outside Local = how you see the external host (usually same as global). Outside Global = the external host's REAL public IP. Focus on the inside pair: Local = private, Global = public. 'Local' always means 'the real address on that side.'",
      practice: "Draw a NAT diagram with inside zone (192.168.1.0/24) and outside zone (internet). Trace a packet from host 192.168.1.10 to 8.8.8.8: label all four address types at each stage. Then verify with 'show ip nat translations' in Packet Tracer and match each column to your diagram. Do this for static NAT, dynamic NAT, and PAT to see how the addresses differ.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 44 (NAT) explains all four address types with diagrams. Wendell Odom OCG Chapters 10-11 cover NAT addressing in depth. The four NAT addresses are tested as drag-and-drop or matching questions. Focus on Inside Local (private) vs Inside Global (public) — that covers 90% of NAT exam questions."
    }
  },

  "4.1.b": {
    info: "<p><strong>Static NAT</strong> creates a permanent, one-to-one mapping between an Inside Local (private) address and an Inside Global (public) address. The mapping exists in the NAT translation table at all times, whether or not traffic is flowing. This makes static NAT ideal for <strong>servers that need to be accessible from the internet</strong> — web servers, email servers, and DNS servers that must be reachable at a consistent public IP.</p><p><strong>Configuration:</strong> <code>ip nat inside source static 192.168.1.10 203.0.113.5</code> — permanently maps private 192.168.1.10 to public 203.0.113.5. Also requires interface designations: <code>ip nat inside</code> on the LAN-facing interface and <code>ip nat outside</code> on the WAN-facing interface. The static mapping is bidirectional — internal-to-external AND external-to-internal traffic is translated.</p><p><strong>How it works:</strong> When host 192.168.1.10 sends a packet outbound, the router replaces the source IP with 203.0.113.5. When an external host sends a packet TO 203.0.113.5, the router translates the destination to 192.168.1.10 and forwards it inward. This bidirectional nature is what makes it suitable for servers — external clients can initiate connections to the public IP, which is mapped to the internal server.</p><p><strong>Verification:</strong> <code>show ip nat translations</code> shows the static mapping even when no traffic is flowing (unlike dynamic NAT entries which only appear when traffic triggers them). The entry includes the protocol, Inside Local, Inside Global, Outside Local, and Outside Global addresses. <code>show ip nat statistics</code> shows the total number of static and dynamic translations.</p><p><strong>Limitation:</strong> Static NAT requires one public IP per private host. If you need to make 10 servers accessible, you need 10 public IPs. This makes it expensive compared to PAT, but necessary when external hosts need to initiate connections to specific internal servers.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Server 192.168.1.10", "Static NAT", "203.0.113.5", "Internet"], color: "#10b981" } },
    hack: {
      memory: "Static NAT = assigned parking spot. One car (private IP), one reserved spot (public IP). 24/7, permanent, even when the car isn't there. Bidirectional — people can find your car by going to the assigned spot. Use for servers that need to be reachable from outside. One public IP consumed per server.",
      practice: "In Packet Tracer: (1) Configure 'ip nat inside source static 192.168.1.10 203.0.113.5'. (2) Set 'ip nat inside' on Gi0/1 and 'ip nat outside' on Gi0/0. (3) Run 'show ip nat translations' — the mapping should exist even before traffic flows. (4) Ping from the inside host to an external IP and verify the translation. (5) Ping from an external host to 203.0.113.5 and verify it reaches 192.168.1.10.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 44 (NAT) covers static NAT configuration. Wendell Odom OCG Chapters 10-11. Static NAT is the simplest NAT type — configure, designate interfaces, verify. The exam tests the command syntax and the fact that static mappings are permanent and bidirectional."
    }
  },

  "4.1.c": {
    info: "<p><strong>Dynamic NAT</strong> automatically assigns Inside Global (public) addresses from a <strong>pool</strong> on a first-come, first-served basis. Unlike static NAT (which is permanent one-to-one), dynamic NAT creates temporary mappings only when traffic is initiated. When a mapping is no longer needed (timeout expires), the public IP returns to the pool for reuse.</p><p><strong>Configuration steps:</strong></p><ol><li>Define the pool: <code>ip nat pool MYPOOL 203.0.113.1 203.0.113.5 netmask 255.255.255.0</code> — creates a pool of 5 public IPs (203.0.113.1 through .5).</li><li>Define which internal hosts can use NAT: <code>access-list 1 permit 192.168.1.0 0.0.0.255</code> — standard ACL matching the internal subnet.</li><li>Link the ACL to the pool: <code>ip nat inside source list 1 pool MYPOOL</code> — tells NAT to translate hosts matching ACL 1 using addresses from MYPOOL.</li><li>Designate interfaces: <code>ip nat inside</code> and <code>ip nat outside</code> on the appropriate interfaces.</li></ol><p><strong>Pool exhaustion:</strong> If all 5 public IPs in the pool are in use and a 6th internal host tries to communicate, the connection is <strong>dropped</strong> — the router cannot translate the packet because no pool address is available. This is the primary disadvantage of dynamic NAT compared to PAT. The mapping eventually times out (default 24 hours for TCP, 60 seconds for non-TCP), freeing the pool address.</p><p><strong>Dynamic NAT is one-to-one</strong> like static NAT (one private IP maps to one public IP at a time), but the mapping is temporary and the specific public IP assigned may differ each time. It is rarely used in modern networks — <strong>PAT has almost entirely replaced it</strong> because PAT allows thousands of hosts to share a single public IP. Dynamic NAT still appears on the CCNA exam for conceptual understanding.</p>",
    visual: { type: "state-machine", params: { states: ["Host initiates traffic", "ACL matches?", "Pool available?", "NAT mapping created", "Pool exhausted -> DROP"], active: 3, transitions: true } },
    hack: {
      memory: "Dynamic NAT = hotel check-in. Guests (hosts) get assigned rooms (pool IPs) when they arrive. No empty rooms? You're turned away (packet dropped). Rooms free up after checkout (timeout). One guest per room = one-to-one, but temporary. In practice, PAT replaced this entirely — dynamic NAT is tested for concept, not real-world use.",
      practice: "In Packet Tracer: (1) Create a pool of 3 public IPs. (2) Create an ACL matching your inside subnet. (3) Configure 'ip nat inside source list 1 pool MYPOOL'. (4) Generate traffic from 4+ inside hosts. (5) Verify the first 3 get translations in 'show ip nat translations'. (6) Verify the 4th host's traffic is dropped (pool exhausted). (7) Check 'show ip nat statistics' for miss count.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 44 (NAT) covers dynamic NAT. Wendell Odom OCG Chapters 10-11. The exam tests: (1) pool exhaustion behavior (connections dropped, not queued), (2) difference between dynamic NAT and PAT (one-to-one vs many-to-one), (3) the keyword 'overload' is what makes it PAT — without it, it's dynamic NAT."
    }
  },

  "4.1.d": {
    info: "<p><strong>PAT (Port Address Translation)</strong>, also called <strong>NAT overload</strong>, is the most commonly used NAT type. It maps <strong>many inside local addresses to a single inside global address</strong> by using unique <strong>source port numbers</strong> to distinguish between different internal hosts' connections. This is how every home router works — all devices share one public IP from the ISP.</p><p><strong>How PAT works:</strong> When host A (192.168.1.10) sends traffic, the router translates the source IP to the public IP (203.0.113.1) and assigns a unique source port (e.g., 1025). When host B (192.168.1.11) also sends traffic, the router uses the same public IP but a different port (e.g., 1026). The NAT table records: 192.168.1.10:1025 = 203.0.113.1:1025, 192.168.1.11:1026 = 203.0.113.1:1026. When replies come back, the router uses the destination port to determine which internal host to forward to.</p><p><strong>Configuration — two common methods:</strong></p><ul><li><strong>Using the outside interface IP:</strong> <code>ip nat inside source list 1 interface GigabitEthernet0/0 overload</code> — uses the outside interface's IP as the public address. Most common method, especially when the ISP assigns a single IP.</li><li><strong>Using a pool:</strong> <code>ip nat inside source list 1 pool MYPOOL overload</code> — uses a pool of public IPs with PAT, providing even more capacity. Each pool IP can support ~65,000 sessions.</li></ul><p><strong>The critical keyword is 'overload.'</strong> Without it, the command configures dynamic NAT (one-to-one). WITH 'overload,' it becomes PAT (many-to-one). This single keyword is the difference between dynamic NAT and PAT — a favorite exam question.</p><p>PAT can support approximately <strong>65,000 simultaneous sessions</strong> per public IP (the range of available port numbers). In practice, this is more than enough for most networks. PAT entries are temporary — they are created when traffic is initiated and removed after a timeout (varies by protocol: TCP established = 24 hours, UDP = 5 minutes). Verify with <code>show ip nat translations</code> — PAT entries show port numbers in the output, while static NAT entries do not.</p>",
    visual: { type: "encapsulation", params: { layers: [{ label: "Host A:1025 -> 203.0.113.1:1025", color: "#3b82f6" }, { label: "Host B:1026 -> 203.0.113.1:1026", color: "#8b5cf6" }, { label: "Host C:1027 -> 203.0.113.1:1027", color: "#10b981" }] } },
    hack: {
      memory: "PAT = apartment building. Everyone shares one street address (public IP) but has different apartment numbers (port numbers). The mailroom (NAT table) uses the apartment number to deliver return mail to the right tenant. The magic keyword is 'overload' — without it, you get dynamic NAT (one-to-one). With it, you get PAT (many-to-one).",
      practice: "In Packet Tracer: (1) Configure ACL 1 to permit your inside subnet. (2) Configure 'ip nat inside source list 1 interface Gi0/0 overload'. (3) Designate inside/outside interfaces. (4) Generate traffic from 5+ inside hosts to an outside destination. (5) Run 'show ip nat translations' and note each host has the SAME inside global IP but DIFFERENT port numbers. Compare this to dynamic NAT output where each host would have a different public IP.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 44 (NAT) covers PAT configuration in detail. Wendell Odom OCG Chapters 10-11. PAT is the most tested NAT type. The exam tests: (1) the 'overload' keyword, (2) how port numbers differentiate hosts, (3) the command syntax using an interface vs a pool. 'Which NAT type allows multiple hosts to share one public IP?' = PAT/NAT overload."
    }
  },

  "4.1.e": {
    info: "<p><strong>NAT interface designations</strong> are required for NAT to function. Every NAT configuration requires at least one <strong>inside interface</strong> and one <strong>outside interface</strong>. Without these designations, the router has no context for which direction to translate addresses, and NAT does absolutely nothing — even if the NAT rules (static, dynamic, PAT) are configured perfectly.</p><p><strong>Configuration:</strong> Under the interface facing the private/internal network: <code>ip nat inside</code>. Under the interface facing the public/external network: <code>ip nat outside</code>. A router can have multiple inside interfaces (e.g., multiple LAN segments) and typically has one outside interface (the WAN/ISP connection).</p><p><strong>How NAT uses the designations:</strong> When a packet enters an <strong>inside interface</strong> destined for an <strong>outside interface</strong>, NAT translates the <strong>source IP</strong> (inside local -> inside global). When a packet enters an <strong>outside interface</strong> destined for an <strong>inside interface</strong>, NAT translates the <strong>destination IP</strong> (inside global -> inside local). The direction of translation depends on which interface the packet enters.</p><p><strong>This is the #1 NAT troubleshooting item:</strong> If NAT isn't working but the NAT rules look correct, check <code>show ip nat statistics</code> — the 'Inside interfaces' and 'Outside interfaces' sections will show which interfaces are designated. If either is empty, NAT won't translate. Also verify with <code>show running-config | include ip nat</code> to see all NAT-related commands including interface designations.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Private LAN", "ip nat inside (Gi0/1)", "Router (NAT)", "ip nat outside (Gi0/0)", "Internet"], color: "#f59e0b" } },
    hack: {
      memory: "Inside = faces your private network. Outside = faces the internet/public. Forgetting these = NAT is configured but does NOTHING. Like installing a translator in a room but not telling them which language to translate FROM and TO. This is the #1 NAT mistake — always check interface designations first when troubleshooting.",
      practice: "In Packet Tracer: (1) Configure complete PAT (ACL, overload, the works) but DO NOT designate interfaces. (2) Test with ping — it won't work. (3) Check 'show ip nat statistics' — inside/outside interfaces will be empty. (4) Add 'ip nat inside' and 'ip nat outside' on the correct interfaces. (5) Test again — now it works. (6) This exercise burns the troubleshooting step into memory.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 44 (NAT) emphasizes interface designations. Wendell Odom OCG Chapters 10-11. Missing interface designations is the #1 NAT troubleshooting scenario on the CCNA. Every NAT troubleshooting question should start here. Verify with 'show ip nat statistics' — if inside/outside interfaces are not listed, that's your problem."
    }
  },

  "4.1.f": {
    info: "<p>An <strong>Access Control List (ACL)</strong> is used with dynamic NAT and PAT to define which <strong>inside hosts are eligible for address translation</strong>. The ACL acts as a filter — only traffic from source IPs that match a <strong>permit</strong> entry in the ACL will be translated by NAT. Traffic from sources that don't match the ACL is <strong>not translated</strong> (but it is also <strong>not blocked</strong> — the ACL is used for selection, not filtering).</p><p><strong>Configuration:</strong> A <strong>standard ACL</strong> is typically used because NAT only needs to match source IP addresses: <code>access-list 1 permit 192.168.1.0 0.0.0.255</code> — permits the 192.168.1.0/24 subnet for translation. This ACL is then referenced in the NAT command: <code>ip nat inside source list 1 pool MYPOOL overload</code>.</p><p><strong>Important distinction:</strong> When an ACL is applied to an interface (via <code>ip access-group</code>), permit = allow traffic, deny = block traffic. But when an ACL is used for NAT, the behavior is different: <strong>permit = translate this traffic</strong>, <strong>deny = don't translate (but don't block either)</strong>. Traffic that doesn't match the NAT ACL is simply forwarded without translation. This is a subtle but important difference tested on the exam.</p><p><strong>Common mistake:</strong> Using an extended ACL when a standard ACL is sufficient. For NAT source translation, you only need to match the source IP — a standard ACL does this. Extended ACLs can be used for more granular control (e.g., only translate HTTP traffic from a specific subnet), but this is beyond typical CCNA scope. Also ensure the wildcard mask in the ACL correctly covers the intended hosts — a wrong wildcard can cause some hosts to be translated and others not.</p>",
    visual: { type: "state-machine", params: { states: ["Packet arrives (inside)", "ACL check: permitted?", "YES -> NAT translates", "NO -> No translation"], active: 2, transitions: true } },
    hack: {
      memory: "The NAT ACL = the guest list at the translation party. Hosts on the list (permit) get translated. Hosts NOT on the list are NOT blocked — they just pass through untranslated. This is different from an interface ACL where deny = drop. In NAT, deny = skip translation. Standard ACL is typically used: 'access-list 1 permit [network] [wildcard].'",
      practice: "In Packet Tracer: (1) Create ACL 1 that permits only 192.168.1.0/24 (not 192.168.2.0/24). (2) Configure PAT with this ACL. (3) Test from a host in 192.168.1.0/24 — traffic should be translated. (4) Test from a host in 192.168.2.0/24 — traffic should NOT be translated (but also not blocked). (5) Verify with 'show ip nat translations' — only 192.168.1.x hosts appear.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 44 (NAT) covers ACLs for NAT. Wendell Odom OCG Chapters 10-11. The exam tests: (1) permit in NAT ACL = translate, not 'allow traffic.' (2) Standard ACLs match source only. (3) Wildcard masks must be correct. A NAT ACL that permits the wrong subnet = some hosts can't reach the internet."
    }
  },

  "4.1.g": {
    info: "<p><strong>NAT verification</strong> uses two primary show commands that reveal different aspects of NAT operation:</p><p><strong><code>show ip nat translations</code></strong> — Displays the current NAT translation table. Each entry shows: protocol (TCP/UDP/ICMP), Inside Local (private IP:port), Inside Global (public IP:port), Outside Local, and Outside Global. Static NAT entries appear permanently (even without active traffic). Dynamic NAT and PAT entries only appear while traffic is flowing or until the mapping times out. For PAT, you'll see the same Inside Global IP with different port numbers for different Inside Local hosts.</p><p><strong><code>show ip nat statistics</code></strong> — Displays overall NAT performance metrics: total active translations, peak translations, hits (successful translations), misses (failed translation attempts — indicates pool exhaustion or ACL mismatch), expired translations, and the configured inside/outside interfaces. This command is critical for diagnosing pool exhaustion (high miss count), verifying interface designations, and monitoring NAT utilization.</p><p><strong>Reading translation table output:</strong> A typical PAT entry looks like: <code>tcp 192.168.1.10:1025 203.0.113.1:1025 8.8.8.8:443 8.8.8.8:443</code>. This means internal host 192.168.1.10 (port 1025) was translated to public IP 203.0.113.1 (port 1025) when connecting to external host 8.8.8.8 (port 443/HTTPS). The exam presents this output and asks you to identify the inside local address (192.168.1.10), inside global address (203.0.113.1), and sometimes the destination.</p><p>Additional useful command: <code>show ip nat translations verbose</code> adds creation time and timeout information for each entry, helping you identify stale translations.</p>",
    visual: { type: "comparison", params: { left: { label: "show ip nat translations", items: ["Active mappings", "Inside local/global", "Outside local/global", "Protocol and ports"] }, right: { label: "show ip nat statistics", items: ["Total translations", "Hits and misses", "Pool utilization", "Expired entries"] } } },
    hack: {
      memory: "'show ip nat translations' = WHO is translated right now (the roster of active mappings). 'show ip nat statistics' = HOW is NAT performing (the scoreboard — hits, misses, pool usage, interface designations). Translations shows individual entries; Statistics shows the big picture. Both are needed for complete verification.",
      practice: "After configuring any NAT type in Packet Tracer: (1) Run 'show ip nat translations' and identify each column (Inside Local, Inside Global, Outside Local, Outside Global). (2) For PAT, note how port numbers differentiate hosts sharing one public IP. (3) Run 'show ip nat statistics' and verify inside/outside interfaces are listed. (4) Check hit count to confirm translations are occurring. If misses are high, diagnose pool exhaustion or ACL issues.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 44 (NAT) demonstrates both verification commands. Wendell Odom OCG Chapters 10-11. The exam shows 'show ip nat translations' output and asks: 'What is the inside local address?' or 'What is the inside global address?' Practice reading this output until you can identify all four address types instantly."
    }
  },

  "4.1.h": {
    info: "<p><strong>NAT troubleshooting</strong> follows a systematic approach. When NAT isn't working, check these items in order:</p><p><strong>Step 1 — Interface designations:</strong> Verify <code>ip nat inside</code> and <code>ip nat outside</code> are configured on the correct interfaces. Check with <code>show ip nat statistics</code> — the 'Inside interfaces' and 'Outside interfaces' sections must list the correct interfaces. This is the <strong>#1 most common NAT problem</strong>.</p><p><strong>Step 2 — ACL configuration:</strong> For dynamic NAT and PAT, verify the ACL permits the correct source networks with the correct wildcard masks. An ACL that denies or doesn't match the source traffic means no translation occurs. Check with <code>show access-lists</code>.</p><p><strong>Step 3 — Pool availability:</strong> For dynamic NAT, check if the pool is exhausted with <code>show ip nat statistics</code>. A high 'misses' count indicates pool exhaustion. For PAT, pool exhaustion is extremely rare (65,000 ports per IP) but can theoretically occur under massive load.</p><p><strong>Step 4 — NAT rule verification:</strong> Confirm the NAT statement links the correct ACL to the correct pool/interface. Check with <code>show running-config | include ip nat</code>. Verify the 'overload' keyword is present for PAT.</p><p><strong>Debugging:</strong> <code>debug ip nat</code> shows real-time translation events — each packet that gets translated (or fails) generates output. Use cautiously on production routers as it can impact CPU. <code>clear ip nat translation *</code> clears all dynamic entries (static entries are permanent and unaffected). Use this to reset the translation table when troubleshooting stale entries.</p><p><strong>Common NAT issues summary:</strong> Missing interface designations (most common), wrong ACL wildcard mask, pool exhaustion (dynamic NAT), missing 'overload' keyword (wanted PAT but got dynamic NAT), and asymmetric routing (outbound traffic goes through NAT but return traffic bypasses it).</p>",
    visual: { type: "state-machine", params: { states: ["NAT not working", "Check interface designations", "Check ACL matches", "Check pool/overload", "debug ip nat"], active: 1, transitions: true } },
    hack: {
      memory: "NAT troubleshooting order: IAPC — Interfaces designated? ACL correct? Pool available? Command syntax right? Always start with interfaces — it's the #1 problem. 'show ip nat statistics' reveals interface designations AND pool exhaustion in one command. 'debug ip nat' is the nuclear option — shows every translation in real time.",
      practice: "In Packet Tracer, intentionally break NAT in four different ways, one at a time: (1) Remove 'ip nat inside' — diagnose and fix. (2) Change the ACL to deny the source — diagnose and fix. (3) For dynamic NAT, exhaust the pool — diagnose and fix. (4) Remove the 'overload' keyword from a PAT config — diagnose and fix. For each scenario, use 'show ip nat statistics', 'show ip nat translations', and 'show access-lists' to identify the problem before fixing it.",
      effort: "high",
      meta: "Jeremy's IT Lab Day 44 (NAT) covers troubleshooting. Wendell Odom OCG Chapters 10-11. The exam's NAT troubleshooting questions are predictable: (1) Missing interface designations, (2) Wrong ACL, (3) Pool exhaustion, (4) Missing 'overload.' Practice identifying each issue from show command output — the exam presents output and asks 'why is NAT not working?'"
    }
  },

  // ── 4.2  Configure and verify NTP ──────────────────────────

  "4.2.a": {
    info: "<p><strong>NTP (Network Time Protocol)</strong> synchronizes device clocks across a network to a common, accurate time source. It uses <strong>UDP port 123</strong> and can achieve millisecond-level accuracy over a LAN. Accurate, consistent time across all network devices is not optional — it is a <strong>critical operational requirement</strong> for multiple reasons:</p><p><strong>Why accurate time matters:</strong></p><ul><li><strong>Log correlation:</strong> When investigating a security incident or troubleshooting an outage, you need to correlate logs from routers, switches, firewalls, and servers. If each device has a different time, matching events across devices becomes nearly impossible.</li><li><strong>Certificate validation:</strong> Digital certificates (used for HTTPS, VPNs, 802.1X) include 'valid from' and 'valid to' timestamps. An inaccurate clock can cause certificates to appear expired or not-yet-valid, breaking secure communications.</li><li><strong>Authentication protocols:</strong> Kerberos (used in Active Directory) requires clocks to be within 5 minutes of each other. Devices with skewed time cannot authenticate.</li><li><strong>Legal compliance:</strong> Many industries require accurate timestamps on logs for regulatory compliance (financial, healthcare, government).</li></ul><p>NTP operates in a hierarchical client-server model. Time flows from the most accurate source (atomic clocks, GPS) down through layers of servers and clients. The <strong>stratum</strong> number indicates how many hops a device is from the authoritative time source. NTP adjusts clocks gradually to avoid sudden jumps — it 'slews' the clock by speeding up or slowing down slightly until synchronized.</p>",
    visual: { type: "hierarchy", params: { root: "Stratum 0 (Atomic Clock)", children: [{ name: "Stratum 1 (NTP Server)", children: [{ name: "Stratum 2 (Core Router)", children: [{ name: "Stratum 3 (Access Switch)" }] }] }] } },
    hack: {
      memory: "NTP = 'Network Time Police.' Everyone must agree on what time it is. Without NTP, your 2:00 PM log and another device's 2:05 PM log look like different events during incident response. UDP port 123. Key uses: logs, certs, Kerberos auth, compliance. NTP slews (gradually adjusts) rather than jumping the clock.",
      practice: "In Packet Tracer: Build a 3-device chain (NTP server -> router -> switch). Configure NTP on each. Verify all clocks match with 'show clock'. Then change the clock on one device manually and watch NTP gradually correct it. This demonstrates why NTP matters — without it, every device drifts independently.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 37 (NTP) covers purpose and configuration. Wendell Odom OCG Chapter 9 covers NTP fundamentals. The exam tests WHY NTP matters (logs, auth, certs) and basic configuration. Know UDP port 123 and that NTP uses a stratum hierarchy."
    }
  },

  "4.2.b": {
    info: "<p><strong>NTP stratum</strong> is a hierarchy from 0 to 16 that indicates how many steps a device is from the authoritative time source. Lower stratum = closer to the source = more accurate time.</p><ul><li><strong>Stratum 0:</strong> The reference clock itself — atomic clock, GPS receiver, or radio clock. This is a hardware device, not a network device. It cannot be directly reached via NTP.</li><li><strong>Stratum 1:</strong> Servers directly connected to a stratum 0 source. These are the most accurate NTP servers available on the network. Public NTP servers (like pool.ntp.org) are typically stratum 1 or 2.</li><li><strong>Stratum 2-15:</strong> Each level is one hop further from the reference clock. A stratum 2 device synchronizes to a stratum 1 server. A stratum 3 device synchronizes to a stratum 2 device. Each hop adds one stratum level. All values from 1 to 15 are considered <strong>valid and usable</strong>.</li><li><strong>Stratum 16:</strong> Special value meaning <strong>unsynchronized</strong>. A device reporting stratum 16 has no valid time source and should not be trusted. Other devices will refuse to sync from a stratum 16 source.</li></ul><p>When a device has multiple NTP sources to choose from, it prefers the one with the <strong>lowest stratum</strong> (closest to the reference clock). If strata are equal, NTP uses other factors like network delay and jitter to select the best source.</p><p>In enterprise networks, a common design has the core router syncing to an external stratum 1-2 server, then distribution switches sync from the core router (stratum 3), and access switches sync from distribution (stratum 4). This creates a clean hierarchy that doesn't burden external NTP servers with thousands of client requests.</p>",
    visual: { type: "layer-stack", params: { layers: ["Stratum 0 — Atomic clock / GPS", "Stratum 1 — Primary server", "Stratum 2 — Secondary server", "Stratum 3 — Client", "Stratum 16 — Unsynchronized"], highlight: 0 } },
    hack: {
      memory: "Stratum = how many handshakes from the atomic clock. Stratum 0 = the clock itself (hardware, not on network). Stratum 1 = directly connected to the clock. Each hop adds 1. Valid: 1-15. Invalid/unsync: 16. Lower = more accurate. Exam question: 'What stratum indicates unsynchronized?' Answer: 16.",
      practice: "In Packet Tracer: Configure 'ntp master 3' on a router (making it a stratum 3 source). Point another device at it with 'ntp server [IP]'. Run 'show ntp status' on the client — it should show stratum 4 (one higher than the server). Point a third device at the second — it becomes stratum 5. Trace the stratum chain to understand the hierarchy.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 37 (NTP) covers stratum hierarchy. Wendell Odom OCG Chapter 9. Memorize: stratum 0 = hardware clock, 1-15 = valid network sources, 16 = unsynchronized/invalid. The exam asks 'what stratum indicates an unsynchronized device?' (16) and 'which stratum is closest to the reference?' (1)."
    }
  },

  "4.2.c": {
    info: "<p><strong>Configuring a Cisco device as an NTP client</strong> is a single command: <code>ntp server [ip-address]</code>. This tells the device to send NTP queries to the specified server and synchronize its clock. The device becomes an NTP client, receiving time from the server and adjusting its local clock accordingly.</p><p><strong>Configuration example:</strong><br><code>ntp server 10.1.1.1</code> — syncs to NTP server at 10.1.1.1.<br><code>ntp server 10.1.1.2</code> — adds a second NTP server for redundancy.<br>When multiple servers are configured, the device evaluates all of them and selects the best source based on stratum, network delay, jitter, and reachability.</p><p><strong>NTP client behavior:</strong> After configuration, the device begins polling the NTP server (default interval starts at 64 seconds and adapts). Initial synchronization may take several minutes as the device evaluates the server's reliability. Once synchronized, the device's stratum is one higher than the server's stratum. The device can also serve as an NTP server to downstream clients — any synchronized device can provide time to others.</p><p><strong>The 'ntp server' vs 'ntp peer' distinction:</strong> <code>ntp server</code> creates a client-server relationship (the device only receives time from the server). <code>ntp peer</code> creates a symmetric peer relationship (both devices can give and receive time from each other). For CCNA, focus on the client-server model using <code>ntp server</code>.</p><p><strong>Verification:</strong> <code>show ntp associations</code> lists all configured NTP sources. The <strong>*</strong> symbol marks the selected (active) time source. The <strong>+</strong> symbol marks a candidate source. The <strong>reach</strong> column shows reachability in octal — 377 means all 8 recent polls were successful.</p>",
    visual: { type: "packet-flow", params: { nodes: ["Switch (client)", "ntp server 10.1.1.1", "NTP Server (10.1.1.1)", "Synchronized"], color: "#3b82f6" } },
    hack: {
      memory: "One command: 'ntp server [IP].' The device asks the server 'what time is it?' and adjusts its clock. Configure multiple servers for redundancy. The device selects the best one (lowest stratum, best reachability). Verify: 'show ntp associations' — * = selected source, reach 377 = perfect reachability.",
      practice: "In Packet Tracer: (1) Configure 'ntp server 10.1.1.1' and 'ntp server 10.1.1.2' on a switch. (2) Run 'show ntp associations' and identify which server has the * (selected). (3) Run 'show ntp status' to verify 'Clock is synchronized' and check the stratum. (4) Run 'show clock' to verify the time matches the server.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 37 (NTP) covers client configuration. Wendell Odom OCG Chapter 9. The exam expects you to configure 'ntp server' and verify with 'show ntp associations.' Know that * = selected source, + = candidate. The 'reach' value of 377 (octal) means all 8 recent polls succeeded."
    }
  },

  "4.2.d": {
    info: "<p>The <code>ntp master [stratum]</code> command configures a Cisco router to act as an <strong>NTP server</strong> using its own internal clock as the time reference. This is used when no external NTP server is available — the router becomes the authoritative time source for its portion of the network. All clients that sync to this router will be one stratum higher than the configured stratum level.</p><p><strong>Configuration:</strong> <code>ntp master 5</code> makes the router advertise itself as a stratum 5 NTP server. Devices that sync to it become stratum 6. Set the stratum wisely — <strong>too low</strong> (e.g., 1 or 2) and this router's internal clock (which is less accurate than a real atomic clock) might outrank legitimate external NTP servers. A stratum of 5-8 is common for internal NTP masters.</p><p><strong>When to use 'ntp master':</strong> (1) Isolated networks with no internet access to reach public NTP servers. (2) Lab environments where external NTP is unavailable. (3) As a backup time source — configure the router to sync to an external server (preferred) AND have 'ntp master' as a fallback. If the external server becomes unreachable, the router falls back to its own clock.</p><p><strong>'ntp server' vs 'ntp master':</strong> <code>ntp server [IP]</code> = 'I receive time from someone else' (client). <code>ntp master [stratum]</code> = 'I provide time to others using my own clock' (server). A router can be BOTH — it can sync to an external NTP server AND serve as an NTP master to downstream devices. If the external source fails, the router's 'ntp master' configuration ensures downstream devices still have a time source.</p>",
    visual: { type: "hierarchy", params: { root: "ntp master 2 (R1)", children: [{ name: "Client R2 (Stratum 3)", children: [{ name: "Client SW1 (Stratum 4)" }] }, { name: "Client R3 (Stratum 3)" }] } },
    hack: {
      memory: "'ntp master [stratum]' = 'I am the clock now.' The router becomes the time authority. Downstream clients become [stratum + 1]. Don't set stratum too low or it outranks real NTP servers. Common values: 5-8. The exam tests: 'ntp server' = I ask for time (client), 'ntp master' = I provide time (server). Both can exist on the same device.",
      practice: "In Packet Tracer: (1) Make R1 an NTP master: 'ntp master 5'. (2) On R2: 'ntp server [R1-IP]'. (3) On SW1: 'ntp server [R2-IP]'. (4) Verify the stratum chain: R1=5, R2=6, SW1=7 using 'show ntp status' on each device. (5) This demonstrates how stratum increments with each hop in the hierarchy.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 37 (NTP) covers 'ntp master.' Wendell Odom OCG Chapter 9. The exam tests the relationship between 'ntp server' (client command) and 'ntp master' (server command). Know that clients always have stratum = server's stratum + 1."
    }
  },

  "4.2.e": {
    info: "<p><strong>NTP authentication</strong> prevents a device from synchronizing to a rogue NTP server that could inject incorrect time. An attacker who controls your network time can undermine certificate validation, bypass Kerberos authentication, and cause log correlation to fail. NTP authentication uses <strong>MD5 or SHA</strong> hash-based keys shared between server and client.</p><p><strong>Configuration steps (required on BOTH server and client):</strong></p><ol><li><code>ntp authenticate</code> — Enables NTP authentication globally on the device.</li><li><code>ntp authentication-key [key-number] md5 [password]</code> — Defines the key (e.g., <code>ntp authentication-key 1 md5 MySecretKey</code>).</li><li><code>ntp trusted-key [key-number]</code> — Marks this key as trusted (e.g., <code>ntp trusted-key 1</code>). Only sources using trusted keys are accepted.</li><li>On the client: <code>ntp server [ip] key [key-number]</code> — Associates the key with the NTP server (e.g., <code>ntp server 10.1.1.1 key 1</code>).</li></ol><p><strong>How it works:</strong> The client sends NTP requests with the key hash included. The server validates the hash against its own key. If the keys match, synchronization proceeds. If they don't match, the server ignores the request (or the client ignores the response). The key number and password must be identical on both sides.</p><p><strong>Important:</strong> Without NTP authentication, any device that responds to NTP queries can become a time source — an attacker could advertise a lower stratum and take over time for the network. Authentication ensures only authorized NTP servers are trusted.</p>",
    visual: { type: "shield", params: { items: ["ntp authenticate", "ntp authentication-key 1 md5 [pass]", "ntp trusted-key 1", "ntp server [ip] key 1"], color: "#10b981" } },
    hack: {
      memory: "NTP auth = secret handshake before syncing clocks. Four steps: (1) Enable auth: 'ntp authenticate'. (2) Define the key: 'ntp authentication-key 1 md5 [pass]'. (3) Trust the key: 'ntp trusted-key 1'. (4) On client: 'ntp server [IP] key 1'. Both sides must have matching key number AND password. Mismatch = no sync.",
      practice: "In Packet Tracer: (1) Configure NTP authentication on both a server and client using matching key 1. (2) Verify synchronization with 'show ntp associations' and 'show ntp status'. (3) Change the password on one side only — verify sync breaks. (4) Fix it and verify sync resumes. This demonstrates why matching keys are critical.",
      effort: "medium",
      meta: "Jeremy's IT Lab Day 37 (NTP) covers authentication. Wendell Odom OCG Chapter 9. NTP auth is less commonly tested than basic NTP, but it does appear. Know the four commands, that MD5 is the hash algorithm, and that key numbers and passwords must match on both sides."
    }
  },

  "4.2.f": {
    info: "<p><strong>NTP verification</strong> uses three primary commands:</p><p><strong><code>show ntp status</code></strong> — The first check: is the device synchronized? Key fields: <strong>'Clock is synchronized'</strong> or <strong>'Clock is unsynchronized'</strong> (indicates NTP isn't working), <strong>stratum</strong> (this device's stratum level), <strong>reference</strong> (the IP of the selected NTP source), and <strong>root delay/dispersion</strong> (network delay and jitter to the reference). If the status says 'unsynchronized,' NTP is not functioning — check server reachability, authentication, and network connectivity.</p><p><strong><code>show ntp associations</code></strong> — Shows all configured NTP servers/peers and their status. Key fields: the prefix symbol (<strong>*</strong> = current selected source, <strong>+</strong> = candidate/acceptable source, <strong>-</strong> = outlier/rejected, no symbol = unconfigured or unreachable), the server's <strong>stratum</strong>, <strong>reach</strong> (reachability in octal — 377 means all 8 recent polls succeeded, lower values indicate packet loss), and <strong>delay</strong> (round-trip time to the server in milliseconds).</p><p><strong><code>show clock</code></strong> — Displays the current device time. If the time doesn't match expectations after NTP configuration, check 'show ntp status' for synchronization state. NTP may take several minutes to initially synchronize — be patient during lab testing.</p><p><strong>Interpreting the 'reach' value:</strong> The reach field uses <strong>octal notation</strong> representing the last 8 NTP polls. Each poll result (success=1, fail=0) is tracked in an 8-bit shift register displayed in octal. 377 (octal) = 11111111 (binary) = all 8 polls succeeded = perfect reachability. 0 = no polls succeeded = server unreachable. Values in between indicate intermittent connectivity.</p>",
    visual: { type: "comparison", params: { left: { label: "show ntp status", items: ["Synchronized / Unsync", "Current stratum", "Reference clock IP", "Root delay"] }, right: { label: "show ntp associations", items: ["All configured peers", "* = selected source", "Reach and delay", "Stratum per peer"] } } },
    hack: {
      memory: "'show ntp status' = Am I synced? What stratum am I? Who's my source? 'show ntp associations' = List all my NTP sources, which one is selected (*), and how's the connection (reach). 'show clock' = What time do I think it is? If status says 'unsynchronized,' start troubleshooting. If associations shows reach = 0, the server is unreachable.",
      practice: "After every NTP configuration in Packet Tracer: (1) Run 'show ntp status' — verify 'Clock is synchronized' and check stratum. (2) Run 'show ntp associations' — identify the * (selected source) and verify reach is 377. (3) Run 'show clock' — verify the time looks correct. Practice reading all three outputs until you can diagnose NTP issues in under 30 seconds.",
      effort: "low",
      meta: "Jeremy's IT Lab Day 37 (NTP) demonstrates all verification commands. Wendell Odom OCG Chapter 9. The exam shows 'show ntp associations' output and asks: 'which server is selected?' Look for the * symbol. 'What does reach 377 mean?' All 8 polls succeeded. 'What does stratum 16 mean in show ntp status?' Unsynchronized — NTP is broken."
    }
  },

  // ── 4.3  Explain DHCP and DNS ──────────────────────────────

  "4.3.a": {
    info: "DHCP uses a four-step process called DORA: Discover (client broadcasts looking for a server), Offer (server responds with an IP address offer), Request (client broadcasts accepting the offer), Acknowledge (server confirms the lease). All four messages use UDP — client sends from port 68, server responds on port 67. The first two steps are broadcast because the client has no IP yet.",
    visual: { type: "handshake", params: { leftLabel: "Client (:68)", rightLabel: "Server (:67)", steps: ["DISCOVER (broadcast) ->", "<- OFFER (unicast/broadcast)", "REQUEST (broadcast) ->", "<- ACKNOWLEDGE"] } },
    hack: {
      memory: "DORA: 'Dora the Explorer Discovers Offers, Requests Acknowledgement.' Or simply: D-O-R-A = Discover, Offer, Request, Ack.",
      practice: "In Packet Tracer, set up a DHCP server and client. Use simulation mode to watch all 4 DORA packets. Note source/destination IPs and ports on each packet.",
      effort: "medium",
      meta: "DORA is tested in every CCNA exam. Know the order, which ones are broadcast, the UDP ports (67/68), and that the client sends Discover and Request as broadcasts."
    }
  },

  "4.3.b": {
    info: "A DHCP server provides clients with: IP address, subnet mask, default gateway, DNS server address(es), and a lease duration. Additional options can include domain name, TFTP server, NTP server, and WINS server. The lease duration determines how long the client can use the assigned IP before it must renew. All of these parameters are configured in the DHCP pool.",
    visual: { type: "encapsulation", params: { layers: [{ label: "IP Address: 192.168.1.10", color: "#3b82f6" }, { label: "Mask: 255.255.255.0", color: "#8b5cf6" }, { label: "Gateway: 192.168.1.1", color: "#10b981" }, { label: "DNS: 8.8.8.8", color: "#f59e0b" }, { label: "Lease: 24 hours", color: "#ef4444" }] } },
    hack: {
      memory: "DHCP gives you the 'Big 4': IP, Mask, Gateway, DNS. Plus a lease timer that says 'come back and renew before this expires.'",
      practice: "Configure a DHCP pool on a router with all parameters. Connect a client and verify it received all settings with 'ipconfig /all' (Windows) or 'show dhcp lease' (Cisco).",
      effort: "medium",
      meta: "The exam asks what DHCP provides. The 'Big 4' plus lease time cover every answer. Know that the default gateway is set with 'default-router' in the DHCP pool config."
    }
  },

  "4.3.c": {
    info: "DHCP lease renewal happens automatically at two intervals: at 50% of the lease time (T1), the client unicasts a renewal request directly to the DHCP server that issued the lease. If that fails, at 87.5% (T2), the client broadcasts a renewal request to any available DHCP server. If both fail and the lease expires, the client must restart the DORA process.",
    visual: { type: "state-machine", params: { states: ["Lease granted", "T1 (50%) — Unicast renew", "T2 (87.5%) — Broadcast renew", "Lease expires — DORA restart"], active: 1, transitions: true } },
    hack: {
      memory: "50% = polite ask (unicast to your server). 87.5% = desperate shout (broadcast to anyone). 100% = game over, start from scratch with DORA.",
      practice: "Set a short DHCP lease time (e.g., 2 minutes) and watch the renewal process with packet captures. Note the unicast at 50% and broadcast at 87.5%.",
      effort: "low",
      meta: "Know the two percentages: 50% unicast, 87.5% broadcast. The exam asks 'at what point does a DHCP client attempt to renew its lease?' Answer: 50% of the lease time."
    }
  },

  "4.3.d": {
    info: "Common DNS record types: A record maps hostname to IPv4 address, AAAA record maps hostname to IPv6 address, CNAME creates an alias (www -> server1), MX defines the mail server for a domain, PTR provides reverse lookup (IP to hostname), and NS identifies authoritative name servers for the domain. SOA defines zone authority and timing parameters.",
    visual: { type: "comparison", params: { left: { label: "Forward Records", items: ["A — host -> IPv4", "AAAA — host -> IPv6", "CNAME — alias -> host", "MX — domain -> mail server"] }, right: { label: "Other Records", items: ["PTR — IPv4 -> host (reverse)", "NS — domain -> name server", "SOA — zone authority", "TXT — arbitrary text"] } } },
    hack: {
      memory: "A = Address (IPv4), AAAA = Address x4 (IPv6, 4 times bigger), CNAME = 'C' for Clone/alias, MX = Mail eXchange, PTR = PoinTeR (reverse), NS = Name Server.",
      practice: "Use 'nslookup' or 'dig' to query each record type for a real domain. Example: 'nslookup -type=MX gmail.com' to see Google's mail servers.",
      effort: "low",
      meta: "Memorize all 6 record types and what they do. The exam asks 'which DNS record type resolves a hostname to an IPv6 address?' Answer: AAAA. These are rapid-fire recall questions."
    }
  },

  "4.3.e": {
    info: "DNS resolution uses two query modes: recursive queries (client asks its DNS server to do all the work and return the final answer — the server queries other servers on behalf of the client) and iterative queries (the DNS server returns a referral to another server and the client follows up). Clients typically use recursive; servers use iterative between themselves.",
    visual: { type: "packet-flow", params: { nodes: ["Client (recursive query)", "Local DNS Server", "Root -> TLD -> Auth (iterative)", "Final Answer returned"], color: "#8b5cf6" } },
    hack: {
      memory: "Recursive = 'Do all the work for me and give me the answer.' Iterative = 'I'll give you a hint, go ask them yourself.' Clients are lazy (recursive), servers are cooperative (iterative).",
      practice: "Use 'dig +trace example.com' to see the full iterative resolution chain from root to authoritative server. Understand each referral step.",
      effort: "low",
      meta: "The exam tests whether you know the difference. Recursive = server does all the work. Iterative = server returns referrals. Clients use recursive queries to their local DNS."
    }
  },

  "4.3.f": {
    info: "DNS hierarchy follows a tree structure: Root servers (.) are the top level, then Top-Level Domains (TLD — .com, .org, .net, .edu), then Second-Level Domains (google.com, cisco.com), then subdomains/hosts (www.cisco.com). Resolution starts at the root and works down. There are 13 root server clusters (A through M) that anchor the entire DNS system.",
    visual: { type: "hierarchy", params: { root: ". (Root)", children: [{ name: ".com (TLD)", children: [{ name: "cisco.com (2nd level)", children: [{ name: "www.cisco.com (host)" }] }, { name: "google.com" }] }, { name: ".org (TLD)", children: [{ name: "wikipedia.org" }] }] } },
    hack: {
      memory: "DNS tree: Root (.) -> TLD (.com) -> Domain (cisco.com) -> Host (www). Like a postal system: Country -> State -> City -> Street address.",
      practice: "Trace a DNS lookup manually: start at root, ask for .com, get referred to .com TLD, ask for cisco.com, get referred to Cisco's authoritative DNS, get the answer.",
      effort: "low",
      meta: "Know the hierarchy: root -> TLD -> second-level -> host. Know there are 13 root server clusters. The exam tests this as conceptual knowledge, not deep configuration."
    }
  },

  // ── 4.4  Explain SNMP ──────────────────────────────────────

  "4.4.a": {
    info: "SNMP architecture has three components: the Manager (NMS — Network Management Station) that polls and receives data, the Agent (software running on managed devices that responds to queries and sends traps), and the MIB (Management Information Base — a hierarchical database of objects the agent can report on, like interface stats, CPU, memory).",
    visual: { type: "packet-flow", params: { nodes: ["NMS (Manager)", "SNMP Get/Set (UDP 161)", "Agent + MIB", "Trap/Inform (UDP 162)"], color: "#6366f1" } },
    hack: {
      memory: "Manager = the boss who asks questions. Agent = the employee on-site who answers. MIB = the employee's clipboard with all the stats. Traps = the employee calling the boss with urgent news.",
      practice: "Set up SNMP on a router and poll it from a manager tool (or use 'snmpwalk' from a Linux box). Watch the Get requests and responses. Configure a trap destination.",
      effort: "medium",
      meta: "Know the three components (Manager, Agent, MIB) and the two port numbers: 161 for queries (Get/Set) and 162 for notifications (Trap/Inform)."
    }
  },

  "4.4.b": {
    info: "SNMP messages: Get (manager requests a specific OID value), GetNext (requests the next OID in the MIB tree), GetBulk (retrieves large chunks of MIB data efficiently — SNMPv2+), Set (manager changes a value on the agent), Trap (agent sends unsolicited alert to manager — unreliable, no acknowledgment), Inform (like Trap but acknowledged by the manager — reliable).",
    visual: { type: "comparison", params: { left: { label: "Manager -> Agent", items: ["Get — request specific value", "GetNext — next OID", "GetBulk — bulk retrieval", "Set — change a value"] }, right: { label: "Agent -> Manager", items: ["Trap — unacknowledged alert", "Inform — acknowledged alert", "(port 162 for both)", "Inform is reliable"] } } },
    hack: {
      memory: "Get = 'tell me this.' GetNext = 'tell me the next thing.' GetBulk = 'dump it all.' Set = 'change this.' Trap = 'FIRE!' (and hope someone heard). Inform = 'FIRE! (please confirm you heard me).'",
      practice: "Create flashcards for each message type with its direction (manager->agent or agent->manager) and whether it's acknowledged.",
      effort: "low",
      meta: "Know Trap vs Inform: both are agent-initiated alerts, but Inform is acknowledged (reliable) and Trap is fire-and-forget. This distinction is a common exam question."
    }
  },

  "4.4.c": {
    info: "SNMPv2c uses community strings for authentication — essentially passwords sent in cleartext. 'Read-only' (ro) community allows Get operations, 'Read-write' (rw) allows Get and Set. The default community strings are 'public' (ro) and 'private' (rw). SNMPv2c has no encryption, so community strings and all SNMP data travel in plaintext on the network.",
    visual: { type: "shield", params: { items: ["Community string: 'public' (ro)", "Community string: 'private' (rw)", "NO encryption", "Plaintext authentication"], color: "#f59e0b" } },
    hack: {
      memory: "SNMPv2c = security by honor system. Community strings are passwords sent in the open. Like yelling a secret password across a crowded room.",
      practice: "Configure SNMPv2c with custom community strings (never use default 'public'/'private'). Use a packet capture to see the community string in plaintext — this demonstrates why v3 is needed.",
      effort: "low",
      meta: "The exam asks 'which SNMP version uses community strings without encryption?' Answer: v2c (and v1). Know that community strings are case-sensitive and sent in cleartext."
    }
  },

  "4.4.d": {
    info: "SNMPv3 offers three security levels: noAuthNoPriv (username only, no authentication or encryption — like v2c with usernames), authNoPriv (authentication via MD5 or SHA but no encryption — verifies identity but data is readable), authPriv (authentication plus encryption via DES, 3DES, or AES — full security). Only authPriv provides confidentiality.",
    visual: { type: "layer-stack", params: { layers: ["noAuthNoPriv — username only", "authNoPriv — auth (MD5/SHA), no encrypt", "authPriv — auth + encrypt (AES)"], highlight: 2 } },
    hack: {
      memory: "The three levels escalate: 'no-no' (nothing), 'auth-no' (ID check only), 'auth-priv' (ID check + locked box). Only the last one encrypts the data.",
      practice: "Memorize the three security level names and what each provides. Create a 3-row table: level name, authentication, encryption (yes/no for each).",
      effort: "low",
      meta: "The exam loves 'which SNMPv3 security level provides both authentication and encryption?' Answer: authPriv. Also know the auth algorithms (MD5/SHA) and encryption (AES/DES)."
    }
  },

  "4.4.e": {
    info: "SNMPv3 is the only version that supports encryption (privacy/confidentiality). SNMPv1 and v2c use plaintext community strings with no encryption option. If confidentiality is required for network management traffic, SNMPv3 with authPriv is the only standard choice. This is a critical security consideration and one of the primary reasons to upgrade from v2c.",
    visual: { type: "comparison", params: { left: { label: "SNMPv1 / v2c", items: ["Community strings", "Plaintext only", "No encryption", "No user-based auth"] }, right: { label: "SNMPv3", items: ["Username/group model", "Auth: MD5, SHA", "Encrypt: AES, DES", "Three security levels"] } } },
    hack: {
      memory: "Only v3 encrypts. Period. v1 and v2c = postcards (anyone can read them). v3 with authPriv = sealed, signed letter.",
      practice: "This is pure memorization. Write it down: 'SNMPv3 authPriv = only version + level with encryption' and review daily until the exam.",
      effort: "low",
      meta: "This is a one-liner exam answer that shows up every cycle. 'Which SNMP version provides encryption?' = SNMPv3. 'Which security level?' = authPriv."
    }
  },

  // ── 4.5  Describe syslog ───────────────────────────────────

  "4.5.a": {
    info: "Syslog Level 0 — Emergency: The system is completely unusable. This is the most severe syslog level. Examples include hardware failures that halt the device or kernel panics. In practice, level 0 messages are extremely rare and indicate catastrophic failure requiring immediate attention.",
    visual: { type: "gauge", params: { level: 100, label: "Level 0 — Emergency (Most Severe)", color: "#ef4444" } },
    hack: {
      memory: "Level 0 = 'Everything is on fire.' The system is dead or dying. Lowest number = highest severity.",
      practice: "Part of the full syslog levels drill — memorize all 8 levels in order using the mnemonic below.",
      effort: "low",
      meta: "Remember: lower number = more severe. Level 0 is the worst. The exam tests the mnemonic and severity order more than individual level definitions."
    }
  },

  "4.5.b": {
    info: "Syslog Level 1 — Alert: Immediate action is needed. The system is still running but a critical component requires urgent intervention. Examples include temperature warnings that could damage hardware or critical process failures that will cascade if not addressed immediately.",
    visual: { type: "gauge", params: { level: 90, label: "Level 1 — Alert (Action Needed Now)", color: "#ef4444" } },
    hack: {
      memory: "Level 1 = 'Red alert — act NOW.' The system is alive but something critical needs fixing immediately or it becomes level 0.",
      practice: "Group study: levels 0-3 are 'crisis levels' that should wake you up at night. Levels 4-7 are 'operational levels' for daily monitoring.",
      effort: "low",
      meta: "Levels 0-1 are rare in practice. The exam focuses more on knowing the order and the mnemonic than defining each one precisely."
    }
  },

  "4.5.c": {
    info: "Syslog Level 2 — Critical: A critical condition exists, such as a hard drive failure, memory exhaustion, or a failed hardware component. The system is running but in a degraded state. These events often trigger automatic alerts to administrators and may require hardware replacement or major intervention.",
    visual: { type: "gauge", params: { level: 80, label: "Level 2 — Critical (Degraded State)", color: "#f97316" } },
    hack: {
      memory: "Level 2 = 'Something important broke.' The system limps along but a major component is down. Think hardware failure.",
      practice: "Associate each level with a real-world scenario: 0=dead, 1=act now, 2=broken part, 3=software error, 4=warning, 5=normal event, 6=FYI, 7=verbose details.",
      effort: "low",
      meta: "Level 2 is the 'C' in the mnemonic — Critical. Pair it with real events (disk failure, fan failure) to remember it."
    }
  },

  "4.5.d": {
    info: "Syslog Level 3 — Error: A non-critical error condition, typically a software issue. The device is functional but an operation failed. Examples: an interface going down/up, a routing protocol error, or a failed authentication attempt. These are important but don't indicate hardware failure or imminent system death.",
    visual: { type: "gauge", params: { level: 70, label: "Level 3 — Error (Operation Failed)", color: "#f97316" } },
    hack: {
      memory: "Level 3 = 'Something didn't work but the system is fine.' Like a single dropped call — the phone still works.",
      practice: "On a Cisco device, shut an interface and check syslog output. Interface down/up messages are level 3 (or level 5 depending on the event) — good way to see real syslog in action.",
      effort: "low",
      meta: "Level 3 errors are the most common 'actionable' syslog messages you'll see day-to-day. The exam tests severity ordering, not individual message classification."
    }
  },

  "4.5.e": {
    info: "Syslog Level 4 — Warning: A warning condition exists — not an error yet, but something that could become one if ignored. Examples: nearing memory thresholds, certificate approaching expiration, or configuration inconsistencies. These are 'heads up' messages that inform proactive administrators.",
    visual: { type: "gauge", params: { level: 55, label: "Level 4 — Warning (Potential Problem)", color: "#f59e0b" } },
    hack: {
      memory: "Level 4 = 'Yellow light — pay attention.' Not broken yet, but could be soon. Like a low fuel warning in your car.",
      practice: "When setting 'logging trap [level]', you capture that level and all more severe levels. 'logging trap warnings' captures 0-4. Practice this filter logic.",
      effort: "low",
      meta: "Know that setting a trap level captures that level AND all lower numbers (more severe). 'logging trap 4' = emergencies + alerts + critical + errors + warnings."
    }
  },

  "4.5.f": {
    info: "Syslog Level 5 — Notification: Normal but significant events. These are routine operational messages that indicate normal operation of note — like a routing neighbor coming up, an interface changing state, or a configuration change being saved. Most syslog monitoring systems focus on level 5 and below.",
    visual: { type: "gauge", params: { level: 40, label: "Level 5 — Notification (Normal, Significant)", color: "#10b981" } },
    hack: {
      memory: "Level 5 = 'FYI — this happened and you should know.' Not an error, not a warning, just something noteworthy. Like 'the front door was opened.'",
      practice: "Most Cisco default logging is set to level 5 or 6. Run 'show logging' to see your current level and what messages you're capturing.",
      effort: "low",
      meta: "The transition between levels 4 (warning) and 5 (notification) is where 'problems' end and 'normal events' begin. Good mental dividing line."
    }
  },

  "4.5.g": {
    info: "Syslog Level 6 — Informational: General operational messages that confirm normal device operation. Examples: successful login, command executed, interface statistics. These messages provide useful context for troubleshooting but are too verbose for continuous alerting. Most devices default to this level for console and buffer logging.",
    visual: { type: "gauge", params: { level: 25, label: "Level 6 — Informational (Normal Operation)", color: "#3b82f6" } },
    hack: {
      memory: "Level 6 = 'Everything's fine, here's what happened.' Like a building's security log — nothing wrong, just keeping records.",
      practice: "Check 'show logging' on a device — the buffer usually shows level 6 messages. Scroll through and identify the message types you see most often.",
      effort: "low",
      meta: "Informational = level 6. The exam sometimes tricks you with 'notification' (5) vs 'informational' (6). Know which number goes with which word."
    }
  },

  "4.5.h": {
    info: "Syslog Level 7 — Debugging: Extremely verbose output generated by debug commands. These messages show granular, real-time protocol behavior and are used for active troubleshooting only. Never leave debugging enabled on production devices — it can overwhelm the CPU and crash the router. Always use 'undebug all' when done.",
    visual: { type: "gauge", params: { level: 10, label: "Level 7 — Debugging (Most Verbose)", color: "#6b7280" } },
    hack: {
      memory: "Level 7 = 'show me EVERYTHING.' Like turning on a firehose — useful briefly for troubleshooting but will flood you if left on. Highest number = least severe = most verbose.",
      practice: "Run 'debug ip icmp' on a router, ping something, then immediately 'undebug all'. See the flood of level 7 messages. Understand why you NEVER leave this on in production.",
      effort: "low",
      meta: "The exam asks: 'Which syslog level should NOT be left enabled on production devices?' Answer: 7 (debugging). Also: 'What is the LEAST severe syslog level?' Answer: 7."
    }
  },

  "4.5.i": {
    info: "The classic mnemonic for syslog levels 0-7: Every Awesome Cisco Engineer Will Need Ice cream Daily. Emergency (0), Alert (1), Critical (2), Error (3), Warning (4), Notification (5), Informational (6), Debugging (7). This maps each level to its name and makes the order easy to recall during the exam.",
    visual: { type: "layer-stack", params: { layers: ["0 — Emergency (Every)", "1 — Alert (Awesome)", "2 — Critical (Cisco)", "3 — Error (Engineer)", "4 — Warning (Will)", "5 — Notification (Need)", "6 — Informational (Ice cream)", "7 — Debugging (Daily)"], highlight: 0 } },
    hack: {
      memory: "Every Awesome Cisco Engineer Will Need Ice cream Daily. Write it on your exam whiteboard first thing. EACEWNI D = Emergency, Alert, Critical, Error, Warning, Notification, Informational, Debugging.",
      practice: "Write the mnemonic 5 times from memory. Then write just the level numbers and names without the mnemonic. When you can do both, you're ready.",
      effort: "low",
      meta: "This mnemonic is the #1 community recommendation for syslog levels. Write it on the whiteboard as part of your brain dump at exam start."
    }
  },

  "4.5.j": {
    info: "Syslog messages can be sent to four destinations: console (real-time display on the console port — 'logging console'), monitor/VTY (terminal monitor sessions — 'logging monitor'), buffer (device RAM — 'logging buffered [size]'), and a remote syslog server ('logging host [ip]'). Each destination can have its own severity level filter.",
    visual: { type: "comparison", params: { left: { label: "Local Destinations", items: ["Console — real-time display", "Monitor — VTY sessions", "Buffer — RAM (survives session)"] }, right: { label: "Remote Destination", items: ["Syslog server — centralized", "UDP port 514", "Survives device reboot"] } } },
    hack: {
      memory: "CMBS: Console (direct), Monitor (VTY), Buffer (RAM), Server (remote). 'Cisco Makes Better Switches' = the four log destinations.",
      practice: "Configure all four destinations on a router with different severity levels. Verify with 'show logging' that each destination shows its configured level.",
      effort: "medium",
      meta: "Know that buffer logging is stored in RAM (lost on reboot) and server logging is the only one that persists long-term. The exam asks 'which survives a reboot?' = syslog server."
    }
  },

  "4.5.k": {
    info: "Syslog configuration commands: 'logging host [ip]' sends messages to a remote server, 'logging trap [level]' sets the severity threshold for messages sent to the syslog server (that level and all more severe), 'logging buffered [size] [level]' configures local RAM logging. 'logging console [level]' and 'logging monitor [level]' control local display. 'service timestamps log datetime' adds timestamps to messages.",
    visual: { type: "shield", params: { items: ["logging host 10.1.1.100", "logging trap informational", "logging buffered 16384", "service timestamps log datetime"], color: "#8b5cf6" } },
    hack: {
      memory: "'logging trap' = what goes to the SERVER. 'logging buffered' = what goes to RAM. 'logging console' = what goes to the screen. Each has its own level filter.",
      practice: "Configure 'logging host 10.1.1.100' and 'logging trap warnings' (level 4). Generate some events and verify only levels 0-4 appear at the server. Then change to 'logging trap debugging' and see everything.",
      effort: "medium",
      meta: "The exam tests 'logging trap' heavily. Remember: the level you set is a FLOOR — that level and all LOWER numbers (more severe) are sent. 'logging trap 4' = levels 0 through 4."
    }
  },

  // ── 4.6  Configure DHCP client and relay ───────────────────

  "4.6.a": {
    info: "To configure a Cisco router interface as a DHCP client (receiving its IP address via DHCP), use the command 'ip address dhcp' on the interface. This is common on interfaces connecting to an ISP where the ISP assigns the IP dynamically. The interface will use DORA to obtain an IP address, subnet mask, and optionally a default gateway.",
    visual: { type: "packet-flow", params: { nodes: ["Router Interface", "ip address dhcp", "DHCP Server (ISP)", "IP assigned"], color: "#3b82f6" } },
    hack: {
      memory: "'ip address dhcp' = 'I don't know my address — please assign me one.' Used on ISP-facing interfaces when the ISP controls addressing.",
      practice: "Configure a router interface with 'ip address dhcp' and point it at a DHCP server in Packet Tracer. Verify the assigned address with 'show ip interface brief'.",
      effort: "low",
      meta: "This is a one-command concept. The exam asks 'which command configures an interface to receive its IP via DHCP?' Answer: 'ip address dhcp'. Simple recall."
    }
  },

  "4.6.b": {
    info: "DHCP relay ('ip helper-address [server-ip]') is configured on the router interface facing the clients (the default gateway interface). It converts the client's DHCP broadcast into a unicast directed at the DHCP server on a different subnet. Without it, DHCP Discover broadcasts never reach a server on another network because broadcasts don't cross router boundaries.",
    visual: { type: "packet-flow", params: { nodes: ["Client (broadcast)", "Gateway (ip helper-address)", "Unicast to DHCP Server", "Server responds"], color: "#10b981" } },
    hack: {
      memory: "'ip helper-address' = the router becomes a relay runner. It catches the client's broadcast (which would die at the router) and hand-delivers it as a unicast to the server across the network.",
      practice: "Set up a topology where the DHCP server is on a different subnet than the clients. Without ip helper-address, clients won't get an IP. Add it to the gateway interface and watch it work.",
      effort: "medium",
      meta: "This is heavily tested. Know: configured on the CLIENT-facing interface (not the server-facing one), points to the SERVER's IP, and is needed because broadcasts don't cross routers."
    }
  },

  "4.6.c": {
    info: "DHCP relay is needed because Layer 3 devices (routers) do not forward broadcast traffic between subnets by default. DHCP Discover and Request messages are broadcasts (255.255.255.255). Without a relay agent, these broadcasts are confined to the local subnet and never reach a DHCP server on a different subnet. This is a fundamental networking principle.",
    visual: { type: "comparison", params: { left: { label: "Without Relay", items: ["Client broadcasts DISCOVER", "Router DROPS broadcast", "Server never sees request", "Client gets no IP"] }, right: { label: "With Relay", items: ["Client broadcasts DISCOVER", "Router RELAYS as unicast", "Server receives and responds", "Client gets IP"] } } },
    hack: {
      memory: "Broadcasts are local — they stop at the router. The relay agent is the translator: 'I heard someone shouting on this side, let me call the server on the other side.'",
      practice: "In Packet Tracer simulation mode, watch what happens to a DHCP Discover packet when it hits a router without ip helper-address — it gets dropped. Then add the command and re-test.",
      effort: "low",
      meta: "The 'why' behind DHCP relay is tested as a concept question: 'Why can't a client on subnet A get a DHCP address from a server on subnet B?' Answer: broadcasts don't cross routers."
    }
  },

  "4.6.d": {
    info: "Configuring a Cisco router as a DHCP server: 'ip dhcp pool [name]' creates the pool, 'network [network] [mask]' defines the scope, 'default-router [gateway-ip]' sets the default gateway for clients, 'dns-server [ip]' sets DNS, 'lease [days hours minutes]' sets the lease time. The router assigns IPs from the pool to clients that request them.",
    visual: { type: "layer-stack", params: { layers: ["ip dhcp pool LAN", "network 192.168.1.0 255.255.255.0", "default-router 192.168.1.1", "dns-server 8.8.8.8", "lease 0 8 0"], highlight: 1 } },
    hack: {
      memory: "DHCP pool recipe: Pool name, Network scope, Default-router (gateway), DNS-server, Lease time. 'Please Name Default DNS Leases.' Five lines, complete server.",
      practice: "Configure a full DHCP pool on a router. Connect PCs and verify they get addresses with correct gateway and DNS. Check 'show ip dhcp binding' for active leases.",
      effort: "medium",
      meta: "The exam expects you to configure a basic DHCP server in a lab sim. Practice the five commands until you can type them from memory. The keyword is 'default-router' not 'default-gateway'."
    }
  },

  "4.6.e": {
    info: "Excluded addresses prevent the DHCP server from assigning specific IPs from the pool. Command: 'ip dhcp excluded-address [start-ip] [end-ip]' (configured in global config, not inside the pool). Always exclude the gateway IP, server IPs, and any other static assignments. Without exclusions, DHCP could assign an IP that's already in use, causing conflicts.",
    visual: { type: "state-machine", params: { states: ["Pool: 192.168.1.0/24", "Exclude: .1 to .10", "Available: .11 to .254", "Assigns from available range"], active: 2, transitions: true } },
    hack: {
      memory: "Exclusions = reserved parking spots in a parking lot. The DHCP valet won't park anyone there because those spots are already taken by static-IP devices.",
      practice: "Configure excluded addresses for the first 10 IPs in your pool. Verify with 'show ip dhcp pool' that the pool's available range doesn't include the excluded addresses.",
      effort: "low",
      meta: "The exam tests this as part of DHCP server configuration. Key detail: the excluded-address command is in GLOBAL config mode, not inside the pool. Students often get the mode wrong."
    }
  },

  "4.6.f": {
    info: "DHCP verification commands: 'show ip dhcp binding' shows all active leases (IP, MAC, lease expiration), 'show ip dhcp pool' shows pool configuration and utilization (addresses assigned, available, excluded), 'show ip dhcp conflict' shows any detected IP conflicts, and 'show ip dhcp server statistics' shows DORA message counts.",
    visual: { type: "comparison", params: { left: { label: "Binding & Pool", items: ["show ip dhcp binding", "— active leases, MACs", "show ip dhcp pool", "— utilization, scope"] }, right: { label: "Troubleshooting", items: ["show ip dhcp conflict", "— IP conflicts detected", "show ip dhcp server statistics", "— DORA message counts"] } } },
    hack: {
      memory: "Binding = who has what IP right now. Pool = how's the pool doing (full? empty?). Conflict = duplicate IP detected. Statistics = how many DORA messages were exchanged.",
      practice: "After setting up a DHCP server, run all four commands. Note which client got which IP in the binding table. Check pool utilization to see remaining addresses.",
      effort: "low",
      meta: "The exam shows 'show ip dhcp binding' output and asks which IP was assigned to which MAC. Practice reading this output — it's a common show-command question."
    }
  },

  // ── 4.7  Explain QoS (PHB) ────────────────────────────────

  "4.7.a": {
    info: "Classification is the first step in QoS — identifying and categorizing traffic into classes based on criteria like source/destination IP, port numbers, protocol, or DSCP markings. Without classification, all traffic is treated equally (best-effort). Common tools: ACLs, NBAR (Network Based Application Recognition), and class-maps. Classification happens at the trust boundary.",
    visual: { type: "state-machine", params: { states: ["Packet arrives", "Classification (identify)", "Marking (label)", "Queuing (prioritize)", "Forwarding"], active: 1, transitions: true } },
    hack: {
      memory: "Classification = sorting mail into bins (voice, video, data, junk). You have to identify what it is before you can decide how to treat it.",
      practice: "Understand the concept — QoS configuration is NOT lab-tested on CCNA. Focus on knowing the QoS processing order: classify -> mark -> queue -> schedule.",
      effort: "low",
      meta: "QoS on the CCNA is conceptual only — you won't configure it. Know the terminology and the processing order. Classification = identification step."
    }
  },

  "4.7.b": {
    info: "Marking tags packets with a QoS value so downstream devices can prioritize them without re-classifying. CoS (Class of Service) is a 3-bit field in the 802.1Q VLAN tag (Layer 2, values 0-7). DSCP (Differentiated Services Code Point) is a 6-bit field in the IP header (Layer 3, values 0-63). DSCP persists across routed networks; CoS is lost when the VLAN tag is removed.",
    visual: { type: "comparison", params: { left: { label: "CoS (Layer 2)", items: ["3-bit field in 802.1Q tag", "Values: 0-7", "Lost at L3 boundary", "Switch-to-switch only"] }, right: { label: "DSCP (Layer 3)", items: ["6-bit field in IP header", "Values: 0-63", "Survives routing", "End-to-end marking"] } } },
    hack: {
      memory: "CoS = sticker on the envelope (removed when opened). DSCP = stamp inside the letter (stays forever). CoS is Layer 2, DSCP is Layer 3.",
      practice: "Memorize: CoS = 3 bits, 0-7, Layer 2, 802.1Q tag. DSCP = 6 bits, 0-63, Layer 3, IP header. Create a simple comparison card.",
      effort: "low",
      meta: "The exam asks 'which marking survives across router hops?' Answer: DSCP (Layer 3). CoS only works within a switched VLAN — it's stripped at the router."
    }
  },

  "4.7.c": {
    info: "Key DSCP values: EF (Expedited Forwarding, DSCP 46) — used for voice, guarantees low latency/jitter/loss. AF (Assured Forwarding) classes: AF11-AF43 organized in 4 classes with 3 drop precedences each. CS (Class Selector) values: CS0-CS7 for backward compatibility with IP Precedence. Default/Best Effort = DSCP 0 (CS0).",
    visual: { type: "layer-stack", params: { layers: ["EF (46) — Voice/real-time", "AF4x — Video streaming", "AF3x — Call signaling", "AF2x — Transactional data", "AF1x — Bulk data", "CS0 / Default (0) — Best effort"], highlight: 0 } },
    hack: {
      memory: "EF = 'Express Freeway' for voice (DSCP 46). AF = 'Assured Forwarding' in tiers. CS = 'Class Selector' for backward compatibility. Default = 0 = no special treatment.",
      practice: "Memorize EF=46 and that AF classes go from 1 (lowest) to 4 (highest). The drop precedence within each class goes from 1 (low drop) to 3 (high drop). AF11 = class 1, low drop.",
      effort: "medium",
      meta: "For the exam: EF=46 (voice), AF classes exist in 4 tiers, CS values backward-compatible with IP precedence. Don't memorize all AF decimal values — just know the structure."
    }
  },

  "4.7.d": {
    info: "Queuing determines the order packets leave the device. LLQ (Low Latency Queuing) provides a strict priority queue for real-time traffic (voice/video) — these packets are always sent first up to a configured limit. CBWFQ (Class-Based Weighted Fair Queuing) assigns bandwidth guarantees to different traffic classes. LLQ is typically combined with CBWFQ for a complete QoS policy.",
    visual: { type: "hierarchy", params: { root: "Output Scheduler", children: [{ name: "LLQ (Priority)", children: [{ name: "Voice — sent first" }] }, { name: "CBWFQ (Bandwidth)", children: [{ name: "Video — 30% BW" }, { name: "Data — 20% BW" }, { name: "Default — remaining" }] }] } },
    hack: {
      memory: "LLQ = VIP line at the club (always goes first). CBWFQ = fair share of the dance floor (guaranteed space for each group). Together they handle real-time + everything else.",
      practice: "Understand the concept: LLQ = strict priority for latency-sensitive traffic, CBWFQ = fair bandwidth allocation for everything else. No configuration needed for CCNA.",
      effort: "low",
      meta: "Know that voice/video uses LLQ (priority queuing) and data uses CBWFQ (bandwidth guarantees). The exam tests the concept, not the configuration."
    }
  },

  "4.7.e": {
    info: "Congestion avoidance proactively drops packets before queues fill completely to prevent tail drop (where ALL new packets are dropped when the queue is full). WRED (Weighted Random Early Detection) selectively drops lower-priority packets earlier as congestion builds, giving higher-priority traffic a better chance of getting through. This prevents TCP global synchronization.",
    visual: { type: "state-machine", params: { states: ["Queue filling up", "WRED: randomly drop low-priority", "Queue stays under limit", "Tail drop avoided"], active: 1, transitions: true } },
    hack: {
      memory: "WRED = bouncer who starts turning away less important guests as the club fills up, so VIPs can still get in. Without WRED, when the club is full, EVERYONE gets turned away (tail drop).",
      practice: "Understand the difference: tail drop = all dropped equally when full, WRED = selective early drops based on priority. Know that WRED prevents TCP global synchronization.",
      effort: "low",
      meta: "The exam asks 'which mechanism prevents tail drop?' Answer: WRED. And 'what problem does WRED solve?' Answer: TCP global synchronization (where all TCP flows back off simultaneously)."
    }
  },

  "4.7.f": {
    info: "Policing enforces a traffic rate limit by immediately dropping (or re-marking) packets that exceed the configured rate. It does NOT buffer excess traffic — it simply discards it. Policing is typically applied at network boundaries (ingress) to enforce SLAs. The traffic rate has a hard ceiling; anything over is dropped instantly.",
    visual: { type: "comparison", params: { left: { label: "Policing", items: ["Drops excess immediately", "No buffering", "Ingress or egress", "Hard rate limit", "Bursty output"] }, right: { label: "Shaping (see 4.7.g)", items: ["Buffers excess", "Smooths traffic", "Egress only", "Soft rate limit", "Smooth output"] } } },
    hack: {
      memory: "Policing = speed camera: over the limit? Ticket (drop) immediately. No second chances, no slowing down — just enforcement.",
      practice: "Memorize the key difference: policing DROPS excess, shaping BUFFERS excess. This is tested as a direct comparison question.",
      effort: "low",
      meta: "Policing vs shaping is a guaranteed exam question. Policing = drops, shaping = delays. That one-line distinction is usually enough to answer correctly."
    }
  },

  "4.7.g": {
    info: "Shaping buffers excess traffic instead of dropping it, smoothing bursts to conform to a desired rate. Packets that exceed the rate are held in a queue and transmitted later when bandwidth is available. Shaping is applied on egress only and introduces latency (because packets wait in the buffer). It's gentler than policing and produces smoother traffic patterns.",
    visual: { type: "state-machine", params: { states: ["Traffic exceeds rate", "Excess buffered (queued)", "Released when BW available", "Smooth output stream"], active: 1, transitions: true } },
    hack: {
      memory: "Shaping = speed bump: it slows you down but doesn't stop you. Traffic waits in a buffer and gets released gradually. Policing = cliff (drop off). Shaping = ramp (slow down).",
      practice: "Draw the policing vs shaping comparison: policing has a flat line with drops above it; shaping has a smooth curve that buffers peaks. This visual helps on exam day.",
      effort: "low",
      meta: "Shaping adds latency (bad for voice), policing doesn't (but drops packets). Know which is appropriate: shaping for WAN links, policing for ingress enforcement."
    }
  },

  "4.7.h": {
    info: "The trust boundary is the network point where QoS markings are first accepted and trusted. Typically, this is at the access layer switch closest to the endpoint. Markings from untrusted sources (like a PC) are overwritten at the trust boundary. Markings from trusted sources (like an IP phone) are preserved. Setting the trust boundary correctly prevents endpoints from gaming QoS.",
    visual: { type: "packet-flow", params: { nodes: ["PC (untrusted DSCP)", "Access Switch (trust boundary)", "Re-marks to correct DSCP", "Network trusts from here"], color: "#f59e0b" } },
    hack: {
      memory: "Trust boundary = security checkpoint. Before this point, anyone can claim to be VIP (mark their own packets as high priority). After this point, only legitimate markings are trusted.",
      practice: "Understand the concept: IP phones are typically trusted at the access switch (their CoS markings are kept). PC traffic behind the phone is untrusted (markings are reset).",
      effort: "low",
      meta: "The exam asks 'where should the trust boundary be placed?' Answer: as close to the source as possible, typically the access switch. This prevents untrusted endpoints from setting their own priority."
    }
  },

  // ── 4.8  Configure SSH ─────────────────────────────────────

  "4.8.a": {
    info: "Step 1 of SSH configuration requires setting a hostname on the device. SSH generates a key pair named '[hostname].[domain-name]', so the default hostname 'Router' or 'Switch' must be changed. Command: 'hostname [name]'. Without a unique hostname, the RSA key generation may fail or produce a generic key that can't be used for SSH.",
    visual: { type: "state-machine", params: { states: ["1. hostname R1", "2. ip domain-name", "3. crypto key generate rsa", "4. ip ssh version 2", "5. username + secret", "6. line vty + SSH"], active: 0, transitions: true } },
    hack: {
      memory: "SSH needs a name to put on the certificate. 'Router' is too generic — give it a real name first. Step 1 of 6.",
      practice: "Configure SSH end-to-end on a Packet Tracer device. Start with hostname and work through all 6 steps. Practice until you can do it in under 2 minutes.",
      effort: "medium",
      meta: "SSH configuration is a guaranteed lab sim. The 6-step process must be memorized in order. Missing any step = SSH won't work."
    }
  },

  "4.8.b": {
    info: "Step 2: Configure a domain name with 'ip domain-name [name]'. The domain name is combined with the hostname to create the RSA key pair label (e.g., 'R1.lab.local'). Without a domain name, 'crypto key generate rsa' will fail. Any valid domain name works — in labs, 'lab.local' or 'ccna.lab' are common choices.",
    visual: { type: "state-machine", params: { states: ["1. hostname R1", "2. ip domain-name lab.local", "3. crypto key generate rsa", "4. ip ssh version 2", "5. username + secret", "6. line vty + SSH"], active: 1, transitions: true } },
    hack: {
      memory: "Domain name = the router's last name. Hostname + domain = full name on the key. No last name = can't make the key.",
      practice: "Try running 'crypto key generate rsa' without setting a domain name first — note the error. Then set the domain name and retry successfully.",
      effort: "low",
      meta: "Students forget this step most often. If RSA key generation fails, check hostname AND domain name — both must be set."
    }
  },

  "4.8.c": {
    info: "Step 3: Generate the RSA key pair with 'crypto key generate rsa modulus 2048'. The modulus determines key strength — 2048 bits is the recommended minimum for SSH version 2 (1024 minimum required for SSHv2, but 2048 is best practice). This key pair enables public-key cryptography for the SSH session. Once generated, SSH becomes available.",
    visual: { type: "shield", params: { items: ["crypto key generate rsa", "modulus 2048 (recommended)", "Enables SSH encryption", "Key pair: hostname.domain"], color: "#10b981" } },
    hack: {
      memory: "RSA 2048 = the deadbolt lock on your SSH door. 1024 is the minimum, but 2048 is what everyone uses. Without this key, there's no encryption and no SSH.",
      practice: "Generate the key and verify with 'show crypto key mypubkey rsa'. Note the key name matches hostname.domain. Know that you can regenerate with a new modulus if needed.",
      effort: "low",
      meta: "The exam expects 'modulus 2048'. If you see 1024 vs 2048 as options, pick 2048. Also know: SSHv2 requires at least 768-bit modulus, but 2048 is the correct exam answer."
    }
  },

  "4.8.d": {
    info: "Step 4: Set SSH to version 2 with 'ip ssh version 2'. SSHv1 has known vulnerabilities and should never be used. Version 2 provides stronger encryption, better authentication, and protection against man-in-the-middle attacks. Without this command, the device may accept both v1 and v2 connections, which is a security risk.",
    visual: { type: "comparison", params: { left: { label: "SSHv1 (Insecure)", items: ["Known vulnerabilities", "Weaker encryption", "MITM susceptible", "Do NOT use"] }, right: { label: "SSHv2 (Required)", items: ["Strong encryption", "Better auth methods", "MITM protection", "Always configure"] } } },
    hack: {
      memory: "SSHv2 = the only acceptable version. SSHv1 is like leaving your door unlocked. One command: 'ip ssh version 2'. Always include this.",
      practice: "Configure SSH without setting version 2, then check 'show ip ssh' — it may show version 1.99 (accepts both). Add 'ip ssh version 2' and verify it now shows version 2 only.",
      effort: "low",
      meta: "The exam asks 'which command restricts SSH to version 2?' Answer: 'ip ssh version 2'. Simple recall, but students sometimes forget to include it in their lab sim."
    }
  },

  "4.8.e": {
    info: "Step 5: Create a local user account with 'username [name] secret [password]'. The 'secret' keyword stores the password using a strong hash (type 5 or type 9) in the running config. Never use 'password' instead of 'secret' — 'password' stores in cleartext or weak encryption. This account will be used for SSH login authentication.",
    visual: { type: "shield", params: { items: ["username admin secret Str0ngP@ss", "'secret' = strong hash (type 5/9)", "Never use 'password' keyword", "Local auth database"], color: "#8b5cf6" } },
    hack: {
      memory: "'secret' = secret (hidden/hashed). 'password' = plain text (visible to anyone who reads the config). Always use 'secret'. Always.",
      practice: "Create a user with 'secret' and another with 'password'. Run 'show running-config' and compare — 'secret' shows a hash, 'password' shows the actual password. Never forget this.",
      effort: "low",
      meta: "The exam will penalize you for using 'password' instead of 'secret'. In lab sims, always use 'username X secret Y'. This is both a security best practice and an exam requirement."
    }
  },

  "4.8.f": {
    info: "Step 6: Configure VTY lines for SSH access: 'line vty 0 15' (all 16 VTY lines), 'transport input ssh' (only allow SSH, block Telnet), 'login local' (authenticate using the local username database). Without 'login local', the VTY line won't know to check the local user database. Without 'transport input ssh', Telnet may still be allowed.",
    visual: { type: "layer-stack", params: { layers: ["line vty 0 15", "transport input ssh", "login local", "(optional) exec-timeout 5 0", "(optional) logging synchronous"], highlight: 1 } },
    hack: {
      memory: "VTY = the virtual door to the device. 'transport input ssh' = only SSH keys work on this door. 'login local' = check the guest list (local users). Both required.",
      practice: "Configure the full SSH setup and test: SSH in from another device. Then try Telnet — it should be refused. Verify with 'show line vty 0 15' and 'show users'.",
      effort: "medium",
      meta: "In lab sims, configure ALL VTY lines (0 15, not just 0 4). Use 'transport input ssh' and 'login local' together. Missing either one means SSH login fails."
    }
  },

  "4.8.g": {
    info: "Disabling Telnet is accomplished by configuring 'transport input ssh' on the VTY lines, which only permits SSH connections and rejects Telnet. Alternatively, 'transport input none' blocks all remote access. Telnet sends all data (including passwords) in cleartext and should never be used on production networks. SSH replaces Telnet for secure remote management.",
    visual: { type: "comparison", params: { left: { label: "Telnet (Insecure)", items: ["Cleartext passwords", "No encryption", "TCP port 23", "NEVER use in production"] }, right: { label: "SSH (Secure)", items: ["Encrypted session", "RSA key exchange", "TCP port 22", "Always preferred"] } } },
    hack: {
      memory: "Telnet = shouting your password across a crowded room. SSH = whispering it through an encrypted tunnel. 'transport input ssh' = shut the Telnet door forever.",
      practice: "After configuring SSH, attempt a Telnet connection to the device. It should be refused with 'Connection refused.' This confirms Telnet is disabled.",
      effort: "low",
      meta: "If the exam asks 'which command prevents Telnet access?' Answer: 'transport input ssh' under line vty. This simultaneously enables SSH-only and blocks Telnet."
    }
  },

  "4.8.h": {
    info: "SSH verification commands: 'show ip ssh' displays SSH version, authentication timeout, and retry count. 'show ssh' displays active SSH sessions (who's connected, version, encryption type). Together, these commands confirm SSH is operational and show current connections. 'show running-config | section line vty' verifies the VTY configuration.",
    visual: { type: "comparison", params: { left: { label: "show ip ssh", items: ["SSH version configured", "Auth timeout (default 120s)", "Auth retries (default 3)", "RSA key status"] }, right: { label: "show ssh", items: ["Active SSH sessions", "Connected users", "Encryption in use", "Session version"] } } },
    hack: {
      memory: "'show ip ssh' = is SSH configured correctly? 'show ssh' = who's connected right now? One is the setup check, the other is the live connection list.",
      practice: "After completing SSH setup, run both commands. SSH in from another device, then run 'show ssh' again to see your active session listed.",
      effort: "low",
      meta: "The exam shows 'show ip ssh' output and asks what version is configured or what the timeout is. Practice reading this output — it's straightforward but must be familiar."
    }
  },

  // ── 4.9  Describe TFTP/FTP ─────────────────────────────────

  "4.9.a": {
    info: "TFTP (Trivial File Transfer Protocol) uses UDP port 69 and provides simple, connectionless file transfers with no authentication. It has no directory listing, no rename, no delete — only read and write operations. TFTP is used for transferring IOS images, configs, and firmware because of its simplicity and low overhead. Not suitable for sensitive data.",
    visual: { type: "comparison", params: { left: { label: "TFTP", items: ["UDP port 69", "No authentication", "No encryption", "Simple: read/write only", "Small file transfers"] }, right: { label: "FTP (see 4.9.b)", items: ["TCP ports 20/21", "Username/password", "No encryption", "Full operations", "Larger file transfers"] } } },
    hack: {
      memory: "TFTP = 'Tiny, Fast, Trivially Plain.' UDP 69, no auth, no frills. Perfect for quick IOS transfers on a trusted management network.",
      practice: "Set up a TFTP server (SolarWinds TFTP or tftpd-hpa) and use 'copy running-config tftp:' on a router. Verify the file was saved on the server.",
      effort: "low",
      meta: "Know: TFTP = UDP 69, no auth. FTP = TCP 20/21, has auth. The exam asks 'which protocol is used for IOS file transfers and uses UDP?' Answer: TFTP."
    }
  },

  "4.9.b": {
    info: "FTP (File Transfer Protocol) uses TCP port 21 for control (commands) and TCP port 20 for data transfer. FTP requires username and password authentication, supports full file operations (list, rename, delete, transfer), and is more reliable than TFTP because it uses TCP. However, FTP sends credentials in cleartext — use SCP or SFTP for secure transfers.",
    visual: { type: "handshake", params: { leftLabel: "Client", rightLabel: "FTP Server", steps: ["TCP 21: Control connection ->", "<- Login (username/password)", "TCP 20: Data transfer ->", "<- File data", "Transfer complete"] } },
    hack: {
      memory: "FTP = two channels: 21 for commands (the manager), 20 for data (the worker). Like calling a warehouse (21) to place an order, then a truck (20) delivers the goods.",
      practice: "Know the port numbers cold: 21 = control, 20 = data. No lab needed — this is a memorization item for the exam.",
      effort: "low",
      meta: "FTP port question is common: 'Which port does FTP use for data transfer?' Answer: 20. 'Control?' Answer: 21. Also know that FTP credentials are sent in cleartext."
    }
  },

  "4.9.c": {
    info: "TFTP is the traditional choice for Cisco IOS image backup and upgrade because of its simplicity. The workflow: 'copy flash: tftp:' to back up the current IOS image to a TFTP server, or 'copy tftp: flash:' to download a new image. TFTP requires the server IP address and the exact filename. The TFTP server must be reachable and the file must fit in flash memory.",
    visual: { type: "packet-flow", params: { nodes: ["Router (flash:)", "copy flash: tftp:", "TFTP Server", "IOS image backup"], color: "#3b82f6" } },
    hack: {
      memory: "'copy [from] [to]' — source first, destination second. 'copy flash: tftp:' = from the router to the server. 'copy tftp: flash:' = from the server to the router.",
      practice: "In Packet Tracer, practice both directions: backup IOS to TFTP, then restore from TFTP. Verify with 'show flash:' that the file is present and correct size.",
      effort: "medium",
      meta: "The 'copy' command syntax is heavily tested: 'copy [source] [destination]'. Know that 'copy tftp: flash:' prompts for server IP, source filename, and destination filename."
    }
  },

  "4.9.d": {
    info: "FTP is used for larger file transfers where reliability and authentication matter. It's more suitable than TFTP for transferring large IOS images or multiple files because TCP handles error correction and retransmission. Configure FTP credentials on the router with 'ip ftp username [user]' and 'ip ftp password [pass]', then use 'copy ftp: flash:' to transfer.",
    visual: { type: "comparison", params: { left: { label: "TFTP Use Cases", items: ["Small config files", "Quick IOS backup/restore", "Trusted management VLAN", "No auth needed"] }, right: { label: "FTP Use Cases", items: ["Large IOS images", "Reliable transfers needed", "Multiple file operations", "Auth required"] } } },
    hack: {
      memory: "TFTP for quick small jobs, FTP for bigger jobs that need reliability. Like sending a postcard (TFTP) vs a registered package (FTP).",
      practice: "Know when to use each: TFTP for simple config backups on the management network, FTP when you need authentication or are transferring large files. No deep lab needed.",
      effort: "low",
      meta: "The exam mostly tests TFTP for IOS operations. FTP appears as a comparison option. Know the key differences: UDP vs TCP, auth vs no auth, simple vs full-featured."
    }
  },

  "4.9.e": {
    info: "Common IOS file operations: 'copy running-config tftp:' saves the current config to a TFTP server, 'copy startup-config tftp:' saves the startup config, 'copy tftp: running-config' restores/merges a config from TFTP, 'copy tftp: flash:' downloads an IOS image. 'show flash:' lists files in flash memory. Always verify flash space before downloading new images.",
    visual: { type: "layer-stack", params: { layers: ["copy running-config tftp:", "copy startup-config tftp:", "copy tftp: flash:", "copy flash: tftp:", "show flash:"], highlight: 2 } },
    hack: {
      memory: "Always 'copy [from] [to]'. Running-config = live config in RAM. Startup-config = saved config in NVRAM. Flash = where IOS lives. TFTP = remote server.",
      practice: "Practice every copy command variant in Packet Tracer: running to TFTP, startup to TFTP, TFTP to flash. Verify each transfer with the appropriate show command.",
      effort: "medium",
      meta: "The 'copy' command variations are tested in lab sims. The tricky one: 'copy tftp: running-config' MERGES (doesn't replace) the current running config. 'copy tftp: startup-config' replaces."
    }
  },

  "4.9.f": {
    info: "SCP (Secure Copy Protocol) transfers files over SSH, providing both encryption and authentication. It's the secure alternative to TFTP and FTP. To use SCP on a Cisco device, SSH must be configured and 'ip scp server enable' must be set. Transfer command: 'copy scp: flash:'. SCP uses TCP port 22 (SSH) and is the recommended method for production file transfers.",
    visual: { type: "shield", params: { items: ["SCP = file transfer over SSH", "TCP port 22 (encrypted)", "Requires SSH configured", "ip scp server enable", "Most secure option"], color: "#10b981" } },
    hack: {
      memory: "SCP = SSH + file copy. Same encryption, same port (22), same authentication. If you already have SSH, SCP is the natural upgrade from TFTP/FTP.",
      practice: "Know that SCP requires SSH to be working first. If SSH is configured (hostname, domain, RSA key, user, VTY), SCP just needs 'ip scp server enable' to activate.",
      effort: "low",
      meta: "The exam asks 'which is the most secure file transfer method?' Answer: SCP (encrypted, authenticated). TFTP = no security, FTP = auth but no encryption, SCP = both."
    }
  }

};
